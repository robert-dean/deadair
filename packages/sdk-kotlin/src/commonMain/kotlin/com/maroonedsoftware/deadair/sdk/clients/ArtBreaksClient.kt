// Auto-generated by @contractkit/plugin-kotlin. Do not edit manually.
package com.maroonedsoftware.deadair.sdk.clients

import com.maroonedsoftware.deadair.sdk.models.BreakArtworkList
import com.maroonedsoftware.deadair.sdk.runtime.SdkHttp
import io.ktor.http.HttpMethod
import io.ktor.http.content.PartData

/** Operations declared in `art.breaks.ck`. */
class ArtBreaksClient(private val http: SdkHttp) {
    /**
     * List break artwork
     * Every kind the station holds a picture for
     */
    suspend fun listBreakArtwork(): BreakArtworkList {
        val response = http.execute(HttpMethod.Get) {
            path("art", "breaks")
        }
        return http.decodeJson(response)
    }

    /**
     * Replace break artwork
     * Puts an operator's own picture behind a kind of break. The id does not change, so a URL already on the wire keeps working and the ETag is what says the picture moved
     * @throws SdkError on 400, 413, 415
     */
    suspend fun replaceBreakArtwork(kind: String, body: List<PartData>): BreakArtworkList {
        val response = http.execute(HttpMethod.Post) {
            path("art", "breaks", segment(kind))
            multipartBody(body)
        }
        return http.decodeJson(response)
    }

    /**
     * Revert break artwork
     * Puts the picture this repository ships back. The shipped file is read at this moment rather than copied at install, so an upgrade that improved it is what comes back
     * @throws SdkError on 404
     */
    suspend fun revertBreakArtwork(kind: String): BreakArtworkList {
        val response = http.execute(HttpMethod.Delete) {
            path("art", "breaks", segment(kind))
        }
        return http.decodeJson(response)
    }
}
