---
'@deadair/web': patch
---

The console's words now come from a message catalog rather than being written into each page, which is the groundwork for showing the console in languages other than English. English is still the only language, and nothing an operator reads has changed. The page now tells the browser which language it is in, which screen readers use.
