// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Net.Http;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading;
using System.Threading.Tasks;
using System.Xml;
using MaroonedSoftware.Deadair.Sdk.Models;
using MaroonedSoftware.Deadair.Sdk.Runtime;

namespace MaroonedSoftware.Deadair.Sdk.Clients;

/// <summary>Operations declared in &lt;c&gt;oauth.ck&lt;/c&gt;.</summary>
public sealed class OauthClient(SdkHttp http)
{
    /// <summary>
    /// Describe authorization request
    /// Validate an app's authorization request for the consent page, and stash it for the signed-in person. A POST because it stashes: what is approved is exactly what was validated here
    /// </summary>
    public async Task<OAuthAuthorizationContextResult> DescribeAuthorizationRequestAsync(OAuthAuthorizationQuery body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "oauth", "authorize", "context"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<OAuthAuthorizationContextResult>(response);
    }

    /// <summary>
    /// Approve authorization request
    /// Let the app act as the signed-in person. Once the account has a strong second factor, this needs one verified in the last five minutes
    /// </summary>
    public async Task<OAuthAuthorizationOutcome> ApproveAuthorizationRequestAsync(OAuthAuthorizationDecision body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "oauth", "authorize", "approve"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<OAuthAuthorizationOutcome>(response);
    }

    /// <summary>
    /// Deny authorization request
    /// Turn the app away. It is told the person said no
    /// </summary>
    public async Task<OAuthAuthorizationOutcome> DenyAuthorizationRequestAsync(OAuthAuthorizationDecision body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "oauth", "authorize", "deny"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<OAuthAuthorizationOutcome>(response);
    }

    /// <summary>
    /// List OAuth clients
    /// Every app registered with the station, by an operator or by itself
    /// </summary>
    public async Task<OAuthClientList> ListOAuthClientsAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("auth", "oauth", "clients"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<OAuthClientList>(response);
    }

    /// <summary>
    /// Create OAuth client
    /// Register an app by hand. The secret, for an app that keeps one, is in this response and nowhere else
    /// </summary>
    public async Task<OAuthClientIssued> CreateOAuthClientAsync(OAuthClientCreate body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "oauth", "clients"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<OAuthClientIssued>(response);
    }

    /// <summary>
    /// Revoke OAuth client
    /// Withdraw an app. Every person's approval of it ends, and so does every token it holds
    /// </summary>
    public async Task RevokeOAuthClientAsync(string clientId, CancellationToken cancellationToken = default)
    {
        await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("auth", "oauth", "clients", http.Segment(clientId)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// List OAuth grants
    /// The apps the signed-in person has let act as them
    /// </summary>
    public async Task<OAuthGrantList> ListOAuthGrantsAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("auth", "oauth", "grants"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<OAuthGrantList>(response);
    }

    /// <summary>
    /// Revoke OAuth grant
    /// Disconnect an app. Every token it holds for this person stops working at once
    /// </summary>
    public async Task RevokeOAuthGrantAsync(Guid id, CancellationToken cancellationToken = default)
    {
        await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("auth", "oauth", "grants", http.Segment(id)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}
