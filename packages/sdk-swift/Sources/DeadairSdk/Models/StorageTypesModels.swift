// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Which store, as a stable id the console can key off rather than a name it renders.
public enum StorageStoreId: String, Codable, CaseIterable, Sendable {
    case tracks = "tracks"
    case art = "art"
    case segments = "segments"
    case voices = "voices"
}

/// One content store: what is on disk, and what the database says should be.
///
/// The two halves are deliberately separate numbers rather than one reconciled figure. They disagree
/// in two directions and each direction means something different — a file nothing claims is what a
/// crash between writing bytes and writing a row leaves behind, and a row whose file is gone is what
/// an operator emptying a directory leaves. Reporting one number would hide both.
public struct StorageStore: Codable, Equatable, Sendable {
    public var id: StorageStoreId
    /// What to call it on a page
    public var label: String
    /// Where it is, so `du` and this can be compared
    public var path: String
    /// Files actually there
    public var files: Int
    /// What they weigh
    public var bytes: Int
    /// Rows pointing at a file. Absent when no table backs this store
    public var rows: Int?
    /// What those rows say those files weigh. Absent where the table does not record a size
    public var accountedBytes: Int?
    /// The limit an operator set, where the store has one. Absent means no limit
    public var capBytes: Int?
    /// Files no row claims. Reported and never cleaned up automatically
    public var orphanFiles: Int
    public var orphanBytes: Int
    /// Claims whose file is not there. The station re-fetches or re-renders these
    public var rowsWithNoFile: Int

    public init(id: StorageStoreId, label: String, path: String, files: Int, bytes: Int, rows: Int? = nil, accountedBytes: Int? = nil, capBytes: Int? = nil, orphanFiles: Int, orphanBytes: Int, rowsWithNoFile: Int) {
        self.id = id
        self.label = label
        self.path = path
        self.files = files
        self.bytes = bytes
        self.rows = rows
        self.accountedBytes = accountedBytes
        self.capBytes = capBytes
        self.orphanFiles = orphanFiles
        self.orphanBytes = orphanBytes
        self.rowsWithNoFile = rowsWithNoFile
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case label = "label"
        case path = "path"
        case files = "files"
        case bytes = "bytes"
        case rows = "rows"
        case accountedBytes = "accountedBytes"
        case capBytes = "capBytes"
        case orphanFiles = "orphanFiles"
        case orphanBytes = "orphanBytes"
        case rowsWithNoFile = "rowsWithNoFile"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(StorageStoreId.self, forKey: .id)
        self.label = try container.decode(String.self, forKey: .label)
        self.path = try container.decode(String.self, forKey: .path)
        self.files = try container.decode(Int.self, forKey: .files)
        self.bytes = try container.decode(Int.self, forKey: .bytes)
        self.rows = try container.decodeIfPresent(Int.self, forKey: .rows)
        self.accountedBytes = try container.decodeIfPresent(Int.self, forKey: .accountedBytes)
        self.capBytes = try container.decodeIfPresent(Int.self, forKey: .capBytes)
        self.orphanFiles = try container.decode(Int.self, forKey: .orphanFiles)
        self.orphanBytes = try container.decode(Int.self, forKey: .orphanBytes)
        self.rowsWithNoFile = try container.decode(Int.self, forKey: .rowsWithNoFile)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.path, forKey: .path)
        try container.encode(self.files, forKey: .files)
        try container.encode(self.bytes, forKey: .bytes)
        try container.encodeIfPresent(self.rows, forKey: .rows)
        try container.encodeIfPresent(self.accountedBytes, forKey: .accountedBytes)
        try container.encodeIfPresent(self.capBytes, forKey: .capBytes)
        try container.encode(self.orphanFiles, forKey: .orphanFiles)
        try container.encode(self.orphanBytes, forKey: .orphanBytes)
        try container.encode(self.rowsWithNoFile, forKey: .rowsWithNoFile)
    }
}

/// One content store: what is on disk, and what the database says should be.
///
/// The two halves are deliberately separate numbers rather than one reconciled figure. They disagree
/// in two directions and each direction means something different — a file nothing claims is what a
/// crash between writing bytes and writing a row leaves behind, and a row whose file is gone is what
/// an operator emptying a directory leaves. Reporting one number would hide both.
public struct StorageStoreInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// Every store, plus the number an operator actually wants first.
///
/// `readAt` is not decoration: the figures come from walking directories, which is real I/O on a
/// station holding tens of thousands of files, so the answer is cached for a short while and this is
/// what stops a page mistaking it for live.
public struct StorageReport: Codable, Equatable, Sendable {
    public var readAt: Date
    public var totalFiles: Int
    public var totalBytes: Int
    public var stores: [StorageStore]

    public init(readAt: Date, totalFiles: Int, totalBytes: Int, stores: [StorageStore]) {
        self.readAt = readAt
        self.totalFiles = totalFiles
        self.totalBytes = totalBytes
        self.stores = stores
    }

    private enum CodingKeys: String, CodingKey {
        case readAt = "readAt"
        case totalFiles = "totalFiles"
        case totalBytes = "totalBytes"
        case stores = "stores"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.readAt = try container.decode(Date.self, forKey: .readAt)
        self.totalFiles = try container.decode(Int.self, forKey: .totalFiles)
        self.totalBytes = try container.decode(Int.self, forKey: .totalBytes)
        self.stores = try container.decode([StorageStore].self, forKey: .stores)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.readAt, forKey: .readAt)
        try container.encode(self.totalFiles, forKey: .totalFiles)
        try container.encode(self.totalBytes, forKey: .totalBytes)
        try container.encode(self.stores, forKey: .stores)
    }
}

/// Every store, plus the number an operator actually wants first.
///
/// `readAt` is not decoration: the figures come from walking directories, which is real I/O on a
/// station holding tens of thousands of files, so the answer is cached for a short while and this is
/// what stops a page mistaking it for live.
public struct StorageReportInput: Codable, Equatable, Sendable {
    public var stores: [StorageStoreInput]

    public init(stores: [StorageStoreInput]) {
        self.stores = stores
    }

    private enum CodingKeys: String, CodingKey {
        case stores = "stores"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.stores = try container.decode([StorageStoreInput].self, forKey: .stores)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.stores, forKey: .stores)
    }
}
