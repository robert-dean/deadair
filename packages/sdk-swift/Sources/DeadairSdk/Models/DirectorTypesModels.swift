// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// What kind of programming the station is running, which decides the rules it runs under
public enum StationMode: String, Codable, CaseIterable, Sendable {
    case rotation = "rotation"
    case setlist = "setlist"
    case feature = "feature"
}

/// What the mount lease is renewed against: `audience` airs only while somebody is listening, `always` airs whenever there is a programme
public enum AirMode: String, Codable, CaseIterable, Sendable {
    case audience = "audience"
    case always = "always"
}

/// Who chose what is on air. `schedule` is a block the clock changed over to and `sustaining` is what it plays in the hours no block claims — both are the schedule driving. `operator` is a person, including one who took over inside a scheduled block, and it holds until the next block begins. `off` is a station stood down
public enum AirSource: String, Codable, CaseIterable, Sendable {
    case off = "off"
    case schedule = "schedule"
    case sustaining = "sustaining"
    case `operator` = "operator"
}

/// How long to keep the schedule off the running order
public struct HoldStationInput: Codable, Equatable, Sendable {
    /// How long the hold lasts, from now. ABSENT means until it is released by hand, which is the answer for an operator who does not know yet — a day is the ceiling because a hold nobody remembers setting is worse than one that lapses
    public var minutes: Int?

    public init(minutes: Int? = nil) {
        self.minutes = minutes
    }

    private enum CodingKeys: String, CodingKey {
        case minutes = "minutes"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.minutes = try container.decodeIfPresent(Int.self, forKey: .minutes)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.minutes, forKey: .minutes)
    }
}

/// What the station does when the running order runs out
public enum StationOnEnd: String, Codable, CaseIterable, Sendable {
    case extend = "extend"
    case `repeat` = "repeat"
    case stop = "stop"
}

/// Where an item of the running order has got to. `handed` is a promise and `airing` is a fact, which is the distinction everything here is built around. The three terminal states that are not `played` are three different facts on a page that has to say why the station is silent: `skipped` is the station passing over an item it reached, `removed` is an operator taking one out before its turn, and `unavailable` is a record the station could not obtain the audio for — the only one of the three an operator can act on, since it names a copy rather than a decision
public enum StationItemState: String, Codable, CaseIterable, Sendable {
    case planned = "planned"
    case handed = "handed"
    case airing = "airing"
    case played = "played"
    case skipped = "skipped"
    case unavailable = "unavailable"
    case removed = "removed"
}

/// Change who is presenting the broadcast that is on air
public struct SetStationHostInput: Codable, Equatable, Sendable {
    /// Who hosts it from here on. Absent hands it back to whichever persona the station has on air, which is what a broadcast that never named one already does
    public var personaId: String?

    public init(personaId: String? = nil) {
        self.personaId = personaId
    }

    private enum CodingKeys: String, CodingKey {
        case personaId = "personaId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.personaId = try container.decodeIfPresent(String.self, forKey: .personaId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.personaId, forKey: .personaId)
    }
}

/// Put something the station says into the running order
public struct AddStationSegmentInput: Codable, Equatable, Sendable {
    public var segmentId: String
    /// Where to put it. Absent puts it at the end. A position already handed to the player is refused
    public var atIndex: Int?
    /// Play it OVER the record that follows, this far into it, rather than in the gap before it. Absent plays it between two records, which is the simpler path
    public var overAtMs: Int?

    public init(segmentId: String, atIndex: Int? = nil, overAtMs: Int? = nil) {
        self.segmentId = segmentId
        self.atIndex = atIndex
        self.overAtMs = overAtMs
    }

    private enum CodingKeys: String, CodingKey {
        case segmentId = "segmentId"
        case atIndex = "atIndex"
        case overAtMs = "overAtMs"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.segmentId = try container.decode(String.self, forKey: .segmentId)
        self.atIndex = try container.decodeIfPresent(Int.self, forKey: .atIndex)
        self.overAtMs = try container.decodeIfPresent(Int.self, forKey: .overAtMs)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.segmentId, forKey: .segmentId)
        try container.encodeIfPresent(self.atIndex, forKey: .atIndex)
        try container.encodeIfPresent(self.overAtMs, forKey: .overAtMs)
    }
}

/// Put a catalog record into the running order. Refused at the door — 404 for a record the catalog does not hold, 422 for one whose audio is not local yet — rather than accepted and left to fail when it comes round
public struct AddStationTrackInput: Codable, Equatable, Sendable {
    public var trackId: UUID
    /// Where to put it. Absent puts it at the end. A position already handed to the player is refused
    public var atIndex: Int?

    public init(trackId: UUID, atIndex: Int? = nil) {
        self.trackId = trackId
        self.atIndex = atIndex
    }

    private enum CodingKeys: String, CodingKey {
        case trackId = "trackId"
        case atIndex = "atIndex"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.trackId = try container.decode(UUID.self, forKey: .trackId)
        self.atIndex = try container.decodeIfPresent(Int.self, forKey: .atIndex)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.trackId, forKey: .trackId)
        try container.encodeIfPresent(self.atIndex, forKey: .atIndex)
    }
}

/// Move an item within the running order
public struct MoveStationItemInput: Codable, Equatable, Sendable {
    public var toIndex: Int

    public init(toIndex: Int) {
        self.toIndex = toIndex
    }

    private enum CodingKeys: String, CodingKey {
        case toIndex = "toIndex"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.toIndex = try container.decode(Int.self, forKey: .toIndex)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.toIndex, forKey: .toIndex)
    }
}

/// Add tracks to the running order now, rather than waiting for it to run short
public struct ExtendStationInput: Codable, Equatable, Sendable {
    public var count: Int?

    public init(count: Int? = nil) {
        self.count = count
    }

    private enum CodingKeys: String, CodingKey {
        case count = "count"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.count = try container.decodeIfPresent(Int.self, forKey: .count)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.count, forKey: .count)
    }
}

/// Throw away everything the player is not already holding and programme it again. Unlike a shuffle, the records themselves change; unlike putting the station on air, the broadcast continues
public struct ReplanStationInput: Codable, Equatable, Sendable {
    /// How many records to programme. Absent is roughly an hour
    public var count: Int?
    /// What the station should play from here on, in your own words. Absent keeps whatever this broadcast was already asked for; an empty string CLEARS it, which hands the programming back to the station's ordinary rotation. It steers every later refill too, not just this one batch
    public var brief: String?

    public init(count: Int? = nil, brief: String? = nil) {
        self.count = count
        self.brief = brief
    }

    private enum CodingKeys: String, CodingKey {
        case count = "count"
        case brief = "brief"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.count = try container.decodeIfPresent(Int.self, forKey: .count)
        self.brief = try container.decodeIfPresent(String.self, forKey: .brief)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.count, forKey: .count)
        try container.encodeIfPresent(self.brief, forKey: .brief)
    }
}

/// Change how the station decides to be on air
public struct SetStationAirInput: Codable, Equatable, Sendable {
    public var airMode: AirMode

    public init(airMode: AirMode) {
        self.airMode = airMode
    }

    private enum CodingKeys: String, CodingKey {
        case airMode = "airMode"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.airMode = try container.decode(AirMode.self, forKey: .airMode)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.airMode, forKey: .airMode)
    }
}

/// What the station is airing, and whether it is driving at all
public struct StationAir: Codable, Equatable, Sendable {
    /// False means the station was stood down. What it was playing is remembered so the console can still say what it was
    public var active: Bool
    /// What puts the station on air. In `audience` mode a station that is active with a full running order is still silent while nobody is connected, which is the intended state and not a fault
    public var airMode: AirMode
    /// What is on. Absent before the station has ever been given anything to play
    public var name: String?
    /// Who built what is on: `import` or `director`
    public var source: String?
    /// Items left before the running order runs out and `onEnd` decides what happens
    public var remaining: Int
    /// Which slot of the schedule this broadcast belongs to. Absent means nothing scheduled it, which is every station with no schedule
    public var slotId: String?
    /// Who is driving the station right now
    public var airSource: AirSource
    /// Whether the schedule has been told to leave this broadcast alone. A takeover is otherwise replaced when the block it started inside ends
    public var held: Bool
    /// When that hold lapses, as an ISO-8601 instant. ABSENT WHILE `held` IS TRUE means until it is released by hand, which is a real state rather than a missing value — `Infinity` is not a thing JSON can carry, so the two facts are two fields
    public var holdUntil: String?

    public init(active: Bool, airMode: AirMode, name: String? = nil, source: String? = nil, remaining: Int, slotId: String? = nil, airSource: AirSource, held: Bool, holdUntil: String? = nil) {
        self.active = active
        self.airMode = airMode
        self.name = name
        self.source = source
        self.remaining = remaining
        self.slotId = slotId
        self.airSource = airSource
        self.held = held
        self.holdUntil = holdUntil
    }

    private enum CodingKeys: String, CodingKey {
        case active = "active"
        case airMode = "airMode"
        case name = "name"
        case source = "source"
        case remaining = "remaining"
        case slotId = "slotId"
        case airSource = "airSource"
        case held = "held"
        case holdUntil = "holdUntil"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.active = try container.decode(Bool.self, forKey: .active)
        self.airMode = try container.decode(AirMode.self, forKey: .airMode)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.source = try container.decodeIfPresent(String.self, forKey: .source)
        self.remaining = try container.decode(Int.self, forKey: .remaining)
        self.slotId = try container.decodeIfPresent(String.self, forKey: .slotId)
        self.airSource = try container.decode(AirSource.self, forKey: .airSource)
        self.held = try container.decode(Bool.self, forKey: .held)
        self.holdUntil = try container.decodeIfPresent(String.self, forKey: .holdUntil)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.active, forKey: .active)
        try container.encode(self.airMode, forKey: .airMode)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.source, forKey: .source)
        try container.encode(self.remaining, forKey: .remaining)
        try container.encodeIfPresent(self.slotId, forKey: .slotId)
        try container.encode(self.airSource, forKey: .airSource)
        try container.encode(self.held, forKey: .held)
        try container.encodeIfPresent(self.holdUntil, forKey: .holdUntil)
    }
}

/// Put the station on air, building its running order from the top
public struct PutOnAirInput: Codable, Equatable, Sendable {
    /// The plugin whose playlist to build from. Absent starts empty and lets the station generate its own programming
    public var pluginId: String?
    /// Required alongside `pluginId`. The playlist is READ at this moment rather than copied, so it is never edited by having been aired
    public var playlistId: String?
    /// A published chart to build from instead, as `pluginId:chartId`. An ALTERNATIVE to `pluginId` and `playlistId` rather than a companion: a chart names records where a playlist names copies, so its entries are looked up and ingested before they can air, and a station with `rotation.discover` off can play almost none of one
    public var chartId: String?
    /// Which way round to play it. `countdown` opens on the lowest rank and ends on number one, which is the shape a chart show has; `ranked` walks the published document from the top; `unordered` leaves the sequence to the station's own artist spacing. Absent is `countdown`. Ignored without `chartId`
    public var chartOrder: PutOnAirInputChartOrder?
    /// What to call this broadcast. Absent names it after the chart, or after the plugin, since only the surface that listed the source knows its own name for it
    public var name: String?
    /// What the station should play, in your own words: "heavy metal hits". It steers every refill for as long as this broadcast runs, not just the first batch, and it needs a model to programme with. Absent programmes the station the way its own rules do
    public var brief: String?
    /// Who is hosting this broadcast. It rides the running order for as long as the broadcast does, so the presenter cannot drift back mid-show. Absent uses whichever persona the station has on air
    public var personaId: String?
    /// The earliest release year this broadcast plays. Absent means no lower bound, and a record whose year the catalog does not know is played whatever the period
    public var eraFrom: Int?
    /// The latest release year, on the same terms. Set with `eraFrom` for a decade; either may stand alone
    public var eraTo: Int?
    /// Whether somebody phones in during this broadcast. A call is a short programme rather than a break: a few turns in a few voices, entering the running order as one block, spaced by `rotation.callinEveryMinutes`. Absent takes the station's own setting, which is off
    public var callins: Bool?
    public var mode: StationMode?
    public var onEnd: StationOnEnd?

    public init(pluginId: String? = nil, playlistId: String? = nil, chartId: String? = nil, chartOrder: PutOnAirInputChartOrder? = nil, name: String? = nil, brief: String? = nil, personaId: String? = nil, eraFrom: Int? = nil, eraTo: Int? = nil, callins: Bool? = nil, mode: StationMode? = nil, onEnd: StationOnEnd? = nil) {
        self.pluginId = pluginId
        self.playlistId = playlistId
        self.chartId = chartId
        self.chartOrder = chartOrder
        self.name = name
        self.brief = brief
        self.personaId = personaId
        self.eraFrom = eraFrom
        self.eraTo = eraTo
        self.callins = callins
        self.mode = mode
        self.onEnd = onEnd
    }

    private enum CodingKeys: String, CodingKey {
        case pluginId = "pluginId"
        case playlistId = "playlistId"
        case chartId = "chartId"
        case chartOrder = "chartOrder"
        case name = "name"
        case brief = "brief"
        case personaId = "personaId"
        case eraFrom = "eraFrom"
        case eraTo = "eraTo"
        case callins = "callins"
        case mode = "mode"
        case onEnd = "onEnd"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pluginId = try container.decodeIfPresent(String.self, forKey: .pluginId)
        self.playlistId = try container.decodeIfPresent(String.self, forKey: .playlistId)
        self.chartId = try container.decodeIfPresent(String.self, forKey: .chartId)
        self.chartOrder = try container.decodeIfPresent(PutOnAirInputChartOrder.self, forKey: .chartOrder)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.brief = try container.decodeIfPresent(String.self, forKey: .brief)
        self.personaId = try container.decodeIfPresent(String.self, forKey: .personaId)
        self.eraFrom = try container.decodeIfPresent(Int.self, forKey: .eraFrom)
        self.eraTo = try container.decodeIfPresent(Int.self, forKey: .eraTo)
        self.callins = try container.decodeIfPresent(Bool.self, forKey: .callins)
        self.mode = try container.decodeIfPresent(StationMode.self, forKey: .mode)
        self.onEnd = try container.decodeIfPresent(StationOnEnd.self, forKey: .onEnd)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.pluginId, forKey: .pluginId)
        try container.encodeIfPresent(self.playlistId, forKey: .playlistId)
        try container.encodeIfPresent(self.chartId, forKey: .chartId)
        try container.encodeIfPresent(self.chartOrder, forKey: .chartOrder)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.brief, forKey: .brief)
        try container.encodeIfPresent(self.personaId, forKey: .personaId)
        try container.encodeIfPresent(self.eraFrom, forKey: .eraFrom)
        try container.encodeIfPresent(self.eraTo, forKey: .eraTo)
        try container.encodeIfPresent(self.callins, forKey: .callins)
        try container.encodeIfPresent(self.mode, forKey: .mode)
        try container.encodeIfPresent(self.onEnd, forKey: .onEnd)
    }
}

/// One item of the live running order, and where it has got to
public struct StationOrderItem: Codable, Equatable, Sendable {
    /// What an edit names, what rides through the player, and what comes back on its readings
    public var id: String
    /// Whether this is a record or something the station says: an ident, a stinger, a talk break
    public var kind: StationOrderItemKind
    public var state: StationItemState
    /// The record's title, or the segment's label. What the mount is labelled with while it airs
    public var title: String
    /// Empty for a segment, which has no artist
    public var artists: [String]
    public var durationMs: Int?
    /// Absent on a segment: the station serves its own audio
    public var pluginId: String?
    /// Absent on a segment
    public var externalId: String?
    public var album: String?
    public var artworkUrl: String?
    public var year: Int?
    /// The canonical catalog track, when this is one the catalog holds
    public var trackId: String?
    /// The canonical artist behind that track, so a console can reach their page from the running order. Absent on a segment, and on a record the catalog has never seen
    public var artistId: String?
    /// The release that track was ingested inside. Absent for the two reasons above and for a third: a single ingested outside any release has none
    public var albumId: String?
    /// What the station thinks of this record, read as the order is drawn rather than stored on it. Absent on a segment, and on a record the catalog has never seen
    public var rating: Rating?
    /// Which segment this plays. Present only on a segment
    public var segmentId: String?
    public var segmentState: StationOrderItemSegmentState?
    /// Whether the station can actually air this segment. One that cannot is SKIPPED when it comes round, rather than held open
    public var playable: Bool?
    /// Why this segment will not air, in a sentence. Present only on a failed one
    public var segmentError: String?
    /// What decided the words: the station's own templates, or the model that wrote them. Absent on a recording somebody made
    public var segmentWriter: String?
    /// Heard OVER the record that follows, this far into it, with the music ducked under it. Such an item is never handed to the player in its own right
    public var overAtMs: Int?

    public init(id: String, kind: StationOrderItemKind, state: StationItemState, title: String, artists: [String], durationMs: Int? = nil, pluginId: String? = nil, externalId: String? = nil, album: String? = nil, artworkUrl: String? = nil, year: Int? = nil, trackId: String? = nil, artistId: String? = nil, albumId: String? = nil, rating: Rating? = nil, segmentId: String? = nil, segmentState: StationOrderItemSegmentState? = nil, playable: Bool? = nil, segmentError: String? = nil, segmentWriter: String? = nil, overAtMs: Int? = nil) {
        self.id = id
        self.kind = kind
        self.state = state
        self.title = title
        self.artists = artists
        self.durationMs = durationMs
        self.pluginId = pluginId
        self.externalId = externalId
        self.album = album
        self.artworkUrl = artworkUrl
        self.year = year
        self.trackId = trackId
        self.artistId = artistId
        self.albumId = albumId
        self.rating = rating
        self.segmentId = segmentId
        self.segmentState = segmentState
        self.playable = playable
        self.segmentError = segmentError
        self.segmentWriter = segmentWriter
        self.overAtMs = overAtMs
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case kind = "kind"
        case state = "state"
        case title = "title"
        case artists = "artists"
        case durationMs = "durationMs"
        case pluginId = "pluginId"
        case externalId = "externalId"
        case album = "album"
        case artworkUrl = "artworkUrl"
        case year = "year"
        case trackId = "trackId"
        case artistId = "artistId"
        case albumId = "albumId"
        case rating = "rating"
        case segmentId = "segmentId"
        case segmentState = "segmentState"
        case playable = "playable"
        case segmentError = "segmentError"
        case segmentWriter = "segmentWriter"
        case overAtMs = "overAtMs"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.kind = try container.decode(StationOrderItemKind.self, forKey: .kind)
        self.state = try container.decode(StationItemState.self, forKey: .state)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode([String].self, forKey: .artists)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.pluginId = try container.decodeIfPresent(String.self, forKey: .pluginId)
        self.externalId = try container.decodeIfPresent(String.self, forKey: .externalId)
        self.album = try container.decodeIfPresent(String.self, forKey: .album)
        self.artworkUrl = try container.decodeIfPresent(String.self, forKey: .artworkUrl)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.trackId = try container.decodeIfPresent(String.self, forKey: .trackId)
        self.artistId = try container.decodeIfPresent(String.self, forKey: .artistId)
        self.albumId = try container.decodeIfPresent(String.self, forKey: .albumId)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating)
        self.segmentId = try container.decodeIfPresent(String.self, forKey: .segmentId)
        self.segmentState = try container.decodeIfPresent(StationOrderItemSegmentState.self, forKey: .segmentState)
        self.playable = try container.decodeIfPresent(Bool.self, forKey: .playable)
        self.segmentError = try container.decodeIfPresent(String.self, forKey: .segmentError)
        self.segmentWriter = try container.decodeIfPresent(String.self, forKey: .segmentWriter)
        self.overAtMs = try container.decodeIfPresent(Int.self, forKey: .overAtMs)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.state, forKey: .state)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.pluginId, forKey: .pluginId)
        try container.encodeIfPresent(self.externalId, forKey: .externalId)
        try container.encodeIfPresent(self.album, forKey: .album)
        try container.encodeIfPresent(self.artworkUrl, forKey: .artworkUrl)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.trackId, forKey: .trackId)
        try container.encodeIfPresent(self.artistId, forKey: .artistId)
        try container.encodeIfPresent(self.albumId, forKey: .albumId)
        try container.encodeIfPresent(self.rating, forKey: .rating)
        try container.encodeIfPresent(self.segmentId, forKey: .segmentId)
        try container.encodeIfPresent(self.segmentState, forKey: .segmentState)
        try container.encodeIfPresent(self.playable, forKey: .playable)
        try container.encodeIfPresent(self.segmentError, forKey: .segmentError)
        try container.encodeIfPresent(self.segmentWriter, forKey: .segmentWriter)
        try container.encodeIfPresent(self.overAtMs, forKey: .overAtMs)
    }
}

/// The station's live running order: what is airing, item by item
public struct StationOrder: Codable, Equatable, Sendable {
    /// What is on, for a console to draw. A label for this broadcast rather than the name of a stored object
    public var name: String
    /// What the operator asked the station to play, in their own words. It keeps steering every refill until the station is put on air again, so a console should show it rather than only accept it
    public var brief: String?
    /// Who is hosting this broadcast, when it named somebody. Absent means whichever persona the station has on air
    public var personaId: String?
    /// What that host is called, resolved as the order is read so a console need not fetch the persona list to draw a name
    public var personaLabel: String?
    public var mode: StationMode
    public var onEnd: StationOnEnd
    /// Who built it: `import`, `chart` or `director`
    public var source: String
    /// Where more material is pulled from, when it came from a playlist
    public var sourcePluginId: String?
    public var sourcePlaylistId: String?
    /// The published chart this broadcast was built from, qualified with the plugin that offered it. Provenance rather than a binding: a chart is a fixed document, so it is read once and never topped up from
    public var sourceChartId: String?
    public var items: [StationOrderItem]

    public init(name: String, brief: String? = nil, personaId: String? = nil, personaLabel: String? = nil, mode: StationMode, onEnd: StationOnEnd, source: String, sourcePluginId: String? = nil, sourcePlaylistId: String? = nil, sourceChartId: String? = nil, items: [StationOrderItem]) {
        self.name = name
        self.brief = brief
        self.personaId = personaId
        self.personaLabel = personaLabel
        self.mode = mode
        self.onEnd = onEnd
        self.source = source
        self.sourcePluginId = sourcePluginId
        self.sourcePlaylistId = sourcePlaylistId
        self.sourceChartId = sourceChartId
        self.items = items
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case brief = "brief"
        case personaId = "personaId"
        case personaLabel = "personaLabel"
        case mode = "mode"
        case onEnd = "onEnd"
        case source = "source"
        case sourcePluginId = "sourcePluginId"
        case sourcePlaylistId = "sourcePlaylistId"
        case sourceChartId = "sourceChartId"
        case items = "items"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.brief = try container.decodeIfPresent(String.self, forKey: .brief)
        self.personaId = try container.decodeIfPresent(String.self, forKey: .personaId)
        self.personaLabel = try container.decodeIfPresent(String.self, forKey: .personaLabel)
        self.mode = try container.decode(StationMode.self, forKey: .mode)
        self.onEnd = try container.decode(StationOnEnd.self, forKey: .onEnd)
        self.source = try container.decode(String.self, forKey: .source)
        self.sourcePluginId = try container.decodeIfPresent(String.self, forKey: .sourcePluginId)
        self.sourcePlaylistId = try container.decodeIfPresent(String.self, forKey: .sourcePlaylistId)
        self.sourceChartId = try container.decodeIfPresent(String.self, forKey: .sourceChartId)
        self.items = try container.decode([StationOrderItem].self, forKey: .items)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.brief, forKey: .brief)
        try container.encodeIfPresent(self.personaId, forKey: .personaId)
        try container.encodeIfPresent(self.personaLabel, forKey: .personaLabel)
        try container.encode(self.mode, forKey: .mode)
        try container.encode(self.onEnd, forKey: .onEnd)
        try container.encode(self.source, forKey: .source)
        try container.encodeIfPresent(self.sourcePluginId, forKey: .sourcePluginId)
        try container.encodeIfPresent(self.sourcePlaylistId, forKey: .sourcePlaylistId)
        try container.encodeIfPresent(self.sourceChartId, forKey: .sourceChartId)
        try container.encode(self.items, forKey: .items)
    }
}

/// Whether this is a record or something the station says: an ident, a stinger, a talk break
public enum StationOrderItemKind: String, Codable, CaseIterable, Sendable {
    case track = "track"
    case segment = "segment"
}

public enum StationOrderItemSegmentState: String, Codable, CaseIterable, Sendable {
    case planned = "planned"
    case writing = "writing"
    case written = "written"
    case rendering = "rendering"
    case ready = "ready"
    case failed = "failed"
    case gone = "gone"
}

/// Which way round to play it. `countdown` opens on the lowest rank and ends on number one, which is the shape a chart show has; `ranked` walks the published document from the top; `unordered` leaves the sequence to the station's own artist spacing. Absent is `countdown`. Ignored without `chartId`
public enum PutOnAirInputChartOrder: String, Codable, CaseIterable, Sendable {
    case countdown = "countdown"
    case ranked = "ranked"
    case unordered = "unordered"
}
