// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `station.ck`.
public final class StationClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// Read station attention
    /// Everything wrong or waiting, worst first, each with the console page that can act on it
    public func readStationAttention() async throws -> StationAttention {
        let request = SdkRequest(method: "GET", path: ["station", "attention"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationAttention.self, from: response)
    }

    /// Read station checkup
    /// The loops the station runs and how much of the library it has looked at
    public func readStationCheckup() async throws -> StationCheckup {
        let request = SdkRequest(method: "GET", path: ["station", "checkup"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationCheckup.self, from: response)
    }

    /// Read station releases
    /// The releases this build contains and what each one changed, newest first
    public func readStationReleases() async throws -> StationReleases {
        let request = SdkRequest(method: "GET", path: ["station", "releases"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationReleases.self, from: response)
    }

    /// Check station releases
    /// Asks GitHub for newer releases now, and answers with what the station then knows
    public func checkStationReleases() async throws -> StationReleases {
        let request = SdkRequest(method: "POST", path: ["station", "releases", "check"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationReleases.self, from: response)
    }
}
