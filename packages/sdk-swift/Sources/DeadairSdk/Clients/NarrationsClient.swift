// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `narrations.ck`.
public final class NarrationsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List series
    /// Every series every installed narration plugin offers
    public func listSeries() async throws -> StationSeriesList {
        let request = SdkRequest(method: "GET", path: ["narrations", "series"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationSeriesList.self, from: response)
    }

    /// List pieces
    /// The pieces the station knows about, in their series' own order, with what it has done with each
    public func listPieces(query: StationPieceQuery? = nil) async throws -> StationPiecePage {
        var request = SdkRequest(method: "GET", path: ["narrations", "pieces"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(StationPiecePage.self, from: response)
    }

    /// Render piece
    /// Has one piece spoken now, rather than waiting for its slot to come near
    public func renderPiece(id: String) async throws -> StationPiece {
        let request = try SdkRequest(method: "POST", path: ["narrations", "pieces", http.segment(id), "render"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationPiece.self, from: response)
    }

    /// Refresh narrations
    /// Reads every series again, in the background, rather than waiting for the next scheduled refresh
    public func refreshNarrations() async throws {
        let request = SdkRequest(method: "POST", path: ["narrations", "refresh"])
        _ = try await http.execute(request)
    }
}
