// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Net.Http;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading;
using System.Threading.Tasks;
using System.Xml;
using MaroonedSoftware.Deadair.Sdk.Models;
using MaroonedSoftware.Deadair.Sdk.Runtime;

namespace MaroonedSoftware.Deadair.Sdk.Clients;

/// <summary>Operations declared in &lt;c&gt;director.ck&lt;/c&gt;.</summary>
public sealed class DirectorClient(SdkHttp http)
{
    /// <summary>
    /// Get station air
    /// What the station is airing, and whether it is driving at all
    /// </summary>
    public async Task<StationAir> GetStationAirAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("director", "air"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationAir>(response);
    }

    /// <summary>
    /// Put the station on air
    /// Puts the station on air, building the running order from a playlist read at this moment. What is playing finishes: changing the programming is not a reason to cut a listener off mid-track
    /// </summary>
    public async Task<StationAir> PutTheStationOnAirAsync(PutOnAirInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("director", "air"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationAir>(response);
    }

    /// <summary>
    /// Set the air mode
    /// Changes what puts the station on air: only while somebody is listening, or whenever there is a programme. Takes effect at once rather than at the next boundary
    /// </summary>
    public async Task<StationAir> SetTheAirModeAsync(SetStationAirInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Patch,
            http.Path("director", "air"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationAir>(response);
    }

    /// <summary>
    /// Get the running order
    /// The live running order, item by item, each saying where it has got to
    /// </summary>
    public async Task<StationOrder> GetTheRunningOrderAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("director", "air", "order"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }

    /// <summary>
    /// Recast the broadcast
    /// Changes who is presenting this broadcast. Breaks already written for it in the outgoing character are written again in the new one
    /// </summary>
    public async Task<StationOrder> RecastTheBroadcastAsync(SetStationHostInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("director", "air", "persona"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }

    /// <summary>
    /// Extend the running order
    /// Queues a refill and returns at once. Generating a set walks the catalog, and an operator pressing a button should not be held open through it
    /// </summary>
    public async Task ExtendTheRunningOrderAsync(ExtendStationInput body, CancellationToken cancellationToken = default)
    {
        await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("director", "air", "extend"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Replan the running order
    /// Queues a fresh set for everything the player is not already holding, and swaps it in once it exists. The old tail keeps playing until then, because emptying the running order first would take the station off air while the model was still choosing
    /// </summary>
    public async Task ReplanTheRunningOrderAsync(ReplanStationInput body, CancellationToken cancellationToken = default)
    {
        await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("director", "air", "replan"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Hold the station against the schedule
    /// Holds the running order against the schedule, so a block boundary does not take back what an operator put on. A takeover is otherwise stamped with whichever slot was in force and is replaced when that block ends, which is correct and gives nobody any warning
    /// </summary>
    public async Task<StationAir> HoldTheStationAgainstTheScheduleAsync(HoldStationInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Patch,
            http.Path("director", "air", "hold"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationAir>(response);
    }

    /// <summary>
    /// Release the station to the schedule
    /// Releases a hold, so the next block boundary changes the station over as it ordinarily would. A station with no hold is unchanged rather than refused
    /// </summary>
    public async Task<StationAir> ReleaseTheStationToTheScheduleAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("director", "air", "hold"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationAir>(response);
    }

    /// <summary>
    /// Shuffle the running order
    /// Shuffles the records not yet handed to the player, and plants the breaks again around the new sequence. The head is already in the player's hands and is left alone
    /// </summary>
    public async Task<StationOrder> ShuffleTheRunningOrderAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("director", "air", "shuffle"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }

    /// <summary>
    /// Add a segment to the running order
    /// Puts something the station says into the running order. A segment with no audio yet is refused here rather than accepted and skipped when it comes round, so an operator is told why it cannot play
    /// </summary>
    public async Task<StationOrder> AddASegmentToTheRunningOrderAsync(AddStationSegmentInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("director", "air", "segments"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }

    /// <summary>
    /// Add a record to the running order
    /// Puts a catalog record into the running order. A record whose audio is not local yet is refused here rather than accepted and held or skipped when its slot comes round, so an operator asking for a specific one is told why it cannot play. What makes this worth having on its own is undo: dropping an item only ever marks a segment, but a track is spliced out of the order entirely, so nothing could put one back until this existed
    /// </summary>
    public async Task<StationOrder> AddARecordToTheRunningOrderAsync(AddStationTrackInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("director", "air", "tracks"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }

    /// <summary>
    /// Move a running order item
    /// Moves an item. A position already handed to the player is refused rather than clamped
    /// </summary>
    public async Task<StationOrder> MoveARunningOrderItemAsync(string itemId, MoveStationItemInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Patch,
            http.Path("director", "air", "items", http.Segment(itemId)),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }

    /// <summary>
    /// Remove a running order item
    /// Drops an item that has not been handed to the player yet
    /// </summary>
    public async Task<StationOrder> RemoveARunningOrderItemAsync(string itemId, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("director", "air", "items", http.Segment(itemId)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StationOrder>(response);
    }
}
