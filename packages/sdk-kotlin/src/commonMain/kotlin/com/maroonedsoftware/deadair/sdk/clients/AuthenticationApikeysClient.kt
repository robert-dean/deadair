// Auto-generated by @contractkit/plugin-kotlin. Do not edit manually.
@file:OptIn(ExperimentalUuidApi::class)

package com.maroonedsoftware.deadair.sdk.clients

import com.maroonedsoftware.deadair.sdk.models.ApiKeyCreate
import com.maroonedsoftware.deadair.sdk.models.ApiKeyIssued
import com.maroonedsoftware.deadair.sdk.models.ApiKeyList
import com.maroonedsoftware.deadair.sdk.runtime.SdkHttp
import io.ktor.http.HttpMethod
import kotlin.uuid.ExperimentalUuidApi
import kotlin.uuid.Uuid

/** Operations declared in `authentication.apikeys.ck`. */
class AuthenticationApikeysClient(private val http: SdkHttp) {
    /**
     * List API keys
     * The signed-in account's API keys, newest first, including revoked and expired ones so the list says what was withdrawn and when
     */
    suspend fun listApiKeys(): ApiKeyList {
        val response = http.execute(HttpMethod.Get) {
            path("auth", "apikeys")
        }
        return http.decodeJson(response)
    }

    /**
     * Create API key
     * Issue a new API key for the signed-in account. The token is in this response and nowhere else, ever. Once the account has a strong second factor, this needs one verified in the last five minutes
     */
    suspend fun createApiKey(body: ApiKeyCreate): ApiKeyIssued {
        val response = http.execute(HttpMethod.Post) {
            path("auth", "apikeys")
            jsonBody(body, "application/json")
        }
        return http.decodeJson(response)
    }

    /**
     * Rotate API key
     * Give a key a new token, so the old one stops working at once. The key keeps its name, scopes and expiry. Needs the same recent second factor as creating one
     */
    suspend fun rotateApiKey(id: Uuid): ApiKeyIssued {
        val response = http.execute(HttpMethod.Post) {
            path("auth", "apikeys", segment(id), "rotate")
        }
        return http.decodeJson(response)
    }

    /**
     * Revoke API key
     * Revoke a key. Every request made with it is refused from the next one on. The key stays in the list, marked revoked
     */
    suspend fun revokeApiKey(id: Uuid) {
        http.execute(HttpMethod.Delete) {
            path("auth", "apikeys", segment(id))
        }
    }
}
