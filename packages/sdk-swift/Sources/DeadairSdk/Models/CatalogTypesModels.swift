// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// What the station has been told about a record. `neutral` is the absence of an opinion rather than
/// a middling one, and it is what rating something back to nothing means.
public enum Rating: String, Codable, CaseIterable, Sendable {
    case liked = "liked"
    case neutral = "neutral"
    case disliked = "disliked"
}

/// One provider's copy of a record, with whatever the station holds of it.
///
/// PER BINDING and never per track, which is the rule the whole page is built on: one canonical
/// record may bind to several copies inside one provider, those copies are different files with
/// different loudness and different cue points, and the one that airs is the one that was resolved.
/// Collapsing them would make "clear the audio" ambiguous about which file it took.
///
/// The failure columns are here rather than hidden because that is the question this page exists to
/// answer. A row with `attempts` and no `fetchedAt` is a remembered failure, and `lastError` with
/// `nextAttemptAt` is the whole of why a perfectly good-looking record will not play.
public struct TrackBinding: Codable, Equatable, Sendable {
    /// `track_sources.id`, which is also what the audio URL carries
    public var sourceId: UUID
    public var pluginId: String
    public var externalId: String
    /// False when the provider still knows the record but will not serve it here
    public var playable: Bool
    /// When the station gave up on this copy. Cleared by the next sync that sees it again
    public var missingAt: Date?
    /// `sync` if a playlist walk saw it, `discovered` if something looked it up
    public var origin: String
    public var bitrate: Int?
    public var format: String?
    public var lastSeenAt: Date?
    /// What the station holds of this copy, absent when nothing has ever fetched it.
    public var byteSize: Int?
    public var fetchedAt: Date?
    public var lastServedAt: Date?
    /// CONSECUTIVE failures. Reset by a fetch that works
    public var attempts: Int
    public var lastError: String?
    public var nextAttemptAt: Date?

    public init(sourceId: UUID, pluginId: String, externalId: String, playable: Bool, missingAt: Date? = nil, origin: String, bitrate: Int? = nil, format: String? = nil, lastSeenAt: Date? = nil, byteSize: Int? = nil, fetchedAt: Date? = nil, lastServedAt: Date? = nil, attempts: Int, lastError: String? = nil, nextAttemptAt: Date? = nil) {
        self.sourceId = sourceId
        self.pluginId = pluginId
        self.externalId = externalId
        self.playable = playable
        self.missingAt = missingAt
        self.origin = origin
        self.bitrate = bitrate
        self.format = format
        self.lastSeenAt = lastSeenAt
        self.byteSize = byteSize
        self.fetchedAt = fetchedAt
        self.lastServedAt = lastServedAt
        self.attempts = attempts
        self.lastError = lastError
        self.nextAttemptAt = nextAttemptAt
    }

    private enum CodingKeys: String, CodingKey {
        case sourceId = "sourceId"
        case pluginId = "pluginId"
        case externalId = "externalId"
        case playable = "playable"
        case missingAt = "missingAt"
        case origin = "origin"
        case bitrate = "bitrate"
        case format = "format"
        case lastSeenAt = "lastSeenAt"
        case byteSize = "byteSize"
        case fetchedAt = "fetchedAt"
        case lastServedAt = "lastServedAt"
        case attempts = "attempts"
        case lastError = "lastError"
        case nextAttemptAt = "nextAttemptAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.sourceId = try container.decode(UUID.self, forKey: .sourceId)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.externalId = try container.decode(String.self, forKey: .externalId)
        self.playable = try container.decode(Bool.self, forKey: .playable)
        self.missingAt = try container.decodeIfPresent(Date.self, forKey: .missingAt)
        self.origin = try container.decode(String.self, forKey: .origin)
        self.bitrate = try container.decodeIfPresent(Int.self, forKey: .bitrate)
        self.format = try container.decodeIfPresent(String.self, forKey: .format)
        self.lastSeenAt = try container.decodeIfPresent(Date.self, forKey: .lastSeenAt)
        self.byteSize = try container.decodeIfPresent(Int.self, forKey: .byteSize)
        self.fetchedAt = try container.decodeIfPresent(Date.self, forKey: .fetchedAt)
        self.lastServedAt = try container.decodeIfPresent(Date.self, forKey: .lastServedAt)
        self.attempts = try container.decode(Int.self, forKey: .attempts)
        self.lastError = try container.decodeIfPresent(String.self, forKey: .lastError)
        self.nextAttemptAt = try container.decodeIfPresent(Date.self, forKey: .nextAttemptAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.sourceId, forKey: .sourceId)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.externalId, forKey: .externalId)
        try container.encode(self.playable, forKey: .playable)
        try container.encodeIfPresent(self.missingAt, forKey: .missingAt)
        try container.encode(self.origin, forKey: .origin)
        try container.encodeIfPresent(self.bitrate, forKey: .bitrate)
        try container.encodeIfPresent(self.format, forKey: .format)
        try container.encodeIfPresent(self.lastSeenAt, forKey: .lastSeenAt)
        try container.encodeIfPresent(self.byteSize, forKey: .byteSize)
        try container.encodeIfPresent(self.fetchedAt, forKey: .fetchedAt)
        try container.encodeIfPresent(self.lastServedAt, forKey: .lastServedAt)
        try container.encode(self.attempts, forKey: .attempts)
        try container.encodeIfPresent(self.lastError, forKey: .lastError)
        try container.encodeIfPresent(self.nextAttemptAt, forKey: .nextAttemptAt)
    }
}

/// One provider's copy of a record, with whatever the station holds of it.
///
/// PER BINDING and never per track, which is the rule the whole page is built on: one canonical
/// record may bind to several copies inside one provider, those copies are different files with
/// different loudness and different cue points, and the one that airs is the one that was resolved.
/// Collapsing them would make "clear the audio" ambiguous about which file it took.
///
/// The failure columns are here rather than hidden because that is the question this page exists to
/// answer. A row with `attempts` and no `fetchedAt` is a remembered failure, and `lastError` with
/// `nextAttemptAt` is the whole of why a perfectly good-looking record will not play.
public struct TrackBindingInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// What the measurement sidecar made of a record.
///
/// `complete` is NOT `analyzedAt`, and the two are separate fields for a reason `0005_music.sql`
/// argues at length: a measurement of a truncated download is confident and wrong, so every reader in
/// the app filters on `complete` and a page that showed only a date would be reporting a record as
/// measured that nothing will use the measurement of.
public struct TrackAnalysis: Codable, Equatable, Sendable {
    public var schemaVersion: Int
    public var complete: Bool
    /// The measuring thing itself, which is not the plugin adapting it
    public var analyzer: String?
    public var analyzerPluginId: String?
    public var analyzedAt: Date?
    public var failedAt: Date?
    public var failureReason: String?

    public init(schemaVersion: Int, complete: Bool, analyzer: String? = nil, analyzerPluginId: String? = nil, analyzedAt: Date? = nil, failedAt: Date? = nil, failureReason: String? = nil) {
        self.schemaVersion = schemaVersion
        self.complete = complete
        self.analyzer = analyzer
        self.analyzerPluginId = analyzerPluginId
        self.analyzedAt = analyzedAt
        self.failedAt = failedAt
        self.failureReason = failureReason
    }

    private enum CodingKeys: String, CodingKey {
        case schemaVersion = "schemaVersion"
        case complete = "complete"
        case analyzer = "analyzer"
        case analyzerPluginId = "analyzerPluginId"
        case analyzedAt = "analyzedAt"
        case failedAt = "failedAt"
        case failureReason = "failureReason"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.schemaVersion = try container.decode(Int.self, forKey: .schemaVersion)
        self.complete = try container.decode(Bool.self, forKey: .complete)
        self.analyzer = try container.decodeIfPresent(String.self, forKey: .analyzer)
        self.analyzerPluginId = try container.decodeIfPresent(String.self, forKey: .analyzerPluginId)
        self.analyzedAt = try container.decodeIfPresent(Date.self, forKey: .analyzedAt)
        self.failedAt = try container.decodeIfPresent(Date.self, forKey: .failedAt)
        self.failureReason = try container.decodeIfPresent(String.self, forKey: .failureReason)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.schemaVersion, forKey: .schemaVersion)
        try container.encode(self.complete, forKey: .complete)
        try container.encodeIfPresent(self.analyzer, forKey: .analyzer)
        try container.encodeIfPresent(self.analyzerPluginId, forKey: .analyzerPluginId)
        try container.encodeIfPresent(self.analyzedAt, forKey: .analyzedAt)
        try container.encodeIfPresent(self.failedAt, forKey: .failedAt)
        try container.encodeIfPresent(self.failureReason, forKey: .failureReason)
    }
}

/// What the measurement sidecar made of a record.
///
/// `complete` is NOT `analyzedAt`, and the two are separate fields for a reason `0005_music.sql`
/// argues at length: a measurement of a truncated download is confident and wrong, so every reader in
/// the app filters on `complete` and a page that showed only a date would be reporting a record as
/// measured that nothing will use the measurement of.
public struct TrackAnalysisInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// One airing of a record, as this page needs it: when, and under which broadcast.
public struct TrackPlay: Codable, Equatable, Sendable {
    public var airedAt: Date
    public var broadcastId: UUID?
    /// What put it in the running order
    public var source: String

    public init(airedAt: Date, broadcastId: UUID? = nil, source: String) {
        self.airedAt = airedAt
        self.broadcastId = broadcastId
        self.source = source
    }

    private enum CodingKeys: String, CodingKey {
        case airedAt = "airedAt"
        case broadcastId = "broadcastId"
        case source = "source"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.airedAt = try container.decode(Date.self, forKey: .airedAt)
        self.broadcastId = try container.decodeIfPresent(UUID.self, forKey: .broadcastId)
        self.source = try container.decode(String.self, forKey: .source)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.airedAt, forKey: .airedAt)
        try container.encodeIfPresent(self.broadcastId, forKey: .broadcastId)
        try container.encode(self.source, forKey: .source)
    }
}

/// One airing of a record, as this page needs it: when, and under which broadcast.
public struct TrackPlayInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// What a clear actually did.
///
/// A count rather than a bare 204, because the interesting answers are the small ones: clearing the
/// audio of a record with three copies and being told `1` is the station saying two of them were
/// never here — which is a fact about the record and not about the button.
public struct TrackClearResult: Codable, Equatable, Sendable {
    public var trackId: UUID
    /// Rows this affected. Zero is an ordinary answer, not a failure
    public var cleared: Int
    /// What happened, in the words the console shows
    public var detail: String

    public init(trackId: UUID, cleared: Int, detail: String) {
        self.trackId = trackId
        self.cleared = cleared
        self.detail = detail
    }

    private enum CodingKeys: String, CodingKey {
        case trackId = "trackId"
        case cleared = "cleared"
        case detail = "detail"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.trackId = try container.decode(UUID.self, forKey: .trackId)
        self.cleared = try container.decode(Int.self, forKey: .cleared)
        self.detail = try container.decode(String.self, forKey: .detail)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.trackId, forKey: .trackId)
        try container.encode(self.cleared, forKey: .cleared)
        try container.encode(self.detail, forKey: .detail)
    }
}

/// What a clear actually did.
///
/// A count rather than a bare 204, because the interesting answers are the small ones: clearing the
/// audio of a record with three copies and being told `1` is the station saying two of them were
/// never here — which is a fact about the record and not about the button.
public struct TrackClearResultInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// Narrow a clear to one provider's answer, for the case where one source is wrong and the rest are
/// not. Absent clears every provider's.
public struct ClearEnrichmentQuery: Codable, Equatable, Sendable {
    public var provider: String?

    public init(provider: String? = nil) {
        self.provider = provider
    }

    private enum CodingKeys: String, CodingKey {
        case provider = "provider"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.provider = try container.decodeIfPresent(String.self, forKey: .provider)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.provider, forKey: .provider)
    }
}

/// What an artist or album list is ordered BY, where `Pagination.sort` says only which direction.
///
///   name    the default, and the only key every row here has
///   albums  how many records the station holds of them. Artists only
///   tracks  how many songs. Artists only
///   year    when the record came out. Albums only
///   rating  the operator's own opinion
///
/// One enum for both lists rather than two, because the alternative is a second near-identical
/// contract whose only content is which two keys it drops. A key the row cannot answer falls back to
/// name order rather than failing: an ordering nobody can serve is a page an operator cannot open.
public enum CatalogSort: String, Codable, CaseIterable, Sendable {
    case name = "name"
    case albums = "albums"
    case tracks = "tracks"
    case year = "year"
    case rating = "rating"
}

/// Which records to show, by what the station has of them rather than by what they are.
///
///   cached      the audio is on this machine, so it can be committed to the running order now
///   uncached    it is not, which for most of a library is ordinary rather than wrong
///   unmeasured  no trustworthy measurement, so no cue points and no level decided before air
///   benched     every copy written off, which is the one state that means it CANNOT air
///   failing     a fetch has failed and is backing off. Not benched yet, and often the state before it
public enum TrackState: String, Codable, CaseIterable, Sendable {
    case cached = "cached"
    case uncached = "uncached"
    case unmeasured = "unmeasured"
    case benched = "benched"
    case failing = "failing"
}

/// What a track list is ordered BY. Its own enum for `TrackQuery`'s own reason: none of these keys
/// means anything about an artist, and `name` is spelled `title` on a song.
///
/// `state` is deliberately absent. It is three independent booleans rather than one column, so there
/// is no ordering of it an operator would agree with: a benched record and an unmeasured one are not
/// more or less than each other.
public enum TrackSort: String, Codable, CaseIterable, Sendable {
    case title = "title"
    case artist = "artist"
    case album = "album"
    case year = "year"
    case duration = "duration"
    case rating = "rating"
}

/// How much of the library is in each state, over the whole filtered set rather than this page.
///
/// The aggregate is what an operator reads first — "13 of 581 measured" is the sentence that made
/// [analysis-queue-ordering](https://github.com/robert-dean/deadair/discussions/5) necessary, and it was a psql query then. `total` is the
/// same number as `meta.total` when nothing is filtered, and is repeated here so the counts can be
/// read as N of M without reaching into the pager.
public struct TrackStateCounts: Codable, Equatable, Sendable {
    public var total: Int
    public var cached: Int
    public var measured: Int
    public var enriched: Int
    public var benched: Int
    public var failing: Int

    public init(total: Int, cached: Int, measured: Int, enriched: Int, benched: Int, failing: Int) {
        self.total = total
        self.cached = cached
        self.measured = measured
        self.enriched = enriched
        self.benched = benched
        self.failing = failing
    }

    private enum CodingKeys: String, CodingKey {
        case total = "total"
        case cached = "cached"
        case measured = "measured"
        case enriched = "enriched"
        case benched = "benched"
        case failing = "failing"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.total = try container.decode(Int.self, forKey: .total)
        self.cached = try container.decode(Int.self, forKey: .cached)
        self.measured = try container.decode(Int.self, forKey: .measured)
        self.enriched = try container.decode(Int.self, forKey: .enriched)
        self.benched = try container.decode(Int.self, forKey: .benched)
        self.failing = try container.decode(Int.self, forKey: .failing)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.total, forKey: .total)
        try container.encode(self.cached, forKey: .cached)
        try container.encode(self.measured, forKey: .measured)
        try container.encode(self.enriched, forKey: .enriched)
        try container.encode(self.benched, forKey: .benched)
        try container.encode(self.failing, forKey: .failing)
    }
}

/// How much of the library is in each state, over the whole filtered set rather than this page.
///
/// The aggregate is what an operator reads first — "13 of 581 measured" is the sentence that made
/// [analysis-queue-ordering](https://github.com/robert-dean/deadair/discussions/5) necessary, and it was a psql query then. `total` is the
/// same number as `meta.total` when nothing is filtered, and is repeated here so the counts can be
/// read as N of M without reaching into the pager.
public struct TrackStateCountsInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

public struct EnrichmentExternalId: Codable, Equatable, Sendable {
    /// e.g. `musicbrainz`, `wikidata`
    public var source: String
    public var id: String

    public init(source: String, id: String) {
        self.source = source
        self.id = id
    }

    private enum CodingKeys: String, CodingKey {
        case source = "source"
        case id = "id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.source = try container.decode(String.self, forKey: .source)
        self.id = try container.decode(String.self, forKey: .id)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.source, forKey: .source)
        try container.encode(self.id, forKey: .id)
    }
}

/// Narrowed to http(s) by the host before it is stored, since the console renders these as
/// something a human clicks.
public struct EnrichmentLink: Codable, Equatable, Sendable {
    public var label: String
    public var url: String

    public init(label: String, url: String) {
        self.label = label
        self.url = url
    }

    private enum CodingKeys: String, CodingKey {
        case label = "label"
        case url = "url"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.label = try container.decode(String.self, forKey: .label)
        self.url = try container.decode(String.self, forKey: .url)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.url, forKey: .url)
    }
}

/// One thing the station believes, and the words it read that say so. Extracted by the host out of
/// an article a plugin handed over, rather than said by any plugin: `sourceUrl` is where a person
/// checks it and `sourceQuote` is the span that supports it, and neither is ever absent.
public struct FactClaim: Codable, Equatable, Sendable {
    public var id: UUID
    /// One sentence, as the DJ would say it
    public var claim: String
    public var category: String
    /// `lead` for the article's own opening, `model` for what a model found
    public var source: String
    public var sourceProvider: String
    public var sourceUrl: String
    public var sourceQuote: String
    public var confidence: Double?
    public var model: String?
    /// Absent means never said on air
    public var lastUsedAt: Date?

    public init(id: UUID, claim: String, category: String, source: String, sourceProvider: String, sourceUrl: String, sourceQuote: String, confidence: Double? = nil, model: String? = nil, lastUsedAt: Date? = nil) {
        self.id = id
        self.claim = claim
        self.category = category
        self.source = source
        self.sourceProvider = sourceProvider
        self.sourceUrl = sourceUrl
        self.sourceQuote = sourceQuote
        self.confidence = confidence
        self.model = model
        self.lastUsedAt = lastUsedAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case claim = "claim"
        case category = "category"
        case source = "source"
        case sourceProvider = "sourceProvider"
        case sourceUrl = "sourceUrl"
        case sourceQuote = "sourceQuote"
        case confidence = "confidence"
        case model = "model"
        case lastUsedAt = "lastUsedAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.claim = try container.decode(String.self, forKey: .claim)
        self.category = try container.decode(String.self, forKey: .category)
        self.source = try container.decode(String.self, forKey: .source)
        self.sourceProvider = try container.decode(String.self, forKey: .sourceProvider)
        self.sourceUrl = try container.decode(String.self, forKey: .sourceUrl)
        self.sourceQuote = try container.decode(String.self, forKey: .sourceQuote)
        self.confidence = try container.decodeIfPresent(Double.self, forKey: .confidence)
        self.model = try container.decodeIfPresent(String.self, forKey: .model)
        self.lastUsedAt = try container.decodeIfPresent(Date.self, forKey: .lastUsedAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.claim, forKey: .claim)
        try container.encode(self.category, forKey: .category)
        try container.encode(self.source, forKey: .source)
        try container.encode(self.sourceProvider, forKey: .sourceProvider)
        try container.encode(self.sourceUrl, forKey: .sourceUrl)
        try container.encode(self.sourceQuote, forKey: .sourceQuote)
        try container.encodeIfPresent(self.confidence, forKey: .confidence)
        try container.encodeIfPresent(self.model, forKey: .model)
        try container.encodeIfPresent(self.lastUsedAt, forKey: .lastUsedAt)
    }
}

/// One thing the station believes, and the words it read that say so. Extracted by the host out of
/// an article a plugin handed over, rather than said by any plugin: `sourceUrl` is where a person
/// checks it and `sourceQuote` is the span that supports it, and neither is ever absent.
public struct FactClaimInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// Rate an artist, a record or a song. Ratings are absolute: a dislike anywhere above a track
/// excludes it, and nothing the station programmes may turn that off.
public struct RateInput: Codable, Equatable, Sendable {
    public var rating: Rating

    public init(rating: Rating) {
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.rating = try container.decode(Rating.self, forKey: .rating)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.rating, forKey: .rating)
    }
}

/// The canonical work, not a binding to a provider. `deadair.artists` minus the columns that
/// only ingest cares about: `artist_key` is a match key, and a row with `merged_into_id` set is
/// never read out at all.
///
/// `imageUrl` on both contracts below is one field with two spellings. An absolute URL is the
/// provider's own, still hotlinked because nothing has cached it yet; a relative `art/<uuid>` is
/// the station's copy, to be resolved against the API base the client already configures (the API
/// mounts at the root and does not know the `/api` prefix the edge adds). Prefer the local one by
/// doing nothing: the switch happens server-side as soon as the art cache pass has the bytes.
public struct Artist: Codable, Equatable, Sendable {
    public var id: UUID
    public var name: String
    /// MusicBrainz artist id, absent until enrichment resolves one
    public var mbid: UUID?
    /// Absolute upstream URL, or an API-relative path to the local copy
    public var imageUrl: String?
    public var rating: Rating
    /// Unmerged albums credited to this artist
    public var albumCount: Int
    /// Unmerged tracks credited to this artist
    public var trackCount: Int

    public init(id: UUID, name: String, mbid: UUID? = nil, imageUrl: String? = nil, rating: Rating = .neutral, albumCount: Int, trackCount: Int) {
        self.id = id
        self.name = name
        self.mbid = mbid
        self.imageUrl = imageUrl
        self.rating = rating
        self.albumCount = albumCount
        self.trackCount = trackCount
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case mbid = "mbid"
        case imageUrl = "imageUrl"
        case rating = "rating"
        case albumCount = "albumCount"
        case trackCount = "trackCount"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.mbid = try container.decodeIfPresent(UUID.self, forKey: .mbid)
        self.imageUrl = try container.decodeIfPresent(String.self, forKey: .imageUrl)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
        self.albumCount = try container.decode(Int.self, forKey: .albumCount)
        self.trackCount = try container.decode(Int.self, forKey: .trackCount)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.mbid, forKey: .mbid)
        try container.encodeIfPresent(self.imageUrl, forKey: .imageUrl)
        try container.encode(self.rating, forKey: .rating)
        try container.encode(self.albumCount, forKey: .albumCount)
        try container.encode(self.trackCount, forKey: .trackCount)
    }
}

/// The canonical work, not a binding to a provider. `deadair.artists` minus the columns that
/// only ingest cares about: `artist_key` is a match key, and a row with `merged_into_id` set is
/// never read out at all.
///
/// `imageUrl` on both contracts below is one field with two spellings. An absolute URL is the
/// provider's own, still hotlinked because nothing has cached it yet; a relative `art/<uuid>` is
/// the station's copy, to be resolved against the API base the client already configures (the API
/// mounts at the root and does not know the `/api` prefix the edge adds). Prefer the local one by
/// doing nothing: the switch happens server-side as soon as the art cache pass has the bytes.
public struct ArtistInput: Codable, Equatable, Sendable {
    public var name: String
    /// MusicBrainz artist id, absent until enrichment resolves one
    public var mbid: UUID?
    /// Absolute upstream URL, or an API-relative path to the local copy
    public var imageUrl: String?
    public var rating: Rating

    public init(name: String, mbid: UUID? = nil, imageUrl: String? = nil, rating: Rating = .neutral) {
        self.name = name
        self.mbid = mbid
        self.imageUrl = imageUrl
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case mbid = "mbid"
        case imageUrl = "imageUrl"
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.mbid = try container.decodeIfPresent(UUID.self, forKey: .mbid)
        self.imageUrl = try container.decodeIfPresent(String.self, forKey: .imageUrl)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.mbid, forKey: .mbid)
        try container.encodeIfPresent(self.imageUrl, forKey: .imageUrl)
        try container.encode(self.rating, forKey: .rating)
    }
}

public struct Album: Codable, Equatable, Sendable {
    public var id: UUID
    public var name: String
    public var artistId: UUID
    /// Joined, so a list renders without a second request per row
    public var artistName: String
    /// MusicBrainz release-group id, absent until enrichment resolves one
    public var mbid: UUID?
    public var year: Int?
    /// Absolute upstream URL, or an API-relative path to the local copy
    public var imageUrl: String?
    public var rating: Rating
    public var trackCount: Int

    public init(id: UUID, name: String, artistId: UUID, artistName: String, mbid: UUID? = nil, year: Int? = nil, imageUrl: String? = nil, rating: Rating = .neutral, trackCount: Int) {
        self.id = id
        self.name = name
        self.artistId = artistId
        self.artistName = artistName
        self.mbid = mbid
        self.year = year
        self.imageUrl = imageUrl
        self.rating = rating
        self.trackCount = trackCount
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case artistId = "artistId"
        case artistName = "artistName"
        case mbid = "mbid"
        case year = "year"
        case imageUrl = "imageUrl"
        case rating = "rating"
        case trackCount = "trackCount"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.artistId = try container.decode(UUID.self, forKey: .artistId)
        self.artistName = try container.decode(String.self, forKey: .artistName)
        self.mbid = try container.decodeIfPresent(UUID.self, forKey: .mbid)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.imageUrl = try container.decodeIfPresent(String.self, forKey: .imageUrl)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
        self.trackCount = try container.decode(Int.self, forKey: .trackCount)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.artistId, forKey: .artistId)
        try container.encode(self.artistName, forKey: .artistName)
        try container.encodeIfPresent(self.mbid, forKey: .mbid)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.imageUrl, forKey: .imageUrl)
        try container.encode(self.rating, forKey: .rating)
        try container.encode(self.trackCount, forKey: .trackCount)
    }
}

public struct AlbumInput: Codable, Equatable, Sendable {
    public var name: String
    /// MusicBrainz release-group id, absent until enrichment resolves one
    public var mbid: UUID?
    public var year: Int?
    /// Absolute upstream URL, or an API-relative path to the local copy
    public var imageUrl: String?
    public var rating: Rating

    public init(name: String, mbid: UUID? = nil, year: Int? = nil, imageUrl: String? = nil, rating: Rating = .neutral) {
        self.name = name
        self.mbid = mbid
        self.year = year
        self.imageUrl = imageUrl
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case mbid = "mbid"
        case year = "year"
        case imageUrl = "imageUrl"
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.mbid = try container.decodeIfPresent(UUID.self, forKey: .mbid)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.imageUrl = try container.decodeIfPresent(String.self, forKey: .imageUrl)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.mbid, forKey: .mbid)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.imageUrl, forKey: .imageUrl)
        try container.encode(self.rating, forKey: .rating)
    }
}

public struct Track: Codable, Equatable, Sendable {
    public var id: UUID
    public var title: String
    public var artistId: UUID
    public var artistName: String
    /// Absent on a single ingested outside any release: `tracks.album_id` is nullable
    public var albumId: UUID?
    public var albumName: String?
    /// The record's cover, in the two spellings `Album.imageUrl` has. Nothing hangs art off a recording
    public var albumImageUrl: String?
    /// Display credit as written on the release ("X feat. Y"), not a join key
    public var artists: String
    public var genre: String?
    public var year: Int?
    public var durationMs: Int?
    public var rating: Rating

    public init(id: UUID, title: String, artistId: UUID, artistName: String, albumId: UUID? = nil, albumName: String? = nil, albumImageUrl: String? = nil, artists: String, genre: String? = nil, year: Int? = nil, durationMs: Int? = nil, rating: Rating = .neutral) {
        self.id = id
        self.title = title
        self.artistId = artistId
        self.artistName = artistName
        self.albumId = albumId
        self.albumName = albumName
        self.albumImageUrl = albumImageUrl
        self.artists = artists
        self.genre = genre
        self.year = year
        self.durationMs = durationMs
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case title = "title"
        case artistId = "artistId"
        case artistName = "artistName"
        case albumId = "albumId"
        case albumName = "albumName"
        case albumImageUrl = "albumImageUrl"
        case artists = "artists"
        case genre = "genre"
        case year = "year"
        case durationMs = "durationMs"
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.title = try container.decode(String.self, forKey: .title)
        self.artistId = try container.decode(UUID.self, forKey: .artistId)
        self.artistName = try container.decode(String.self, forKey: .artistName)
        self.albumId = try container.decodeIfPresent(UUID.self, forKey: .albumId)
        self.albumName = try container.decodeIfPresent(String.self, forKey: .albumName)
        self.albumImageUrl = try container.decodeIfPresent(String.self, forKey: .albumImageUrl)
        self.artists = try container.decode(String.self, forKey: .artists)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artistId, forKey: .artistId)
        try container.encode(self.artistName, forKey: .artistName)
        try container.encodeIfPresent(self.albumId, forKey: .albumId)
        try container.encodeIfPresent(self.albumName, forKey: .albumName)
        try container.encodeIfPresent(self.albumImageUrl, forKey: .albumImageUrl)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.rating, forKey: .rating)
    }
}

public struct TrackInput: Codable, Equatable, Sendable {
    public var title: String
    /// Display credit as written on the release ("X feat. Y"), not a join key
    public var artists: String
    public var genre: String?
    public var year: Int?
    public var durationMs: Int?
    public var rating: Rating

    public init(title: String, artists: String, genre: String? = nil, year: Int? = nil, durationMs: Int? = nil, rating: Rating = .neutral) {
        self.title = title
        self.artists = artists
        self.genre = genre
        self.year = year
        self.durationMs = durationMs
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case title = "title"
        case artists = "artists"
        case genre = "genre"
        case year = "year"
        case durationMs = "durationMs"
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode(String.self, forKey: .artists)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.rating, forKey: .rating)
    }
}

/// Pagination plus a name filter. Every list operation here takes it, so the console's search box
/// narrows server-side rather than filtering one page client-side and lying about the total.
public struct CatalogQuery: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    /// The total number of items
    public var total: Int
    public var search: String?
    public var sortBy: CatalogSort?

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, total: Int, search: String? = nil, sortBy: CatalogSort? = nil) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.total = total
        self.search = search
        self.sortBy = sortBy
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case total = "total"
        case search = "search"
        case sortBy = "sortBy"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.total = try container.decode(Int.self, forKey: .total)
        self.search = try container.decodeIfPresent(String.self, forKey: .search)
        self.sortBy = try container.decodeIfPresent(CatalogSort.self, forKey: .sortBy)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encode(self.total, forKey: .total)
        try container.encodeIfPresent(self.search, forKey: .search)
        try container.encodeIfPresent(self.sortBy, forKey: .sortBy)
    }
}

/// Pagination plus a name filter. Every list operation here takes it, so the console's search box
/// narrows server-side rather than filtering one page client-side and lying about the total.
public struct CatalogQueryInput: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    public var search: String?
    public var sortBy: CatalogSort?

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, search: String? = nil, sortBy: CatalogSort? = nil) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.search = search
        self.sortBy = sortBy
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case search = "search"
        case sortBy = "sortBy"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.search = try container.decodeIfPresent(String.self, forKey: .search)
        self.sortBy = try container.decodeIfPresent(CatalogSort.self, forKey: .sortBy)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encodeIfPresent(self.search, forKey: .search)
        try container.encodeIfPresent(self.sortBy, forKey: .sortBy)
    }
}

/// `releaseDate` is a string and not `datetime` because it is a partial date: MusicBrainz answers
/// `1997`, `1997-06` or `1997-06-24` depending on what is actually known about the release, and the
/// SDK types it the same way. A `datetime` would reject the first two or invent a day and a time
/// for them, which is a precision the source never claimed.
public struct TrackEnrichmentData: Codable, Equatable, Sendable {
    public var artist: String?
    public var title: String?
    public var album: String?
    public var year: Int?
    public var releaseDate: String?
    public var genres: [String]?
    public var moods: [String]?
    public var biography: String?
    /// Short lines, each independently speakable
    public var facts: [String]?
    /// Not an integer: a tempo a source measured rather than declared is fractional
    public var bpm: Double?
    public var musicalKey: String?
    public var label: String?
    public var isrc: String?
    public var artworkUrl: String?
    public var externalIds: [EnrichmentExternalId]?
    public var links: [EnrichmentLink]?
    /// What the plugin said that the SDK has no field for. Per provider only: the merged view drops it
    public var extra: [String: JSONValue]?

    public init(artist: String? = nil, title: String? = nil, album: String? = nil, year: Int? = nil, releaseDate: String? = nil, genres: [String]? = nil, moods: [String]? = nil, biography: String? = nil, facts: [String]? = nil, bpm: Double? = nil, musicalKey: String? = nil, label: String? = nil, isrc: String? = nil, artworkUrl: String? = nil, externalIds: [EnrichmentExternalId]? = nil, links: [EnrichmentLink]? = nil, extra: [String: JSONValue]? = nil) {
        self.artist = artist
        self.title = title
        self.album = album
        self.year = year
        self.releaseDate = releaseDate
        self.genres = genres
        self.moods = moods
        self.biography = biography
        self.facts = facts
        self.bpm = bpm
        self.musicalKey = musicalKey
        self.label = label
        self.isrc = isrc
        self.artworkUrl = artworkUrl
        self.externalIds = externalIds
        self.links = links
        self.extra = extra
    }

    private enum CodingKeys: String, CodingKey {
        case artist = "artist"
        case title = "title"
        case album = "album"
        case year = "year"
        case releaseDate = "releaseDate"
        case genres = "genres"
        case moods = "moods"
        case biography = "biography"
        case facts = "facts"
        case bpm = "bpm"
        case musicalKey = "musicalKey"
        case label = "label"
        case isrc = "isrc"
        case artworkUrl = "artworkUrl"
        case externalIds = "externalIds"
        case links = "links"
        case extra = "extra"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.artist = try container.decodeIfPresent(String.self, forKey: .artist)
        self.title = try container.decodeIfPresent(String.self, forKey: .title)
        self.album = try container.decodeIfPresent(String.self, forKey: .album)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.releaseDate = try container.decodeIfPresent(String.self, forKey: .releaseDate)
        self.genres = try container.decodeIfPresent([String].self, forKey: .genres)
        self.moods = try container.decodeIfPresent([String].self, forKey: .moods)
        self.biography = try container.decodeIfPresent(String.self, forKey: .biography)
        self.facts = try container.decodeIfPresent([String].self, forKey: .facts)
        self.bpm = try container.decodeIfPresent(Double.self, forKey: .bpm)
        self.musicalKey = try container.decodeIfPresent(String.self, forKey: .musicalKey)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
        self.isrc = try container.decodeIfPresent(String.self, forKey: .isrc)
        self.artworkUrl = try container.decodeIfPresent(String.self, forKey: .artworkUrl)
        self.externalIds = try container.decodeIfPresent([EnrichmentExternalId].self, forKey: .externalIds)
        self.links = try container.decodeIfPresent([EnrichmentLink].self, forKey: .links)
        self.extra = try container.decodeIfPresent([String: JSONValue].self, forKey: .extra)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.artist, forKey: .artist)
        try container.encodeIfPresent(self.title, forKey: .title)
        try container.encodeIfPresent(self.album, forKey: .album)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.releaseDate, forKey: .releaseDate)
        try container.encodeIfPresent(self.genres, forKey: .genres)
        try container.encodeIfPresent(self.moods, forKey: .moods)
        try container.encodeIfPresent(self.biography, forKey: .biography)
        try container.encodeIfPresent(self.facts, forKey: .facts)
        try container.encodeIfPresent(self.bpm, forKey: .bpm)
        try container.encodeIfPresent(self.musicalKey, forKey: .musicalKey)
        try container.encodeIfPresent(self.label, forKey: .label)
        try container.encodeIfPresent(self.isrc, forKey: .isrc)
        try container.encodeIfPresent(self.artworkUrl, forKey: .artworkUrl)
        try container.encodeIfPresent(self.externalIds, forKey: .externalIds)
        try container.encodeIfPresent(self.links, forKey: .links)
        try container.encodeIfPresent(self.extra, forKey: .extra)
    }
}

public struct ArtistEnrichmentData: Codable, Equatable, Sendable {
    public var name: String?
    public var biography: String?
    public var imageUrl: String?
    public var genres: [String]?
    public var facts: [String]?
    public var externalIds: [EnrichmentExternalId]?
    public var links: [EnrichmentLink]?
    public var extra: [String: JSONValue]?

    public init(name: String? = nil, biography: String? = nil, imageUrl: String? = nil, genres: [String]? = nil, facts: [String]? = nil, externalIds: [EnrichmentExternalId]? = nil, links: [EnrichmentLink]? = nil, extra: [String: JSONValue]? = nil) {
        self.name = name
        self.biography = biography
        self.imageUrl = imageUrl
        self.genres = genres
        self.facts = facts
        self.externalIds = externalIds
        self.links = links
        self.extra = extra
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case biography = "biography"
        case imageUrl = "imageUrl"
        case genres = "genres"
        case facts = "facts"
        case externalIds = "externalIds"
        case links = "links"
        case extra = "extra"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.biography = try container.decodeIfPresent(String.self, forKey: .biography)
        self.imageUrl = try container.decodeIfPresent(String.self, forKey: .imageUrl)
        self.genres = try container.decodeIfPresent([String].self, forKey: .genres)
        self.facts = try container.decodeIfPresent([String].self, forKey: .facts)
        self.externalIds = try container.decodeIfPresent([EnrichmentExternalId].self, forKey: .externalIds)
        self.links = try container.decodeIfPresent([EnrichmentLink].self, forKey: .links)
        self.extra = try container.decodeIfPresent([String: JSONValue].self, forKey: .extra)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.biography, forKey: .biography)
        try container.encodeIfPresent(self.imageUrl, forKey: .imageUrl)
        try container.encodeIfPresent(self.genres, forKey: .genres)
        try container.encodeIfPresent(self.facts, forKey: .facts)
        try container.encodeIfPresent(self.externalIds, forKey: .externalIds)
        try container.encodeIfPresent(self.links, forKey: .links)
        try container.encodeIfPresent(self.extra, forKey: .extra)
    }
}

public struct AlbumEnrichmentData: Codable, Equatable, Sendable {
    public var name: String?
    /// The record's own credit, which is not always the track's
    public var artist: String?
    public var year: Int?
    /// Partial, exactly as on TrackEnrichmentData
    public var releaseDate: String?
    public var label: String?
    public var genres: [String]?
    public var facts: [String]?
    public var artworkUrl: String?
    public var externalIds: [EnrichmentExternalId]?
    public var links: [EnrichmentLink]?
    public var extra: [String: JSONValue]?

    public init(name: String? = nil, artist: String? = nil, year: Int? = nil, releaseDate: String? = nil, label: String? = nil, genres: [String]? = nil, facts: [String]? = nil, artworkUrl: String? = nil, externalIds: [EnrichmentExternalId]? = nil, links: [EnrichmentLink]? = nil, extra: [String: JSONValue]? = nil) {
        self.name = name
        self.artist = artist
        self.year = year
        self.releaseDate = releaseDate
        self.label = label
        self.genres = genres
        self.facts = facts
        self.artworkUrl = artworkUrl
        self.externalIds = externalIds
        self.links = links
        self.extra = extra
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case artist = "artist"
        case year = "year"
        case releaseDate = "releaseDate"
        case label = "label"
        case genres = "genres"
        case facts = "facts"
        case artworkUrl = "artworkUrl"
        case externalIds = "externalIds"
        case links = "links"
        case extra = "extra"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.artist = try container.decodeIfPresent(String.self, forKey: .artist)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.releaseDate = try container.decodeIfPresent(String.self, forKey: .releaseDate)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
        self.genres = try container.decodeIfPresent([String].self, forKey: .genres)
        self.facts = try container.decodeIfPresent([String].self, forKey: .facts)
        self.artworkUrl = try container.decodeIfPresent(String.self, forKey: .artworkUrl)
        self.externalIds = try container.decodeIfPresent([EnrichmentExternalId].self, forKey: .externalIds)
        self.links = try container.decodeIfPresent([EnrichmentLink].self, forKey: .links)
        self.extra = try container.decodeIfPresent([String: JSONValue].self, forKey: .extra)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.artist, forKey: .artist)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.releaseDate, forKey: .releaseDate)
        try container.encodeIfPresent(self.label, forKey: .label)
        try container.encodeIfPresent(self.genres, forKey: .genres)
        try container.encodeIfPresent(self.facts, forKey: .facts)
        try container.encodeIfPresent(self.artworkUrl, forKey: .artworkUrl)
        try container.encodeIfPresent(self.externalIds, forKey: .externalIds)
        try container.encodeIfPresent(self.links, forKey: .links)
        try container.encodeIfPresent(self.extra, forKey: .extra)
    }
}

/// One page of artists, with the totals the request was counted against
public struct ArtistPage: Codable, Equatable, Sendable {
    public var meta: Pagination
    public var data: [Artist]

    public init(meta: Pagination, data: [Artist]) {
        self.meta = meta
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case meta = "meta"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.meta = try container.decode(Pagination.self, forKey: .meta)
        self.data = try container.decode([Artist].self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.meta, forKey: .meta)
        try container.encode(self.data, forKey: .data)
    }
}

/// One page of artists, with the totals the request was counted against
public struct ArtistPageInput: Codable, Equatable, Sendable {
    public var meta: PaginationInput
    public var data: [ArtistInput]

    public init(meta: PaginationInput, data: [ArtistInput]) {
        self.meta = meta
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case meta = "meta"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.meta = try container.decode(PaginationInput.self, forKey: .meta)
        self.data = try container.decode([ArtistInput].self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.meta, forKey: .meta)
        try container.encode(self.data, forKey: .data)
    }
}

/// One page of albums
public struct AlbumPage: Codable, Equatable, Sendable {
    public var meta: Pagination
    public var data: [Album]

    public init(meta: Pagination, data: [Album]) {
        self.meta = meta
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case meta = "meta"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.meta = try container.decode(Pagination.self, forKey: .meta)
        self.data = try container.decode([Album].self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.meta, forKey: .meta)
        try container.encode(self.data, forKey: .data)
    }
}

/// One page of albums
public struct AlbumPageInput: Codable, Equatable, Sendable {
    public var meta: PaginationInput
    public var data: [AlbumInput]

    public init(meta: PaginationInput, data: [AlbumInput]) {
        self.meta = meta
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case meta = "meta"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.meta = try container.decode(PaginationInput.self, forKey: .meta)
        self.data = try container.decode([AlbumInput].self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.meta, forKey: .meta)
        try container.encode(self.data, forKey: .data)
    }
}

/// Everything one record has accumulated, in one read.
///
/// The enrichment is deliberately NOT here. It has its own operation already, answering
/// `TrackEnrichmentDetail` with every provider's payload and the station's own sourced claims, and
/// the console draws it through the same panel the list uses. One enrichment shape rather than two.
public struct TrackDetail: Codable, Equatable, Sendable {
    public var id: UUID
    public var title: String
    public var artistId: UUID
    public var artistName: String
    /// Absent on a single ingested outside any release: `tracks.album_id` is nullable
    public var albumId: UUID?
    public var albumName: String?
    /// The record's cover, in the two spellings `Album.imageUrl` has. Nothing hangs art off a recording
    public var albumImageUrl: String?
    /// Display credit as written on the release ("X feat. Y"), not a join key
    public var artists: String
    public var genre: String?
    public var year: Int?
    public var durationMs: Int?
    public var rating: Rating
    public var bindings: [TrackBinding]
    /// Absent for a record the walk has not reached
    public var analysis: TrackAnalysis?
    /// The most recent airings, newest first
    public var plays: [TrackPlay]
    /// How many times in all, which the list above is only the head of
    public var playCount: Int

    public init(id: UUID, title: String, artistId: UUID, artistName: String, albumId: UUID? = nil, albumName: String? = nil, albumImageUrl: String? = nil, artists: String, genre: String? = nil, year: Int? = nil, durationMs: Int? = nil, rating: Rating = .neutral, bindings: [TrackBinding], analysis: TrackAnalysis? = nil, plays: [TrackPlay], playCount: Int) {
        self.id = id
        self.title = title
        self.artistId = artistId
        self.artistName = artistName
        self.albumId = albumId
        self.albumName = albumName
        self.albumImageUrl = albumImageUrl
        self.artists = artists
        self.genre = genre
        self.year = year
        self.durationMs = durationMs
        self.rating = rating
        self.bindings = bindings
        self.analysis = analysis
        self.plays = plays
        self.playCount = playCount
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case title = "title"
        case artistId = "artistId"
        case artistName = "artistName"
        case albumId = "albumId"
        case albumName = "albumName"
        case albumImageUrl = "albumImageUrl"
        case artists = "artists"
        case genre = "genre"
        case year = "year"
        case durationMs = "durationMs"
        case rating = "rating"
        case bindings = "bindings"
        case analysis = "analysis"
        case plays = "plays"
        case playCount = "playCount"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.title = try container.decode(String.self, forKey: .title)
        self.artistId = try container.decode(UUID.self, forKey: .artistId)
        self.artistName = try container.decode(String.self, forKey: .artistName)
        self.albumId = try container.decodeIfPresent(UUID.self, forKey: .albumId)
        self.albumName = try container.decodeIfPresent(String.self, forKey: .albumName)
        self.albumImageUrl = try container.decodeIfPresent(String.self, forKey: .albumImageUrl)
        self.artists = try container.decode(String.self, forKey: .artists)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
        self.bindings = try container.decode([TrackBinding].self, forKey: .bindings)
        self.analysis = try container.decodeIfPresent(TrackAnalysis.self, forKey: .analysis)
        self.plays = try container.decode([TrackPlay].self, forKey: .plays)
        self.playCount = try container.decode(Int.self, forKey: .playCount)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artistId, forKey: .artistId)
        try container.encode(self.artistName, forKey: .artistName)
        try container.encodeIfPresent(self.albumId, forKey: .albumId)
        try container.encodeIfPresent(self.albumName, forKey: .albumName)
        try container.encodeIfPresent(self.albumImageUrl, forKey: .albumImageUrl)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.rating, forKey: .rating)
        try container.encode(self.bindings, forKey: .bindings)
        try container.encodeIfPresent(self.analysis, forKey: .analysis)
        try container.encode(self.plays, forKey: .plays)
        try container.encode(self.playCount, forKey: .playCount)
    }
}

/// Everything one record has accumulated, in one read.
///
/// The enrichment is deliberately NOT here. It has its own operation already, answering
/// `TrackEnrichmentDetail` with every provider's payload and the station's own sourced claims, and
/// the console draws it through the same panel the list uses. One enrichment shape rather than two.
public struct TrackDetailInput: Codable, Equatable, Sendable {
    public var title: String
    /// Display credit as written on the release ("X feat. Y"), not a join key
    public var artists: String
    public var genre: String?
    public var year: Int?
    public var durationMs: Int?
    public var rating: Rating
    public var bindings: [TrackBindingInput]
    /// Absent for a record the walk has not reached
    public var analysis: TrackAnalysisInput?
    /// The most recent airings, newest first
    public var plays: [TrackPlayInput]

    public init(title: String, artists: String, genre: String? = nil, year: Int? = nil, durationMs: Int? = nil, rating: Rating = .neutral, bindings: [TrackBindingInput], analysis: TrackAnalysisInput? = nil, plays: [TrackPlayInput]) {
        self.title = title
        self.artists = artists
        self.genre = genre
        self.year = year
        self.durationMs = durationMs
        self.rating = rating
        self.bindings = bindings
        self.analysis = analysis
        self.plays = plays
    }

    private enum CodingKeys: String, CodingKey {
        case title = "title"
        case artists = "artists"
        case genre = "genre"
        case year = "year"
        case durationMs = "durationMs"
        case rating = "rating"
        case bindings = "bindings"
        case analysis = "analysis"
        case plays = "plays"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode(String.self, forKey: .artists)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
        self.bindings = try container.decode([TrackBindingInput].self, forKey: .bindings)
        self.analysis = try container.decodeIfPresent(TrackAnalysisInput.self, forKey: .analysis)
        self.plays = try container.decode([TrackPlayInput].self, forKey: .plays)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.rating, forKey: .rating)
        try container.encode(self.bindings, forKey: .bindings)
        try container.encodeIfPresent(self.analysis, forKey: .analysis)
        try container.encode(self.plays, forKey: .plays)
    }
}

/// A track as a LIST shows it: the record, plus three facts about what the station has of it.
///
/// Three booleans and no more, deliberately. They are what a row can afford — one `exists` each, off
/// the query that was already running — and everything wider (which providers, how many bytes, why the
/// last fetch failed) is `TrackDetail`'s, one click away. A fourth would be the beginning of putting
/// the detail page in a table cell.
public struct TrackRow: Codable, Equatable, Sendable {
    public var id: UUID
    public var title: String
    public var artistId: UUID
    public var artistName: String
    /// Absent on a single ingested outside any release: `tracks.album_id` is nullable
    public var albumId: UUID?
    public var albumName: String?
    /// The record's cover, in the two spellings `Album.imageUrl` has. Nothing hangs art off a recording
    public var albumImageUrl: String?
    /// Display credit as written on the release ("X feat. Y"), not a join key
    public var artists: String
    public var genre: String?
    public var year: Int?
    public var durationMs: Int?
    public var rating: Rating
    /// The bytes are on this machine
    public var hasAudio: Bool
    /// Measured, COMPLETE, and at a schema version the station still trusts
    public var measured: Bool
    /// At least one provider has answered about it
    public var enriched: Bool

    public init(id: UUID, title: String, artistId: UUID, artistName: String, albumId: UUID? = nil, albumName: String? = nil, albumImageUrl: String? = nil, artists: String, genre: String? = nil, year: Int? = nil, durationMs: Int? = nil, rating: Rating = .neutral, hasAudio: Bool, measured: Bool, enriched: Bool) {
        self.id = id
        self.title = title
        self.artistId = artistId
        self.artistName = artistName
        self.albumId = albumId
        self.albumName = albumName
        self.albumImageUrl = albumImageUrl
        self.artists = artists
        self.genre = genre
        self.year = year
        self.durationMs = durationMs
        self.rating = rating
        self.hasAudio = hasAudio
        self.measured = measured
        self.enriched = enriched
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case title = "title"
        case artistId = "artistId"
        case artistName = "artistName"
        case albumId = "albumId"
        case albumName = "albumName"
        case albumImageUrl = "albumImageUrl"
        case artists = "artists"
        case genre = "genre"
        case year = "year"
        case durationMs = "durationMs"
        case rating = "rating"
        case hasAudio = "hasAudio"
        case measured = "measured"
        case enriched = "enriched"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.title = try container.decode(String.self, forKey: .title)
        self.artistId = try container.decode(UUID.self, forKey: .artistId)
        self.artistName = try container.decode(String.self, forKey: .artistName)
        self.albumId = try container.decodeIfPresent(UUID.self, forKey: .albumId)
        self.albumName = try container.decodeIfPresent(String.self, forKey: .albumName)
        self.albumImageUrl = try container.decodeIfPresent(String.self, forKey: .albumImageUrl)
        self.artists = try container.decode(String.self, forKey: .artists)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
        self.hasAudio = try container.decode(Bool.self, forKey: .hasAudio)
        self.measured = try container.decode(Bool.self, forKey: .measured)
        self.enriched = try container.decode(Bool.self, forKey: .enriched)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artistId, forKey: .artistId)
        try container.encode(self.artistName, forKey: .artistName)
        try container.encodeIfPresent(self.albumId, forKey: .albumId)
        try container.encodeIfPresent(self.albumName, forKey: .albumName)
        try container.encodeIfPresent(self.albumImageUrl, forKey: .albumImageUrl)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.rating, forKey: .rating)
        try container.encode(self.hasAudio, forKey: .hasAudio)
        try container.encode(self.measured, forKey: .measured)
        try container.encode(self.enriched, forKey: .enriched)
    }
}

/// A track as a LIST shows it: the record, plus three facts about what the station has of it.
///
/// Three booleans and no more, deliberately. They are what a row can afford — one `exists` each, off
/// the query that was already running — and everything wider (which providers, how many bytes, why the
/// last fetch failed) is `TrackDetail`'s, one click away. A fourth would be the beginning of putting
/// the detail page in a table cell.
public struct TrackRowInput: Codable, Equatable, Sendable {
    public var title: String
    /// Display credit as written on the release ("X feat. Y"), not a join key
    public var artists: String
    public var genre: String?
    public var year: Int?
    public var durationMs: Int?
    public var rating: Rating

    public init(title: String, artists: String, genre: String? = nil, year: Int? = nil, durationMs: Int? = nil, rating: Rating = .neutral) {
        self.title = title
        self.artists = artists
        self.genre = genre
        self.year = year
        self.durationMs = durationMs
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case title = "title"
        case artists = "artists"
        case genre = "genre"
        case year = "year"
        case durationMs = "durationMs"
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode(String.self, forKey: .artists)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.rating = try container.decodeIfPresent(Rating.self, forKey: .rating) ?? .neutral
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.rating, forKey: .rating)
    }
}

/// A track list, narrowed by what the station has of each record as well as by name.
///
/// Its own contract rather than a field on `CatalogQuery`, because that one is shared with the artist
/// and album lists where none of these states means anything.
public struct TrackQuery: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    /// The total number of items
    public var total: Int
    public var search: String?
    public var sortBy: TrackSort?
    public var state: TrackState?

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, total: Int, search: String? = nil, sortBy: TrackSort? = nil, state: TrackState? = nil) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.total = total
        self.search = search
        self.sortBy = sortBy
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case total = "total"
        case search = "search"
        case sortBy = "sortBy"
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.total = try container.decode(Int.self, forKey: .total)
        self.search = try container.decodeIfPresent(String.self, forKey: .search)
        self.sortBy = try container.decodeIfPresent(TrackSort.self, forKey: .sortBy)
        self.state = try container.decodeIfPresent(TrackState.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encode(self.total, forKey: .total)
        try container.encodeIfPresent(self.search, forKey: .search)
        try container.encodeIfPresent(self.sortBy, forKey: .sortBy)
        try container.encodeIfPresent(self.state, forKey: .state)
    }
}

/// A track list, narrowed by what the station has of each record as well as by name.
///
/// Its own contract rather than a field on `CatalogQuery`, because that one is shared with the artist
/// and album lists where none of these states means anything.
public struct TrackQueryInput: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    public var search: String?
    public var sortBy: TrackSort?
    public var state: TrackState?

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, search: String? = nil, sortBy: TrackSort? = nil, state: TrackState? = nil) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.search = search
        self.sortBy = sortBy
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case search = "search"
        case sortBy = "sortBy"
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.search = try container.decodeIfPresent(String.self, forKey: .search)
        self.sortBy = try container.decodeIfPresent(TrackSort.self, forKey: .sortBy)
        self.state = try container.decodeIfPresent(TrackState.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encodeIfPresent(self.search, forKey: .search)
        try container.encodeIfPresent(self.sortBy, forKey: .sortBy)
        try container.encodeIfPresent(self.state, forKey: .state)
    }
}

/// One provider's stored answer. `found: false` is a recorded miss, which is a fact rather than a
/// failure: the provider was asked, had nothing, and is not asked again until `expiresAt`. A provider
/// that could not be asked at all is `failed` instead, and the two never both hold.
public struct TrackEnrichmentSource: Codable, Equatable, Sendable {
    public var provider: String
    /// The id it was fetched under. Provenance, not identity
    public var providerRef: String?
    public var fetchedAt: Date
    public var expiresAt: Date?
    /// Past its TTL, so the next pass will ask again
    public var stale: Bool
    public var found: Bool
    /// The last attempt errored, so `expiresAt` is a backoff rather than a TTL
    public var failed: Bool
    public var data: TrackEnrichmentData

    public init(provider: String, providerRef: String? = nil, fetchedAt: Date, expiresAt: Date? = nil, stale: Bool, found: Bool, failed: Bool, data: TrackEnrichmentData) {
        self.provider = provider
        self.providerRef = providerRef
        self.fetchedAt = fetchedAt
        self.expiresAt = expiresAt
        self.stale = stale
        self.found = found
        self.failed = failed
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case provider = "provider"
        case providerRef = "providerRef"
        case fetchedAt = "fetchedAt"
        case expiresAt = "expiresAt"
        case stale = "stale"
        case found = "found"
        case failed = "failed"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.provider = try container.decode(String.self, forKey: .provider)
        self.providerRef = try container.decodeIfPresent(String.self, forKey: .providerRef)
        self.fetchedAt = try container.decode(Date.self, forKey: .fetchedAt)
        self.expiresAt = try container.decodeIfPresent(Date.self, forKey: .expiresAt)
        self.stale = try container.decode(Bool.self, forKey: .stale)
        self.found = try container.decode(Bool.self, forKey: .found)
        self.failed = try container.decode(Bool.self, forKey: .failed)
        self.data = try container.decode(TrackEnrichmentData.self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.provider, forKey: .provider)
        try container.encodeIfPresent(self.providerRef, forKey: .providerRef)
        try container.encode(self.fetchedAt, forKey: .fetchedAt)
        try container.encodeIfPresent(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.stale, forKey: .stale)
        try container.encode(self.found, forKey: .found)
        try container.encode(self.failed, forKey: .failed)
        try container.encode(self.data, forKey: .data)
    }
}

/// One provider's stored answer. `found: false` is a recorded miss, which is a fact rather than a
/// failure: the provider was asked, had nothing, and is not asked again until `expiresAt`. A provider
/// that could not be asked at all is `failed` instead, and the two never both hold.
public struct TrackEnrichmentSourceInput: Codable, Equatable, Sendable {
    public var data: TrackEnrichmentData

    public init(data: TrackEnrichmentData) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.data = try container.decode(TrackEnrichmentData.self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.data, forKey: .data)
    }
}

public struct ArtistEnrichmentSource: Codable, Equatable, Sendable {
    public var provider: String
    public var providerRef: String?
    public var fetchedAt: Date
    public var expiresAt: Date?
    public var stale: Bool
    public var found: Bool
    /// The last attempt errored, so `expiresAt` is a backoff rather than a TTL
    public var failed: Bool
    public var data: ArtistEnrichmentData

    public init(provider: String, providerRef: String? = nil, fetchedAt: Date, expiresAt: Date? = nil, stale: Bool, found: Bool, failed: Bool, data: ArtistEnrichmentData) {
        self.provider = provider
        self.providerRef = providerRef
        self.fetchedAt = fetchedAt
        self.expiresAt = expiresAt
        self.stale = stale
        self.found = found
        self.failed = failed
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case provider = "provider"
        case providerRef = "providerRef"
        case fetchedAt = "fetchedAt"
        case expiresAt = "expiresAt"
        case stale = "stale"
        case found = "found"
        case failed = "failed"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.provider = try container.decode(String.self, forKey: .provider)
        self.providerRef = try container.decodeIfPresent(String.self, forKey: .providerRef)
        self.fetchedAt = try container.decode(Date.self, forKey: .fetchedAt)
        self.expiresAt = try container.decodeIfPresent(Date.self, forKey: .expiresAt)
        self.stale = try container.decode(Bool.self, forKey: .stale)
        self.found = try container.decode(Bool.self, forKey: .found)
        self.failed = try container.decode(Bool.self, forKey: .failed)
        self.data = try container.decode(ArtistEnrichmentData.self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.provider, forKey: .provider)
        try container.encodeIfPresent(self.providerRef, forKey: .providerRef)
        try container.encode(self.fetchedAt, forKey: .fetchedAt)
        try container.encodeIfPresent(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.stale, forKey: .stale)
        try container.encode(self.found, forKey: .found)
        try container.encode(self.failed, forKey: .failed)
        try container.encode(self.data, forKey: .data)
    }
}

public struct ArtistEnrichmentSourceInput: Codable, Equatable, Sendable {
    public var data: ArtistEnrichmentData

    public init(data: ArtistEnrichmentData) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.data = try container.decode(ArtistEnrichmentData.self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.data, forKey: .data)
    }
}

public struct AlbumEnrichmentSource: Codable, Equatable, Sendable {
    public var provider: String
    public var providerRef: String?
    public var fetchedAt: Date
    public var expiresAt: Date?
    public var stale: Bool
    public var found: Bool
    /// The last attempt errored, so `expiresAt` is a backoff rather than a TTL
    public var failed: Bool
    public var data: AlbumEnrichmentData

    public init(provider: String, providerRef: String? = nil, fetchedAt: Date, expiresAt: Date? = nil, stale: Bool, found: Bool, failed: Bool, data: AlbumEnrichmentData) {
        self.provider = provider
        self.providerRef = providerRef
        self.fetchedAt = fetchedAt
        self.expiresAt = expiresAt
        self.stale = stale
        self.found = found
        self.failed = failed
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case provider = "provider"
        case providerRef = "providerRef"
        case fetchedAt = "fetchedAt"
        case expiresAt = "expiresAt"
        case stale = "stale"
        case found = "found"
        case failed = "failed"
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.provider = try container.decode(String.self, forKey: .provider)
        self.providerRef = try container.decodeIfPresent(String.self, forKey: .providerRef)
        self.fetchedAt = try container.decode(Date.self, forKey: .fetchedAt)
        self.expiresAt = try container.decodeIfPresent(Date.self, forKey: .expiresAt)
        self.stale = try container.decode(Bool.self, forKey: .stale)
        self.found = try container.decode(Bool.self, forKey: .found)
        self.failed = try container.decode(Bool.self, forKey: .failed)
        self.data = try container.decode(AlbumEnrichmentData.self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.provider, forKey: .provider)
        try container.encodeIfPresent(self.providerRef, forKey: .providerRef)
        try container.encode(self.fetchedAt, forKey: .fetchedAt)
        try container.encodeIfPresent(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.stale, forKey: .stale)
        try container.encode(self.found, forKey: .found)
        try container.encode(self.failed, forKey: .failed)
        try container.encode(self.data, forKey: .data)
    }
}

public struct AlbumEnrichmentSourceInput: Codable, Equatable, Sendable {
    public var data: AlbumEnrichmentData

    public init(data: AlbumEnrichmentData) {
        self.data = data
    }

    private enum CodingKeys: String, CodingKey {
        case data = "data"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.data = try container.decode(AlbumEnrichmentData.self, forKey: .data)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.data, forKey: .data)
    }
}

/// One page of tracks, with what the station has of each and of the whole set
public struct TrackPage: Codable, Equatable, Sendable {
    public var meta: Pagination
    public var data: [TrackRow]
    public var states: TrackStateCounts

    public init(meta: Pagination, data: [TrackRow], states: TrackStateCounts) {
        self.meta = meta
        self.data = data
        self.states = states
    }

    private enum CodingKeys: String, CodingKey {
        case meta = "meta"
        case data = "data"
        case states = "states"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.meta = try container.decode(Pagination.self, forKey: .meta)
        self.data = try container.decode([TrackRow].self, forKey: .data)
        self.states = try container.decode(TrackStateCounts.self, forKey: .states)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.meta, forKey: .meta)
        try container.encode(self.data, forKey: .data)
        try container.encode(self.states, forKey: .states)
    }
}

/// One page of tracks, with what the station has of each and of the whole set
public struct TrackPageInput: Codable, Equatable, Sendable {
    public var meta: PaginationInput
    public var data: [TrackRowInput]
    public var states: TrackStateCountsInput

    public init(meta: PaginationInput, data: [TrackRowInput], states: TrackStateCountsInput) {
        self.meta = meta
        self.data = data
        self.states = states
    }

    private enum CodingKeys: String, CodingKey {
        case meta = "meta"
        case data = "data"
        case states = "states"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.meta = try container.decode(PaginationInput.self, forKey: .meta)
        self.data = try container.decode([TrackRowInput].self, forKey: .data)
        self.states = try container.decode(TrackStateCountsInput.self, forKey: .states)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.meta, forKey: .meta)
        try container.encode(self.data, forKey: .data)
        try container.encode(self.states, forKey: .states)
    }
}

/// Every provider's answer, plus the same merge the promotion step used, so the console and the
/// canonical columns cannot tell different stories. `sources` is empty on a row the walk has not
/// reached yet.
///
/// `claims` sits beside them rather than inside `merged`, because a claim is the host's own and not
/// any provider's. The articles they were read out of are deliberately NOT here: raw source prose is
/// stored and never sent.
public struct TrackEnrichmentDetail: Codable, Equatable, Sendable {
    public var trackId: UUID
    public var merged: TrackEnrichmentData
    public var sources: [TrackEnrichmentSource]
    public var claims: [FactClaim]

    public init(trackId: UUID, merged: TrackEnrichmentData, sources: [TrackEnrichmentSource], claims: [FactClaim]) {
        self.trackId = trackId
        self.merged = merged
        self.sources = sources
        self.claims = claims
    }

    private enum CodingKeys: String, CodingKey {
        case trackId = "trackId"
        case merged = "merged"
        case sources = "sources"
        case claims = "claims"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.trackId = try container.decode(UUID.self, forKey: .trackId)
        self.merged = try container.decode(TrackEnrichmentData.self, forKey: .merged)
        self.sources = try container.decode([TrackEnrichmentSource].self, forKey: .sources)
        self.claims = try container.decode([FactClaim].self, forKey: .claims)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.trackId, forKey: .trackId)
        try container.encode(self.merged, forKey: .merged)
        try container.encode(self.sources, forKey: .sources)
        try container.encode(self.claims, forKey: .claims)
    }
}

/// Every provider's answer, plus the same merge the promotion step used, so the console and the
/// canonical columns cannot tell different stories. `sources` is empty on a row the walk has not
/// reached yet.
///
/// `claims` sits beside them rather than inside `merged`, because a claim is the host's own and not
/// any provider's. The articles they were read out of are deliberately NOT here: raw source prose is
/// stored and never sent.
public struct TrackEnrichmentDetailInput: Codable, Equatable, Sendable {
    public var merged: TrackEnrichmentData
    public var sources: [TrackEnrichmentSourceInput]
    public var claims: [FactClaimInput]

    public init(merged: TrackEnrichmentData, sources: [TrackEnrichmentSourceInput], claims: [FactClaimInput]) {
        self.merged = merged
        self.sources = sources
        self.claims = claims
    }

    private enum CodingKeys: String, CodingKey {
        case merged = "merged"
        case sources = "sources"
        case claims = "claims"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.merged = try container.decode(TrackEnrichmentData.self, forKey: .merged)
        self.sources = try container.decode([TrackEnrichmentSourceInput].self, forKey: .sources)
        self.claims = try container.decode([FactClaimInput].self, forKey: .claims)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.merged, forKey: .merged)
        try container.encode(self.sources, forKey: .sources)
        try container.encode(self.claims, forKey: .claims)
    }
}

public struct ArtistEnrichmentDetail: Codable, Equatable, Sendable {
    public var artistId: UUID
    public var merged: ArtistEnrichmentData
    public var sources: [ArtistEnrichmentSource]
    public var claims: [FactClaim]

    public init(artistId: UUID, merged: ArtistEnrichmentData, sources: [ArtistEnrichmentSource], claims: [FactClaim]) {
        self.artistId = artistId
        self.merged = merged
        self.sources = sources
        self.claims = claims
    }

    private enum CodingKeys: String, CodingKey {
        case artistId = "artistId"
        case merged = "merged"
        case sources = "sources"
        case claims = "claims"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.artistId = try container.decode(UUID.self, forKey: .artistId)
        self.merged = try container.decode(ArtistEnrichmentData.self, forKey: .merged)
        self.sources = try container.decode([ArtistEnrichmentSource].self, forKey: .sources)
        self.claims = try container.decode([FactClaim].self, forKey: .claims)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.artistId, forKey: .artistId)
        try container.encode(self.merged, forKey: .merged)
        try container.encode(self.sources, forKey: .sources)
        try container.encode(self.claims, forKey: .claims)
    }
}

public struct ArtistEnrichmentDetailInput: Codable, Equatable, Sendable {
    public var merged: ArtistEnrichmentData
    public var sources: [ArtistEnrichmentSourceInput]
    public var claims: [FactClaimInput]

    public init(merged: ArtistEnrichmentData, sources: [ArtistEnrichmentSourceInput], claims: [FactClaimInput]) {
        self.merged = merged
        self.sources = sources
        self.claims = claims
    }

    private enum CodingKeys: String, CodingKey {
        case merged = "merged"
        case sources = "sources"
        case claims = "claims"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.merged = try container.decode(ArtistEnrichmentData.self, forKey: .merged)
        self.sources = try container.decode([ArtistEnrichmentSourceInput].self, forKey: .sources)
        self.claims = try container.decode([FactClaimInput].self, forKey: .claims)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.merged, forKey: .merged)
        try container.encode(self.sources, forKey: .sources)
        try container.encode(self.claims, forKey: .claims)
    }
}

public struct AlbumEnrichmentDetail: Codable, Equatable, Sendable {
    public var albumId: UUID
    public var merged: AlbumEnrichmentData
    public var sources: [AlbumEnrichmentSource]
    public var claims: [FactClaim]

    public init(albumId: UUID, merged: AlbumEnrichmentData, sources: [AlbumEnrichmentSource], claims: [FactClaim]) {
        self.albumId = albumId
        self.merged = merged
        self.sources = sources
        self.claims = claims
    }

    private enum CodingKeys: String, CodingKey {
        case albumId = "albumId"
        case merged = "merged"
        case sources = "sources"
        case claims = "claims"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.albumId = try container.decode(UUID.self, forKey: .albumId)
        self.merged = try container.decode(AlbumEnrichmentData.self, forKey: .merged)
        self.sources = try container.decode([AlbumEnrichmentSource].self, forKey: .sources)
        self.claims = try container.decode([FactClaim].self, forKey: .claims)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.albumId, forKey: .albumId)
        try container.encode(self.merged, forKey: .merged)
        try container.encode(self.sources, forKey: .sources)
        try container.encode(self.claims, forKey: .claims)
    }
}

public struct AlbumEnrichmentDetailInput: Codable, Equatable, Sendable {
    public var merged: AlbumEnrichmentData
    public var sources: [AlbumEnrichmentSourceInput]
    public var claims: [FactClaimInput]

    public init(merged: AlbumEnrichmentData, sources: [AlbumEnrichmentSourceInput], claims: [FactClaimInput]) {
        self.merged = merged
        self.sources = sources
        self.claims = claims
    }

    private enum CodingKeys: String, CodingKey {
        case merged = "merged"
        case sources = "sources"
        case claims = "claims"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.merged = try container.decode(AlbumEnrichmentData.self, forKey: .merged)
        self.sources = try container.decode([AlbumEnrichmentSourceInput].self, forKey: .sources)
        self.claims = try container.decode([FactClaimInput].self, forKey: .claims)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.merged, forKey: .merged)
        try container.encode(self.sources, forKey: .sources)
        try container.encode(self.claims, forKey: .claims)
    }
}
