// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Net.Http;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading;
using System.Threading.Tasks;
using System.Xml;
using MaroonedSoftware.Deadair.Sdk.Models;
using MaroonedSoftware.Deadair.Sdk.Runtime;

namespace MaroonedSoftware.Deadair.Sdk.Clients;

/// <summary>Operations declared in &lt;c&gt;authentication.factor.ck&lt;/c&gt;.</summary>
public sealed class AuthenticationFactorClient(SdkHttp http)
{
    /// <summary>
    /// List factors
    /// List authentication factors
    /// </summary>
    public async Task<List<AuthenticationFactor>> ListFactorsAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("auth", "factors"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<List<AuthenticationFactor>>(response);
    }

    /// <summary>
    /// Register factor
    /// Register an authentication factor
    /// </summary>
    public async Task<AuthenticationFactorRegistrationResponse> RegisterFactorAsync(AuthenticationFactorRegistration body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "factors", "register"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<AuthenticationFactorRegistrationResponse>(response);
    }

    /// <summary>
    /// Verify factor registration
    /// Verify an authentication factor registration
    /// </summary>
    public async Task<AuthenticationToken> VerifyFactorRegistrationAsync(AuthenticationFactorRegistrationVerification body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "factors", "verify"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<AuthenticationToken>(response);
    }

    /// <summary>
    /// Start factor challenge
    /// Issue a factor verification challenge for a pending MFA round. Authenticated via the short-lived `mfa_challenge_id` in the body, not by session — this is the only /auth/factors/* route that does not require an authenticated session.
    /// </summary>
    public async Task<FactorChallengeStartResponse> StartFactorChallengeAsync(FactorChallengeStartRequest body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "factors", "start"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<FactorChallengeStartResponse>(response);
    }

    /// <summary>
    /// Start MFA challenge
    /// Mint a fresh MFA challenge for the *current* authenticated session so the SPA can satisfy a `step_up_required` denial. Optionally filters eligible factors against an inbound `StepUpRequirement` hint. Returns `enrollment_required` when no enrolled factor matches the requirement so the SPA can route the user into enrollment instead of getting stuck.
    /// </summary>
    public async Task<StepUpStartResponse> StartMfaChallengeAsync(StepUpStartRequest body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("auth", "mfa", "start"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<StepUpStartResponse>(response);
    }

    /// <summary>
    /// Remove factor
    /// Remove one of the caller's own factors. Answered only for `authenticator` today, and only after a recent strong-factor verification: the same gate enrolment sits behind once a strong factor exists, so a stolen session cannot quietly switch the second factor off. Removing the last authenticator turns the sign-in challenge off for that account.
    /// </summary>
    public async Task RemoveFactorAsync(AuthenticationFactorMethod method, string methodId, CancellationToken cancellationToken = default)
    {
        await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("auth", "factors", http.Segment(method), http.Segment(methodId)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}
