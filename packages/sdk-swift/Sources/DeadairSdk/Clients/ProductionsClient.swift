// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `productions.ck`.
public final class ProductionsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List productions
    /// Everything the station has made or is making, newest first
    public func listProductions() async throws -> ProductionList {
        let request = SdkRequest(method: "GET", path: ["productions"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ProductionList.self, from: response)
    }

    /// Request production
    /// Asks the station to make one. It is queued, not started
    public func requestProduction(body: ProductionRequest) async throws -> Production {
        var request = SdkRequest(method: "POST", path: ["productions"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(Production.self, from: response)
    }

    /// Cancel production
    /// Stops a production being made, for good
    public func cancelProduction(id: String) async throws -> Production {
        let request = try SdkRequest(method: "POST", path: ["productions", http.segment(id), "cancel"])
        let response = try await http.execute(request)
        return try http.decodeJSON(Production.self, from: response)
    }
}
