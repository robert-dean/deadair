// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Lifecycle state of a plugin the host knows about
public enum PluginStatus: String, Codable, CaseIterable, Sendable {
    case discovered = "discovered"
    case disabled = "disabled"
    case misconfigured = "misconfigured"
    case active = "active"
    case failed = "failed"
}

/// Where the station found a plugin: shipped inside the image, or installed by the operator into the plugins
/// directory on the data volume. Says nothing about trust; both kinds run inside the station with its privileges
public enum PluginOrigin: String, Codable, CaseIterable, Sendable {
    case bundled = "bundled"
    case installed = "installed"
}

public enum ConfigFieldType: String, Codable, CaseIterable, Sendable {
    case string = "string"
    case text = "text"
    case url = "url"
    case secret = "secret"
    case number = "number"
    case boolean = "boolean"
    case select = "select"
    case multiselect = "multiselect"
    case list = "list"
    case note = "note"
}

/// What a `number` field's value is measured in. The stored value is always in this unit; only the
/// control the operator touches changes, so a byte count stays a byte count everywhere it is read and
/// a `fraction` stays the share between 0 and 1 that the code multiplying by it wants
public enum ConfigFieldUnit: String, Codable, CaseIterable, Sendable {
    case bytes = "bytes"
    case fraction = "fraction"
}

/// The control a field asks to be drawn with, where the ordinary one for its type reads badly. Opt-in
/// per field rather than inferred, because a slider is right for a value you feel for and wrong for
/// one you have to hit exactly, and `tags` is right for a comma-separated line that is really a SET
/// and wrong for one that is prose. Nothing about the stored value changes either way
public enum ConfigFieldControl: String, Codable, CaseIterable, Sendable {
    case slider = "slider"
    case tags = "tags"
}

/// One choice of a `select` config field
public struct ConfigFieldOption: Codable, Equatable, Sendable {
    public var value: String
    public var label: String

    public init(value: String, label: String) {
        self.value = value
        self.label = label
    }

    private enum CodingKeys: String, CodingKey {
        case value = "value"
        case label = "label"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.value = try container.decode(String.self, forKey: .value)
        self.label = try container.decode(String.self, forKey: .label)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.value, forKey: .value)
        try container.encode(self.label, forKey: .label)
    }
}

/// Where a field's or a column's choices come from when only the console can enumerate them: the
/// station's own tables, the platform's zone list, the enabled plugins that can do one of four jobs,
/// or the models the selected model plugin currently offers. Resolved by the console either way
public enum ConfigFieldOptionSource: String, Codable, CaseIterable, Sendable {
    case stationNewsCategories = "station.newsCategories"
    case stationNewsFeeds = "station.newsFeeds"
    case intlTimeZones = "intl.timeZones"
    case pluginsSpeech = "plugins.speech"
    case pluginsLlm = "plugins.llm"
    case pluginsMixer = "plugins.mixer"
    case pluginsAnalysis = "plugins.analysis"
    case llmModels = "llm.models"
}

public enum PluginLogLevel: String, Codable, CaseIterable, Sendable {
    case debug = "debug"
    case info = "info"
    case warn = "warn"
    case error = "error"
}

/// A submitted settings form. Secret values arrive in here and are never echoed back
public struct PluginConfigInput: Codable, Equatable, Sendable {
    public var config: [String: JSONValue]

    public init(config: [String: JSONValue]) {
        self.config = config
    }

    private enum CodingKeys: String, CodingKey {
        case config = "config"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.config = try container.decode([String: JSONValue].self, forKey: .config)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.config, forKey: .config)
    }
}

/// What a plugin may do with a capability it asked for. Denied is the default and needs no row: a
/// capability is refused until somebody allows it, so "never answered" and "refused" are one state
public enum GrantDecision: String, Codable, CaseIterable, Sendable {
    case allowed = "allowed"
    case denied = "denied"
}

/// Outcome of the plugin's own `testConnection()`
public struct PluginTestResult: Codable, Equatable, Sendable {
    public var ok: Bool
    public var message: String?

    public init(ok: Bool, message: String? = nil) {
        self.ok = ok
        self.message = message
    }

    private enum CodingKeys: String, CodingKey {
        case ok = "ok"
        case message = "message"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.ok = try container.decode(Bool.self, forKey: .ok)
        self.message = try container.decodeIfPresent(String.self, forKey: .message)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.ok, forKey: .ok)
        try container.encodeIfPresent(self.message, forKey: .message)
    }
}

/// Where the console should send the browser to obtain the operator's consent. Reported rather than
/// redirected to: the route is behind the Bearer floor, so a browser cannot follow a redirect from it
public struct PluginOAuthStart: Codable, Equatable, Sendable {
    public var url: String

    public init(url: String) {
        self.url = url
    }

    private enum CodingKeys: String, CodingKey {
        case url = "url"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.url = try container.decode(String.self, forKey: .url)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.url, forKey: .url)
    }
}

/// Outcome of an OAuth callback
public struct PluginOAuthResult: Codable, Equatable, Sendable {
    public var pluginId: String
    public var ok: Bool
    public var message: String?

    public init(pluginId: String, ok: Bool, message: String? = nil) {
        self.pluginId = pluginId
        self.ok = ok
        self.message = message
    }

    private enum CodingKeys: String, CodingKey {
        case pluginId = "pluginId"
        case ok = "ok"
        case message = "message"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.ok = try container.decode(Bool.self, forKey: .ok)
        self.message = try container.decodeIfPresent(String.self, forKey: .message)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.ok, forKey: .ok)
        try container.encodeIfPresent(self.message, forKey: .message)
    }
}

public struct PluginOAuthCallbackQuery: Codable, Equatable, Sendable {
    public var code: String?
    public var state: String?
    public var error: String?
    public var ubi: String?
    /// What a desktop-style flow returns instead of `code`: the provider mints a token before the
    /// consent screen and hands the same one back, which the plugin exchanges for a session. Last.fm's
    /// auth works this way. Listed here because the route parses this query strictly, so an
    /// undeclared parameter is a 400 before any plugin code runs
    public var token: String?

    public init(code: String? = nil, state: String? = nil, error: String? = nil, ubi: String? = nil, token: String? = nil) {
        self.code = code
        self.state = state
        self.error = error
        self.ubi = ubi
        self.token = token
    }

    private enum CodingKeys: String, CodingKey {
        case code = "code"
        case state = "state"
        case error = "error"
        case ubi = "ubi"
        case token = "token"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.code = try container.decodeIfPresent(String.self, forKey: .code)
        self.state = try container.decodeIfPresent(String.self, forKey: .state)
        self.error = try container.decodeIfPresent(String.self, forKey: .error)
        self.ubi = try container.decodeIfPresent(String.self, forKey: .ubi)
        self.token = try container.decodeIfPresent(String.self, forKey: .token)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.code, forKey: .code)
        try container.encodeIfPresent(self.state, forKey: .state)
        try container.encodeIfPresent(self.error, forKey: .error)
        try container.encodeIfPresent(self.ubi, forKey: .ubi)
        try container.encodeIfPresent(self.token, forKey: .token)
    }
}

/// Live choices for a plugin's config fields, keyed by field key, out of the plugin's own
/// `suggestConfigOptions()`. What `ConfigFieldDescriptor.options` cannot be: fixed when the manifest
/// was written, where these are whatever the operator's own server currently says
public struct PluginFieldSuggestions: Codable, Equatable, Sendable {
    /// Keys the plugin had nothing to say about are simply absent, rather than present and empty
    public var fields: [String: [ConfigFieldOption]]
    /// False when the plugin does not implement suggestions at all, so a console can tell "nothing to
    /// suggest" from "asked and got nothing", and draw a refresh control only where one would do something
    public var supported: Bool

    public init(fields: [String: [ConfigFieldOption]], supported: Bool) {
        self.fields = fields
        self.supported = supported
    }

    private enum CodingKeys: String, CodingKey {
        case fields = "fields"
        case supported = "supported"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.fields = try container.decode([String: [ConfigFieldOption]].self, forKey: .fields)
        self.supported = try container.decode(Bool.self, forKey: .supported)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.fields, forKey: .fields)
        try container.encode(self.supported, forKey: .supported)
    }
}

/// One column of a `list` field. Every ordinary cell is stored as a string in the row, so this describes the
/// control rather than the value; a `secret` cell is encrypted on its own and is never in the row at all
public struct ConfigFieldColumn: Codable, Equatable, Sendable {
    public var key: String
    public var label: String
    public var type: ConfigFieldColumnType
    public var required: Bool?
    public var placeholder: String?
    public var options: [ConfigFieldOption]?
    public var optionsFrom: ConfigFieldOptionSource?
    /// Key of another column in the same list. This cell applies only to a row whose cell there holds one of `dependsOnValues`. Stronger than a field's `dependsOn`, which only hides a control: a cell that does not apply is neither sent by the console nor read by the host, so a `url` column that does not apply to a row contributes no hostname to the plugin's allowlist. A target this list does not declare, or a target cell still empty, shows the cell
    public var dependsOn: String?
    /// The values of the `dependsOn` cell this one applies to. Omitted means any non-empty value; ignored without a target
    public var dependsOnValues: [String]?

    public init(key: String, label: String, type: ConfigFieldColumnType, required: Bool? = nil, placeholder: String? = nil, options: [ConfigFieldOption]? = nil, optionsFrom: ConfigFieldOptionSource? = nil, dependsOn: String? = nil, dependsOnValues: [String]? = nil) {
        self.key = key
        self.label = label
        self.type = type
        self.required = required
        self.placeholder = placeholder
        self.options = options
        self.optionsFrom = optionsFrom
        self.dependsOn = dependsOn
        self.dependsOnValues = dependsOnValues
    }

    private enum CodingKeys: String, CodingKey {
        case key = "key"
        case label = "label"
        case type = "type"
        case required = "required"
        case placeholder = "placeholder"
        case options = "options"
        case optionsFrom = "optionsFrom"
        case dependsOn = "dependsOn"
        case dependsOnValues = "dependsOnValues"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.type = try container.decode(ConfigFieldColumnType.self, forKey: .type)
        self.required = try container.decodeIfPresent(Bool.self, forKey: .required)
        self.placeholder = try container.decodeIfPresent(String.self, forKey: .placeholder)
        self.options = try container.decodeIfPresent([ConfigFieldOption].self, forKey: .options)
        self.optionsFrom = try container.decodeIfPresent(ConfigFieldOptionSource.self, forKey: .optionsFrom)
        self.dependsOn = try container.decodeIfPresent(String.self, forKey: .dependsOn)
        self.dependsOnValues = try container.decodeIfPresent([String].self, forKey: .dependsOnValues)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.type, forKey: .type)
        try container.encodeIfPresent(self.required, forKey: .required)
        try container.encodeIfPresent(self.placeholder, forKey: .placeholder)
        try container.encodeIfPresent(self.options, forKey: .options)
        try container.encodeIfPresent(self.optionsFrom, forKey: .optionsFrom)
        try container.encodeIfPresent(self.dependsOn, forKey: .dependsOn)
        try container.encodeIfPresent(self.dependsOnValues, forKey: .dependsOnValues)
    }
}

public struct PluginLogEntry: Codable, Equatable, Sendable {
    public var ts: String
    public var level: PluginLogLevel
    /// Must match MAX_LINE_BYTES_CEILING in apps/api/src/logging/rotating.log.store.ts. Change both together
    public var text: String

    public init(ts: String, level: PluginLogLevel, text: String) {
        self.ts = ts
        self.level = level
        self.text = text
    }

    private enum CodingKeys: String, CodingKey {
        case ts = "ts"
        case level = "level"
        case text = "text"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.ts = try container.decode(String.self, forKey: .ts)
        self.level = try container.decode(PluginLogLevel.self, forKey: .level)
        self.text = try container.decode(String.self, forKey: .text)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.ts, forKey: .ts)
        try container.encode(self.level, forKey: .level)
        try container.encode(self.text, forKey: .text)
    }
}

public struct PluginLogQuery: Codable, Equatable, Sendable {
    public var limit: Int?
    public var level: PluginLogLevel?

    public init(limit: Int? = nil, level: PluginLogLevel? = nil) {
        self.limit = limit
        self.level = level
    }

    private enum CodingKeys: String, CodingKey {
        case limit = "limit"
        case level = "level"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.limit = try container.decodeIfPresent(Int.self, forKey: .limit)
        self.level = try container.decodeIfPresent(PluginLogLevel.self, forKey: .level)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.limit, forKey: .limit)
        try container.encodeIfPresent(self.level, forKey: .level)
    }
}

public struct PluginLogLevelInput: Codable, Equatable, Sendable {
    public var level: PluginLogLevel

    public init(level: PluginLogLevel) {
        self.level = level
    }

    private enum CodingKeys: String, CodingKey {
        case level = "level"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.level = try container.decode(PluginLogLevel.self, forKey: .level)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.level, forKey: .level)
    }
}

/// One capability a plugin asked for, with the station's answer. The ask is the plugin's manifest and
/// the answer is a row, so a plugin that stops asking stops appearing here whatever was stored
public struct PluginGrant: Codable, Equatable, Sendable {
    public var pluginId: String
    public var pluginName: String
    /// The host's own id for it, e.g. `network.open`
    public var capability: String
    /// What the host calls the capability
    public var label: String
    /// What allowing it opens up, in the station's words
    public var describes: String
    /// Why this plugin says it needs it, in the plugin's words
    public var reason: String
    public var decision: GrantDecision

    public init(pluginId: String, pluginName: String, capability: String, label: String, describes: String, reason: String, decision: GrantDecision) {
        self.pluginId = pluginId
        self.pluginName = pluginName
        self.capability = capability
        self.label = label
        self.describes = describes
        self.reason = reason
        self.decision = decision
    }

    private enum CodingKeys: String, CodingKey {
        case pluginId = "pluginId"
        case pluginName = "pluginName"
        case capability = "capability"
        case label = "label"
        case describes = "describes"
        case reason = "reason"
        case decision = "decision"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.pluginName = try container.decode(String.self, forKey: .pluginName)
        self.capability = try container.decode(String.self, forKey: .capability)
        self.label = try container.decode(String.self, forKey: .label)
        self.describes = try container.decode(String.self, forKey: .describes)
        self.reason = try container.decode(String.self, forKey: .reason)
        self.decision = try container.decode(GrantDecision.self, forKey: .decision)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.pluginName, forKey: .pluginName)
        try container.encode(self.capability, forKey: .capability)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.describes, forKey: .describes)
        try container.encode(self.reason, forKey: .reason)
        try container.encode(self.decision, forKey: .decision)
    }
}

public struct PluginGrantInput: Codable, Equatable, Sendable {
    public var capability: String
    public var decision: GrantDecision

    public init(capability: String, decision: GrantDecision) {
        self.capability = capability
        self.decision = decision
    }

    private enum CodingKeys: String, CodingKey {
        case capability = "capability"
        case decision = "decision"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.capability = try container.decode(String.self, forKey: .capability)
        self.decision = try container.decode(GrantDecision.self, forKey: .decision)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.capability, forKey: .capability)
        try container.encode(self.decision, forKey: .decision)
    }
}

/// Mirrors the plugin SDK's `ConfigField`: enough for a console to render the settings form with no per-plugin code
public struct ConfigFieldDescriptor: Codable, Equatable, Sendable {
    public var key: String
    public var label: String
    public var type: ConfigFieldType
    public var required: Bool?
    public var `default`: ConfigFieldDescriptorDefault?
    /// `number` only, and ignored elsewhere
    public var unit: ConfigFieldUnit?
    /// `slider` for a `number` with both `min` and `max`, `tags` for a `string` holding a comma-separated set
    public var control: ConfigFieldControl?
    /// How coarsely a `control` moves, in the field's own unit. Ignored without one, and defaults to 1
    public var step: Double?
    /// `number` only: the smallest value that will be accepted, inclusive
    public var min: Double?
    /// `number` only: the largest value that will be accepted, inclusive
    public var max: Double?
    public var placeholder: String?
    public var help: String?
    public var options: [ConfigFieldOption]?
    /// Choices only the console can enumerate. Merged where a plugin's own suggestions are, and outranked by them
    public var optionsFrom: ConfigFieldOptionSource?
    /// `list` only, and ignored elsewhere
    public var columns: [ConfigFieldColumn]?
    /// Key of the field this one is only relevant to
    public var dependsOn: String?
    /// Key of the `number` field that is the upper end of the range this one opens, declared on the lower end only. Still two settings, each validated by name; the console draws them as one control whose handles cannot cross
    public var rangeWith: String?

    public init(key: String, label: String, type: ConfigFieldType, required: Bool? = nil, `default`: ConfigFieldDescriptorDefault? = nil, unit: ConfigFieldUnit? = nil, control: ConfigFieldControl? = nil, step: Double? = nil, min: Double? = nil, max: Double? = nil, placeholder: String? = nil, help: String? = nil, options: [ConfigFieldOption]? = nil, optionsFrom: ConfigFieldOptionSource? = nil, columns: [ConfigFieldColumn]? = nil, dependsOn: String? = nil, rangeWith: String? = nil) {
        self.key = key
        self.label = label
        self.type = type
        self.required = required
        self.`default` = `default`
        self.unit = unit
        self.control = control
        self.step = step
        self.min = min
        self.max = max
        self.placeholder = placeholder
        self.help = help
        self.options = options
        self.optionsFrom = optionsFrom
        self.columns = columns
        self.dependsOn = dependsOn
        self.rangeWith = rangeWith
    }

    private enum CodingKeys: String, CodingKey {
        case key = "key"
        case label = "label"
        case type = "type"
        case required = "required"
        case `default` = "default"
        case unit = "unit"
        case control = "control"
        case step = "step"
        case min = "min"
        case max = "max"
        case placeholder = "placeholder"
        case help = "help"
        case options = "options"
        case optionsFrom = "optionsFrom"
        case columns = "columns"
        case dependsOn = "dependsOn"
        case rangeWith = "rangeWith"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.type = try container.decode(ConfigFieldType.self, forKey: .type)
        self.required = try container.decodeIfPresent(Bool.self, forKey: .required)
        self.`default` = try container.decodeIfPresent(ConfigFieldDescriptorDefault.self, forKey: .`default`)
        self.unit = try container.decodeIfPresent(ConfigFieldUnit.self, forKey: .unit)
        self.control = try container.decodeIfPresent(ConfigFieldControl.self, forKey: .control)
        self.step = try container.decodeIfPresent(Double.self, forKey: .step)
        self.min = try container.decodeIfPresent(Double.self, forKey: .min)
        self.max = try container.decodeIfPresent(Double.self, forKey: .max)
        self.placeholder = try container.decodeIfPresent(String.self, forKey: .placeholder)
        self.help = try container.decodeIfPresent(String.self, forKey: .help)
        self.options = try container.decodeIfPresent([ConfigFieldOption].self, forKey: .options)
        self.optionsFrom = try container.decodeIfPresent(ConfigFieldOptionSource.self, forKey: .optionsFrom)
        self.columns = try container.decodeIfPresent([ConfigFieldColumn].self, forKey: .columns)
        self.dependsOn = try container.decodeIfPresent(String.self, forKey: .dependsOn)
        self.rangeWith = try container.decodeIfPresent(String.self, forKey: .rangeWith)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.type, forKey: .type)
        try container.encodeIfPresent(self.required, forKey: .required)
        try container.encodeIfPresent(self.`default`, forKey: .`default`)
        try container.encodeIfPresent(self.unit, forKey: .unit)
        try container.encodeIfPresent(self.control, forKey: .control)
        try container.encodeIfPresent(self.step, forKey: .step)
        try container.encodeIfPresent(self.min, forKey: .min)
        try container.encodeIfPresent(self.max, forKey: .max)
        try container.encodeIfPresent(self.placeholder, forKey: .placeholder)
        try container.encodeIfPresent(self.help, forKey: .help)
        try container.encodeIfPresent(self.options, forKey: .options)
        try container.encodeIfPresent(self.optionsFrom, forKey: .optionsFrom)
        try container.encodeIfPresent(self.columns, forKey: .columns)
        try container.encodeIfPresent(self.dependsOn, forKey: .dependsOn)
        try container.encodeIfPresent(self.rangeWith, forKey: .rangeWith)
    }
}

public struct PluginLogPage: Codable, Equatable, Sendable {
    public var pluginId: String
    public var level: PluginLogLevel
    /// Newest first, as the activity feed and the script history send. The download is the file as written, oldest first
    public var entries: [PluginLogEntry]

    public init(pluginId: String, level: PluginLogLevel, entries: [PluginLogEntry]) {
        self.pluginId = pluginId
        self.level = level
        self.entries = entries
    }

    private enum CodingKeys: String, CodingKey {
        case pluginId = "pluginId"
        case level = "level"
        case entries = "entries"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.level = try container.decode(PluginLogLevel.self, forKey: .level)
        self.entries = try container.decode([PluginLogEntry].self, forKey: .entries)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.level, forKey: .level)
        try container.encode(self.entries, forKey: .entries)
    }
}

public struct PluginGrantList: Codable, Equatable, Sendable {
    /// Every capability every installed plugin is asking for, refused ones included
    public var grants: [PluginGrant]

    public init(grants: [PluginGrant]) {
        self.grants = grants
    }

    private enum CodingKeys: String, CodingKey {
        case grants = "grants"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grants = try container.decode([PluginGrant].self, forKey: .grants)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grants, forKey: .grants)
    }
}

/// A plugin as the settings list sees it. Carries no configured VALUES, only which secrets are set
public struct PluginSummary: Codable, Equatable, Sendable {
    public var id: String
    public var name: String
    public var version: String
    public var capabilities: [String]
    /// Whether the manifest declares the `trackFetcher` permission, so its records reach air through the station's own track fetcher and that fetcher needs its own authorization. Not the same as the `stream` capability, which a plugin that mints its own stream URLs declares too. Absent means it does not
    public var usesTrackFetcher: Bool?
    public var status: PluginStatus
    public var origin: PluginOrigin
    public var enabled: Bool
    public var description: String?
    public var icon: String?
    public var configFields: [ConfigFieldDescriptor]
    /// Whether a value is currently stored, per `secret` field under its own key and per `secret` cell under `field/rowId/column`. Never the value itself
    public var secretsConfigured: [String: Bool]
    /// When this plugin was first ever enabled. Absent means it never has been, so the console asks before it is
    public var firstEnabledAt: Date?
    /// The last recorded failure. Absent means it is not currently unhappy
    public var lastError: String?
    /// When the breaker will probe this plugin again on its own. Absent means no probe is pending
    public var nextProbeAt: Date?

    public init(id: String, name: String, version: String, capabilities: [String], usesTrackFetcher: Bool? = nil, status: PluginStatus, origin: PluginOrigin, enabled: Bool, description: String? = nil, icon: String? = nil, configFields: [ConfigFieldDescriptor], secretsConfigured: [String: Bool], firstEnabledAt: Date? = nil, lastError: String? = nil, nextProbeAt: Date? = nil) {
        self.id = id
        self.name = name
        self.version = version
        self.capabilities = capabilities
        self.usesTrackFetcher = usesTrackFetcher
        self.status = status
        self.origin = origin
        self.enabled = enabled
        self.description = description
        self.icon = icon
        self.configFields = configFields
        self.secretsConfigured = secretsConfigured
        self.firstEnabledAt = firstEnabledAt
        self.lastError = lastError
        self.nextProbeAt = nextProbeAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case version = "version"
        case capabilities = "capabilities"
        case usesTrackFetcher = "usesTrackFetcher"
        case status = "status"
        case origin = "origin"
        case enabled = "enabled"
        case description = "description"
        case icon = "icon"
        case configFields = "configFields"
        case secretsConfigured = "secretsConfigured"
        case firstEnabledAt = "firstEnabledAt"
        case lastError = "lastError"
        case nextProbeAt = "nextProbeAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.version = try container.decode(String.self, forKey: .version)
        self.capabilities = try container.decode([String].self, forKey: .capabilities)
        self.usesTrackFetcher = try container.decodeIfPresent(Bool.self, forKey: .usesTrackFetcher)
        self.status = try container.decode(PluginStatus.self, forKey: .status)
        self.origin = try container.decode(PluginOrigin.self, forKey: .origin)
        self.enabled = try container.decode(Bool.self, forKey: .enabled)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
        self.icon = try container.decodeIfPresent(String.self, forKey: .icon)
        self.configFields = try container.decode([ConfigFieldDescriptor].self, forKey: .configFields)
        self.secretsConfigured = try container.decode([String: Bool].self, forKey: .secretsConfigured)
        self.firstEnabledAt = try container.decodeIfPresent(Date.self, forKey: .firstEnabledAt)
        self.lastError = try container.decodeIfPresent(String.self, forKey: .lastError)
        self.nextProbeAt = try container.decodeIfPresent(Date.self, forKey: .nextProbeAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.version, forKey: .version)
        try container.encode(self.capabilities, forKey: .capabilities)
        try container.encodeIfPresent(self.usesTrackFetcher, forKey: .usesTrackFetcher)
        try container.encode(self.status, forKey: .status)
        try container.encode(self.origin, forKey: .origin)
        try container.encode(self.enabled, forKey: .enabled)
        try container.encodeIfPresent(self.description, forKey: .description)
        try container.encodeIfPresent(self.icon, forKey: .icon)
        try container.encode(self.configFields, forKey: .configFields)
        try container.encode(self.secretsConfigured, forKey: .secretsConfigured)
        try container.encodeIfPresent(self.firstEnabledAt, forKey: .firstEnabledAt)
        try container.encodeIfPresent(self.lastError, forKey: .lastError)
        try container.encodeIfPresent(self.nextProbeAt, forKey: .nextProbeAt)
    }
}

/// A plugin as the settings list sees it. Carries no configured VALUES, only which secrets are set
public struct PluginSummaryInput: Codable, Equatable, Sendable {
    public var id: String
    public var name: String
    public var version: String
    public var capabilities: [String]
    /// Whether the manifest declares the `trackFetcher` permission, so its records reach air through the station's own track fetcher and that fetcher needs its own authorization. Not the same as the `stream` capability, which a plugin that mints its own stream URLs declares too. Absent means it does not
    public var usesTrackFetcher: Bool?
    public var status: PluginStatus
    public var origin: PluginOrigin
    public var enabled: Bool
    public var description: String?
    public var icon: String?
    public var configFields: [ConfigFieldDescriptor]
    /// Whether a value is currently stored, per `secret` field under its own key and per `secret` cell under `field/rowId/column`. Never the value itself
    public var secretsConfigured: [String: Bool]
    /// The last recorded failure. Absent means it is not currently unhappy
    public var lastError: String?

    public init(id: String, name: String, version: String, capabilities: [String], usesTrackFetcher: Bool? = nil, status: PluginStatus, origin: PluginOrigin, enabled: Bool, description: String? = nil, icon: String? = nil, configFields: [ConfigFieldDescriptor], secretsConfigured: [String: Bool], lastError: String? = nil) {
        self.id = id
        self.name = name
        self.version = version
        self.capabilities = capabilities
        self.usesTrackFetcher = usesTrackFetcher
        self.status = status
        self.origin = origin
        self.enabled = enabled
        self.description = description
        self.icon = icon
        self.configFields = configFields
        self.secretsConfigured = secretsConfigured
        self.lastError = lastError
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case version = "version"
        case capabilities = "capabilities"
        case usesTrackFetcher = "usesTrackFetcher"
        case status = "status"
        case origin = "origin"
        case enabled = "enabled"
        case description = "description"
        case icon = "icon"
        case configFields = "configFields"
        case secretsConfigured = "secretsConfigured"
        case lastError = "lastError"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.version = try container.decode(String.self, forKey: .version)
        self.capabilities = try container.decode([String].self, forKey: .capabilities)
        self.usesTrackFetcher = try container.decodeIfPresent(Bool.self, forKey: .usesTrackFetcher)
        self.status = try container.decode(PluginStatus.self, forKey: .status)
        self.origin = try container.decode(PluginOrigin.self, forKey: .origin)
        self.enabled = try container.decode(Bool.self, forKey: .enabled)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
        self.icon = try container.decodeIfPresent(String.self, forKey: .icon)
        self.configFields = try container.decode([ConfigFieldDescriptor].self, forKey: .configFields)
        self.secretsConfigured = try container.decode([String: Bool].self, forKey: .secretsConfigured)
        self.lastError = try container.decodeIfPresent(String.self, forKey: .lastError)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.version, forKey: .version)
        try container.encode(self.capabilities, forKey: .capabilities)
        try container.encodeIfPresent(self.usesTrackFetcher, forKey: .usesTrackFetcher)
        try container.encode(self.status, forKey: .status)
        try container.encode(self.origin, forKey: .origin)
        try container.encode(self.enabled, forKey: .enabled)
        try container.encodeIfPresent(self.description, forKey: .description)
        try container.encodeIfPresent(self.icon, forKey: .icon)
        try container.encode(self.configFields, forKey: .configFields)
        try container.encode(self.secretsConfigured, forKey: .secretsConfigured)
        try container.encodeIfPresent(self.lastError, forKey: .lastError)
    }
}

/// A summary plus the stored NON-SECRET configuration
public struct PluginDetail: Codable, Equatable, Sendable {
    public var id: String
    public var name: String
    public var version: String
    public var capabilities: [String]
    /// Whether the manifest declares the `trackFetcher` permission, so its records reach air through the station's own track fetcher and that fetcher needs its own authorization. Not the same as the `stream` capability, which a plugin that mints its own stream URLs declares too. Absent means it does not
    public var usesTrackFetcher: Bool?
    public var status: PluginStatus
    public var origin: PluginOrigin
    public var enabled: Bool
    public var description: String?
    public var icon: String?
    public var configFields: [ConfigFieldDescriptor]
    /// Whether a value is currently stored, per `secret` field under its own key and per `secret` cell under `field/rowId/column`. Never the value itself
    public var secretsConfigured: [String: Bool]
    /// When this plugin was first ever enabled. Absent means it never has been, so the console asks before it is
    public var firstEnabledAt: Date?
    /// The last recorded failure. Absent means it is not currently unhappy
    public var lastError: String?
    /// When the breaker will probe this plugin again on its own. Absent means no probe is pending
    public var nextProbeAt: Date?
    /// Absolute path of the plugin's directory on the station
    public var dir: String
    public var config: [String: JSONValue]
    public var oauthConnected: Bool?
    public var logLevel: PluginLogLevel

    public init(id: String, name: String, version: String, capabilities: [String], usesTrackFetcher: Bool? = nil, status: PluginStatus, origin: PluginOrigin, enabled: Bool, description: String? = nil, icon: String? = nil, configFields: [ConfigFieldDescriptor], secretsConfigured: [String: Bool], firstEnabledAt: Date? = nil, lastError: String? = nil, nextProbeAt: Date? = nil, dir: String, config: [String: JSONValue], oauthConnected: Bool? = nil, logLevel: PluginLogLevel) {
        self.id = id
        self.name = name
        self.version = version
        self.capabilities = capabilities
        self.usesTrackFetcher = usesTrackFetcher
        self.status = status
        self.origin = origin
        self.enabled = enabled
        self.description = description
        self.icon = icon
        self.configFields = configFields
        self.secretsConfigured = secretsConfigured
        self.firstEnabledAt = firstEnabledAt
        self.lastError = lastError
        self.nextProbeAt = nextProbeAt
        self.dir = dir
        self.config = config
        self.oauthConnected = oauthConnected
        self.logLevel = logLevel
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case version = "version"
        case capabilities = "capabilities"
        case usesTrackFetcher = "usesTrackFetcher"
        case status = "status"
        case origin = "origin"
        case enabled = "enabled"
        case description = "description"
        case icon = "icon"
        case configFields = "configFields"
        case secretsConfigured = "secretsConfigured"
        case firstEnabledAt = "firstEnabledAt"
        case lastError = "lastError"
        case nextProbeAt = "nextProbeAt"
        case dir = "dir"
        case config = "config"
        case oauthConnected = "oauthConnected"
        case logLevel = "logLevel"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.version = try container.decode(String.self, forKey: .version)
        self.capabilities = try container.decode([String].self, forKey: .capabilities)
        self.usesTrackFetcher = try container.decodeIfPresent(Bool.self, forKey: .usesTrackFetcher)
        self.status = try container.decode(PluginStatus.self, forKey: .status)
        self.origin = try container.decode(PluginOrigin.self, forKey: .origin)
        self.enabled = try container.decode(Bool.self, forKey: .enabled)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
        self.icon = try container.decodeIfPresent(String.self, forKey: .icon)
        self.configFields = try container.decode([ConfigFieldDescriptor].self, forKey: .configFields)
        self.secretsConfigured = try container.decode([String: Bool].self, forKey: .secretsConfigured)
        self.firstEnabledAt = try container.decodeIfPresent(Date.self, forKey: .firstEnabledAt)
        self.lastError = try container.decodeIfPresent(String.self, forKey: .lastError)
        self.nextProbeAt = try container.decodeIfPresent(Date.self, forKey: .nextProbeAt)
        self.dir = try container.decode(String.self, forKey: .dir)
        self.config = try container.decode([String: JSONValue].self, forKey: .config)
        self.oauthConnected = try container.decodeIfPresent(Bool.self, forKey: .oauthConnected)
        self.logLevel = try container.decode(PluginLogLevel.self, forKey: .logLevel)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.version, forKey: .version)
        try container.encode(self.capabilities, forKey: .capabilities)
        try container.encodeIfPresent(self.usesTrackFetcher, forKey: .usesTrackFetcher)
        try container.encode(self.status, forKey: .status)
        try container.encode(self.origin, forKey: .origin)
        try container.encode(self.enabled, forKey: .enabled)
        try container.encodeIfPresent(self.description, forKey: .description)
        try container.encodeIfPresent(self.icon, forKey: .icon)
        try container.encode(self.configFields, forKey: .configFields)
        try container.encode(self.secretsConfigured, forKey: .secretsConfigured)
        try container.encodeIfPresent(self.firstEnabledAt, forKey: .firstEnabledAt)
        try container.encodeIfPresent(self.lastError, forKey: .lastError)
        try container.encodeIfPresent(self.nextProbeAt, forKey: .nextProbeAt)
        try container.encode(self.dir, forKey: .dir)
        try container.encode(self.config, forKey: .config)
        try container.encodeIfPresent(self.oauthConnected, forKey: .oauthConnected)
        try container.encode(self.logLevel, forKey: .logLevel)
    }
}

/// A summary plus the stored NON-SECRET configuration
public struct PluginDetailInput: Codable, Equatable, Sendable {
    public var id: String
    public var name: String
    public var version: String
    public var capabilities: [String]
    /// Whether the manifest declares the `trackFetcher` permission, so its records reach air through the station's own track fetcher and that fetcher needs its own authorization. Not the same as the `stream` capability, which a plugin that mints its own stream URLs declares too. Absent means it does not
    public var usesTrackFetcher: Bool?
    public var status: PluginStatus
    public var origin: PluginOrigin
    public var enabled: Bool
    public var description: String?
    public var icon: String?
    public var configFields: [ConfigFieldDescriptor]
    /// Whether a value is currently stored, per `secret` field under its own key and per `secret` cell under `field/rowId/column`. Never the value itself
    public var secretsConfigured: [String: Bool]
    /// The last recorded failure. Absent means it is not currently unhappy
    public var lastError: String?
    /// Absolute path of the plugin's directory on the station
    public var dir: String
    public var config: [String: JSONValue]
    public var oauthConnected: Bool?
    public var logLevel: PluginLogLevel

    public init(id: String, name: String, version: String, capabilities: [String], usesTrackFetcher: Bool? = nil, status: PluginStatus, origin: PluginOrigin, enabled: Bool, description: String? = nil, icon: String? = nil, configFields: [ConfigFieldDescriptor], secretsConfigured: [String: Bool], lastError: String? = nil, dir: String, config: [String: JSONValue], oauthConnected: Bool? = nil, logLevel: PluginLogLevel) {
        self.id = id
        self.name = name
        self.version = version
        self.capabilities = capabilities
        self.usesTrackFetcher = usesTrackFetcher
        self.status = status
        self.origin = origin
        self.enabled = enabled
        self.description = description
        self.icon = icon
        self.configFields = configFields
        self.secretsConfigured = secretsConfigured
        self.lastError = lastError
        self.dir = dir
        self.config = config
        self.oauthConnected = oauthConnected
        self.logLevel = logLevel
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case version = "version"
        case capabilities = "capabilities"
        case usesTrackFetcher = "usesTrackFetcher"
        case status = "status"
        case origin = "origin"
        case enabled = "enabled"
        case description = "description"
        case icon = "icon"
        case configFields = "configFields"
        case secretsConfigured = "secretsConfigured"
        case lastError = "lastError"
        case dir = "dir"
        case config = "config"
        case oauthConnected = "oauthConnected"
        case logLevel = "logLevel"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.version = try container.decode(String.self, forKey: .version)
        self.capabilities = try container.decode([String].self, forKey: .capabilities)
        self.usesTrackFetcher = try container.decodeIfPresent(Bool.self, forKey: .usesTrackFetcher)
        self.status = try container.decode(PluginStatus.self, forKey: .status)
        self.origin = try container.decode(PluginOrigin.self, forKey: .origin)
        self.enabled = try container.decode(Bool.self, forKey: .enabled)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
        self.icon = try container.decodeIfPresent(String.self, forKey: .icon)
        self.configFields = try container.decode([ConfigFieldDescriptor].self, forKey: .configFields)
        self.secretsConfigured = try container.decode([String: Bool].self, forKey: .secretsConfigured)
        self.lastError = try container.decodeIfPresent(String.self, forKey: .lastError)
        self.dir = try container.decode(String.self, forKey: .dir)
        self.config = try container.decode([String: JSONValue].self, forKey: .config)
        self.oauthConnected = try container.decodeIfPresent(Bool.self, forKey: .oauthConnected)
        self.logLevel = try container.decode(PluginLogLevel.self, forKey: .logLevel)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.version, forKey: .version)
        try container.encode(self.capabilities, forKey: .capabilities)
        try container.encodeIfPresent(self.usesTrackFetcher, forKey: .usesTrackFetcher)
        try container.encode(self.status, forKey: .status)
        try container.encode(self.origin, forKey: .origin)
        try container.encode(self.enabled, forKey: .enabled)
        try container.encodeIfPresent(self.description, forKey: .description)
        try container.encodeIfPresent(self.icon, forKey: .icon)
        try container.encode(self.configFields, forKey: .configFields)
        try container.encode(self.secretsConfigured, forKey: .secretsConfigured)
        try container.encodeIfPresent(self.lastError, forKey: .lastError)
        try container.encode(self.dir, forKey: .dir)
        try container.encode(self.config, forKey: .config)
        try container.encodeIfPresent(self.oauthConnected, forKey: .oauthConnected)
        try container.encode(self.logLevel, forKey: .logLevel)
    }
}

public enum ConfigFieldColumnType: String, Codable, CaseIterable, Sendable {
    case string = "string"
    case url = "url"
    case select = "select"
    case secret = "secret"
}

public indirect enum ConfigFieldDescriptorDefault: Codable, Equatable, Sendable {
    case string(String)
    case number(Double)
    case boolean(Bool)

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = try? container.decode(String.self) {
            self = .string(value)
        } else if let value = try? container.decode(Double.self) {
            self = .number(value)
        } else if let value = try? container.decode(Bool.self) {
            self = .boolean(value)
        } else {
            throw DecodingError.dataCorruptedError(in: container, debugDescription: "No ConfigFieldDescriptorDefault member matched the payload")
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .string(let value):
            try container.encode(value)
        case .number(let value):
            try container.encode(value)
        case .boolean(let value):
            try container.encode(value)
        }
    }
}
