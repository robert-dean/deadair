// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `stream.ck`.
public final class StreamClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// Get HLS playlist
    /// One HLS playlist, and the tick that says somebody is still listening to it
    /// - Throws: `SdkError` on 404
    public func getHlsPlaylist(name: String) async throws -> GetHlsPlaylistResult {
        let request = try SdkRequest(method: "GET", path: ["hls", http.segment(name)])
        let response = try await http.execute(request)
        let headers = try GetHlsPlaylistHeaders(
            cacheControl: http.optionalHeader(response, "cache-control", as: String.self)
        )
        return GetHlsPlaylistResult(data: response.body, headers: headers)
    }

    /// Read fetcher authorization
    /// What the track fetcher holds by way of a Spotify login, and whether an authorization is already waiting to be finished
    public func readFetcherAuthorization() async throws -> FetcherAuthorization {
        let request = SdkRequest(method: "GET", path: ["stream", "authorization"])
        let response = try await http.execute(request)
        return try http.decodeJSON(FetcherAuthorization.self, from: response)
    }

    /// Start fetcher authorization
    /// Starts the fetcher's one-time authorization and answers with the URL to open. Starting another replaces whichever was pending
    public func startFetcherAuthorization() async throws -> FetcherAuthorizationStart {
        let request = SdkRequest(method: "POST", path: ["stream", "authorization"])
        let response = try await http.execute(request)
        return try http.decodeJSON(FetcherAuthorizationStart.self, from: response)
    }

    /// Finish fetcher authorization
    /// Finishes an authorization from the address the operator's browser ended up at
    public func finishFetcherAuthorization(body: FetcherAuthorizationInput) async throws -> FetcherAuthorizationFinished {
        var request = SdkRequest(method: "POST", path: ["stream", "authorization", "complete"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(FetcherAuthorizationFinished.self, from: response)
    }
}

/// Response headers declared on GET /hls/{name}.
public struct GetHlsPlaylistHeaders: Equatable, Sendable {
    public let cacheControl: String?

    public init(cacheControl: String?) {
        self.cacheControl = cacheControl
    }
}

/// The body of GET /hls/{name}, with the response headers the contract declares.
public struct GetHlsPlaylistResult: Equatable, Sendable {
    public let data: Data
    public let headers: GetHlsPlaylistHeaders

    public init(data: Data, headers: GetHlsPlaylistHeaders) {
        self.data = data
        self.headers = headers
    }
}
