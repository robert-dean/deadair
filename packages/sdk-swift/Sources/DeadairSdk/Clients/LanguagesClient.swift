// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `languages.ck`.
public final class LanguagesClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List console languages
    /// Every language this station holds a pack for, without the strings
    public func listConsoleLanguages() async throws -> ConsoleLanguageList {
        let request = SdkRequest(method: "GET", path: ["console", "languages"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ConsoleLanguageList.self, from: response)
    }

    /// Get console language
    /// One language's pack, strings and all, as it was imported
    public func getConsoleLanguage(locale: String) async throws -> ConsoleLanguagePack {
        let request = try SdkRequest(method: "GET", path: ["console", "languages", http.segment(locale)])
        let response = try await http.execute(request)
        return try http.decodeJSON(ConsoleLanguagePack.self, from: response)
    }

    /// Import console language
    /// Installs a language pack, replacing any pack already installed for the language. The tag in the path must be the pack's own
    public func importConsoleLanguage(locale: String, body: ConsoleLanguagePack) async throws -> ConsoleLanguageList {
        var request = try SdkRequest(method: "PUT", path: ["console", "languages", http.segment(locale)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ConsoleLanguageList.self, from: response)
    }

    /// Remove console language
    /// Removes a language. Anybody who had chosen it sees English
    public func removeConsoleLanguage(locale: String) async throws -> ConsoleLanguageList {
        let request = try SdkRequest(method: "DELETE", path: ["console", "languages", http.segment(locale)])
        let response = try await http.execute(request)
        return try http.decodeJSON(ConsoleLanguageList.self, from: response)
    }
}
