# docs/todo

Work that has been designed and deliberately deferred, plus the seam each piece drops into when it
lands. With one named exception (`todo.md`, at the bottom of the table), this is not a backlog of
ideas: everything here was scoped against the real tree during some piece of work, then cut to keep
that pass on one thing. The point of writing it down is that none of it needs re-deciding, only
building.

Two rules for this directory:

- **A file here describes the current tree**, with two exceptions: `from-v1.md`, which is explicitly
  a map of the pre-re-scaffold station and says so on every claim, and `todo.md`, which is the raw
  idea list and is scoped against nothing. Calls that were made and closed are argued in the scoped
  files themselves — `docs/internals/`, `apps/api/CLAUDE.md`, `packages/plugin-sdk/CLAUDE.md` — under
  the headings the root `CLAUDE.md` lists.
- **Verify before building.** These notes name tables, columns and modules as they stood on the date
  in each file's header. Check `apps/api/src/modules/modules.ts` and the migrations before relying
  on any of it.

| File | What it covers |
| --- | --- |
| [dj-voice.md](dj-voice.md) | **Both pieces built 2026-08-12.** What stood between a station that plays segments and one with a DJ. The TTS, the `llm` capability, the deterministic writer, the operator's own phrasings and the model binding are all in. Kept for the smaller things it leaves behind: cue visibility, segment duration, a console for segments, and play history for what the station SAID |
| [produced-episodes.md](produced-episodes.md) | **Built 2026-08-16**, the same day it was written as a deferral. All five decisions landed as written: an admission is a PASS or a BEAT and never an episode (the KV-cache argument that makes `converse` hold across a tool loop does not survive four different conversations, and the cost is the station on its deterministic floor for the length of a show), a job per pass with the row as the checkpoint, a production entering the running order whole or not at all, and timing that never comes from the model. The tiers came out FOUR rather than three, with `breaking` above `air`, and a production moves between two of them on its real slot rather than on an invented aging rule. Kept for that reasoning, and for the six things decided in building the CAST on 2026-08-25 — a caller is a persona of kind `caller`, a turn is planned in its own word band, `OutlineBeat.lead` was deleted rather than read because who speaks is arithmetic, a caller with a `latitude` may say what it thinks while the host is told not to confirm it, a production beat is never recast alone, and a production writes `script_history` now so a character accumulates from a programme. What is still not built is SFX: the performance cues widened with the callers, but a dog barking under a voice is mixing that does not happen in Node. **Commissioning on a schedule turned out to be built too** — `ProductionScheduler` reads anchored clock bands three hours ahead — so that line was corrected on 2026-08-19, along with the division it implies (clock bands schedule what the station MAKES; the daypart schedule decides what it PLAYS BETWEEN) and the one hazard a changeover brings to an episode that has been inserted but not yet heard |
| [director-and-lineups.md](director-and-lineups.md) | Segments, an LLM DJ, live provider search, **the daypart schedule (BUILT 2026-08-19, as `deadair.schedule_slots` plus a minute's tick that posts the operator's own `putOnAir`)**, station permissions, plugins that programme the station, push destinations, rotation rules as settings, palette steering, the station console page |
| [station-intelligence.md](station-intelligence.md) | The layer above the rules. **§1, the LLM DJ, built 2026-08-12**; **§8 built in full 2026-08-13**, both the silence diagnosis and the activity feed, with the load-bearing calls of each written up there and a `Heartbeat` primitive under the first; §3/§4 before both; **§2 deliberately deferred** against its own ordering claim, with the reasoning kept for the day the model stops being self-hosted, and **measured at last on 2026-08-28**: the number that section said it lacked is 289,108 tokens over 5.3 days, about 54,400 a day, and the column a cap would be built over has a hole in it, since 13 of 176 model attempts occupied the model and recorded no usage at all. The meter is complete wherever an answer came back and blank wherever one did not, which is to say blank in exactly the two shapes a runaway takes. What is left: the freshness bubble, genre and era correctness, and listener signal. **§5, never-play rules, was rescoped out on 2026-08-19** into the file below, which supersedes it for the build |
| [never-play-rules.md](never-play-rules.md) | Telling the station to refuse a KIND of thing rather than a thing: a predicate over genre and tag, scoped by season, daypart, lineup mode and schedule slot. Small because the chokepoint already exists — `PickResolver` is the one step every pick from every generator passes through — and the real work is that `RotationCandidate` carries nothing a predicate could read, so it needs a `tagsFor` beside the `ratingsFor` that already makes a dislike true for a pick the catalog never drew. Plus the one-directional word-boundary genre matcher that makes `Punk` catch `Punk Rock` without `Rap` catching `Trap`, and three refusals: no `require` direction, no weight column (a veto and a lean are different mechanisms), and no loose SQL exclusion, because over-fetching is only safe in the positive direction |
| [repeat-overrules.md](repeat-overrules.md) | Telling the station to stop refusing repeats for one stretch of the day, which is what a "Metallica hour" actually needs (2026-08-19). Half built and with no writer: `resolveRules` already resolves station defaults under a lineup's own per-field overrides and `station_lineup.rules` already persists them, but `PutOnAirInput` has no `rules`, `schedule_slots` has no column, and nothing anywhere sets one. Three rules have to come off rather than one — the artist cooldown is what is actually refusing the records, the per-artist cap is what trims the batch, and only then does the repeat window matter — and `mode: 'feature'` is explicitly not the shortcut, because its `NO_RULES` baseline also silences the DJ, drops the welcome and stops the order extending. Plus the second half, which is a change to the draw: nothing reads a play count, so a lifted window shuffles the same four records, and the fix is a bias in `weightOf` over a 120-day count keyed on `song_key`, never a filter and never a strict ordering |
| [embedding-axis.md](embedding-axis.md) | **The largest deferred thing here, and the only one that unblocks a file marked blocked** (2026-08-28, promoted out of the comparable-stations survey). A joint text-and-audio embedding per record gives the station a timbral axis from the AUDIO, which is a real answer to `EnrichedTrack.moods` having no source — no tags, no upstream. It carries a measured calibration trap that has to be built in from the first pass rather than found later: scores from one model against different prompts are not comparable, and ranking them raw put `energetic` on 61.7% of a library against `calm` at 0.9%. Plus the two second-order rules (prune per mood, never a middle bucket), the costs counted up front (no pgvector, a second model in the sidecar and so the WEIGHTS' licence), and the one rule that is invisible if got wrong: the tagger's own output must never be embedded, because the labels come from the vectors |
| [operator-break-kinds.md](operator-break-kinds.md) | **The only deferred item that adds an operator SURFACE rather than a field** (2026-08-28, promoted out of the same survey). Adding a kind of break is a code change in five places today, and every one of those places is already built to take one — `segments.kind` is unconstrained text and says so. The argument is a catalogue rather than a design: a two-line poem about the moment, a note on taping music off the radio, a first-time-I-heard-this memory, none of which needs code and none of which would ever have been specified. Three things to decide first, and the one that sets the size is whether an authored kind runs code, because a kind that fetches is a plugin wearing a different name |
| [small-wins.md](small-wins.md) | Three that are small, independent, and costed against the tree (2026-08-28): a second scrobble destination, where most of the work is already built for enrichment and the one rule is that the two destinations fail independently; `.pls` and `.m3u` endpoints, pointless on a LAN and not pointless behind a tunnel; and an MCP surface, which **writing this down corrected** — the mechanism is small and the authorization is not, so it waits on [service-actors.md](service-actors.md) rather than sitting beside the other two |
| [station-moment.md](station-moment.md) | The clock, the calendar and the weather as one resolver both the selector and the writers read: an operator-editable mood vocabulary, the day's lean, occasions, and why mood-biased selection is blocked and mood-flavoured talk is not |
| [personas.md](personas.md) | What is left after the character sheet, which is built: one station has ONE persona and the news bulletin is the case that broke it (a kind-scoped persona, and the partial unique index that has to move with it); **persona MEMORY, BUILT 2026-08-22** as `deadair.persona_notes` over the `persona_key` on `script_history` it named as its prerequisite, with the two open questions answered as BOTH kinds split by evidence — what the character said goes into use, what it has settled into is proposed — plus the precision bug the smoke script caught, where a millisecond watermark over a microsecond column re-reads its own last row forever; **a schedule choosing the persona, BUILT 2026-08-19** with the daypart schedule and costing exactly the one column it predicted; and the CHANGEOVER, which is what that schedule leaves unsaid — one break in the incoming host's voice, because `putOnAir` retracts the outgoing order before a sign-off could ride it. **§4, the rehearsal, built 2026-08-16**, and its open half closed since: it takes the `preview` tier, and it is spoken. **Extended 2026-09-09 into the AUDITION** — the same question asked over a playlist rather than one invented pair, as `POST /personas/{id}/auditions`, a job per transition and nothing aired. Two things a single rehearsal never had to answer: a run carries its OWN `recent`, or it measures a signature phrase repeating in a way no broadcast would allow; and a model lost to the station is retried rather than recorded, or a busy Tuesday is reported as a fact about the sheet. Its closing section is one entry shorter as of 2026-08-21: a persona choosing its own music is RESOLVED in the other direction, since the `music` line is deleted and a persona is now purely a voice. **§6 added 2026-08-28**, and it is what §2 left unsaid: the notebook has no exit but an operator's hand, `last_used_at` rotates a growing pool rather than bounding it, and the distil pass reads scripts written under the notes it already holds, whose fixed point is a character with one idea. Three candidate rules, cheapest first, none to be built before [break-ratings.md](break-ratings.md) gives them an opinion to decay on rather than the age-and-use proxies for one. **Sharpened 2026-08-30** into a choice of TIER rather than of rule: a continuity store can be bounded by soft decay, by a boundary reset or by an operator's purge, this tree already has the boundary tier built and correct one layer down in `BreakWriteRequest.recent`, and the notebook must NOT have it, since outliving a broadcast is the whole feature. So the notebook wants soft decay specifically, and an avoid-list is not a forget mechanism |
| [break-ratings.md](break-ratings.md) | **Nothing judges what the station SAID** (2026-08-22). The only automatic signal is `CharacterFault`, which is the station's own rubric and is gameable in the way `overusedWords` already documents — the cheapest way to pass a marker check is to say the marker list again — so a pass tuning a sheet to reduce declines steers straight at it. The audience is a count and says nothing about a sentence, and the operator's only recorded opinions are about music. A `deadair.script_ratings` table rather than a column, because `script_history` is append-only on the stated grounds that a row is a fact about a moment; on the ATTEMPT rather than the persona, so the floor's own phrasings can be rated too and every narrower verdict stays a `group by`. Three readers in the order the value arrives: the notebook's distil selection (one `where` clause), a persona performance read, and the deferred sheet proposals — which propose rather than write, carry their evidence, and are judged by `PersonaRehearsalService` against its deliberately fixed pair |
| [tool-plugins.md](tool-plugins.md) | A `tool` capability so a plugin can be something the model calls mid-sentence (weather, news, RSS), and which tools are host-side sources instead |
| [news-and-bulletins.md](news-and-bulletins.md) | **The source and the bulletin are both built** (2026-08-15): a `news` capability, an RSS plugin over an SDK feed parser, `NewsService`, a `read_news` tool, and a `news` segment kind that `:30 news` on the station clock now produces — a model writer over a floor that reads published headlines as published, both declining when there is nothing to report. Kept for the reasoning, for the one question left open (whether a bulletin attributes its sources out loud), and for what is still deferred: the watcher that would post a breaking-news `BreakRequest`, which is deliberately a loop over any polled source rather than a news feature, and the conditional GET that becomes worth it the day something polls |
| [chart-discovery.md](chart-discovery.md) | A `charts` capability so an operator can point the station at the week's hits, and the three seams it drops into (a tool the DJ can call, a generator that needs no model, chart history as patter). Small now only because `PickResolver` already ingests a name the library has never held — plus which chart services actually answer without auth, and why Billboard is a name to reach rather than the first plugin |
| [fact-enrichment.md](fact-enrichment.md) | **Built 2026-08-15**, except the list at its bottom: `deadair.facts` holds one sourced claim each, extracted host-side out of prose a plugin handed over, with `source_url` and `source_quote` `not null` so a claim with no source is not expressible. Kept for the record of what was decided (a plugin fetches, the host thinks; the lead sentence as a floor that needs no model; the separate verification conversation) and for what was deliberately left: a second prose source, categories driving persona choice, a console write surface, and why facts never retire |
| [track-lyrics.md](track-lyrics.md) | Lyrics as enrichment (2026-08-16), and the negative decision that shapes it: a lyric is the one payload the station may read and may never say, so it does NOT arrive as a `SourceDocument` and never becomes a fact, because the floor extractor takes an opening sentence verbatim and cannot decline. What they are actually for, ranked: a vocal onset for the talk-up limit that needs no model and answers half of `track-analysis.md`'s deferred field, then derived labels, then a DJ that knows the subject. **The provider table was written from memory and has now been verified live (2026-09-02), which moved two rows and the phase order.** LRCLIB answered 59 of 60 of this station's own records on the strictest match with no fallback ever reached, 52 of them with synced timings, and it is MIT-licensed with published dumps so it can be taken off the network entirely: it is the first plugin, and the operator's own library is not, because this install has no library. Genius returns lyric text after all, in referent fragments, so the ANNOTATION path inherits this file's hazard rather than escaping it and `fact-enrichment.md` needs telling. Musixmatch puts every time-synced endpoint behind the paid tier, which is exactly the phase that justifies the feature, and its mandatory usage tracking exists only as a display pixel a station with no page cannot satisfy. Plus the measured trap under the vocal onset: 12 of 52 synced lyrics start under three seconds and 6 under one, so an unfloored `min(introEnd, vocalOnset - safety)` hands a quarter of the library a talk-up of zero. **A second pass the same day went looking for prior art and found the phase built elsewhere**, which changed the shape of the answer rather than the choice of source: derive vocal RANGES over the whole track rather than one onset, because the END of the last range is where the singing stops and nothing else in this tree can say; return a tri-state so unsynced text reads as "cannot say" rather than zero; an instrumental sometimes arrives disguised as a lyric line; and the library call the file originally named is the one that discards the timings. Plus an aggregator that takes the second-source slot, a word-level database keyed by a platform id this station already holds, a use of a lyric nobody here had listed (an excerpt in the embedding text, which is what stops similarity degenerating into artist-name matching), and the finding that phase 5 has no prior art anywhere |
| [clean-copy-matching.md](clean-copy-matching.md) | **The advisory policy is built** (2026-08-16): `track_sources.advisory` per copy, `rotation.advisory` in three states, and binding selection doing the work because a clean edit and the explicit original are one track with two copies. What is deferred is going LOOKING for a clean copy the playlists never carried — a separate matcher from `ProviderTrackLookup` rather than a flag on it, because it needs to be looser on the title (`(Clean)`, `(Radio Edit)`) and stricter on identity, and the strict path must not grow a mode in which it is not strict. Plus the pairing cache, why a NEGATIVE result has to be stored too, and the one measurement it is all blocked on: whether the operator's own account-level explicit filter actually binds on the shim's fetch path, which the plugin now reports and deliberately does not enforce |
| [pick-artist-matching.md](pick-artist-matching.md) | **CLOSED by its own re-measure, 2026-08-28, without a line of code.** A model named a record correctly and the strict lookup refused it, because the pick held a display credit where an identity belongs (2026-08-15, measured: eight of ten drops in one refill were `X featuring Y`). The file insisted on re-measuring first, on the theory that the rotation keying fix had removed the cause rather than half of it. It had: **0 of 372 named picks carry a credit line**, over 50 captures, and the same `rap` brief that produced the failure now has the strict lookup discovering 42 of 66 with 2 misses. The model found the right shape by itself — the feature goes in the TITLE and the lead stays in the artist field, which is the provider's own spelling and resolves 7 of 7. Kept for the argument against loosening the comparison, for the joiner list that excludes `&` and `,` (`Tyler, The Creator` is live in the sample and splitting would destroy it), and for the named conditions that would re-open it. Also the source of a bigger loose number: **29 of those 50 model runs produced no picks at all** |
| [pick-rationale.md](pick-rationale.md) | **Why this record followed that one**, which the station cannot say: `SetGeneratorChain.announce` reports who named how many and nothing asks a generator for a reason or has anywhere to keep one. What it would make checkable (a brief being followed, the chain topping up, what `rotation.briefOnly` costs), and the three things that make it upstream of a console: the model has to be asked and that is not free, the deterministic floor has no reason and must not invent one, and a pick is not a row anywhere between being named and being aired. Three phases, the first needing no migration |
| [multi-station.md](multi-station.md) | A `deadair.stations` table so one install runs several stations, and what it subsumes |
| [break-removal.md](break-removal.md) | **The bug is fixed** (2026-08-12): deleting a talk break used to be forgotten by the next commit pass, because a spliced-out break left a gap the planner could not tell from one never planted into. A removed break is a `removed` item now — its own state, because an operator's cut and the station passing over an item it reached are opposite facts — and its row is retired. Kept for **the quiet spell**, which is the feature the operator actually wanted and is now scoped in three phases (a count of RECORDS on `station_air`, gating the planter and withdrawing the tail), plus the move case and what the fix cost in `committedThrough` |
| [render-plugin-readiness.md](render-plugin-readiness.md) | **A break written off for a plugin that was three hundred milliseconds from being up** (2026-08-15), with the log line. Workers start consuming before `PluginsModule` initializes, which `JobsModule` documents and is right to allow — but `RenderSegmentJob` marks the row `failed` for it, and what that costs splits: a planted break burns one of three retries, and one waiting for its audio is lost outright because `ripen` walks the running order and it is deliberately outside it. Three pieces, the first two worth having alone |
| [rundown.md](rundown.md) | What the rundown deliberately does not do yet: persistence and playhead corroboration, and why neither turned out to be a prerequisite for breaks |
| [crossfades.md](crossfades.md) | **Built 2026-08-12.** Blending one rundown item into the next, on the measured cue points. Kept for the blend POLICY inside the buffer, which still wants the beat layer and the licence question that carries |
| [track-analysis.md](track-analysis.md) | **Mostly done** (2026-08-11): the sidecar, the `analysis` capability, the four cue points and the loudness layer are built and measured. What remains is the beat layer (tempo, confidence, downbeats, vocal onset and curve) and the licence question it carries |
| [track-state-console.md](track-state-console.md) | Everything a record accumulates — bindings, local bytes, measurement, enrichment, opinion, what aired — in one place, plus a verb per cache to clear it. Seven axes exist as rows and the console can read two, which is why "why will this record not air" is a `psql` question; the three phases (a track detail read, then state columns with counts and filters, then the clears), and the commit-window rule any clear of the audio has to hold |
| [track-cache-eviction.md](track-cache-eviction.md) | **The bill for keeping every fetched record** (2026-08-14), now that `playout.trackCache` is gone and its off state stopped being expressible. Nothing evicts, discovery has no ceiling, and the one piece that cannot be retrofitted without a period of evicting the wrong files is `last_served_at` |
| [provider-audio-failures.md](provider-audio-failures.md) | **The blocker under three other files** (2026-08-11): the shim's session is healthy and individual tracks return 502, so 13 of 581 are measured, a hand-built running order had four of its eight tracks skipped, and the mount put two seconds of digital silence on air. Why that silence is three correct behaviours stacked on one upstream failure, where to look, and why raising the analysis batch size makes it worse |
| [analysis-queue-ordering.md](analysis-queue-ordering.md) | Which tracks the measurement walk picks and in what order (2026-08-11): why gating it on "already handed over" does not buy the download back, ordering the queue by `play_history` instead, splitting an unfetchable binding from an undecodable file, and the tee off the shim that would make measurement free |
| [mixer-settings-in-db.md](mixer-settings-in-db.md) | The four constants that decide how a break sounds, and the settings seam they bypass. **Smaller than it was** (2026-08-11): the restart trigger this file called the real work now exists, so what is left is four `STREAM_KEYS` entries and one decision about when to spend the restart |
| [listening-loop.md](listening-loop.md) | **Piece 1 is solved and not in this tree** (2026-08-16): a Cloudflare tunnel fronts the mount and the operator listens on a phone and an AVR, so the compose comments about an edge now describe a real deployment that lives in a Cloudflare account. That closes the piece the other two were queued behind, and it demotes rather than promotes them: history out loses most of its value now that the station is audible where it was for. Kept for the taste signal coming back from what is played elsewhere, the Spotify re-consent it should be batched into, and the three questions the public mount makes live (anonymity and `service-actors.md`, an Opus mount with a real argument at last, and the AVR as `now-playing-displays.md`) |
| [spotify-listening-profile.md](spotify-listening-profile.md) | The top-tracks, top-artists and saved-library data the Spotify grant already asks for and never reads, the two shapes that could carry it, and what the February 2026 API round did and did not take |
| [spotify-api-currency.md](spotify-api-currency.md) | Where the Spotify plugin has fallen behind the Web API. The two search-limit bugs were fixed on the spot; still open are the two scopes that unlock nothing, the SDK's removed batch overload, `account_id`, and the Premium requirement development mode grew |
| [provider-id-stability.md](provider-id-stability.md) | **Phases 1 and 2 both done 2026-08-28**, the day it was written as a deferral. Phase 2's guard found a live bug that had nothing to do with the deadline: a walk stopped at `PLUGIN_MAX_PAGES` returned looking complete, so the sweep retired everything past ten thousand items for never having been enumerated — the sweep's real precondition was "this walk saw the whole library", standing on the absence of an exception. Guard shipped as `catalog.sweepMaxPercent` (default 50, 100 means off) over a floor of twenty bindings. Phase 1 **confirmed the report and corrected it twice over**: the change is merged upstream and NOT YET RELEASED, so reading the release notes would have said it was wrong; it moves ~87% of song ids on a current install rather than all of them, which is the argument against ever tuning the threshold up toward 100; and album, artist and MusicBrainz ids are explicitly untouched, which turns phase 3 from a fuzzy search into an mbid-then-isrc ladder (745 of 766 tracks here carry an mbid). Also: **it can reach nothing on either install** — neither has ever bound a local library — and this file's original blast radius counted the streaming provider's 766 bindings without checking whose they were. Phase 3 is left conditional rather than deferred: it earns its keep only for a library bound BEFORE the operator upgrades their server, so if the upgrade lands first the phase closes unbuilt. **An external deadline, from a report rather than a measurement**: a Subsonic library server changing how it generates song ids, which would invalidate every `track_sources.external_id` at once. Why the station's own correct behaviours are what turn that into a rebuild (the missing sweep marks a full library, and `track_audio` cascades off the binding), the rule it argues for (a provider id is a LOCATOR and identity is the resolver's `artist_key`/`title_key`, which the export design already assumes), and three phases of which the second is worth doing alone: a walk that recognises NOTHING is not evidence the library was deleted, which is `AudienceWatch`'s argument about a failed reading reached from another direction. Phase 3 re-binds through `selectSong` rather than re-ingesting, and has to be stricter on identity than the ordinary lookup because it overwrites an answer instead of proposing one |
| [youtube-music.md](youtube-music.md) | A third music provider, split into a catalog half that is ordinary plugin code and an audio half that is a second track fetcher and a sidecar, plus the two pieces of the host that stop being Spotify-shaped when it lands. **Re-checked 2026-08-25**: the plan held and three premises did not, so the segment protocol is now an import rather than a project, the licence warning against depending on anyone's client was wrong (the JavaScript chain is MIT throughout), and the identity ladder has lost its anonymous rungs, which makes the pasted cookie the happy path and attestation a thing to plan for. Now also names who else has built this shape and what it cost them |
| [stream-logs.md](stream-logs.md) | **A diagnosis this blocked, with the receipt** (2026-08-12): Liquidsoap logs to stdout and only to stdout, so an hour went on control experiments and three wrong inferences before one `tail` named the file and line of a one-line bug (`4a1dd80`). What Liquidsoap offers instead of rotation (nothing — no size cap, no strftime, and SIGUSR1 from a logrotate we do not have), the three ways to fix it, and which half is a setting now that `config-watch.sh` performs the restart |
| [stream-formats.md](stream-formats.md) | **Mostly built.** What each output is for and why MP3 is a compatibility floor rather than a preference, why HLS carries AAC alone (and why the MP3 variant that used to sit beside it closed the output to every Chromium browser), what the encoders needed on the pinned image, and the hourly archive that is still the one output not served |
| [stream-quality-ceiling.md](stream-quality-ceiling.md) | How good the stream could actually get, stage by stage: why the ceiling is the SOURCE (Spotify's Vorbis 320, and a 24 kHz voice model) rather than the encoder, why MP3 128 over a lossy source is the one real loss the station chooses, the tier table, and the Icecast byte-denominated burst and queue that silently shorten in seconds when the bitrate goes up (now rendered rather than fixed) |
| [stream-latency.md](stream-latency.md) | **Optional, and deliberately not on the order below.** What the monitor's "2.3s behind" actually measures (one browser's buffer, not end-to-end), why the server side is already at its floor, the one unrun diagnostic that would settle it, and the real-time transport that would close it properly. Kept for the five things that transport collides with, one of which would silently mute the station |
| [now-playing-displays.md](now-playing-displays.md) | **Closed** (2026-08-19/20). Artwork and split track fields on a hardware player (BluOS), why the stream itself can never carry them, and what the probe of a live M10 found. The desktop app now PLAYS on such a player through a plugin (2026-09-08), which routes the audio and changes nothing about the display; the wire measurements have moved to `apps/desktop/CLAUDE.md` under `Plugins: BluOS` |
| [icecast-2.5.md](icecast-2.5.md) | **Mostly done** (2026-08-10): the container runs 2.5.0, the poll reads `/admin/publicstats` and the audience follows `/admin/eventfeed`. Kept for the two document shapes measured off a live 2.5, which match neither the old endpoint nor upstream's source, and for the small leftovers — now including the one 2.5 has put a deadline on (2026-08-23): it warns that the world-readable `icecast.xml` the app renders may be REFUSED by a future version, the single-container image already renders it `0640`, and what is left is measuring which uid the compose stack's containers read it as before narrowing the default under them |
| [stream-server-alternatives.md](stream-server-alternatives.md) | **Recommends keeping Icecast** (2026-08-18). The survey of what else could serve the mount, kept so it is not re-run: why the audience gate rather than the audio is what constrains the choice, and the eight candidates ranked against it. RSAS got the real pass and came out cheaper to build and weaker to want than it looked, both measured off its docs: it reads `icecast.xml` directly so there is no second config renderer, and its HLS is $25/month for something Liquidsoap does free and no faster. What it has no answer for is `/admin/eventfeed`, so the gate degrades to a poll. Plus the three phases if the seam is wanted anyway, and the two things to measure rather than reason about |
| [service-actors.md](service-actors.md) | Giving Liquidsoap and Icecast their own credentials, actor kind and permission tuples instead of one shared bridge secret |
| [backup-and-restore.md](backup-and-restore.md) | **Import and export for everything that is AUTHORED** (2026-08-22), with backup/restore falling out of it rather than being the target — because for one install `pg_dump` plus the `media/` tree is strictly better and needs no code, and what a dump cannot do is move authored configuration onto an install whose catalog ids are all different, or share one piece of it. Four tiers, and only the first is in scope: authored (settings, personas, topics, the format clock, the schedule, the lexicon, playlists, the three `rating` columns, plugin configs and grants, recorded idents), regenerable (out), the record (its own flag, and it is why a persona that moves installs arrives with its sheet and no memory), identity and live state (never). The decision the whole thing turns on is that an export is keyed by NATURAL keys and never by uuid — a rating exported as `track_id` restores onto nothing, silently, after a re-sync — using the resolver's own `artist_key`/`title_key` rather than a second matcher that can disagree with the one deciding what airs. Plus secrets defaulting to REDACTED with a named list to re-enter (ciphertext is useless for sharing and fails silently on the wrong key; plaintext is a credential file in the downloads folder), an import that writes through the SERVICES because a settings row needs the deferred reload and a plugin config needs the `AfterCommit` reinit, and replace requiring off air where merge does not, since `recast` already means "take effect now" |
| [comparable-stations.md](comparable-stations.md) | **A survey done once, kept so it is not re-run** (2026-08-22): another self-hosted station of the same shape read end to end, and what it does that this one does not. Nine of the fourteen findings already have a home in this directory, so the value is the evidence and the re-ranking rather than the ideas. Chiefly: the ending-shaped fade, the talk-up limit and the vocal-onset half of lyrics are ONE measurement pass, the beat layer. Five are genuinely absent and three of those are operator surfaces: nothing correlates one decision's calls, usage is per attempt and never adds up, and nothing assembles a station check-up. Plus the measured trap under the embedding axis that would unblock `station-moment.md` (scores from one model against different prompts are not comparable, at 61.7% against 0.9% on a library that is not this one), the small ones (a second scrobble destination, `.pls`/`.m3u`, an MCP surface), and what is deliberately NOT wanted, including file-based IPC and the evidence against it. **A fourth pass, 2026-08-28**, applied the third's closing lesson as a method and measured every answer: the model meter is blank for the 13 attempts that occupied the model and produced nothing (a 60-second timeout and a mid-flight disposal among them), which is exactly where a cap would need it; artist name-variant folding was recorded as a decision already made here and is not one, though it is measured as harmless today at 0 collisions and 0 key disagreements; and the beat layer at rank 2 inherits two guards, since a tempo confidence that is not wrong-tempo-aware is worse than none. **A fifth pass, 2026-08-30**, and the first whose main finding is about code written HERE: weather shipped as a capability and did not inherit `break.claims.ts`, so a reading fetched at write time carries an `observedAt` nothing reads and airs up to eight records later with no expiry, on a line that is the only one in `write.break.job.ts` not told when the break airs. That is now rank 0. Also: the non-Latin speech class is measured at 0 on this library, which is a fact about the operator's records rather than about the code, and the method itself is re-ranked — what is worth repeating is not reading somebody else's tree but running their list of hard-won questions against ours whenever this one grows a new surface. **A sixth pass, 2026-09-02**, narrows the method further and gets the most per question of any of them: instead of re-reading anything, take one deferred file about to be built and ask whether the other station has already built it. It had, and the answer reshaped `track-lyrics.md`'s central phase (ranges rather than an onset, a second free marker at the end of the vocal, a tri-state, and three traps). It also produced the survey's first genuine DISAGREEMENT between two real measurements — they use a synced lyric to replace their vocal detector, we measured our own catalogue and will only let it narrow a measured cue — which is an argument for measuring rather than for either answer. **A seventh pass, 2026-09-02**, read the other station's public fault list instead of its tree, 281 substantive entries against this code: nineteen are live here, two of them claims in THIS directory the code contradicts (a playlist put on air passes no veto, era or advisory gate at all; the voice selector guesses alphabetically while its help text says it refuses, measured at 56 silent breaks in nine days), and the rest are a batch-seam artist gap, a talk-over recorded as skipped after it aired, a sidecar whose per-decode memory is five times its README's number and whose two image definitions have already diverged, and a model call that sends a key some servers reject with nothing able to turn it off |
| [from-v1.md](from-v1.md) | Things the previous station did that this tree will eventually want back: the render pipeline, breaks, shows, the monitoring feed, extra sources, now-playing sinks |
| [todo.md](todo.md) | The station's raw idea list, and **the one file here that is exempt from the two rules above**: unscoped, undesigned, and not checked against the tree. An entry graduates out of it by being designed against real code, at which point it becomes a file above and the line here points at it |

## What is next, and against what test

**Set 2026-08-09.** Most of this directory is ordered by subsystem. The near-term work is not: the
target is a station the operator leaves on all day in place of a streaming service, and that test
ranks these files differently from how they read. Selection is already good enough for it and needs
no pass. What is left, in order:

1. ~~**The deterministic break writer**, and `BreakPlanner` planting `planned` segments rather than
   only choosing ready idents.~~ **Built 2026-08-09**, alternating with recorded idents. Cue
   visibility did NOT ride with it, because breaks are planted between records rather than over them;
   see [dj-voice.md](dj-voice.md) for what shipped and what that leaves.
2. ~~**[Crossfades](crossfades.md)**, once breaks are landing reliably, because the cross buffer
   moves the clock those breaks are timed against.~~ **Built 2026-08-12**, on the cue points the
   sidecar measured the day before, so `buffer = min(outgoing.outro, incoming.intro)` is a real
   number per pair. The blend POLICY inside the buffer still wants the beat layer, which is not
   built and carries the licence question.
3. ~~**A model as the writer's second binding**, with the deterministic one kept underneath as the
   floor rather than as scaffolding.~~ **Built 2026-08-12**, and it came out with THREE bindings
   rather than two: the model, the operator's own phrasings, and the station's five underneath both.
   The floor stopped being code and became a setting. See [dj-voice.md](dj-voice.md) for what
   shipped and the four things that came with it that were not on this list.
4. ~~**The activity feed** over the segment transitions the writers produce.~~ **Built 2026-08-13**,
   with its other half, the silence diagnosis, a few hours ahead of it. It really was a transport
   over rows that already exist: `GET /activity` unions `segment_events` and `play_history` where
   they are and adds one table of its own, `station_events`, for the facts that happen to the station
   as a whole and had no row anywhere — the silence cause changing, an air toggle, a gap that
   outlived the loop meant to close it. "Why was the station silent at 3am" is the question the
   diagnosis cannot answer and this one now does. See
   [station-intelligence.md](station-intelligence.md) §8 for the four decisions holding it up, and
   note the one thing it deliberately does not carry: plugin call logs, which stay behind
   `platform.manage` because plugin output can contain a token.

This list is done. What comes next is whatever the table above makes the case for.
[listening-loop.md](listening-loop.md) was the one competing on its own terms rather than queueing
behind anything, for the reason at the bottom of this file; **that is settled as of 2026-08-16**, and
the new order is at the very bottom.

**Landed since, 2026-08-11**, and none of it was on this list: the measurement layer under item 2.
`analysis/` is a sidecar, `plugins/analyzer` the adapter, `analysis` a capability of its own rather
than a kind of enrichment, and `deadair.track_analysis` the row. Two things an operator can hear
came with it and were not deferred behind the crossfade — the dead air trimmed off the head and tail
of every record (`liq_cue_in` / `liq_cue_out`), and a per-record level decided before air rather than
chased by a follower ([station-intelligence.md](station-intelligence.md) §4). The beat layer under
the same sidecar is untouched.

**One thing does pull itself forward**, added the same day: the four above assume listening at the
desk, which is where the station is audible and nowhere else. Replacing a streaming service means
being audible on a phone and in a car too, and that is infrastructure rather than app code. See
[listening-loop.md](listening-loop.md). It competed with item 1 rather than slotting behind it: the
writer makes the station worth listening to, reachability makes it possible to, and which comes first
depends on whether the next week of listening happens at the desk or not. With every item on that
list built, it is no longer competing with anything. **Closed 2026-08-16**; see the last section of
this file.

**Landed with item 3, 2026-08-12, and none of it was on this list.** Four things, because the writer
turned out to sit on plumbing that was not there:

- **Everything the station writes is kept.** `deadair.script_history`, one row per write ATTEMPT, so
  a model that declined and the floor that covered for it are two rows rather than one misleading
  one. It outlives its segment, is swept nightly against `render.scriptHistoryDays`, and keeps the
  prompt and the raw answer only while `llm.captureWrites` is on.
- **A state per stage of making a break** (`planned → writing → written → rendering → ready`), so a
  retry after a failed render re-speaks the words already written rather than paying for new ones,
  and so a console can tell "being written" from "waiting on the renderer".
- **A break is written when its slot comes near** rather than when it is planted, cutting the horizon
  from about an hour of model and speech work to about fifteen minutes of it.
- **A break's forward claim is checked before it airs**, which is `dj-voice.md` correction 5 and was
  a live defect in the deterministic writer rather than anything the model introduced.

**Selection got a pass after all, 2026-08-12.** The premise at the top of this list — "selection is
already good enough for it and needs no pass" — held right up until a second thing was allowed to
choose. [station-intelligence.md](station-intelligence.md) §1 is built: a model picks first and the
weighted catalog draw finishes whatever it did not, behind `llm.setGenerator` and off by default.
Item 4 is still the whole of what is left on this list; this was not on it and did not displace it.

Two things came with it that are worth knowing before designing anything else near selection:

- **A pick is judged where it becomes a track, not inside the generator that named it.** The rotation
  rules moved to `PickResolver`, because a NAME arriving from a second binding had been bypassing
  every one of them including the dislike filter, which is an instruction rather than a preference.
  Anything that grows a third way to name a record inherits the rules for free and must not add its
  own.
- **§2's budget was deliberately deferred**, against that section's own instruction to build it
  first. One call site behind one chokepoint, a self-hosted model with no bill, and an invariant that
  turned out structural. The reasoning is in the file so it can be re-opened rather than re-argued.

## The reachability question is closed, and what that leaves

**2026-08-16.** The operator fronted the mount with a Cloudflare tunnel and now listens on a phone
and an AVR. That is [listening-loop.md](listening-loop.md) piece 1, solved outside this repository,
and it is the last thing on this page that was competing with the table rather than sitting in it.
The goal at the top of this section (a station left on all day in place of a streaming service) is no
longer blocked on infrastructure.

So the ranking is the table's, and the table's case is for **defects that were measured**, ahead of
any feature:

1. ~~**[provider-audio-failures.md](provider-audio-failures.md)**~~, called the blocker under three
   other files and with the worst numbers on this page: 13 of 581 tracks measured, four of eight
   skipped in a hand-built order, two seconds of digital silence on air. **Defused 2026-08-14** by
   `docs/internals/director.md` § "Nothing airs until its bytes are here": a record is not committed
   until its audio is local, so the silence this describes is no longer reachable by that path.
2. ~~**[render-plugin-readiness.md](render-plugin-readiness.md)**~~, a break written off for a plugin
   three hundred milliseconds from being up. **Pieces 1 and 2 built 2026-08-21**, which that file
   says are the two worth having; piece 3 is deferred on its own argument and it calls it the weakest
   of the three.
3. ~~**[pick-artist-matching.md](pick-artist-matching.md)**~~, starting with the re-measure rather
   than the fix, because the rotation-keying change may already have removed half the cause. **It had
   removed all of it — closed 2026-08-28 by the re-measure, no code written.** The instruction to
   measure before building is the whole reason this cost an afternoon instead of a feature.

**This list is now empty**, which is worth saying out loud because it was the thing ranked above every
feature on this page. All three closed without the fix any of them proposed being built: two by work
done elsewhere for other reasons, one by counting. **The next thing is the ranked list in
[comparable-stations.md](comparable-stations.md)**, which resumes at its rank 2.

Three things sit alongside rather than in that order. **Productions are surfaced now**: the five
decisions in [produced-episodes.md](produced-episodes.md) are all built, a clock band commissions one
ahead of its slot, and as of 2026-08-25 a `callin` band produces an actual phone-in — a cast, short
alternating turns, one voice each. And the public mount makes two questions live that were
theoretical while it was on the LAN: whether it stays anonymous
([service-actors.md](service-actors.md)) and what a phone on mobile data wants. The second of those
is answered as of 2026-08-28 and the answer was not Opus: a phone moving between wifi and mobile
drops the mount outright, because the connection is bound to a source address that changes, so what
it wanted was HLS ([stream-formats.md](stream-formats.md)).

**The station can say why it is quiet, 2026-08-13.** [station-intelligence.md](station-intelligence.md)
§8's silence half, written up in full there. This is the first item on this list that was taken
because the LIST said so rather than because something pulled it forward, and it came in under item 4
rather than beside it. Three things are worth knowing outside that file:

- **The bug it was designed around was real and was not the one advertised.** The file predicted the
  console could not tell an audience-gated silence from a broken lease. What it actually could not
  tell was an empty room from an Icecast whose stats endpoint had gone: `IcecastStatsClient.listeners()`
  answers `undefined` for "could not read" and documents that as deliberately not `0`, and
  `AudienceWatch` was discarding it. In `audience` mode that is permanent silence with the console
  saying `ready`.
- **Two defects came out of RUNNING it rather than writing it**, both in code that compiled and
  read correctly: `notDriving` claiming there was an audience underneath the gate saying there was
  none, and `playout.smoke.ts` inheriting a player queue that was already at the lead so nothing was
  ever handed over. Both were found by `apps/api/scripts/silence.smoke.ts` and the scripts repair on
  its heels, which is the argument for keeping smoke scripts that drive real services.
- **`apps/api/scripts/` is type-checked now** (`pnpm --filter @deadair/api typecheck`, split into
  `typecheck:tests` and `typecheck:scripts`). Four of five scripts had silently stopped compiling
  against the code they drive, because nothing ever compiled them. Anything added there is now held
  to the same bar as a caller in `src/`.
