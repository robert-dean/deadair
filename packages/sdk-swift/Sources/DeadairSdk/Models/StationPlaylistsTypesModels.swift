// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// A playlist the station owns: records it holds in its own library, in an order somebody chose, cloned
/// from somewhere else and free to differ from it afterwards
public struct StationPlaylist: Codable, Equatable, Sendable {
    public var id: String
    public var name: String
    /// What this playlist is for, in the operator's own words. Empty when nobody said
    public var prompt: String
    /// The plugin this was cloned from, for a badge and nothing else. Absent for one read from a file or made here
    public var originPluginId: String?
    /// Every row, placeholders included
    public var trackCount: Int
    /// The rows that name a record in the library, which are the ones that can air
    public var resolvedCount: Int
    public var createdAt: Date
    public var updatedAt: Date

    public init(id: String, name: String, prompt: String, originPluginId: String? = nil, trackCount: Int, resolvedCount: Int, createdAt: Date, updatedAt: Date) {
        self.id = id
        self.name = name
        self.prompt = prompt
        self.originPluginId = originPluginId
        self.trackCount = trackCount
        self.resolvedCount = resolvedCount
        self.createdAt = createdAt
        self.updatedAt = updatedAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case prompt = "prompt"
        case originPluginId = "originPluginId"
        case trackCount = "trackCount"
        case resolvedCount = "resolvedCount"
        case createdAt = "createdAt"
        case updatedAt = "updatedAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.prompt = try container.decode(String.self, forKey: .prompt)
        self.originPluginId = try container.decodeIfPresent(String.self, forKey: .originPluginId)
        self.trackCount = try container.decode(Int.self, forKey: .trackCount)
        self.resolvedCount = try container.decode(Int.self, forKey: .resolvedCount)
        self.createdAt = try container.decode(Date.self, forKey: .createdAt)
        self.updatedAt = try container.decode(Date.self, forKey: .updatedAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.prompt, forKey: .prompt)
        try container.encodeIfPresent(self.originPluginId, forKey: .originPluginId)
        try container.encode(self.trackCount, forKey: .trackCount)
        try container.encode(self.resolvedCount, forKey: .resolvedCount)
        try container.encode(self.createdAt, forKey: .createdAt)
        try container.encode(self.updatedAt, forKey: .updatedAt)
    }
}

/// A playlist the station owns: records it holds in its own library, in an order somebody chose, cloned
/// from somewhere else and free to differ from it afterwards
public struct StationPlaylistInput: Codable, Equatable, Sendable {
    public var name: String
    /// What this playlist is for, in the operator's own words. Empty when nobody said
    public var prompt: String

    public init(name: String, prompt: String) {
        self.name = name
        self.prompt = prompt
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case prompt = "prompt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.prompt = try container.decode(String.self, forKey: .prompt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.prompt, forKey: .prompt)
    }
}

/// One row of a station playlist: a record in the library, or a placeholder for one it does not hold yet
public struct StationPlaylistTrack: Codable, Equatable, Sendable {
    /// The row's own id, not the record's
    public var id: String
    public var position: Int
    public var title: String
    /// Ordered, primary artist first
    public var artists: [String]
    public var album: String?
    public var durationMs: Int?
    /// The library record this row plays. Absent on a placeholder
    public var trackId: String?
    public var artistId: String?
    public var albumId: String?
    /// On a placeholder, the plugin whose copy it was cloned from. Absent on one read from a file, which names a record and no copy of it
    public var originPluginId: String?

    public init(id: String, position: Int, title: String, artists: [String], album: String? = nil, durationMs: Int? = nil, trackId: String? = nil, artistId: String? = nil, albumId: String? = nil, originPluginId: String? = nil) {
        self.id = id
        self.position = position
        self.title = title
        self.artists = artists
        self.album = album
        self.durationMs = durationMs
        self.trackId = trackId
        self.artistId = artistId
        self.albumId = albumId
        self.originPluginId = originPluginId
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case position = "position"
        case title = "title"
        case artists = "artists"
        case album = "album"
        case durationMs = "durationMs"
        case trackId = "trackId"
        case artistId = "artistId"
        case albumId = "albumId"
        case originPluginId = "originPluginId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.position = try container.decode(Int.self, forKey: .position)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode([String].self, forKey: .artists)
        self.album = try container.decodeIfPresent(String.self, forKey: .album)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.trackId = try container.decodeIfPresent(String.self, forKey: .trackId)
        self.artistId = try container.decodeIfPresent(String.self, forKey: .artistId)
        self.albumId = try container.decodeIfPresent(String.self, forKey: .albumId)
        self.originPluginId = try container.decodeIfPresent(String.self, forKey: .originPluginId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.position, forKey: .position)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.album, forKey: .album)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.trackId, forKey: .trackId)
        try container.encodeIfPresent(self.artistId, forKey: .artistId)
        try container.encodeIfPresent(self.albumId, forKey: .albumId)
        try container.encodeIfPresent(self.originPluginId, forKey: .originPluginId)
    }
}

/// What may change about a station playlist after it exists. Absent fields are left alone
public struct StationPlaylistUpdate: Codable, Equatable, Sendable {
    public var name: String?
    public var prompt: String?

    public init(name: String? = nil, prompt: String? = nil) {
        self.name = name
        self.prompt = prompt
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case prompt = "prompt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.prompt = try container.decodeIfPresent(String.self, forKey: .prompt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.prompt, forKey: .prompt)
    }
}

/// Where a record in a file came from, when it came from a provider's copy. Carried so a record the
/// receiving library does not hold can still be matched by that copy later
public struct PlaylistFileOrigin: Codable, Equatable, Sendable {
    public var pluginId: String
    public var externalId: String

    public init(pluginId: String, externalId: String) {
        self.pluginId = pluginId
        self.externalId = externalId
    }

    private enum CodingKeys: String, CodingKey {
        case pluginId = "pluginId"
        case externalId = "externalId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.externalId = try container.decode(String.self, forKey: .externalId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.externalId, forKey: .externalId)
    }
}

/// A playlist one of the station's music sources holds, named the way the playlists listing names it
public struct PlaylistProviderRef: Codable, Equatable, Sendable {
    public var pluginId: String
    public var playlistId: String

    public init(pluginId: String, playlistId: String) {
        self.pluginId = pluginId
        self.playlistId = playlistId
    }

    private enum CodingKeys: String, CodingKey {
        case pluginId = "pluginId"
        case playlistId = "playlistId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.playlistId = try container.decode(String.self, forKey: .playlistId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.playlistId, forKey: .playlistId)
    }
}

/// What importing one record would do here
public struct PlaylistImportEntry: Codable, Equatable, Sendable {
    public var position: Int
    public var title: String
    public var artists: [String]
    /// `matched`: the library holds it. `toAdd`: it names a provider's copy, which the station can add to its library. `toLookUp`: it names only a record, which the station has to search its providers for
    public var outcome: PlaylistImportEntryOutcome
    /// The library record a `matched` row plays
    public var trackId: String?

    public init(position: Int, title: String, artists: [String], outcome: PlaylistImportEntryOutcome, trackId: String? = nil) {
        self.position = position
        self.title = title
        self.artists = artists
        self.outcome = outcome
        self.trackId = trackId
    }

    private enum CodingKeys: String, CodingKey {
        case position = "position"
        case title = "title"
        case artists = "artists"
        case outcome = "outcome"
        case trackId = "trackId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.position = try container.decode(Int.self, forKey: .position)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode([String].self, forKey: .artists)
        self.outcome = try container.decode(PlaylistImportEntryOutcome.self, forKey: .outcome)
        self.trackId = try container.decodeIfPresent(String.self, forKey: .trackId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.position, forKey: .position)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encode(self.outcome, forKey: .outcome)
        try container.encodeIfPresent(self.trackId, forKey: .trackId)
    }
}

public struct StationPlaylistList: Codable, Equatable, Sendable {
    public var playlists: [StationPlaylist]

    public init(playlists: [StationPlaylist]) {
        self.playlists = playlists
    }

    private enum CodingKeys: String, CodingKey {
        case playlists = "playlists"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.playlists = try container.decode([StationPlaylist].self, forKey: .playlists)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.playlists, forKey: .playlists)
    }
}

public struct StationPlaylistListInput: Codable, Equatable, Sendable {
    public var playlists: [StationPlaylistInput]

    public init(playlists: [StationPlaylistInput]) {
        self.playlists = playlists
    }

    private enum CodingKeys: String, CodingKey {
        case playlists = "playlists"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.playlists = try container.decode([StationPlaylistInput].self, forKey: .playlists)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.playlists, forKey: .playlists)
    }
}

public struct StationPlaylistDetail: Codable, Equatable, Sendable {
    public var id: String
    public var name: String
    /// What this playlist is for, in the operator's own words. Empty when nobody said
    public var prompt: String
    /// The plugin this was cloned from, for a badge and nothing else. Absent for one read from a file or made here
    public var originPluginId: String?
    /// Every row, placeholders included
    public var trackCount: Int
    /// The rows that name a record in the library, which are the ones that can air
    public var resolvedCount: Int
    public var createdAt: Date
    public var updatedAt: Date
    public var tracks: [StationPlaylistTrack]

    public init(id: String, name: String, prompt: String, originPluginId: String? = nil, trackCount: Int, resolvedCount: Int, createdAt: Date, updatedAt: Date, tracks: [StationPlaylistTrack]) {
        self.id = id
        self.name = name
        self.prompt = prompt
        self.originPluginId = originPluginId
        self.trackCount = trackCount
        self.resolvedCount = resolvedCount
        self.createdAt = createdAt
        self.updatedAt = updatedAt
        self.tracks = tracks
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case name = "name"
        case prompt = "prompt"
        case originPluginId = "originPluginId"
        case trackCount = "trackCount"
        case resolvedCount = "resolvedCount"
        case createdAt = "createdAt"
        case updatedAt = "updatedAt"
        case tracks = "tracks"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.name = try container.decode(String.self, forKey: .name)
        self.prompt = try container.decode(String.self, forKey: .prompt)
        self.originPluginId = try container.decodeIfPresent(String.self, forKey: .originPluginId)
        self.trackCount = try container.decode(Int.self, forKey: .trackCount)
        self.resolvedCount = try container.decode(Int.self, forKey: .resolvedCount)
        self.createdAt = try container.decode(Date.self, forKey: .createdAt)
        self.updatedAt = try container.decode(Date.self, forKey: .updatedAt)
        self.tracks = try container.decode([StationPlaylistTrack].self, forKey: .tracks)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.prompt, forKey: .prompt)
        try container.encodeIfPresent(self.originPluginId, forKey: .originPluginId)
        try container.encode(self.trackCount, forKey: .trackCount)
        try container.encode(self.resolvedCount, forKey: .resolvedCount)
        try container.encode(self.createdAt, forKey: .createdAt)
        try container.encode(self.updatedAt, forKey: .updatedAt)
        try container.encode(self.tracks, forKey: .tracks)
    }
}

public struct StationPlaylistDetailInput: Codable, Equatable, Sendable {
    public var name: String
    /// What this playlist is for, in the operator's own words. Empty when nobody said
    public var prompt: String
    public var tracks: [StationPlaylistTrack]

    public init(name: String, prompt: String, tracks: [StationPlaylistTrack]) {
        self.name = name
        self.prompt = prompt
        self.tracks = tracks
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case prompt = "prompt"
        case tracks = "tracks"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.prompt = try container.decode(String.self, forKey: .prompt)
        self.tracks = try container.decode([StationPlaylistTrack].self, forKey: .tracks)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.prompt, forKey: .prompt)
        try container.encode(self.tracks, forKey: .tracks)
    }
}

/// One record as a playlist file names it. By its words and its ISRC, never by an id of this station's:
/// ids are minted afresh by every library, so a file keyed by them would restore onto nothing
public struct PlaylistFileTrack: Codable, Equatable, Sendable {
    public var title: String
    /// Ordered, primary artist first
    public var artists: [String]
    public var album: String?
    public var durationMs: Int?
    public var isrc: String?
    public var origin: PlaylistFileOrigin?

    public init(title: String, artists: [String], album: String? = nil, durationMs: Int? = nil, isrc: String? = nil, origin: PlaylistFileOrigin? = nil) {
        self.title = title
        self.artists = artists
        self.album = album
        self.durationMs = durationMs
        self.isrc = isrc
        self.origin = origin
    }

    private enum CodingKeys: String, CodingKey {
        case title = "title"
        case artists = "artists"
        case album = "album"
        case durationMs = "durationMs"
        case isrc = "isrc"
        case origin = "origin"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.title = try container.decode(String.self, forKey: .title)
        self.artists = try container.decode([String].self, forKey: .artists)
        self.album = try container.decodeIfPresent(String.self, forKey: .album)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.isrc = try container.decodeIfPresent(String.self, forKey: .isrc)
        self.origin = try container.decodeIfPresent(PlaylistFileOrigin.self, forKey: .origin)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artists, forKey: .artists)
        try container.encodeIfPresent(self.album, forKey: .album)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.isrc, forKey: .isrc)
        try container.encodeIfPresent(self.origin, forKey: .origin)
    }
}

/// What an import WOULD do, written nowhere
public struct PlaylistImportPlan: Codable, Equatable, Sendable {
    public var name: String
    public var matched: Int
    public var toAdd: Int
    public var toLookUp: Int
    /// Lines of the source that named no record the station could read
    public var skipped: Int
    public var entries: [PlaylistImportEntry]
    /// Anything about the source as a whole an operator should know before importing it
    public var notices: [String]

    public init(name: String, matched: Int, toAdd: Int, toLookUp: Int, skipped: Int, entries: [PlaylistImportEntry], notices: [String]) {
        self.name = name
        self.matched = matched
        self.toAdd = toAdd
        self.toLookUp = toLookUp
        self.skipped = skipped
        self.entries = entries
        self.notices = notices
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case matched = "matched"
        case toAdd = "toAdd"
        case toLookUp = "toLookUp"
        case skipped = "skipped"
        case entries = "entries"
        case notices = "notices"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.matched = try container.decode(Int.self, forKey: .matched)
        self.toAdd = try container.decode(Int.self, forKey: .toAdd)
        self.toLookUp = try container.decode(Int.self, forKey: .toLookUp)
        self.skipped = try container.decode(Int.self, forKey: .skipped)
        self.entries = try container.decode([PlaylistImportEntry].self, forKey: .entries)
        self.notices = try container.decode([String].self, forKey: .notices)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.matched, forKey: .matched)
        try container.encode(self.toAdd, forKey: .toAdd)
        try container.encode(self.toLookUp, forKey: .toLookUp)
        try container.encode(self.skipped, forKey: .skipped)
        try container.encode(self.entries, forKey: .entries)
        try container.encode(self.notices, forKey: .notices)
    }
}

/// A playlist as a file: everything somebody would need to rebuild it on another station
public struct PlaylistFile: Codable, Equatable, Sendable {
    /// What shape this is, so a file from a later build says so rather than being read wrongly
    public var format: String
    /// When it was exported, ISO-8601
    public var takenAt: String
    /// The station it was taken from. Provenance only
    public var station: String?
    public var name: String
    public var prompt: String?
    public var tracks: [PlaylistFileTrack]

    public init(format: String, takenAt: String, station: String? = nil, name: String, prompt: String? = nil, tracks: [PlaylistFileTrack]) {
        self.format = format
        self.takenAt = takenAt
        self.station = station
        self.name = name
        self.prompt = prompt
        self.tracks = tracks
    }

    private enum CodingKeys: String, CodingKey {
        case format = "format"
        case takenAt = "takenAt"
        case station = "station"
        case name = "name"
        case prompt = "prompt"
        case tracks = "tracks"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.format = try container.decode(String.self, forKey: .format)
        self.takenAt = try container.decode(String.self, forKey: .takenAt)
        self.station = try container.decodeIfPresent(String.self, forKey: .station)
        self.name = try container.decode(String.self, forKey: .name)
        self.prompt = try container.decodeIfPresent(String.self, forKey: .prompt)
        self.tracks = try container.decode([PlaylistFileTrack].self, forKey: .tracks)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.format, forKey: .format)
        try container.encode(self.takenAt, forKey: .takenAt)
        try container.encodeIfPresent(self.station, forKey: .station)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.prompt, forKey: .prompt)
        try container.encode(self.tracks, forKey: .tracks)
    }
}

public struct PlaylistImportResult: Codable, Equatable, Sendable {
    public var plan: PlaylistImportPlan
    public var playlist: StationPlaylist

    public init(plan: PlaylistImportPlan, playlist: StationPlaylist) {
        self.plan = plan
        self.playlist = playlist
    }

    private enum CodingKeys: String, CodingKey {
        case plan = "plan"
        case playlist = "playlist"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.plan = try container.decode(PlaylistImportPlan.self, forKey: .plan)
        self.playlist = try container.decode(StationPlaylist.self, forKey: .playlist)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.plan, forKey: .plan)
        try container.encode(self.playlist, forKey: .playlist)
    }
}

public struct PlaylistImportResultInput: Codable, Equatable, Sendable {
    public var plan: PlaylistImportPlan
    public var playlist: StationPlaylistInput

    public init(plan: PlaylistImportPlan, playlist: StationPlaylistInput) {
        self.plan = plan
        self.playlist = playlist
    }

    private enum CodingKeys: String, CodingKey {
        case plan = "plan"
        case playlist = "playlist"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.plan = try container.decode(PlaylistImportPlan.self, forKey: .plan)
        self.playlist = try container.decode(StationPlaylistInput.self, forKey: .playlist)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.plan, forKey: .plan)
        try container.encode(self.playlist, forKey: .playlist)
    }
}

/// Something to import a playlist from. Exactly one source
public struct PlaylistImportInput: Codable, Equatable, Sendable {
    /// A playlist exported from a deadair station
    public var file: PlaylistFile?
    /// A playlist another program wrote, as its text: an M3U, a CSV with a header row, or one `Artist - Title` per line
    public var text: String?
    /// Which of those `text` is. Absent works it out from the text
    public var format: PlaylistImportInputFormat?
    /// The name of the file `text` came from, which names the playlist when the text does not
    public var fileName: String?
    /// A link to a playlist at one of the station's music sources, as a browser or an app shows it
    public var url: String?
    /// A playlist a music source lists here, by its plugin and its id
    public var providerPlaylist: PlaylistProviderRef?
    /// What to call the new playlist. Absent keeps the name the source gives it
    public var name: String?

    public init(file: PlaylistFile? = nil, text: String? = nil, format: PlaylistImportInputFormat? = nil, fileName: String? = nil, url: String? = nil, providerPlaylist: PlaylistProviderRef? = nil, name: String? = nil) {
        self.file = file
        self.text = text
        self.format = format
        self.fileName = fileName
        self.url = url
        self.providerPlaylist = providerPlaylist
        self.name = name
    }

    private enum CodingKeys: String, CodingKey {
        case file = "file"
        case text = "text"
        case format = "format"
        case fileName = "fileName"
        case url = "url"
        case providerPlaylist = "providerPlaylist"
        case name = "name"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.file = try container.decodeIfPresent(PlaylistFile.self, forKey: .file)
        self.text = try container.decodeIfPresent(String.self, forKey: .text)
        self.format = try container.decodeIfPresent(PlaylistImportInputFormat.self, forKey: .format)
        self.fileName = try container.decodeIfPresent(String.self, forKey: .fileName)
        self.url = try container.decodeIfPresent(String.self, forKey: .url)
        self.providerPlaylist = try container.decodeIfPresent(PlaylistProviderRef.self, forKey: .providerPlaylist)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.file, forKey: .file)
        try container.encodeIfPresent(self.text, forKey: .text)
        try container.encodeIfPresent(self.format, forKey: .format)
        try container.encodeIfPresent(self.fileName, forKey: .fileName)
        try container.encodeIfPresent(self.url, forKey: .url)
        try container.encodeIfPresent(self.providerPlaylist, forKey: .providerPlaylist)
        try container.encodeIfPresent(self.name, forKey: .name)
    }
}

/// Which of those `text` is. Absent works it out from the text
public enum PlaylistImportInputFormat: String, Codable, CaseIterable, Sendable {
    case m3u = "m3u"
    case csv = "csv"
    case text = "text"
}

/// `matched`: the library holds it. `toAdd`: it names a provider's copy, which the station can add to its library. `toLookUp`: it names only a record, which the station has to search its providers for
public enum PlaylistImportEntryOutcome: String, Codable, CaseIterable, Sendable {
    case matched = "matched"
    case toAdd = "toAdd"
    case toLookUp = "toLookUp"
}
