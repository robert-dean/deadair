// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `authentication.factor.ck`.
public final class AuthenticationFactorClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List factors
    /// List authentication factors
    public func listFactors() async throws -> [AuthenticationFactor] {
        let request = SdkRequest(method: "GET", path: ["auth", "factors"])
        let response = try await http.execute(request)
        return try http.decodeJSON([AuthenticationFactor].self, from: response)
    }

    /// Register factor
    /// Register an authentication factor
    public func registerFactor(body: AuthenticationFactorRegistration) async throws -> AuthenticationFactorRegistrationResponse {
        var request = SdkRequest(method: "POST", path: ["auth", "factors", "register"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(AuthenticationFactorRegistrationResponse.self, from: response)
    }

    /// Verify factor registration
    /// Verify an authentication factor registration
    public func verifyFactorRegistration(body: AuthenticationFactorRegistrationVerification) async throws -> AuthenticationToken {
        var request = SdkRequest(method: "POST", path: ["auth", "factors", "verify"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(AuthenticationToken.self, from: response)
    }

    /// Start factor challenge
    /// Issue a factor verification challenge for a pending MFA round. Authenticated via the short-lived `mfa_challenge_id` in the body, not by session — this is the only /auth/factors/* route that does not require an authenticated session.
    public func startFactorChallenge(body: FactorChallengeStartRequest) async throws -> FactorChallengeStartResponse {
        var request = SdkRequest(method: "POST", path: ["auth", "factors", "start"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(FactorChallengeStartResponse.self, from: response)
    }

    /// Start MFA challenge
    /// Mint a fresh MFA challenge for the *current* authenticated session so the SPA can satisfy a `step_up_required` denial. Optionally filters eligible factors against an inbound `StepUpRequirement` hint. Returns `enrollment_required` when no enrolled factor matches the requirement so the SPA can route the user into enrollment instead of getting stuck.
    public func startMfaChallenge(body: StepUpStartRequest) async throws -> StepUpStartResponse {
        var request = SdkRequest(method: "POST", path: ["auth", "mfa", "start"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(StepUpStartResponse.self, from: response)
    }

    /// Remove factor
    /// Remove one of the caller's own factors. Answered only for `authenticator` today, and only after a recent strong-factor verification: the same gate enrolment sits behind once a strong factor exists, so a stolen session cannot quietly switch the second factor off. Removing the last authenticator turns the sign-in challenge off for that account.
    public func removeFactor(method: AuthenticationFactorMethod, methodId: String) async throws {
        let request = try SdkRequest(method: "DELETE", path: ["auth", "factors", http.segment(method), http.segment(methodId)])
        _ = try await http.execute(request)
    }
}
