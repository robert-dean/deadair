// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `catalog.ck`.
public final class CatalogClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List artists
    /// Every artist the station has ingested, ordered by name
    public func listArtists(query: CatalogQueryInput? = nil) async throws -> ArtistPage {
        var request = SdkRequest(method: "GET", path: ["catalog", "artists"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(ArtistPage.self, from: response)
    }

    /// Get artist
    /// One artist. 404s on an id that was merged away, since reads never return merged rows
    public func getArtist(id: UUID) async throws -> Artist {
        let request = try SdkRequest(method: "GET", path: ["catalog", "artists", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(Artist.self, from: response)
    }

    /// Get artist enrichment
    /// What every enrichment provider said about this artist, and when each of them said it
    public func getArtistEnrichment(id: UUID) async throws -> ArtistEnrichmentDetail {
        let request = try SdkRequest(method: "GET", path: ["catalog", "artists", http.segment(id), "enrichment"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ArtistEnrichmentDetail.self, from: response)
    }

    /// List artist albums
    /// The albums credited to one artist
    public func listArtistAlbums(id: UUID, query: CatalogQueryInput? = nil) async throws -> AlbumPage {
        var request = try SdkRequest(method: "GET", path: ["catalog", "artists", http.segment(id), "albums"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(AlbumPage.self, from: response)
    }

    /// Rate artist
    /// What the station thinks of this artist. A dislike here excludes every record they are credited on
    public func rateArtist(id: UUID, body: RateInput) async throws -> Artist {
        var request = try SdkRequest(method: "PUT", path: ["catalog", "artists", http.segment(id), "rating"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(Artist.self, from: response)
    }

    /// List albums
    public func listAlbums(query: CatalogQueryInput? = nil) async throws -> AlbumPage {
        var request = SdkRequest(method: "GET", path: ["catalog", "albums"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(AlbumPage.self, from: response)
    }

    /// Get album
    public func getAlbum(id: UUID) async throws -> Album {
        let request = try SdkRequest(method: "GET", path: ["catalog", "albums", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(Album.self, from: response)
    }

    /// Get album enrichment
    /// The record's own enrichment: the label, pressing and cover belong to the release, not to a track on it
    public func getAlbumEnrichment(id: UUID) async throws -> AlbumEnrichmentDetail {
        let request = try SdkRequest(method: "GET", path: ["catalog", "albums", http.segment(id), "enrichment"])
        let response = try await http.execute(request)
        return try http.decodeJSON(AlbumEnrichmentDetail.self, from: response)
    }

    /// List album tracks
    /// One album's tracks
    public func listAlbumTracks(id: UUID, query: TrackQueryInput? = nil) async throws -> TrackPage {
        var request = try SdkRequest(method: "GET", path: ["catalog", "albums", http.segment(id), "tracks"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackPage.self, from: response)
    }

    /// Rate album
    /// What the station thinks of this record. A dislike here excludes every track on it
    public func rateAlbum(id: UUID, body: RateInput) async throws -> Album {
        var request = try SdkRequest(method: "PUT", path: ["catalog", "albums", http.segment(id), "rating"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(Album.self, from: response)
    }

    /// Get track
    /// One record and everything it has accumulated: its copies, its bytes, its measurement, what it has aired
    public func getTrack(id: UUID) async throws -> TrackDetail {
        let request = try SdkRequest(method: "GET", path: ["catalog", "tracks", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackDetail.self, from: response)
    }

    /// Clear track audio
    /// Drop the station's own copies of this record. The next play fetches them again
    /// - Throws: `SdkError` on 409
    public func clearTrackAudio(id: UUID) async throws -> TrackClearResult {
        let request = try SdkRequest(method: "DELETE", path: ["catalog", "tracks", http.segment(id), "audio"])
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackClearResult.self, from: response)
    }

    /// Clear track analysis
    /// Forget the measurement, so the walk takes it again
    public func clearTrackAnalysis(id: UUID) async throws -> TrackClearResult {
        let request = try SdkRequest(method: "DELETE", path: ["catalog", "tracks", http.segment(id), "analysis"])
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackClearResult.self, from: response)
    }

    /// Retry track audio
    /// Try this record's copies again now, rather than when the backoff says
    public func retryTrackAudio(id: UUID) async throws -> TrackClearResult {
        let request = try SdkRequest(method: "POST", path: ["catalog", "tracks", http.segment(id), "retry"])
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackClearResult.self, from: response)
    }

    /// Offer track copies again
    /// Put copies a provider refused back on offer, and clear their backoff so they are tried now
    public func offerTrackCopiesAgain(id: UUID) async throws -> TrackClearResult {
        let request = try SdkRequest(method: "POST", path: ["catalog", "tracks", http.segment(id), "offer"])
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackClearResult.self, from: response)
    }

    /// Get track enrichment
    /// What the providers said about one recording, including everything no canonical column holds
    public func getTrackEnrichment(id: UUID) async throws -> TrackEnrichmentDetail {
        let request = try SdkRequest(method: "GET", path: ["catalog", "tracks", http.segment(id), "enrichment"])
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackEnrichmentDetail.self, from: response)
    }

    /// Clear track enrichment
    /// Forget what the providers said, so the enrichment pass asks again
    public func clearTrackEnrichment(id: UUID, query: ClearEnrichmentQuery? = nil) async throws -> TrackClearResult {
        var request = try SdkRequest(method: "DELETE", path: ["catalog", "tracks", http.segment(id), "enrichment"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackClearResult.self, from: response)
    }

    /// List tracks
    /// Every track, flat. The only way to answer "do we have this song?" without knowing its artist
    public func listTracks(query: TrackQueryInput? = nil) async throws -> TrackPage {
        var request = SdkRequest(method: "GET", path: ["catalog", "tracks"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(TrackPage.self, from: response)
    }

    /// Rate track
    /// What the station thinks of this song, which is the narrowest thing an opinion can be about
    public func rateTrack(id: UUID, body: RateInput) async throws -> Track {
        var request = try SdkRequest(method: "PUT", path: ["catalog", "tracks", http.segment(id), "rating"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(Track.self, from: response)
    }
}
