{
    "name": "deadair-plugin-apple-music-charts",
    "version": "0.1.0",
    "private": true,
    "description": "An example deadair plugin: Apple Music's most-played songs, by country, as a chart the station can programme from.",
    "license": "MIT",
    "type": "module",
    "main": "./dist/index.js",
    "files": [
        "dist/**"
    ],
    "deadair": {
        "plugin": "./dist/index.js"
    },
    "scripts": {
        "build": "tsup",
        "typecheck": "tsc --noEmit",
        "test": "vitest run"
    },
    "peerDependencies": {
        "@deadair/plugin-sdk": ">=0.1.0",
        "zod": "^4.0.0"
    },
    "devDependencies": {
        "@deadair/plugin-sdk": ">=0.1.0",
        "@types/node": "^26.1.2",
        "tsup": "^8.5.1",
        "typescript": "^6.0.3",
        "vitest": "^4.1.10",
        "zod": "^4.5.4"
    }
}
