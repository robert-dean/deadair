// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// What a listener is hearing right now: a record, or the station talking
public struct NowPlayingTrack: Codable, Equatable, Sendable {
    /// `record` is music. `break` is the station speaking on its own between two records (an ident, a bulletin, a talk break), with `artist` empty and `title` the break's own label. A presenter talking over the start of a record is not a break: the record is what is on air, and it stays `record`. Absent means `record`, which is all a station older than this field ever reported
    public var kind: NowPlayingTrackKind
    public var title: String
    /// Comma-joined, as a display line rather than a list: this is what a player or a device shows, not something to iterate. Empty for a `break`
    public var artist: String
    public var album: String?
    /// The station's own cached cover where there is one, the provider's URL otherwise. Relative values are paths under the API root
    public var artworkUrl: String?
    public var durationMs: Int?
    /// Unix epoch millis, as observed when the player reported the track started
    public var startedAt: Int
    /// Absent when the decoder cannot say. It leads the listener by the encoder and client buffers, so it is a display value and not a schedule
    public var remainingMs: Int?

    public init(kind: NowPlayingTrackKind = .record, title: String, artist: String, album: String? = nil, artworkUrl: String? = nil, durationMs: Int? = nil, startedAt: Int, remainingMs: Int? = nil) {
        self.kind = kind
        self.title = title
        self.artist = artist
        self.album = album
        self.artworkUrl = artworkUrl
        self.durationMs = durationMs
        self.startedAt = startedAt
        self.remainingMs = remainingMs
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case title = "title"
        case artist = "artist"
        case album = "album"
        case artworkUrl = "artworkUrl"
        case durationMs = "durationMs"
        case startedAt = "startedAt"
        case remainingMs = "remainingMs"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decodeIfPresent(NowPlayingTrackKind.self, forKey: .kind) ?? .record
        self.title = try container.decode(String.self, forKey: .title)
        self.artist = try container.decode(String.self, forKey: .artist)
        self.album = try container.decodeIfPresent(String.self, forKey: .album)
        self.artworkUrl = try container.decodeIfPresent(String.self, forKey: .artworkUrl)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.startedAt = try container.decode(Int.self, forKey: .startedAt)
        self.remainingMs = try container.decodeIfPresent(Int.self, forKey: .remainingMs)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artist, forKey: .artist)
        try container.encodeIfPresent(self.album, forKey: .album)
        try container.encodeIfPresent(self.artworkUrl, forKey: .artworkUrl)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encode(self.startedAt, forKey: .startedAt)
        try container.encodeIfPresent(self.remainingMs, forKey: .remainingMs)
    }
}

/// One way to listen to this station right now
public struct NowPlayingMount: Codable, Equatable, Sendable {
    /// `hls` is the master playlist rather than an Icecast mount, which is why this enum has an arm `PlayoutMount` does not
    public var format: NowPlayingMountFormat
    /// Same-origin path, leading slash included. A path and not a URL: the station is reached through whatever edge served this answer, never at the address the app itself uses
    public var path: String
    /// Absent for FLAC, which is lossless and has no rate to set, and for HLS, whose rate is the AAC variant's
    public var bitrateKbps: Int?

    public init(format: NowPlayingMountFormat, path: String, bitrateKbps: Int? = nil) {
        self.format = format
        self.path = path
        self.bitrateKbps = bitrateKbps
    }

    private enum CodingKeys: String, CodingKey {
        case format = "format"
        case path = "path"
        case bitrateKbps = "bitrateKbps"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.format = try container.decode(NowPlayingMountFormat.self, forKey: .format)
        self.path = try container.decode(String.self, forKey: .path)
        self.bitrateKbps = try container.decodeIfPresent(Int.self, forKey: .bitrateKbps)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.format, forKey: .format)
        try container.encode(self.path, forKey: .path)
        try container.encodeIfPresent(self.bitrateKbps, forKey: .bitrateKbps)
    }
}

/// The programme on air, as a listener would be told it
public struct NowPlayingShow: Codable, Equatable, Sendable {
    /// What this broadcast is called. It changes the moment the station changes programme, which can be one record before the new programme's first record is heard: a changeover never cuts a listener off mid-record
    public var name: String
    /// Who is presenting, by the name they go by on air. Absent when there is no name to give: no persona on air with one, and no station-wide presenter name set. Never the persona's console label
    public var host: String?

    public init(name: String, host: String? = nil) {
        self.name = name
        self.host = host
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case host = "host"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.host = try container.decodeIfPresent(String.self, forKey: .host)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.host, forKey: .host)
    }
}

/// What the station is playing, for anything that wants to display it
public struct NowPlaying: Codable, Equatable, Sendable {
    /// The station's on-air name
    public var station: String
    /// False means the station is not broadcasting. `track` is absent in that case, which is an ordinary state and not an error
    public var onAir: Bool
    /// How many people are listening right now. Public because it says only what Icecast's own status document already says to anyone who asks, and a station page should not need a session to show it
    public var listeners: Int
    /// Every way to listen, MP3 first. Never empty: MP3 has no switch. A format the operator has not switched on is ABSENT rather than present and disabled, because a client asking this wants the mounts that are actually there — and a client that had to find out by connecting to each one would put an audience-gated station on air to do it
    public var mounts: [NowPlayingMount]
    /// Present whenever `track` is and the station has said what programme it belongs to. Absent off air, and while a station warming up has nothing airing yet
    public var show: NowPlayingShow?
    public var track: NowPlayingTrack?

    public init(station: String, onAir: Bool, listeners: Int, mounts: [NowPlayingMount], show: NowPlayingShow? = nil, track: NowPlayingTrack? = nil) {
        self.station = station
        self.onAir = onAir
        self.listeners = listeners
        self.mounts = mounts
        self.show = show
        self.track = track
    }

    private enum CodingKeys: String, CodingKey {
        case station = "station"
        case onAir = "onAir"
        case listeners = "listeners"
        case mounts = "mounts"
        case show = "show"
        case track = "track"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.station = try container.decode(String.self, forKey: .station)
        self.onAir = try container.decode(Bool.self, forKey: .onAir)
        self.listeners = try container.decode(Int.self, forKey: .listeners)
        self.mounts = try container.decode([NowPlayingMount].self, forKey: .mounts)
        self.show = try container.decodeIfPresent(NowPlayingShow.self, forKey: .show)
        self.track = try container.decodeIfPresent(NowPlayingTrack.self, forKey: .track)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.station, forKey: .station)
        try container.encode(self.onAir, forKey: .onAir)
        try container.encode(self.listeners, forKey: .listeners)
        try container.encode(self.mounts, forKey: .mounts)
        try container.encodeIfPresent(self.show, forKey: .show)
        try container.encodeIfPresent(self.track, forKey: .track)
    }
}

/// `record` is music. `break` is the station speaking on its own between two records (an ident, a bulletin, a talk break), with `artist` empty and `title` the break's own label. A presenter talking over the start of a record is not a break: the record is what is on air, and it stays `record`. Absent means `record`, which is all a station older than this field ever reported
public enum NowPlayingTrackKind: String, Codable, CaseIterable, Sendable {
    case record = "record"
    case `break` = "break"
}

/// `hls` is the master playlist rather than an Icecast mount, which is why this enum has an arm `PlayoutMount` does not
public enum NowPlayingMountFormat: String, Codable, CaseIterable, Sendable {
    case mp3 = "mp3"
    case opus = "opus"
    case aac = "aac"
    case flac = "flac"
    case hls = "hls"
}
