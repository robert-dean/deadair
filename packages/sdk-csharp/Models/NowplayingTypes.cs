// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace MaroonedSoftware.Deadair.Sdk.Models;

/// <summary>What a listener is hearing right now: a record, or the station talking</summary>
public sealed record NowPlayingTrack
{
    /// <summary>`record` is music. `break` is the station speaking on its own between two records (an ident, a bulletin, a talk break), with `artist` empty and `title` the break's own label. A presenter talking over the start of a record is not a break: the record is what is on air, and it stays `record`. Absent means `record`, which is all a station older than this field ever reported</summary>
    [JsonPropertyName("kind")]
    public NowPlayingTrackKind Kind { get; init; } = NowPlayingTrackKind.Record;

    [JsonPropertyName("title")]
    public required string Title { get; init; }

    /// <summary>Comma-joined, as a display line rather than a list: this is what a player or a device shows, not something to iterate. Empty for a `break`</summary>
    [JsonPropertyName("artist")]
    public required string Artist { get; init; }

    [JsonPropertyName("album")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Album { get; init; }

    /// <summary>The station's own cached cover where there is one, the provider's URL otherwise. Relative values are paths under the API root</summary>
    [JsonPropertyName("artworkUrl")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? ArtworkUrl { get; init; }

    [JsonPropertyName("durationMs")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? DurationMs { get; init; }

    /// <summary>Unix epoch millis, as observed when the player reported the track started</summary>
    [JsonPropertyName("startedAt")]
    public required long StartedAt { get; init; }

    /// <summary>Absent when the decoder cannot say. It leads the listener by the encoder and client buffers, so it is a display value and not a schedule</summary>
    [JsonPropertyName("remainingMs")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? RemainingMs { get; init; }
}

/// <summary>One way to listen to this station right now</summary>
public sealed record NowPlayingMount
{
    /// <summary>`hls` is the master playlist rather than an Icecast mount, which is why this enum has an arm `PlayoutMount` does not</summary>
    [JsonPropertyName("format")]
    public required NowPlayingMountFormat Format { get; init; }

    /// <summary>Same-origin path, leading slash included. A path and not a URL: the station is reached through whatever edge served this answer, never at the address the app itself uses</summary>
    [JsonPropertyName("path")]
    public required string Path { get; init; }

    /// <summary>Absent for FLAC, which is lossless and has no rate to set, and for HLS, whose rate is the AAC variant's</summary>
    [JsonPropertyName("bitrateKbps")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? BitrateKbps { get; init; }
}

/// <summary>The programme on air, as a listener would be told it</summary>
public sealed record NowPlayingShow
{
    /// <summary>What this broadcast is called. It changes the moment the station changes programme, which can be one record before the new programme's first record is heard: a changeover never cuts a listener off mid-record</summary>
    [JsonPropertyName("name")]
    public required string Name { get; init; }

    /// <summary>Who is presenting, by the name they go by on air. Absent when there is no name to give: no persona on air with one, and no station-wide presenter name set. Never the persona's console label</summary>
    [JsonPropertyName("host")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Host { get; init; }
}

/// <summary>What the station is playing, for anything that wants to display it</summary>
public sealed record NowPlaying
{
    /// <summary>The station's on-air name</summary>
    [JsonPropertyName("station")]
    public required string Station { get; init; }

    /// <summary>False means the station is not broadcasting. `track` is absent in that case, which is an ordinary state and not an error</summary>
    [JsonPropertyName("onAir")]
    public required bool OnAir { get; init; }

    /// <summary>How many people are listening right now. Public because it says only what Icecast's own status document already says to anyone who asks, and a station page should not need a session to show it</summary>
    [JsonPropertyName("listeners")]
    public required long Listeners { get; init; }

    /// <summary>Every way to listen, MP3 first. Never empty: MP3 has no switch. A format the operator has not switched on is ABSENT rather than present and disabled, because a client asking this wants the mounts that are actually there — and a client that had to find out by connecting to each one would put an audience-gated station on air to do it</summary>
    [JsonPropertyName("mounts")]
    public required List<NowPlayingMount> Mounts { get; init; }

    /// <summary>Present whenever `track` is and the station has said what programme it belongs to. Absent off air, and while a station warming up has nothing airing yet</summary>
    [JsonPropertyName("show")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public NowPlayingShow? Show { get; init; }

    [JsonPropertyName("track")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public NowPlayingTrack? Track { get; init; }
}

/// <summary>`record` is music. `break` is the station speaking on its own between two records (an ident, a bulletin, a talk break), with `artist` empty and `title` the break's own label. A presenter talking over the start of a record is not a break: the record is what is on air, and it stays `record`. Absent means `record`, which is all a station older than this field ever reported</summary>
[JsonConverter(typeof(JsonStringEnumConverter<NowPlayingTrackKind>))]
public enum NowPlayingTrackKind
{
    [JsonStringEnumMemberName("record")]
    Record,

    [JsonStringEnumMemberName("break")]
    Break,
}

/// <summary>`hls` is the master playlist rather than an Icecast mount, which is why this enum has an arm `PlayoutMount` does not</summary>
[JsonConverter(typeof(JsonStringEnumConverter<NowPlayingMountFormat>))]
public enum NowPlayingMountFormat
{
    [JsonStringEnumMemberName("mp3")]
    Mp3,

    [JsonStringEnumMemberName("opus")]
    Opus,

    [JsonStringEnumMemberName("aac")]
    Aac,

    [JsonStringEnumMemberName("flac")]
    Flac,

    [JsonStringEnumMemberName("hls")]
    Hls,
}
