// Auto-generated by @contractkit/plugin-kotlin. Do not edit manually.
package com.maroonedsoftware.deadair.sdk.clients

import com.maroonedsoftware.deadair.sdk.models.ConsoleLanguageList
import com.maroonedsoftware.deadair.sdk.models.ConsoleLanguagePack
import com.maroonedsoftware.deadair.sdk.runtime.SdkHttp
import io.ktor.http.HttpMethod

/** Operations declared in `languages.ck`. */
class LanguagesClient(private val http: SdkHttp) {
    /**
     * List console languages
     * Every language this station holds a pack for, without the strings
     */
    suspend fun listConsoleLanguages(): ConsoleLanguageList {
        val response = http.execute(HttpMethod.Get) {
            path("console", "languages")
        }
        return http.decodeJson(response)
    }

    /**
     * Get console language
     * One language's pack, strings and all, as it was imported
     */
    suspend fun getConsoleLanguage(locale: String): ConsoleLanguagePack {
        val response = http.execute(HttpMethod.Get) {
            path("console", "languages", segment(locale))
        }
        return http.decodeJson(response)
    }

    /**
     * Import console language
     * Installs a language pack, replacing any pack already installed for the language. The tag in the path must be the pack's own
     */
    suspend fun importConsoleLanguage(locale: String, body: ConsoleLanguagePack): ConsoleLanguageList {
        val response = http.execute(HttpMethod.Put) {
            path("console", "languages", segment(locale))
            jsonBody(body, "application/json")
        }
        return http.decodeJson(response)
    }

    /**
     * Remove console language
     * Removes a language. Anybody who had chosen it sees English
     */
    suspend fun removeConsoleLanguage(locale: String): ConsoleLanguageList {
        val response = http.execute(HttpMethod.Delete) {
            path("console", "languages", segment(locale))
        }
        return http.decodeJson(response)
    }
}
