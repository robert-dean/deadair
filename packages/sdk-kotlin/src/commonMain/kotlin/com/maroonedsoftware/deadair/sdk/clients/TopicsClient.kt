// Auto-generated by @contractkit/plugin-kotlin. Do not edit manually.
package com.maroonedsoftware.deadair.sdk.clients

import com.maroonedsoftware.deadair.sdk.models.TopicInput
import com.maroonedsoftware.deadair.sdk.models.TopicKindList
import com.maroonedsoftware.deadair.sdk.models.TopicList
import com.maroonedsoftware.deadair.sdk.models.TopicQuery
import com.maroonedsoftware.deadair.sdk.runtime.SdkHttp
import io.ktor.http.HttpMethod

/** Operations declared in `topics.ck`. */
class TopicsClient(private val http: SdkHttp) {
    /**
     * List topics
     * Every subject this station has named, for one sort of break or for all of them
     */
    suspend fun listTopics(query: TopicQuery? = null): TopicList {
        val response = http.execute(HttpMethod.Get) {
            path("topics")
            params(query)
        }
        return http.decodeJson(response)
    }

    /**
     * Create topic
     * Names a new subject. Nothing uses it until something points at it
     */
    suspend fun createTopic(body: TopicInput): TopicList {
        val response = http.execute(HttpMethod.Post) {
            path("topics")
            jsonBody(body, "application/json")
        }
        return http.decodeJson(response)
    }

    /**
     * List topic kinds
     * Which sorts of break have subjects, and the form each one's settings are edited with
     */
    suspend fun listTopicKinds(): TopicKindList {
        val response = http.execute(HttpMethod.Get) {
            path("topics", "kinds")
        }
        return http.decodeJson(response)
    }

    /**
     * Update topic
     * Rewrites one subject. A break already written keeps the words it was given
     */
    suspend fun updateTopic(id: String, body: TopicInput): TopicList {
        val response = http.execute(HttpMethod.Put) {
            path("topics", segment(id))
            jsonBody(body, "application/json")
        }
        return http.decodeJson(response)
    }

    /**
     * Delete topic
     * Removes a subject, and any band on the format clock that asked for it
     */
    suspend fun deleteTopic(id: String): TopicList {
        val response = http.execute(HttpMethod.Delete) {
            path("topics", segment(id))
        }
        return http.decodeJson(response)
    }
}
