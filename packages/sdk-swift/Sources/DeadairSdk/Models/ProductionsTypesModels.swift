// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// One person in a production: the presenter, or somebody cast to phone in. A snapshot rather than a
/// reference, because the persona it names may be edited or deleted while the programme is still being
/// made and what the turns were written as has to be what an operator reads back
public struct ProductionCastMember: Codable, Equatable, Sendable {
    public var role: ProductionCastMemberRole
    /// What they are called on air
    public var name: String?
    /// The persona key, for a link back to the character
    public var persona: String?

    public init(role: ProductionCastMemberRole, name: String? = nil, persona: String? = nil) {
        self.role = role
        self.name = name
        self.persona = persona
    }

    private enum CodingKeys: String, CodingKey {
        case role = "role"
        case name = "name"
        case persona = "persona"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.role = try container.decode(ProductionCastMemberRole.self, forKey: .role)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.persona = try container.decodeIfPresent(String.self, forKey: .persona)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.role, forKey: .role)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.persona, forKey: .persona)
    }
}

/// What an operator asks for. Everything else about a production is decided by the passes that make it
public struct ProductionRequest: Codable, Equatable, Sendable {
    public var kind: String?
    /// Absent is named after its kind and the moment it was asked for, which is what somebody taking a call now wants rather than a box to fill in
    public var title: String?
    public var brief: String?
    public var personaId: String?
    /// Absent takes the station's `render.productionWritingMode`
    public var writingMode: ProductionRequestWritingMode?
    public var targetMs: Int?
    public var scheduledFor: Date?

    public init(kind: String? = nil, title: String? = nil, brief: String? = nil, personaId: String? = nil, writingMode: ProductionRequestWritingMode? = nil, targetMs: Int? = nil, scheduledFor: Date? = nil) {
        self.kind = kind
        self.title = title
        self.brief = brief
        self.personaId = personaId
        self.writingMode = writingMode
        self.targetMs = targetMs
        self.scheduledFor = scheduledFor
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case title = "title"
        case brief = "brief"
        case personaId = "personaId"
        case writingMode = "writingMode"
        case targetMs = "targetMs"
        case scheduledFor = "scheduledFor"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decodeIfPresent(String.self, forKey: .kind)
        self.title = try container.decodeIfPresent(String.self, forKey: .title)
        self.brief = try container.decodeIfPresent(String.self, forKey: .brief)
        self.personaId = try container.decodeIfPresent(String.self, forKey: .personaId)
        self.writingMode = try container.decodeIfPresent(ProductionRequestWritingMode.self, forKey: .writingMode)
        self.targetMs = try container.decodeIfPresent(Int.self, forKey: .targetMs)
        self.scheduledFor = try container.decodeIfPresent(Date.self, forKey: .scheduledFor)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.title, forKey: .title)
        try container.encodeIfPresent(self.brief, forKey: .brief)
        try container.encodeIfPresent(self.personaId, forKey: .personaId)
        try container.encodeIfPresent(self.writingMode, forKey: .writingMode)
        try container.encodeIfPresent(self.targetMs, forKey: .targetMs)
        try container.encodeIfPresent(self.scheduledFor, forKey: .scheduledFor)
    }
}

/// Something the station makes rather than something it says: several beats of speech, written in several passes, that airs as one block
public struct Production: Codable, Equatable, Sendable {
    public var id: String
    /// What sort of production: podcast, bulletin, feature. Free text, so a station that wants a documentary strand needs no migration
    public var kind: String
    public var title: String
    /// What was asked for, in the operator's own words. Distinct from the title, which is only a label
    public var brief: String?
    /// Who presents it. Absent falls back to the station's active persona when a pass runs
    public var personaId: String?
    /// How many passes to spend on it
    public var writingMode: ProductionWritingMode
    /// How long it should run. What the beat count and the per-beat word budgets are computed from
    public var targetMs: Int
    /// `stitching` is the beats being joined into one piece of audio, and it leads to `ready` whether that worked or not
    public var state: ProductionState
    /// Why making it did not work
    public var error: String?
    /// When it should air. Absent means as soon as it is made
    public var scheduledFor: Date?
    public var cancelledAt: Date?
    /// How many beats exist so far, which is how far along the drafting is
    public var beats: Int
    /// Who is on it, decided by the first pass that ran. Empty for one nobody has started, and for a programme the presenter reads alone
    public var cast: [ProductionCastMember]
    public var createdAt: Date

    public init(id: String, kind: String, title: String, brief: String? = nil, personaId: String? = nil, writingMode: ProductionWritingMode, targetMs: Int, state: ProductionState, error: String? = nil, scheduledFor: Date? = nil, cancelledAt: Date? = nil, beats: Int, cast: [ProductionCastMember], createdAt: Date) {
        self.id = id
        self.kind = kind
        self.title = title
        self.brief = brief
        self.personaId = personaId
        self.writingMode = writingMode
        self.targetMs = targetMs
        self.state = state
        self.error = error
        self.scheduledFor = scheduledFor
        self.cancelledAt = cancelledAt
        self.beats = beats
        self.cast = cast
        self.createdAt = createdAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case kind = "kind"
        case title = "title"
        case brief = "brief"
        case personaId = "personaId"
        case writingMode = "writingMode"
        case targetMs = "targetMs"
        case state = "state"
        case error = "error"
        case scheduledFor = "scheduledFor"
        case cancelledAt = "cancelledAt"
        case beats = "beats"
        case cast = "cast"
        case createdAt = "createdAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.title = try container.decode(String.self, forKey: .title)
        self.brief = try container.decodeIfPresent(String.self, forKey: .brief)
        self.personaId = try container.decodeIfPresent(String.self, forKey: .personaId)
        self.writingMode = try container.decode(ProductionWritingMode.self, forKey: .writingMode)
        self.targetMs = try container.decode(Int.self, forKey: .targetMs)
        self.state = try container.decode(ProductionState.self, forKey: .state)
        self.error = try container.decodeIfPresent(String.self, forKey: .error)
        self.scheduledFor = try container.decodeIfPresent(Date.self, forKey: .scheduledFor)
        self.cancelledAt = try container.decodeIfPresent(Date.self, forKey: .cancelledAt)
        self.beats = try container.decode(Int.self, forKey: .beats)
        self.cast = try container.decode([ProductionCastMember].self, forKey: .cast)
        self.createdAt = try container.decode(Date.self, forKey: .createdAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.title, forKey: .title)
        try container.encodeIfPresent(self.brief, forKey: .brief)
        try container.encodeIfPresent(self.personaId, forKey: .personaId)
        try container.encode(self.writingMode, forKey: .writingMode)
        try container.encode(self.targetMs, forKey: .targetMs)
        try container.encode(self.state, forKey: .state)
        try container.encodeIfPresent(self.error, forKey: .error)
        try container.encodeIfPresent(self.scheduledFor, forKey: .scheduledFor)
        try container.encodeIfPresent(self.cancelledAt, forKey: .cancelledAt)
        try container.encode(self.beats, forKey: .beats)
        try container.encode(self.cast, forKey: .cast)
        try container.encode(self.createdAt, forKey: .createdAt)
    }
}

/// Something the station makes rather than something it says: several beats of speech, written in several passes, that airs as one block
public struct ProductionInput: Codable, Equatable, Sendable {
    /// What sort of production: podcast, bulletin, feature. Free text, so a station that wants a documentary strand needs no migration
    public var kind: String
    public var title: String
    /// What was asked for, in the operator's own words. Distinct from the title, which is only a label
    public var brief: String?
    /// Who presents it. Absent falls back to the station's active persona when a pass runs
    public var personaId: String?
    /// How many passes to spend on it
    public var writingMode: ProductionWritingMode
    /// How long it should run. What the beat count and the per-beat word budgets are computed from
    public var targetMs: Int
    /// When it should air. Absent means as soon as it is made
    public var scheduledFor: Date?

    public init(kind: String, title: String, brief: String? = nil, personaId: String? = nil, writingMode: ProductionWritingMode, targetMs: Int, scheduledFor: Date? = nil) {
        self.kind = kind
        self.title = title
        self.brief = brief
        self.personaId = personaId
        self.writingMode = writingMode
        self.targetMs = targetMs
        self.scheduledFor = scheduledFor
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case title = "title"
        case brief = "brief"
        case personaId = "personaId"
        case writingMode = "writingMode"
        case targetMs = "targetMs"
        case scheduledFor = "scheduledFor"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.title = try container.decode(String.self, forKey: .title)
        self.brief = try container.decodeIfPresent(String.self, forKey: .brief)
        self.personaId = try container.decodeIfPresent(String.self, forKey: .personaId)
        self.writingMode = try container.decode(ProductionWritingMode.self, forKey: .writingMode)
        self.targetMs = try container.decode(Int.self, forKey: .targetMs)
        self.scheduledFor = try container.decodeIfPresent(Date.self, forKey: .scheduledFor)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.title, forKey: .title)
        try container.encodeIfPresent(self.brief, forKey: .brief)
        try container.encodeIfPresent(self.personaId, forKey: .personaId)
        try container.encode(self.writingMode, forKey: .writingMode)
        try container.encode(self.targetMs, forKey: .targetMs)
        try container.encodeIfPresent(self.scheduledFor, forKey: .scheduledFor)
    }
}

public struct ProductionList: Codable, Equatable, Sendable {
    public var productions: [Production]

    public init(productions: [Production]) {
        self.productions = productions
    }

    private enum CodingKeys: String, CodingKey {
        case productions = "productions"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.productions = try container.decode([Production].self, forKey: .productions)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.productions, forKey: .productions)
    }
}

public struct ProductionListInput: Codable, Equatable, Sendable {
    public var productions: [ProductionInput]

    public init(productions: [ProductionInput]) {
        self.productions = productions
    }

    private enum CodingKeys: String, CodingKey {
        case productions = "productions"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.productions = try container.decode([ProductionInput].self, forKey: .productions)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.productions, forKey: .productions)
    }
}

public enum ProductionCastMemberRole: String, Codable, CaseIterable, Sendable {
    case host = "host"
    case caller = "caller"
}

/// How many passes to spend on it
public enum ProductionWritingMode: String, Codable, CaseIterable, Sendable {
    case quick = "quick"
    case outlined = "outlined"
    case polished = "polished"
}

/// `stitching` is the beats being joined into one piece of audio, and it leads to `ready` whether that worked or not
public enum ProductionState: String, Codable, CaseIterable, Sendable {
    case planned = "planned"
    case outlining = "outlining"
    case drafting = "drafting"
    case checking = "checking"
    case rendering = "rendering"
    case stitching = "stitching"
    case ready = "ready"
    case aired = "aired"
    case failed = "failed"
    case cancelled = "cancelled"
}

/// Absent takes the station's `render.productionWritingMode`
public enum ProductionRequestWritingMode: String, Codable, CaseIterable, Sendable {
    case quick = "quick"
    case outlined = "outlined"
    case polished = "polished"
}
