// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `requests.ck`.
public final class RequestsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// Search requestable records
    /// Records the station could be asked to play, matching a title or an artist
    public func searchRequestableRecords(query: SearchRequestableRecordsQuery) async throws -> RequestableTrackList {
        var request = SdkRequest(method: "GET", path: ["requests", "search"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(RequestableTrackList.self, from: response)
    }

    /// List requests
    /// Every recent request, for the operator deciding on them
    public func listRequests(query: ListRequestsQuery? = nil) async throws -> ListenerRequestList {
        var request = SdkRequest(method: "GET", path: ["requests"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(ListenerRequestList.self, from: response)
    }

    /// Create request
    /// Ask the station to play a record. Answers with the request whatever became of it, so a refusal says why in `reason`
    public func createRequest(body: ListenerRequestCreate) async throws -> ListenerRequest {
        var request = SdkRequest(method: "POST", path: ["requests"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ListenerRequest.self, from: response)
    }

    /// List my requests
    /// The signed-in account's own recent requests
    public func listMyRequests() async throws -> ListenerRequestList {
        let request = SdkRequest(method: "GET", path: ["requests", "mine"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ListenerRequestList.self, from: response)
    }

    /// Grant request
    /// Let a waiting request through. It goes into the running order once its audio is here
    public func grantRequest(id: UUID) async throws -> ListenerRequest {
        let request = try SdkRequest(method: "POST", path: ["requests", http.segment(id), "grant"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ListenerRequest.self, from: response)
    }

    /// Decline request
    /// Turn a request down. One already in the running order is left there; take it out of the order instead
    public func declineRequest(id: UUID, body: ListenerRequestDecline) async throws -> ListenerRequest {
        var request = try SdkRequest(method: "POST", path: ["requests", http.segment(id), "decline"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ListenerRequest.self, from: response)
    }
}

/// Query parameters for GET /requests/search.
public struct SearchRequestableRecordsQuery: Encodable, Equatable, Sendable {
    public var q: String
    public var limit: Int?

    public init(q: String, limit: Int? = nil) {
        self.q = q
        self.limit = limit
    }

    private enum CodingKeys: String, CodingKey {
        case q = "q"
        case limit = "limit"
    }
}

/// Query parameters for GET /requests.
public struct ListRequestsQuery: Encodable, Equatable, Sendable {
    public var status: RequestStatus?

    public init(status: RequestStatus? = nil) {
        self.status = status
    }

    private enum CodingKeys: String, CodingKey {
        case status = "status"
    }
}
