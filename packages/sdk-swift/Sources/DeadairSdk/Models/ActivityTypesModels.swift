// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Which part of the station an entry came from, and the console's one filter axis
public enum ActivityModule: String, Codable, CaseIterable, Sendable {
    case playout = "playout"
    case director = "director"
    case render = "render"
    case catalog = "catalog"
    case plugins = "plugins"
}

/// How an entry reads, not how bad it is. There is deliberately no `waiting`: a station idling for
/// want of a listener says so in its own words and stays `info`, for the same reason the transport
/// reports it as `ready` rather than as a mild fault
public enum ActivitySeverity: String, Codable, CaseIterable, Sendable {
    case info = "info"
    case warn = "warn"
    case fault = "fault"
}

/// One thing that happened, from whichever of the feed's sources holds it
public struct ActivityEntry: Codable, Equatable, Sendable {
    /// Unique across the whole feed, and half of the cursor below
    public var id: String
    /// When it happened, as the database recorded it
    public var at: Date
    public var module: ActivityModule
    /// Dotted and stable: `silence.cause`, `air.on`, `segment.ready`, `track.aired`. What a console draws a line with, never something a decision is made on
    public var kind: String
    public var severity: ActivitySeverity
    /// The sentence a person reads, phrased by whatever produced it
    public var detail: String
    /// The structured half, for a reader that wants to filter or chart rather than read
    public var data: [String: JSONValue]?
    /// The segment this is about, for an entry that came from one
    public var segmentId: String?
    /// The catalog track this is about, for an entry that came from one
    public var trackId: String?

    public init(id: String, at: Date, module: ActivityModule, kind: String, severity: ActivitySeverity, detail: String, data: [String: JSONValue]? = nil, segmentId: String? = nil, trackId: String? = nil) {
        self.id = id
        self.at = at
        self.module = module
        self.kind = kind
        self.severity = severity
        self.detail = detail
        self.data = data
        self.segmentId = segmentId
        self.trackId = trackId
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case at = "at"
        case module = "module"
        case kind = "kind"
        case severity = "severity"
        case detail = "detail"
        case data = "data"
        case segmentId = "segmentId"
        case trackId = "trackId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.at = try container.decode(Date.self, forKey: .at)
        self.module = try container.decode(ActivityModule.self, forKey: .module)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.severity = try container.decode(ActivitySeverity.self, forKey: .severity)
        self.detail = try container.decode(String.self, forKey: .detail)
        self.data = try container.decodeIfPresent([String: JSONValue].self, forKey: .data)
        self.segmentId = try container.decodeIfPresent(String.self, forKey: .segmentId)
        self.trackId = try container.decodeIfPresent(String.self, forKey: .trackId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.at, forKey: .at)
        try container.encode(self.module, forKey: .module)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.severity, forKey: .severity)
        try container.encode(self.detail, forKey: .detail)
        try container.encodeIfPresent(self.data, forKey: .data)
        try container.encodeIfPresent(self.segmentId, forKey: .segmentId)
        try container.encodeIfPresent(self.trackId, forKey: .trackId)
    }
}

/// One page of the feed, newest first
public struct ActivityQuery: Codable, Equatable, Sendable {
    public var limit: Int?
    /// Where the previous page ended. Opaque, and a keyset rather than an offset because rows arrive at the head continuously: an offset would re-show a row on every page as the feed grew under it. Pass back whatever `nextBefore` said and nothing else
    public var before: String?
    public var module: ActivityModule?
    /// The floor, not the exact match: `warn` answers with warnings and faults. Absent is everything
    public var minSeverity: ActivitySeverity?

    public init(limit: Int? = nil, before: String? = nil, module: ActivityModule? = nil, minSeverity: ActivitySeverity? = nil) {
        self.limit = limit
        self.before = before
        self.module = module
        self.minSeverity = minSeverity
    }

    private enum CodingKeys: String, CodingKey {
        case limit = "limit"
        case before = "before"
        case module = "module"
        case minSeverity = "minSeverity"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.limit = try container.decodeIfPresent(Int.self, forKey: .limit)
        self.before = try container.decodeIfPresent(String.self, forKey: .before)
        self.module = try container.decodeIfPresent(ActivityModule.self, forKey: .module)
        self.minSeverity = try container.decodeIfPresent(ActivitySeverity.self, forKey: .minSeverity)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.limit, forKey: .limit)
        try container.encodeIfPresent(self.before, forKey: .before)
        try container.encodeIfPresent(self.module, forKey: .module)
        try container.encodeIfPresent(self.minSeverity, forKey: .minSeverity)
    }
}

public struct ActivityPage: Codable, Equatable, Sendable {
    public var entries: [ActivityEntry]
    /// The cursor for the page after this one, absent once the feed has been read to its end
    public var nextBefore: String?

    public init(entries: [ActivityEntry], nextBefore: String? = nil) {
        self.entries = entries
        self.nextBefore = nextBefore
    }

    private enum CodingKeys: String, CodingKey {
        case entries = "entries"
        case nextBefore = "nextBefore"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.entries = try container.decode([ActivityEntry].self, forKey: .entries)
        self.nextBefore = try container.decodeIfPresent(String.self, forKey: .nextBefore)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.entries, forKey: .entries)
        try container.encodeIfPresent(self.nextBefore, forKey: .nextBefore)
    }
}
