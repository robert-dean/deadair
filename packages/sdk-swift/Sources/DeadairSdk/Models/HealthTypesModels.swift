// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// What a liveness probe gets back from the API
public struct Health: Codable, Equatable, Sendable {
    /// Constant. This route answers 200 or nothing at all: a process that can serve it is up, and a process that cannot never reaches the handler
    public var status: String
    /// How long this process has been running, so a probe can tell a live server from one that has just restarted under it
    public var uptimeMs: Int
    /// The commit this station was built from, as the image's `org.opencontainers.image.revision` label says it. Absent when nothing set one, which is what a development tree and a hand-built image both honestly are
    public var revision: String?
    /// The release this station is, as the image's `org.opencontainers.image.version` label says it. Absent on every build that is not a tagged release, which is most of them: `latest` follows main, so a station tracking it honestly has a commit and no version
    public var version: String?

    public init(status: String = "ok", uptimeMs: Int, revision: String? = nil, version: String? = nil) {
        self.status = status
        self.uptimeMs = uptimeMs
        self.revision = revision
        self.version = version
    }

    private enum CodingKeys: String, CodingKey {
        case status = "status"
        case uptimeMs = "uptimeMs"
        case revision = "revision"
        case version = "version"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.status = try container.decode(String.self, forKey: .status)
        guard self.status == "ok" else {
            throw DecodingError.dataCorruptedError(forKey: .status, in: container, debugDescription: "Expected \"ok\"")
        }
        self.uptimeMs = try container.decode(Int.self, forKey: .uptimeMs)
        self.revision = try container.decodeIfPresent(String.self, forKey: .revision)
        self.version = try container.decodeIfPresent(String.self, forKey: .version)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.status, forKey: .status)
        try container.encode(self.uptimeMs, forKey: .uptimeMs)
        try container.encodeIfPresent(self.revision, forKey: .revision)
        try container.encodeIfPresent(self.version, forKey: .version)
    }
}
