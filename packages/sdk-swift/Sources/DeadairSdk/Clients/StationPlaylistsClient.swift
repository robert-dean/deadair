// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `station.playlists.ck`.
public final class StationPlaylistsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List station playlists
    /// Every playlist the station owns, newest first
    public func listStationPlaylists() async throws -> StationPlaylistList {
        let request = SdkRequest(method: "GET", path: ["station-playlists"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationPlaylistList.self, from: response)
    }

    /// Get station playlist
    /// One station playlist with its records in order, placeholders included
    /// - Throws: `SdkError` on 404
    public func getStationPlaylist(id: UUID) async throws -> StationPlaylistDetail {
        let request = try SdkRequest(method: "GET", path: ["station-playlists", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationPlaylistDetail.self, from: response)
    }

    /// Update station playlist
    /// Renames a station playlist, or rewrites what it is for
    /// - Throws: `SdkError` on 404
    public func updateStationPlaylist(id: UUID, body: StationPlaylistUpdate) async throws -> StationPlaylist {
        var request = try SdkRequest(method: "PATCH", path: ["station-playlists", http.segment(id)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(StationPlaylist.self, from: response)
    }

    /// Delete station playlist
    /// Deletes a station playlist. The records it named stay in the library
    /// - Throws: `SdkError` on 404
    public func deleteStationPlaylist(id: UUID) async throws {
        let request = try SdkRequest(method: "DELETE", path: ["station-playlists", http.segment(id)])
        _ = try await http.execute(request)
    }

    /// Fill station playlist
    /// Looks up the records this playlist names and the library does not hold, in the background, and adds the ones a provider has. The activity feed says how it went
    /// - Throws: `SdkError` on 404
    public func fillStationPlaylist(id: UUID) async throws {
        let request = try SdkRequest(method: "POST", path: ["station-playlists", http.segment(id), "fill"])
        _ = try await http.execute(request)
    }

    /// Export station playlist
    /// One station playlist as a file another station can import
    /// - Throws: `SdkError` on 404
    public func exportStationPlaylist(id: UUID) async throws -> ExportStationPlaylistResult {
        let request = try SdkRequest(method: "GET", path: ["station-playlists", http.segment(id), "export"])
        let response = try await http.execute(request)
        let headers = try ExportStationPlaylistHeaders(
            contentDisposition: http.optionalHeader(response, "Content-Disposition", as: String.self)
        )
        return ExportStationPlaylistResult(data: try http.decodeJSON(PlaylistFile.self, from: response), headers: headers)
    }

    /// Preview playlist import
    /// Reads a source and reports which of its records the library holds and which it would have to find. Writes nothing
    public func previewPlaylistImport(body: PlaylistImportInput) async throws -> PlaylistImportPlan {
        var request = SdkRequest(method: "POST", path: ["station-playlists", "import", "preview"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PlaylistImportPlan.self, from: response)
    }

    /// Import playlist
    /// Makes a new station playlist from a source and answers with what it did
    public func importPlaylist(body: PlaylistImportInput) async throws -> PlaylistImportResult {
        var request = SdkRequest(method: "POST", path: ["station-playlists", "import"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PlaylistImportResult.self, from: response)
    }
}

/// Response headers declared on GET /station-playlists/{id}/export.
public struct ExportStationPlaylistHeaders: Equatable, Sendable {
    public let contentDisposition: String?

    public init(contentDisposition: String?) {
        self.contentDisposition = contentDisposition
    }
}

/// The body of GET /station-playlists/{id}/export, with the response headers the contract declares.
public struct ExportStationPlaylistResult: Equatable, Sendable {
    public let data: PlaylistFile
    public let headers: ExportStationPlaylistHeaders

    public init(data: PlaylistFile, headers: ExportStationPlaylistHeaders) {
        self.data = data
        self.headers = headers
    }
}
