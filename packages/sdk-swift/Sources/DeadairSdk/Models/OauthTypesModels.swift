// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// An app's authorization request, as the consent page received it
public struct OAuthAuthorizationQuery: Codable, Equatable, Sendable {
    /// The query string the app sent the browser to the consent page with, as `window.location.search` holds it
    public var query: String

    public init(query: String) {
        self.query = query
    }

    private enum CodingKeys: String, CodingKey {
        case query = "query"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.query = try container.decode(String.self, forKey: .query)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.query, forKey: .query)
    }
}

/// How the station knows an app: registered by an operator, registered by itself, or described by a document on its own website
public enum OAuthClientKind: String, Codable, CaseIterable, Sendable {
    case preregistered = "preregistered"
    case dynamic = "dynamic"
    case metadataDocument = "metadata_document"
}

/// A request with something wrong that the app should be told about: send the browser back to it
public struct OAuthAuthorizationRedirect: Codable, Equatable, Sendable {
    /// Discriminator
    public var kind: String
    /// Where to send the browser
    public var redirectUrl: String

    public init(kind: String = "redirect", redirectUrl: String) {
        self.kind = kind
        self.redirectUrl = redirectUrl
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case redirectUrl = "redirectUrl"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        guard self.kind == "redirect" else {
            throw DecodingError.dataCorruptedError(forKey: .kind, in: container, debugDescription: "Expected \"redirect\"")
        }
        self.redirectUrl = try container.decode(String.self, forKey: .redirectUrl)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.redirectUrl, forKey: .redirectUrl)
    }
}

/// A request that names no app the station knows, or an address the app did not register. Never sent anywhere
public struct OAuthAuthorizationRefusal: Codable, Equatable, Sendable {
    /// Discriminator
    public var kind: String
    /// The OAuth error code
    public var error: String
    /// What was wrong, in words
    public var description: String

    public init(kind: String = "refuse", error: String, description: String) {
        self.kind = kind
        self.error = error
        self.description = description
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case error = "error"
        case description = "description"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        guard self.kind == "refuse" else {
            throw DecodingError.dataCorruptedError(forKey: .kind, in: container, debugDescription: "Expected \"refuse\"")
        }
        self.error = try container.decode(String.self, forKey: .error)
        self.description = try container.decode(String.self, forKey: .description)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.error, forKey: .error)
        try container.encode(self.description, forKey: .description)
    }
}

/// Approving or denying a stashed request
public struct OAuthAuthorizationDecision: Codable, Equatable, Sendable {
    /// From the context
    public var requestId: String

    public init(requestId: String) {
        self.requestId = requestId
    }

    private enum CodingKeys: String, CodingKey {
        case requestId = "requestId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.requestId = try container.decode(String.self, forKey: .requestId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.requestId, forKey: .requestId)
    }
}

/// Where to send the browser now
public struct OAuthAuthorizationOutcome: Codable, Equatable, Sendable {
    /// The app's own address, carrying the code or the refusal
    public var redirectUrl: String

    public init(redirectUrl: String) {
        self.redirectUrl = redirectUrl
    }

    private enum CodingKeys: String, CodingKey {
        case redirectUrl = "redirectUrl"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.redirectUrl = try container.decode(String.self, forKey: .redirectUrl)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.redirectUrl, forKey: .redirectUrl)
    }
}

/// How the app proves itself at the token endpoint. `none` is a public client, which is what apps on somebody's own device are
public enum OAuthClientAuthMethod: String, Codable, CaseIterable, Sendable {
    case none = "none"
    case clientSecretPost = "client_secret_post"
    case clientSecretBasic = "client_secret_basic"
}

/// An app the signed-in person has let act as them
public struct OAuthGrant: Codable, Equatable, Sendable {
    /// For disconnecting it
    public var id: UUID
    /// The app's client id
    public var clientId: String
    /// What the app calls itself, when the station can still find out
    public var clientName: String?
    /// What it can reach
    public var resource: String
    /// What it asked for
    public var scope: [String]
    /// When it was first approved
    public var createdAt: Date
    /// When it last obtained a token
    public var lastUsedAt: Date?

    public init(id: UUID, clientId: String, clientName: String? = nil, resource: String, scope: [String], createdAt: Date, lastUsedAt: Date? = nil) {
        self.id = id
        self.clientId = clientId
        self.clientName = clientName
        self.resource = resource
        self.scope = scope
        self.createdAt = createdAt
        self.lastUsedAt = lastUsedAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case clientId = "clientId"
        case clientName = "clientName"
        case resource = "resource"
        case scope = "scope"
        case createdAt = "createdAt"
        case lastUsedAt = "lastUsedAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.clientId = try container.decode(String.self, forKey: .clientId)
        self.clientName = try container.decodeIfPresent(String.self, forKey: .clientName)
        self.resource = try container.decode(String.self, forKey: .resource)
        self.scope = try container.decode([String].self, forKey: .scope)
        self.createdAt = try container.decode(Date.self, forKey: .createdAt)
        self.lastUsedAt = try container.decodeIfPresent(Date.self, forKey: .lastUsedAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.clientId, forKey: .clientId)
        try container.encodeIfPresent(self.clientName, forKey: .clientName)
        try container.encode(self.resource, forKey: .resource)
        try container.encode(self.scope, forKey: .scope)
        try container.encode(self.createdAt, forKey: .createdAt)
        try container.encodeIfPresent(self.lastUsedAt, forKey: .lastUsedAt)
    }
}

/// A valid request, stashed for the signed-in person to approve or deny
public struct OAuthAuthorizationContext: Codable, Equatable, Sendable {
    /// Discriminator
    public var kind: String
    /// What approving or denying names. Good for a few minutes, and only for the person it was shown to
    public var requestId: String
    /// The app's client id
    public var clientId: String
    /// How the station knows the app
    public var clientKind: OAuthClientKind
    /// What the app calls itself
    public var clientName: String?
    /// The app's own website, when it gave one
    public var clientUri: String?
    /// The app's logo, when it gave one
    public var logoUri: String?
    /// The host the browser is sent back to, which is who actually receives the approval
    public var redirectHost: String
    /// Whether every address the app registered is this computer's own, which only an app running on it should use
    public var loopbackOnly: Bool
    /// What the app asked for. It acts as the person approving it whatever this says
    public var scope: [String]
    /// What the app will be able to reach: the station's MCP endpoint
    public var resource: String

    public init(kind: String = "context", requestId: String, clientId: String, clientKind: OAuthClientKind, clientName: String? = nil, clientUri: String? = nil, logoUri: String? = nil, redirectHost: String, loopbackOnly: Bool, scope: [String], resource: String) {
        self.kind = kind
        self.requestId = requestId
        self.clientId = clientId
        self.clientKind = clientKind
        self.clientName = clientName
        self.clientUri = clientUri
        self.logoUri = logoUri
        self.redirectHost = redirectHost
        self.loopbackOnly = loopbackOnly
        self.scope = scope
        self.resource = resource
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case requestId = "requestId"
        case clientId = "clientId"
        case clientKind = "clientKind"
        case clientName = "clientName"
        case clientUri = "clientUri"
        case logoUri = "logoUri"
        case redirectHost = "redirectHost"
        case loopbackOnly = "loopbackOnly"
        case scope = "scope"
        case resource = "resource"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        guard self.kind == "context" else {
            throw DecodingError.dataCorruptedError(forKey: .kind, in: container, debugDescription: "Expected \"context\"")
        }
        self.requestId = try container.decode(String.self, forKey: .requestId)
        self.clientId = try container.decode(String.self, forKey: .clientId)
        self.clientKind = try container.decode(OAuthClientKind.self, forKey: .clientKind)
        self.clientName = try container.decodeIfPresent(String.self, forKey: .clientName)
        self.clientUri = try container.decodeIfPresent(String.self, forKey: .clientUri)
        self.logoUri = try container.decodeIfPresent(String.self, forKey: .logoUri)
        self.redirectHost = try container.decode(String.self, forKey: .redirectHost)
        self.loopbackOnly = try container.decode(Bool.self, forKey: .loopbackOnly)
        self.scope = try container.decode([String].self, forKey: .scope)
        self.resource = try container.decode(String.self, forKey: .resource)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.requestId, forKey: .requestId)
        try container.encode(self.clientId, forKey: .clientId)
        try container.encode(self.clientKind, forKey: .clientKind)
        try container.encodeIfPresent(self.clientName, forKey: .clientName)
        try container.encodeIfPresent(self.clientUri, forKey: .clientUri)
        try container.encodeIfPresent(self.logoUri, forKey: .logoUri)
        try container.encode(self.redirectHost, forKey: .redirectHost)
        try container.encode(self.loopbackOnly, forKey: .loopbackOnly)
        try container.encode(self.scope, forKey: .scope)
        try container.encode(self.resource, forKey: .resource)
    }
}

/// An app registered with the station
public struct OAuthClientSummary: Codable, Equatable, Sendable {
    /// Its client id
    public var clientId: String
    /// Created by an operator, or registered by itself
    public var kind: OAuthClientSummaryKind
    /// What it is called
    public var name: String?
    /// Where it may be sent back to
    public var redirectUris: [String]
    /// How it proves itself
    public var tokenEndpointAuthMethod: OAuthClientAuthMethod
    /// When it was registered
    public var createdAt: Date
    /// When it last obtained a token
    public var lastUsedAt: Date?
    /// When a self-registered app lapses unless used again
    public var expiresAt: Date?

    public init(clientId: String, kind: OAuthClientSummaryKind, name: String? = nil, redirectUris: [String], tokenEndpointAuthMethod: OAuthClientAuthMethod, createdAt: Date, lastUsedAt: Date? = nil, expiresAt: Date? = nil) {
        self.clientId = clientId
        self.kind = kind
        self.name = name
        self.redirectUris = redirectUris
        self.tokenEndpointAuthMethod = tokenEndpointAuthMethod
        self.createdAt = createdAt
        self.lastUsedAt = lastUsedAt
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case clientId = "clientId"
        case kind = "kind"
        case name = "name"
        case redirectUris = "redirectUris"
        case tokenEndpointAuthMethod = "tokenEndpointAuthMethod"
        case createdAt = "createdAt"
        case lastUsedAt = "lastUsedAt"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.clientId = try container.decode(String.self, forKey: .clientId)
        self.kind = try container.decode(OAuthClientSummaryKind.self, forKey: .kind)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.redirectUris = try container.decode([String].self, forKey: .redirectUris)
        self.tokenEndpointAuthMethod = try container.decode(OAuthClientAuthMethod.self, forKey: .tokenEndpointAuthMethod)
        self.createdAt = try container.decode(Date.self, forKey: .createdAt)
        self.lastUsedAt = try container.decodeIfPresent(Date.self, forKey: .lastUsedAt)
        self.expiresAt = try container.decodeIfPresent(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.clientId, forKey: .clientId)
        try container.encode(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encode(self.redirectUris, forKey: .redirectUris)
        try container.encode(self.tokenEndpointAuthMethod, forKey: .tokenEndpointAuthMethod)
        try container.encode(self.createdAt, forKey: .createdAt)
        try container.encodeIfPresent(self.lastUsedAt, forKey: .lastUsedAt)
        try container.encodeIfPresent(self.expiresAt, forKey: .expiresAt)
    }
}

/// An app an operator registers by hand, for a client that cannot register itself
public struct OAuthClientCreate: Codable, Equatable, Sendable {
    /// What to call it
    public var name: String
    /// Where it may be sent back to. At least one; https, or this computer's own address
    public var redirectUris: [String]
    /// `none` for an app on somebody's own device, otherwise a secret it keeps
    public var tokenEndpointAuthMethod: OAuthClientAuthMethod

    public init(name: String, redirectUris: [String], tokenEndpointAuthMethod: OAuthClientAuthMethod) {
        self.name = name
        self.redirectUris = redirectUris
        self.tokenEndpointAuthMethod = tokenEndpointAuthMethod
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
        case redirectUris = "redirectUris"
        case tokenEndpointAuthMethod = "tokenEndpointAuthMethod"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.name = try container.decode(String.self, forKey: .name)
        self.redirectUris = try container.decode([String].self, forKey: .redirectUris)
        self.tokenEndpointAuthMethod = try container.decode(OAuthClientAuthMethod.self, forKey: .tokenEndpointAuthMethod)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.redirectUris, forKey: .redirectUris)
        try container.encode(self.tokenEndpointAuthMethod, forKey: .tokenEndpointAuthMethod)
    }
}

public struct OAuthGrantList: Codable, Equatable, Sendable {
    /// Most recent first
    public var grants: [OAuthGrant]

    public init(grants: [OAuthGrant]) {
        self.grants = grants
    }

    private enum CodingKeys: String, CodingKey {
        case grants = "grants"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grants = try container.decode([OAuthGrant].self, forKey: .grants)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grants, forKey: .grants)
    }
}

public struct OAuthClientList: Codable, Equatable, Sendable {
    /// Newest first. Apps that describe themselves are not listed: nothing is stored for them
    public var clients: [OAuthClientSummary]

    public init(clients: [OAuthClientSummary]) {
        self.clients = clients
    }

    private enum CodingKeys: String, CodingKey {
        case clients = "clients"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.clients = try container.decode([OAuthClientSummary].self, forKey: .clients)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.clients, forKey: .clients)
    }
}

/// A registered app, with its secret. The only time the secret is ever returned
public struct OAuthClientIssued: Codable, Equatable, Sendable {
    /// The app as it will appear in the list
    public var client: OAuthClientSummary
    /// Present for an app that keeps a secret. Store it now: nothing can show it again
    public var clientSecret: String?

    public init(client: OAuthClientSummary, clientSecret: String? = nil) {
        self.client = client
        self.clientSecret = clientSecret
    }

    private enum CodingKeys: String, CodingKey {
        case client = "client"
        case clientSecret = "clientSecret"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.client = try container.decode(OAuthClientSummary.self, forKey: .client)
        self.clientSecret = try container.decodeIfPresent(String.self, forKey: .clientSecret)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.client, forKey: .client)
        try container.encodeIfPresent(self.clientSecret, forKey: .clientSecret)
    }
}

public indirect enum OAuthAuthorizationContextResult: Codable, Equatable, Sendable {
    case oAuthAuthorizationContext(OAuthAuthorizationContext)
    case oAuthAuthorizationRedirect(OAuthAuthorizationRedirect)
    case oAuthAuthorizationRefusal(OAuthAuthorizationRefusal)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "kind"))
        switch tag {
        case .string("context"):
            self = .oAuthAuthorizationContext(try OAuthAuthorizationContext(from: decoder))
        case .string("redirect"):
            self = .oAuthAuthorizationRedirect(try OAuthAuthorizationRedirect(from: decoder))
        case .string("refuse"):
            self = .oAuthAuthorizationRefusal(try OAuthAuthorizationRefusal(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "kind"), in: container, debugDescription: "Unknown OAuthAuthorizationContextResult kind: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .oAuthAuthorizationContext(let value):
            try value.encode(to: encoder)
        case .oAuthAuthorizationRedirect(let value):
            try value.encode(to: encoder)
        case .oAuthAuthorizationRefusal(let value):
            try value.encode(to: encoder)
        }
    }
}

/// Created by an operator, or registered by itself
public enum OAuthClientSummaryKind: String, Codable, CaseIterable, Sendable {
    case preregistered = "preregistered"
    case dynamic = "dynamic"
}
