// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Net.Http;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading;
using System.Threading.Tasks;
using System.Xml;
using MaroonedSoftware.Deadair.Sdk.Models;
using MaroonedSoftware.Deadair.Sdk.Runtime;

namespace MaroonedSoftware.Deadair.Sdk.Clients;

/// <summary>Operations declared in &lt;c&gt;playout.ck&lt;/c&gt;.</summary>
public sealed class PlayoutClient(SdkHttp http)
{
    /// <summary>
    /// Get playout status
    /// What the station is playing and what is queued behind it. The console polls this
    /// </summary>
    public async Task<PlayoutStatus> GetPlayoutStatusAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("playout", "status"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PlayoutStatus>(response);
    }

    /// <summary>
    /// Play a playlist
    /// Loads a plugin playlist into the running order and starts handing it to the player. Replaces whatever was queued; what is on air finishes rather than being cut off
    /// </summary>
    public async Task<PlayoutStatus> PlayAPlaylistAsync(PlayoutPlaylistInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("playout", "playlist"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PlayoutStatus>(response);
    }

    /// <summary>
    /// Play a chart
    /// Builds the running order from a published chart and starts handing it to the player. The same replacement a playlist makes, from a document somebody else ranked
    /// </summary>
    public async Task<PlayoutStatus> PlayAChartAsync(PlayoutChartInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("playout", "chart"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PlayoutStatus>(response);
    }

    /// <summary>
    /// Skip the current item
    /// Ends the item on air so the next one starts immediately. The station owns the decoder, so this lands at once rather than waiting out audio already committed to a player
    /// </summary>
    public async Task<PlayoutStatus> SkipTheCurrentItemAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("playout", "skip"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PlayoutStatus>(response);
    }

    /// <summary>
    /// Start playout
    /// Puts the station back on air with the running order it already has, picking it up where Stop left it. Distinct from putting a playlist on air, which builds a new broadcast and throws away what was there. Refused when there is nothing left to resume
    /// </summary>
    public async Task<PlayoutStatus> StartPlayoutAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("playout", "start"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PlayoutStatus>(response);
    }

    /// <summary>
    /// Stop playout
    /// Stands the station down: stops what is on air at once and hands the mount back. The running order is LEFT as it is, so `/playout/start` can pick it up where this stopped it. deadair holds the mount on a lease it renews while it has something to play, so stopping goes quiet rather than falling through to a bed nobody programmed
    /// </summary>
    public async Task<PlayoutStatus> StopPlayoutAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("playout", "stop"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PlayoutStatus>(response);
    }
}
