/** The Phrasings page's own framing. The boxes on it are settings, whose labels come from the API. */
export const phrasings = {
    title: 'Phrasings',
    description:
        'What the station says around the things it reads out, whenever no model writes the words. Each box is a set to pick between, one per line. What a character says between records is on its own sheet, under Characters.',
} as const;
