// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `podcasts.ck`.
public final class PodcastsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List shows
    /// Every programme every installed podcast plugin carries
    public func listShows() async throws -> StationShowList {
        let request = SdkRequest(method: "GET", path: ["podcasts", "shows"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationShowList.self, from: response)
    }

    /// Search podcast directory
    /// Looks a show up in the directories the installed podcast plugins can search
    public func searchPodcastDirectory(query: StationDirectoryQuery? = nil) async throws -> StationDirectoryPage {
        var request = SdkRequest(method: "GET", path: ["podcasts", "search"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(StationDirectoryPage.self, from: response)
    }

    /// List episodes
    /// The episodes the station knows about, newest first, with what it has done with each
    public func listEpisodes(query: StationEpisodeQuery? = nil) async throws -> StationEpisodePage {
        var request = SdkRequest(method: "GET", path: ["podcasts", "episodes"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(StationEpisodePage.self, from: response)
    }

    /// Fetch episode
    /// Fetches one episode's audio into the station's store now, rather than waiting for its slot to come near
    public func fetchEpisode(id: String) async throws -> StationEpisode {
        let request = try SdkRequest(method: "POST", path: ["podcasts", "episodes", http.segment(id), "fetch"])
        let response = try await http.execute(request)
        return try http.decodeJSON(StationEpisode.self, from: response)
    }

    /// Refresh podcasts
    /// Reads every show's feed again, in the background, rather than waiting for the next scheduled refresh
    public func refreshPodcasts() async throws {
        let request = SdkRequest(method: "POST", path: ["podcasts", "refresh"])
        _ = try await http.execute(request)
    }
}
