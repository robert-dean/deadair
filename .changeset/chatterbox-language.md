---
'@deadair/plugin-chatterbox': patch
---

On a station set to a language other than English, Chatterbox's multilingual model is asked to speak that language, whenever the model lists it. The plugin also reports which languages the loaded model speaks, so the station can warn you when the model can't speak the station's language: the other Chatterbox models only speak English.
