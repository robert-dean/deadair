// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace MaroonedSoftware.Deadair.Sdk.Models;

/// <summary>Which store, as a stable id the console can key off rather than a name it renders.</summary>
[JsonConverter(typeof(JsonStringEnumConverter<StorageStoreId>))]
public enum StorageStoreId
{
    [JsonStringEnumMemberName("tracks")]
    Tracks,

    [JsonStringEnumMemberName("art")]
    Art,

    [JsonStringEnumMemberName("segments")]
    Segments,

    [JsonStringEnumMemberName("voices")]
    Voices,
}

/// <summary>
/// One content store: what is on disk, and what the database says should be.
///
/// The two halves are deliberately separate numbers rather than one reconciled figure. They disagree
/// in two directions and each direction means something different — a file nothing claims is what a
/// crash between writing bytes and writing a row leaves behind, and a row whose file is gone is what
/// an operator emptying a directory leaves. Reporting one number would hide both.
/// </summary>
public sealed record StorageStore
{
    [JsonPropertyName("id")]
    public required StorageStoreId Id { get; init; }

    /// <summary>What to call it on a page</summary>
    [JsonPropertyName("label")]
    public required string Label { get; init; }

    /// <summary>Where it is, so `du` and this can be compared</summary>
    [JsonPropertyName("path")]
    public required string Path { get; init; }

    /// <summary>Files actually there</summary>
    [JsonPropertyName("files")]
    public required long Files { get; init; }

    /// <summary>What they weigh</summary>
    [JsonPropertyName("bytes")]
    public required long Bytes { get; init; }

    /// <summary>Rows pointing at a file. Absent when no table backs this store</summary>
    [JsonPropertyName("rows")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Rows { get; init; }

    /// <summary>What those rows say those files weigh. Absent where the table does not record a size</summary>
    [JsonPropertyName("accountedBytes")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? AccountedBytes { get; init; }

    /// <summary>The limit an operator set, where the store has one. Absent means no limit</summary>
    [JsonPropertyName("capBytes")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? CapBytes { get; init; }

    /// <summary>Files no row claims. Reported and never cleaned up automatically</summary>
    [JsonPropertyName("orphanFiles")]
    public required long OrphanFiles { get; init; }

    [JsonPropertyName("orphanBytes")]
    public required long OrphanBytes { get; init; }

    /// <summary>Claims whose file is not there. The station re-fetches or re-renders these</summary>
    [JsonPropertyName("rowsWithNoFile")]
    public required long RowsWithNoFile { get; init; }
}

/// <summary>
/// One content store: what is on disk, and what the database says should be.
///
/// The two halves are deliberately separate numbers rather than one reconciled figure. They disagree
/// in two directions and each direction means something different — a file nothing claims is what a
/// crash between writing bytes and writing a row leaves behind, and a row whose file is gone is what
/// an operator emptying a directory leaves. Reporting one number would hide both.
/// </summary>
public sealed record StorageStoreInput;

/// <summary>
/// Every store, plus the number an operator actually wants first.
///
/// `readAt` is not decoration: the figures come from walking directories, which is real I/O on a
/// station holding tens of thousands of files, so the answer is cached for a short while and this is
/// what stops a page mistaking it for live.
/// </summary>
public sealed record StorageReport
{
    [JsonPropertyName("readAt")]
    public required DateTimeOffset ReadAt { get; init; }

    [JsonPropertyName("totalFiles")]
    public required long TotalFiles { get; init; }

    [JsonPropertyName("totalBytes")]
    public required long TotalBytes { get; init; }

    [JsonPropertyName("stores")]
    public required List<StorageStore> Stores { get; init; }
}

/// <summary>
/// Every store, plus the number an operator actually wants first.
///
/// `readAt` is not decoration: the figures come from walking directories, which is real I/O on a
/// station holding tens of thousands of files, so the answer is cached for a short while and this is
/// what stops a page mistaking it for live.
/// </summary>
public sealed record StorageReportInput
{
    [JsonPropertyName("stores")]
    public required List<StorageStoreInput> Stores { get; init; }
}
