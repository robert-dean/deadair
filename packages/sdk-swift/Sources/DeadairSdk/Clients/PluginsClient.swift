// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `plugins.ck`.
public final class PluginsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List plugins
    /// Lists every plugin the host knows about
    public func listPlugins() async throws -> [PluginSummary] {
        let request = SdkRequest(method: "GET", path: ["plugins"])
        let response = try await http.execute(request)
        return try http.decodeJSON([PluginSummary].self, from: response)
    }

    /// List plugin grants
    /// Every capability an installed plugin is asking the operator for, with the answer so far
    public func listPluginGrants() async throws -> PluginGrantList {
        let request = SdkRequest(method: "GET", path: ["plugins", "grants"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginGrantList.self, from: response)
    }

    /// Rescan plugins
    /// Rescans the mounted plugin directory: registers new plugins, unloads removed ones
    public func rescanPlugins() async throws -> [PluginSummary] {
        let request = SdkRequest(method: "POST", path: ["plugins", "rescan"])
        let response = try await http.execute(request)
        return try http.decodeJSON([PluginSummary].self, from: response)
    }

    /// Import plugin
    /// Takes a plugin in from the browser as the tarball npm pack writes and puts it in the plugins directory. It lands disabled, and a newer version of an installed plugin replaces the older one
    /// - Throws: `SdkError` on 400, 409, 413, 415, 422
    public func importPlugin(body: [MultipartPart]) async throws -> PluginImportResult {
        var request = SdkRequest(method: "POST", path: ["plugins", "import"])
        http.setMultipartBody(&request, body)
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginImportResult.self, from: response)
    }

    /// Get plugin
    /// One plugin, including its stored non-secret configuration and last error
    public func getPlugin(id: String) async throws -> PluginDetail {
        let request = try SdkRequest(method: "GET", path: ["plugins", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Update plugin configuration
    /// Validates against the plugin's own config schema, encrypts secrets, persists, and reinitializes
    public func updatePluginConfiguration(id: String, body: PluginConfigInput) async throws -> PluginDetail {
        var request = try SdkRequest(method: "PUT", path: ["plugins", http.segment(id), "config"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Enable plugin
    /// Enables a plugin without resubmitting its configuration
    public func enablePlugin(id: String) async throws -> PluginDetail {
        let request = try SdkRequest(method: "POST", path: ["plugins", http.segment(id), "enable"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Disable plugin
    /// Disables a plugin and tears its instance down, keeping its configuration
    public func disablePlugin(id: String) async throws -> PluginDetail {
        let request = try SdkRequest(method: "POST", path: ["plugins", http.segment(id), "disable"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Decide plugin grant
    /// Answers one capability this plugin asked for. Takes effect on the next fetch, with no reload
    public func decidePluginGrant(id: String, body: PluginGrantInput) async throws -> PluginGrantList {
        var request = try SdkRequest(method: "PUT", path: ["plugins", http.segment(id), "grants"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginGrantList.self, from: response)
    }

    /// Reload plugin
    /// Reapplies the plugin's stored configuration: disposes the running instance and initializes it again
    public func reloadPlugin(id: String) async throws -> PluginDetail {
        let request = try SdkRequest(method: "POST", path: ["plugins", http.segment(id), "reload"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Test plugin connection
    /// Runs the plugin's own `testConnection()` through the invoker
    public func testPluginConnection(id: String) async throws -> PluginTestResult {
        let request = try SdkRequest(method: "POST", path: ["plugins", http.segment(id), "test"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginTestResult.self, from: response)
    }

    /// Suggest plugin config options
    /// Asks the plugin what to offer for its config fields right now, through the invoker
    public func suggestPluginConfigOptions(id: String) async throws -> PluginFieldSuggestions {
        let request = try SdkRequest(method: "POST", path: ["plugins", http.segment(id), "config", "suggestions"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginFieldSuggestions.self, from: response)
    }

    /// Get plugin logs
    /// Returns the plugin's buffered log lines at or above the current log level
    public func getPluginLogs(id: String, query: PluginLogQuery? = nil) async throws -> PluginLogPage {
        var request = try SdkRequest(method: "GET", path: ["plugins", http.segment(id), "logs"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginLogPage.self, from: response)
    }

    /// Download plugin logs
    /// Streams the plugin's full retained log as a plain-text attachment
    public func downloadPluginLogs(id: String) async throws -> DownloadPluginLogsResult {
        let request = try SdkRequest(method: "GET", path: ["plugins", http.segment(id), "logs", "download"])
        let response = try await http.execute(request)
        let headers = try DownloadPluginLogsHeaders(
            contentDisposition: http.optionalHeader(response, "Content-Disposition", as: String.self)
        )
        return DownloadPluginLogsResult(data: response.text, headers: headers)
    }

    /// Set plugin log level
    /// Sets the minimum severity the plugin's log store retains going forward
    public func setPluginLogLevel(id: String, body: PluginLogLevelInput) async throws -> PluginDetail {
        var request = try SdkRequest(method: "PUT", path: ["plugins", http.segment(id), "logs", "level"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Start plugin OAuth authorization
    /// Reports where to send the operator for the provider's consent screen
    public func startPluginOAuthAuthorization(id: String) async throws -> PluginOAuthStart {
        let request = try SdkRequest(method: "GET", path: ["plugins", http.segment(id), "oauth", "authorize"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginOAuthStart.self, from: response)
    }

    /// Disconnect plugin OAuth
    /// Forgets the plugin's stored OAuth tokens and reinitializes it
    public func disconnectPluginOAuth(id: String) async throws -> PluginDetail {
        let request = try SdkRequest(method: "DELETE", path: ["plugins", http.segment(id), "oauth"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginDetail.self, from: response)
    }

    /// Complete plugin OAuth authorization
    /// Completes the flow. Anonymous: the provider redirects the browser here with no session of ours
    public func completePluginOAuthAuthorization(id: String, query: PluginOAuthCallbackQuery? = nil) async throws -> PluginOAuthResult {
        var request = try SdkRequest(method: "GET", path: ["plugins", http.segment(id), "oauth", "callback"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(PluginOAuthResult.self, from: response)
    }
}

/// Response headers declared on GET /plugins/{id}/logs/download.
public struct DownloadPluginLogsHeaders: Equatable, Sendable {
    public let contentDisposition: String?

    public init(contentDisposition: String?) {
        self.contentDisposition = contentDisposition
    }
}

/// The body of GET /plugins/{id}/logs/download, with the response headers the contract declares.
public struct DownloadPluginLogsResult: Equatable, Sendable {
    public let data: String
    public let headers: DownloadPluginLogsHeaders

    public init(data: String, headers: DownloadPluginLogsHeaders) {
        self.data = data
        self.headers = headers
    }
}
