// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// One rule on the station's format clock: a sort of break, and when it happens
public struct ClockBand: Codable, Equatable, Sendable {
    public var id: String
    /// Which sort of break this slot wants, as `segments.kind` spells it. Free text: a station that wants sponsor spots writes `sponsor` and drops the recordings in
    public var kind: String
    /// `clock` is a time of day and `interval` is a spacing rule for a kind the station's own interval does not cover
    public var at: ClockBandAt
    /// For a `clock` band: the hour it happens at. Absent means every hour, which is the common case
    public var hour: Int?
    /// For a `clock` band: minutes past the hour
    public var minute: Int?
    /// For an `interval` band: how far apart, in milliseconds
    public var everyMs: Int?
    /// Where this sits in the operator's own order, which is what settles a boundary two rules both want
    public var position: Int
    /// A rule turned off without being lost
    public var enabled: Bool
    /// What this band is about, as a subject of its own kind: a news category, later a weather location. Absent means it covers whatever it finds
    public var topicId: String?
    /// That subject's name, so a list can be drawn without a second call
    public var topicLabel: String?

    public init(id: String, kind: String, at: ClockBandAt, hour: Int? = nil, minute: Int? = nil, everyMs: Int? = nil, position: Int, enabled: Bool, topicId: String? = nil, topicLabel: String? = nil) {
        self.id = id
        self.kind = kind
        self.at = at
        self.hour = hour
        self.minute = minute
        self.everyMs = everyMs
        self.position = position
        self.enabled = enabled
        self.topicId = topicId
        self.topicLabel = topicLabel
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case kind = "kind"
        case at = "at"
        case hour = "hour"
        case minute = "minute"
        case everyMs = "everyMs"
        case position = "position"
        case enabled = "enabled"
        case topicId = "topicId"
        case topicLabel = "topicLabel"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.at = try container.decode(ClockBandAt.self, forKey: .at)
        self.hour = try container.decodeIfPresent(Int.self, forKey: .hour)
        self.minute = try container.decodeIfPresent(Int.self, forKey: .minute)
        self.everyMs = try container.decodeIfPresent(Int.self, forKey: .everyMs)
        self.position = try container.decode(Int.self, forKey: .position)
        self.enabled = try container.decode(Bool.self, forKey: .enabled)
        self.topicId = try container.decodeIfPresent(String.self, forKey: .topicId)
        self.topicLabel = try container.decodeIfPresent(String.self, forKey: .topicLabel)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.at, forKey: .at)
        try container.encodeIfPresent(self.hour, forKey: .hour)
        try container.encodeIfPresent(self.minute, forKey: .minute)
        try container.encodeIfPresent(self.everyMs, forKey: .everyMs)
        try container.encode(self.position, forKey: .position)
        try container.encode(self.enabled, forKey: .enabled)
        try container.encodeIfPresent(self.topicId, forKey: .topicId)
        try container.encodeIfPresent(self.topicLabel, forKey: .topicLabel)
    }
}

/// One rule on the station's format clock: a sort of break, and when it happens
public struct ClockBandInput: Codable, Equatable, Sendable {
    /// Which sort of break this slot wants, as `segments.kind` spells it. Free text: a station that wants sponsor spots writes `sponsor` and drops the recordings in
    public var kind: String
    /// `clock` is a time of day and `interval` is a spacing rule for a kind the station's own interval does not cover
    public var at: ClockBandAt
    /// For a `clock` band: the hour it happens at. Absent means every hour, which is the common case
    public var hour: Int?
    /// For a `clock` band: minutes past the hour
    public var minute: Int?
    /// For an `interval` band: how far apart, in milliseconds
    public var everyMs: Int?
    /// Where this sits in the operator's own order, which is what settles a boundary two rules both want
    public var position: Int
    /// A rule turned off without being lost
    public var enabled: Bool
    /// What this band is about, as a subject of its own kind: a news category, later a weather location. Absent means it covers whatever it finds
    public var topicId: String?

    public init(kind: String, at: ClockBandAt, hour: Int? = nil, minute: Int? = nil, everyMs: Int? = nil, position: Int, enabled: Bool, topicId: String? = nil) {
        self.kind = kind
        self.at = at
        self.hour = hour
        self.minute = minute
        self.everyMs = everyMs
        self.position = position
        self.enabled = enabled
        self.topicId = topicId
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case at = "at"
        case hour = "hour"
        case minute = "minute"
        case everyMs = "everyMs"
        case position = "position"
        case enabled = "enabled"
        case topicId = "topicId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.at = try container.decode(ClockBandAt.self, forKey: .at)
        self.hour = try container.decodeIfPresent(Int.self, forKey: .hour)
        self.minute = try container.decodeIfPresent(Int.self, forKey: .minute)
        self.everyMs = try container.decodeIfPresent(Int.self, forKey: .everyMs)
        self.position = try container.decode(Int.self, forKey: .position)
        self.enabled = try container.decode(Bool.self, forKey: .enabled)
        self.topicId = try container.decodeIfPresent(String.self, forKey: .topicId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.at, forKey: .at)
        try container.encodeIfPresent(self.hour, forKey: .hour)
        try container.encodeIfPresent(self.minute, forKey: .minute)
        try container.encodeIfPresent(self.everyMs, forKey: .everyMs)
        try container.encode(self.position, forKey: .position)
        try container.encode(self.enabled, forKey: .enabled)
        try container.encodeIfPresent(self.topicId, forKey: .topicId)
    }
}

public struct ClockBandList: Codable, Equatable, Sendable {
    public var bands: [ClockBand]
    /// Which sorts of break this station can actually make right now: one it can write and speak, one it has recordings of, or one it produces as an episode. A band naming anything else claims its boundary and then passes over it
    public var producibleKinds: [String]

    public init(bands: [ClockBand], producibleKinds: [String]) {
        self.bands = bands
        self.producibleKinds = producibleKinds
    }

    private enum CodingKeys: String, CodingKey {
        case bands = "bands"
        case producibleKinds = "producibleKinds"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.bands = try container.decode([ClockBand].self, forKey: .bands)
        self.producibleKinds = try container.decode([String].self, forKey: .producibleKinds)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.bands, forKey: .bands)
        try container.encode(self.producibleKinds, forKey: .producibleKinds)
    }
}

public struct ClockBandListInput: Codable, Equatable, Sendable {
    public var bands: [ClockBandInput]
    /// Which sorts of break this station can actually make right now: one it can write and speak, one it has recordings of, or one it produces as an episode. A band naming anything else claims its boundary and then passes over it
    public var producibleKinds: [String]

    public init(bands: [ClockBandInput], producibleKinds: [String]) {
        self.bands = bands
        self.producibleKinds = producibleKinds
    }

    private enum CodingKeys: String, CodingKey {
        case bands = "bands"
        case producibleKinds = "producibleKinds"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.bands = try container.decode([ClockBandInput].self, forKey: .bands)
        self.producibleKinds = try container.decode([String].self, forKey: .producibleKinds)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.bands, forKey: .bands)
        try container.encode(self.producibleKinds, forKey: .producibleKinds)
    }
}

/// `clock` is a time of day and `interval` is a spacing rule for a kind the station's own interval does not cover
public enum ClockBandAt: String, Codable, CaseIterable, Sendable {
    case clock = "clock"
    case interval = "interval"
}
