// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// What a break can be about: a news category, and later a weather location. The operator's own vocabulary, per sort of break
public struct Topic: Codable, Equatable, Sendable {
    public var id: String
    /// Which sort of break this is a subject for, as `segments.kind` spells it
    public var kind: String
    /// A stable slug, unique within its kind. What the format clock points a band at and what a seeded topic is recognised by
    public var key: String
    /// What a break calls it out loud, so it is the phrasing you would want to hear
    public var label: String
    /// This sort of break's own settings for this subject, in the fields its kind declares. Nothing generic reads them
    public var config: [String: JSONValue]
    /// Your own order, for the list. Two subjects never contest anything, so it means nothing else
    public var position: Int

    public init(id: String, kind: String, key: String, label: String, config: [String: JSONValue], position: Int) {
        self.id = id
        self.kind = kind
        self.key = key
        self.label = label
        self.config = config
        self.position = position
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case kind = "kind"
        case key = "key"
        case label = "label"
        case config = "config"
        case position = "position"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.config = try container.decode([String: JSONValue].self, forKey: .config)
        self.position = try container.decode(Int.self, forKey: .position)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.config, forKey: .config)
        try container.encode(self.position, forKey: .position)
    }
}

/// What a break can be about: a news category, and later a weather location. The operator's own vocabulary, per sort of break
public struct TopicInput: Codable, Equatable, Sendable {
    /// Which sort of break this is a subject for, as `segments.kind` spells it
    public var kind: String
    /// A stable slug, unique within its kind. What the format clock points a band at and what a seeded topic is recognised by
    public var key: String
    /// What a break calls it out loud, so it is the phrasing you would want to hear
    public var label: String
    /// This sort of break's own settings for this subject, in the fields its kind declares. Nothing generic reads them
    public var config: [String: JSONValue]
    /// Your own order, for the list. Two subjects never contest anything, so it means nothing else
    public var position: Int

    public init(kind: String, key: String, label: String, config: [String: JSONValue], position: Int) {
        self.kind = kind
        self.key = key
        self.label = label
        self.config = config
        self.position = position
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case key = "key"
        case label = "label"
        case config = "config"
        case position = "position"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.config = try container.decode([String: JSONValue].self, forKey: .config)
        self.position = try container.decode(Int.self, forKey: .position)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.config, forKey: .config)
        try container.encode(self.position, forKey: .position)
    }
}

/// A sort of break that has subjects at all, and how one of its subjects is edited. `ConfigFieldDescriptor` is the plugins area's, shared for the reason a station setting shares it: one form component renders them all
public struct TopicKindDescriptor: Codable, Equatable, Sendable {
    public var kind: String
    /// What to call one of these: a news subject is a category and a weather subject is a location
    public var nounOne: String
    public var nounMany: String
    public var description: String
    public var fields: [ConfigFieldDescriptor]

    public init(kind: String, nounOne: String, nounMany: String, description: String, fields: [ConfigFieldDescriptor]) {
        self.kind = kind
        self.nounOne = nounOne
        self.nounMany = nounMany
        self.description = description
        self.fields = fields
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
        case nounOne = "nounOne"
        case nounMany = "nounMany"
        case description = "description"
        case fields = "fields"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.nounOne = try container.decode(String.self, forKey: .nounOne)
        self.nounMany = try container.decode(String.self, forKey: .nounMany)
        self.description = try container.decode(String.self, forKey: .description)
        self.fields = try container.decode([ConfigFieldDescriptor].self, forKey: .fields)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.nounOne, forKey: .nounOne)
        try container.encode(self.nounMany, forKey: .nounMany)
        try container.encode(self.description, forKey: .description)
        try container.encode(self.fields, forKey: .fields)
    }
}

public struct TopicQuery: Codable, Equatable, Sendable {
    /// One sort of break, or absent for every subject this station has named
    public var kind: String?

    public init(kind: String? = nil) {
        self.kind = kind
    }

    private enum CodingKeys: String, CodingKey {
        case kind = "kind"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kind = try container.decodeIfPresent(String.self, forKey: .kind)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.kind, forKey: .kind)
    }
}

public struct TopicList: Codable, Equatable, Sendable {
    public var topics: [Topic]

    public init(topics: [Topic]) {
        self.topics = topics
    }

    private enum CodingKeys: String, CodingKey {
        case topics = "topics"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.topics = try container.decode([Topic].self, forKey: .topics)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.topics, forKey: .topics)
    }
}

public struct TopicListInput: Codable, Equatable, Sendable {
    public var topics: [TopicInput]

    public init(topics: [TopicInput]) {
        self.topics = topics
    }

    private enum CodingKeys: String, CodingKey {
        case topics = "topics"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.topics = try container.decode([TopicInput].self, forKey: .topics)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.topics, forKey: .topics)
    }
}

public struct TopicKindList: Codable, Equatable, Sendable {
    public var kinds: [TopicKindDescriptor]

    public init(kinds: [TopicKindDescriptor]) {
        self.kinds = kinds
    }

    private enum CodingKeys: String, CodingKey {
        case kinds = "kinds"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.kinds = try container.decode([TopicKindDescriptor].self, forKey: .kinds)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.kinds, forKey: .kinds)
    }
}
