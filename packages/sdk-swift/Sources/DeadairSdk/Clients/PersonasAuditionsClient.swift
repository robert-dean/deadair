// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `personas.auditions.ck`.
public final class PersonasAuditionsClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List persona auditions
    /// Every audition of this character, newest first, without their breaks
    public func listPersonaAuditions(id: String) async throws -> PersonaAuditionList {
        let request = try SdkRequest(method: "GET", path: ["personas", http.segment(id), "auditions"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaAuditionList.self, from: response)
    }

    /// Start persona audition
    /// Asks the station to put this character through a playlist. It is queued, not written
    public func startPersonaAudition(id: String, body: PersonaAuditionRequest) async throws -> PersonaAudition {
        var request = try SdkRequest(method: "POST", path: ["personas", http.segment(id), "auditions"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaAudition.self, from: response)
    }

    /// Get persona audition
    /// One audition with every break it has written so far, in order
    public func getPersonaAudition(id: String, auditionId: String) async throws -> PersonaAudition {
        let request = try SdkRequest(method: "GET", path: ["personas", http.segment(id), "auditions", http.segment(auditionId)])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaAudition.self, from: response)
    }

    /// Cancel persona audition
    /// Stops an audition where it stands, keeping the breaks it has already written
    public func cancelPersonaAudition(id: String, auditionId: String) async throws -> PersonaAuditionSummary {
        let request = try SdkRequest(method: "POST", path: ["personas", http.segment(id), "auditions", http.segment(auditionId), "cancel"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaAuditionSummary.self, from: response)
    }
}
