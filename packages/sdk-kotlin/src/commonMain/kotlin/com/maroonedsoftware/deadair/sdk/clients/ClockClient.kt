// Auto-generated by @contractkit/plugin-kotlin. Do not edit manually.
package com.maroonedsoftware.deadair.sdk.clients

import com.maroonedsoftware.deadair.sdk.models.ClockBandInput
import com.maroonedsoftware.deadair.sdk.models.ClockBandList
import com.maroonedsoftware.deadair.sdk.runtime.SdkHttp
import io.ktor.http.HttpMethod

/** Operations declared in `clock.ck`. */
class ClockClient(private val http: SdkHttp) {
    /**
     * List clock bands
     * Every band on this station's clock, including the ones switched off, in the operator's own order
     */
    suspend fun listClockBands(): ClockBandList {
        val response = http.execute(HttpMethod.Get) {
            path("clock", "bands")
        }
        return http.decodeJson(response)
    }

    /**
     * Create clock band
     * Adds a band. It claims its first boundary on the next commit pass
     */
    suspend fun createClockBand(body: ClockBandInput): ClockBandList {
        val response = http.execute(HttpMethod.Post) {
            path("clock", "bands")
            jsonBody(body, "application/json")
        }
        return http.decodeJson(response)
    }

    /**
     * Update clock band
     * Rewrites one band. Breaks it has already planted stay where they are: the running order is the memory
     */
    suspend fun updateClockBand(id: String, body: ClockBandInput): ClockBandList {
        val response = http.execute(HttpMethod.Put) {
            path("clock", "bands", segment(id))
            jsonBody(body, "application/json")
        }
        return http.decodeJson(response)
    }

    /**
     * Delete clock band
     * Removes a band, which costs it the boundaries it had not claimed yet and nothing else
     */
    suspend fun deleteClockBand(id: String): ClockBandList {
        val response = http.execute(HttpMethod.Delete) {
            path("clock", "bands", segment(id))
        }
        return http.decodeJson(response)
    }
}
