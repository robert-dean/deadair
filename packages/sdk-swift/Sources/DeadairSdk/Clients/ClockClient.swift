// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `clock.ck`.
public final class ClockClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List clock bands
    /// Every band on this station's clock, including the ones switched off, in the operator's own order
    public func listClockBands() async throws -> ClockBandList {
        let request = SdkRequest(method: "GET", path: ["clock", "bands"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ClockBandList.self, from: response)
    }

    /// Create clock band
    /// Adds a band. It claims its first boundary on the next commit pass
    public func createClockBand(body: ClockBandInput) async throws -> ClockBandList {
        var request = SdkRequest(method: "POST", path: ["clock", "bands"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ClockBandList.self, from: response)
    }

    /// Update clock band
    /// Rewrites one band. Breaks it has already planted stay where they are: the running order is the memory
    public func updateClockBand(id: String, body: ClockBandInput) async throws -> ClockBandList {
        var request = try SdkRequest(method: "PUT", path: ["clock", "bands", http.segment(id)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ClockBandList.self, from: response)
    }

    /// Delete clock band
    /// Removes a band, which costs it the boundaries it had not claimed yet and nothing else
    public func deleteClockBand(id: String) async throws -> ClockBandList {
        let request = try SdkRequest(method: "DELETE", path: ["clock", "bands", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(ClockBandList.self, from: response)
    }
}
