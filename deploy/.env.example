# deadair — production environment.
#
# Copy to `.env` beside the compose file and fill in. Everything the station does at runtime is
# configured from the console and stored in the database; what is here is only what has to be
# known before there is a database to read.

# ─── Which image, and where it listens ────────────────────────────────────────────────────────
# latest = speech server bundled, database and cache are yours.
# full   = database and cache bundled too. Nothing else needed on the host.
# slim   = no speech server either; point the speech plugin at your own.
VARIANT=latest
# The published port. Serves the console, /api, and the stream at /live.mp3 — plus the Opus,
# AAC and FLAC mounts and the HLS output at /hls/live.m3u8, if you switch any of them on.
HTTP_PORT=8080
# Where the station keeps everything. Back this up.
DATA_DIR=./data

# ─── Database ─────────────────────────────────────────────────────────────────────────────────
# Required on `latest` and `slim`; ignored on `full`, which runs its own.
# This role owns the schema: migrations run as it on every boot.
DATABASE_HOST=
DATABASE_PORT=5432
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_NAME=deadair
# Optional. A second, non-owner role for the runtime pool, holding DML grants only, so the
# request path cannot alter the schema. Leave both blank to run everything as the owner.
DATABASE_APP_USER=
DATABASE_APP_PASSWORD=

# Set to false only if you apply migrations yourself. Left on, the station brings its own schema
# up to date at every boot — an out-of-date schema otherwise degrades quietly rather than failing.
MIGRATE_ON_BOOT=true

# ─── Cache ────────────────────────────────────────────────────────────────────────────────────
# Sessions and refresh-token families live here rather than in the database. Required on
# `latest` and `slim`; ignored on `full`.
REDIS_HOST=
REDIS_PORT=6379

# ─── Secrets ──────────────────────────────────────────────────────────────────────────────────
# Generate once, keep forever, back up. The first encrypts every credential the station stores
# (provider tokens, the mail password, stream passwords); losing it means re-entering the ones you
# typed, while the stream's own passwords need nothing. The second signs sessions; losing it signs
# everybody out and nothing worse.
#
# Hex, and it must be hex: it is read as hex bytes, so a base64 key is silently a different and
# much weaker key than the one you thought you generated.
#
#     openssl rand -hex 32
#
KMS_LOCAL_ROOT_KEY=
#
# An RSA private key in PEM. Sessions are signed RS256, so an elliptic-curve key is refused.
#
#     openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048
#
# A key is several lines and this file holds one per value, so any of three spellings is accepted:
# the PEM as it comes out, the same thing with its line breaks written as \n, or the whole key
# base64-ed onto one line — `openssl genpkey … | base64 -w0`, which is the one that survives being
# pasted through anything.
AUTHENTICATION_SESSION_JWT_PRIVATE_KEY=

# ─── Addresses ────────────────────────────────────────────────────────────────────────────────
# The address a browser types to reach the station, whole: the scheme, the host, and the port unless
# it is 80 or 443, with nothing after it. On your own network that is the server's address and the
# port you published, such as http://192.168.1.10:8080; behind a proxy that terminates TLS it is the
# https:// name. Sign-in links and a music provider's authorization are sent back here, so it must be
# the URL a browser visits, not the container's own. Both are the same address, because one port
# serves the console and the API. See "The station's address" in README.md.
APP_BASE_URL=https://radio.example.com
SPA_BASE_URL=https://radio.example.com

# ─── Where the station is ─────────────────────────────────────────────────────────────────────
# An IANA zone name. The station reads the clock in this when it says the time or works out what
# half of the day it is, and a container with no TZ set reports UTC — which is a presenter saying
# "tonight" through somebody's afternoon everywhere west of Greenwich.
#
# This is only the FALLBACK. The station's own "Where the station is" setting, on the settings
# page, wins over it, and that is the right place to answer the question: a station is a place and
# its listeners are in it, which is not necessarily where the server is.
TZ=Etc/UTC

# ─── A proxy in front ─────────────────────────────────────────────────────────────────────────
# Leave both empty unless a tunnel or a reverse proxy sits in front of this station. When one does,
# every listener reaches the station as THAT — so they all share one rate-limit bucket, and the
# audience count collapses to however many different players are tuned in rather than how many
# people are listening.
#
# Naming the proxy here lets the station read the real address out of a header instead. An address
# or a CIDR; 172.16.0.0/12 covers another container on the same box. Only name something you put
# there yourself: this means believing an address that arrived in a header, and a header can say
# anything.
REAL_IP_FROM=
# Which header that proxy puts the listener's address in. X-Forwarded-For suits almost everything;
# a Cloudflare tunnel also sends CF-Connecting-IP. Ignored while the field above is empty.
REAL_IP_HEADER=X-Forwarded-For

# ─── Sign-in ──────────────────────────────────────────────────────────────────────────────────
# Optional, and only for signing in through Google. Everything else about sign-in is a setting
# rather than an environment variable: the mail server that sends one-time codes and sign-in links
# is on the settings page, under Mail. Without one the station signs you in on your password alone
# and says so — it will not offer a code it cannot send.
#
# In the Google Cloud console, the OAuth client's authorized redirect URI is APP_BASE_URL followed
# by /api/auth/login/oidc/callback, so https://radio.example.com/api/auth/login/oidc/callback for
# the address above. Google refuses a sign-in whose redirect is not registered exactly.
GOOGLE_OIDC_CLIENT_ID=
GOOGLE_OIDC_CLIENT_SECRET=

# Nothing else belongs here. In particular the log-rotation limits (LOG_MAX_*) are deliberately
# absent: they are validated strictly and a value the parser cannot read stops the station at
# boot, which is a poor trade for a setting almost nobody changes.
