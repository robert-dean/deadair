// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `oauth.ck`.
public final class OauthClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// Describe authorization request
    /// Validate an app's authorization request for the consent page, and stash it for the signed-in person. A POST because it stashes: what is approved is exactly what was validated here
    public func describeAuthorizationRequest(body: OAuthAuthorizationQuery) async throws -> OAuthAuthorizationContextResult {
        var request = SdkRequest(method: "POST", path: ["auth", "oauth", "authorize", "context"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(OAuthAuthorizationContextResult.self, from: response)
    }

    /// Approve authorization request
    /// Let the app act as the signed-in person. Once the account has a strong second factor, this needs one verified in the last five minutes
    public func approveAuthorizationRequest(body: OAuthAuthorizationDecision) async throws -> OAuthAuthorizationOutcome {
        var request = SdkRequest(method: "POST", path: ["auth", "oauth", "authorize", "approve"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(OAuthAuthorizationOutcome.self, from: response)
    }

    /// Deny authorization request
    /// Turn the app away. It is told the person said no
    public func denyAuthorizationRequest(body: OAuthAuthorizationDecision) async throws -> OAuthAuthorizationOutcome {
        var request = SdkRequest(method: "POST", path: ["auth", "oauth", "authorize", "deny"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(OAuthAuthorizationOutcome.self, from: response)
    }

    /// List OAuth clients
    /// Every app registered with the station, by an operator or by itself
    public func listOAuthClients() async throws -> OAuthClientList {
        let request = SdkRequest(method: "GET", path: ["auth", "oauth", "clients"])
        let response = try await http.execute(request)
        return try http.decodeJSON(OAuthClientList.self, from: response)
    }

    /// Create OAuth client
    /// Register an app by hand. The secret, for an app that keeps one, is in this response and nowhere else
    public func createOAuthClient(body: OAuthClientCreate) async throws -> OAuthClientIssued {
        var request = SdkRequest(method: "POST", path: ["auth", "oauth", "clients"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(OAuthClientIssued.self, from: response)
    }

    /// Revoke OAuth client
    /// Withdraw an app. Every person's approval of it ends, and so does every token it holds
    public func revokeOAuthClient(clientId: String) async throws {
        let request = try SdkRequest(method: "DELETE", path: ["auth", "oauth", "clients", http.segment(clientId)])
        _ = try await http.execute(request)
    }

    /// List OAuth grants
    /// The apps the signed-in person has let act as them
    public func listOAuthGrants() async throws -> OAuthGrantList {
        let request = SdkRequest(method: "GET", path: ["auth", "oauth", "grants"])
        let response = try await http.execute(request)
        return try http.decodeJSON(OAuthGrantList.self, from: response)
    }

    /// Revoke OAuth grant
    /// Disconnect an app. Every token it holds for this person stops working at once
    public func revokeOAuthGrant(id: UUID) async throws {
        let request = try SdkRequest(method: "DELETE", path: ["auth", "oauth", "grants", http.segment(id)])
        _ = try await http.execute(request)
    }
}
