// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `schedule.ck`.
public final class ScheduleClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List schedule
    /// Every slot in this station's schedule, earliest in the day first
    public func listSchedule() async throws -> ScheduleSlotList {
        let request = SdkRequest(method: "GET", path: ["schedule"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ScheduleSlotList.self, from: response)
    }

    /// Create schedule slot
    /// Adds a slot. The station does not change over until its start time comes round
    public func createScheduleSlot(body: ScheduleSlotInput) async throws -> ScheduleSlotList {
        var request = SdkRequest(method: "POST", path: ["schedule"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ScheduleSlotList.self, from: response)
    }

    /// Read current slot
    /// Which slot the clock says should be on, and which one the station is actually airing
    public func readCurrentSlot() async throws -> ScheduleNow {
        let request = SdkRequest(method: "GET", path: ["schedule", "current"])
        let response = try await http.execute(request)
        return try http.decodeJSON(ScheduleNow.self, from: response)
    }

    /// Read timetable
    /// The station's day as blocks, contiguous and gapless, for drawing
    public func readTimetable(query: ScheduleTimetableQuery? = nil) async throws -> ScheduleTimetable {
        var request = SdkRequest(method: "GET", path: ["schedule", "timetable"])
        try http.addQuery(&request, query)
        let response = try await http.execute(request)
        return try http.decodeJSON(ScheduleTimetable.self, from: response)
    }

    /// Update schedule slot
    /// Rewrites a slot. Takes effect at its next boundary rather than immediately
    public func updateScheduleSlot(id: String, body: ScheduleSlotInput) async throws -> ScheduleSlotList {
        var request = try SdkRequest(method: "PUT", path: ["schedule", http.segment(id)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(ScheduleSlotList.self, from: response)
    }

    /// Delete schedule slot
    /// Removes a slot. Whatever is on air stays on until the next slot begins
    public func deleteScheduleSlot(id: String) async throws -> ScheduleSlotList {
        let request = try SdkRequest(method: "DELETE", path: ["schedule", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(ScheduleSlotList.self, from: response)
    }
}
