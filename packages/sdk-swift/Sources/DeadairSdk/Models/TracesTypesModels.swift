// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Whether a call produced what it was asked for. Two values on purpose: every finer distinction —
/// timed out, was preempted, came back empty — is a fact the caller knew and the recorder did not, so
/// it lives in `detail` where it can be named
public enum TraceOutcome: String, Codable, CaseIterable, Sendable {
    case ok = "ok"
    case failed = "failed"
}

/// One decision, folded: a job execution or a request
public struct TraceDecision: Codable, Equatable, Sendable {
    /// The job id or the request id. Already the station's correlation id, never generated for this
    public var id: String
    /// A queue name, or a method and path
    public var kind: String
    /// The decision that enqueued this one. Absent on a request, a cron job and anything at boot
    public var parent: String?
    /// When its first recorded call ended
    public var at: Date
    /// Wall clock, off the `job.run` span. Zero for a decision recorded before that span existed
    public var ms: Int
    /// Everything it did, not counting the `job.run` that contains them
    public var calls: Int
    /// How many of those did not produce what they were asked for
    public var failed: Int

    public init(id: String, kind: String, parent: String? = nil, at: Date, ms: Int, calls: Int, failed: Int) {
        self.id = id
        self.kind = kind
        self.parent = parent
        self.at = at
        self.ms = ms
        self.calls = calls
        self.failed = failed
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case kind = "kind"
        case parent = "parent"
        case at = "at"
        case ms = "ms"
        case calls = "calls"
        case failed = "failed"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.parent = try container.decodeIfPresent(String.self, forKey: .parent)
        self.at = try container.decode(Date.self, forKey: .at)
        self.ms = try container.decode(Int.self, forKey: .ms)
        self.calls = try container.decode(Int.self, forKey: .calls)
        self.failed = try container.decode(Int.self, forKey: .failed)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.parent, forKey: .parent)
        try container.encode(self.at, forKey: .at)
        try container.encode(self.ms, forKey: .ms)
        try container.encode(self.calls, forKey: .calls)
        try container.encode(self.failed, forKey: .failed)
    }
}

/// Which slice of the kept window to read
public struct TracesQuery: Codable, Equatable, Sendable {
    public var limit: Int?
    /// An exact queue name or route, for reading one kind of decision on its own
    public var kind: String?
    /// Only decisions carrying at least one failed call
    public var failedOnly: Bool?

    public init(limit: Int? = nil, kind: String? = nil, failedOnly: Bool? = nil) {
        self.limit = limit
        self.kind = kind
        self.failedOnly = failedOnly
    }

    private enum CodingKeys: String, CodingKey {
        case limit = "limit"
        case kind = "kind"
        case failedOnly = "failedOnly"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.limit = try container.decodeIfPresent(Int.self, forKey: .limit)
        self.kind = try container.decodeIfPresent(String.self, forKey: .kind)
        self.failedOnly = try container.decodeIfPresent(Bool.self, forKey: .failedOnly)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.limit, forKey: .limit)
        try container.encodeIfPresent(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.failedOnly, forKey: .failedOnly)
    }
}

/// One call inside a decision, and what it cost
public struct TraceSpan: Codable, Equatable, Sendable {
    /// When the call ended, which is when its cost was known
    public var at: Date
    /// Dotted and stable: `job.run`, `plugin.invoke`, `llm.generate`
    public var op: String
    /// Which one: a plugin and its method, or a model
    public var target: String?
    /// How long it held, measured around the call rather than reported by it
    public var ms: Int
    public var outcome: TraceOutcome
    /// The failure, summarized to a shape rather than a stack
    public var error: String?
    /// Whatever this `op` is worth reading back: tokens, a finish reason, the bound it was given
    public var detail: [String: JSONValue]?

    public init(at: Date, op: String, target: String? = nil, ms: Int, outcome: TraceOutcome, error: String? = nil, detail: [String: JSONValue]? = nil) {
        self.at = at
        self.op = op
        self.target = target
        self.ms = ms
        self.outcome = outcome
        self.error = error
        self.detail = detail
    }

    private enum CodingKeys: String, CodingKey {
        case at = "at"
        case op = "op"
        case target = "target"
        case ms = "ms"
        case outcome = "outcome"
        case error = "error"
        case detail = "detail"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.at = try container.decode(Date.self, forKey: .at)
        self.op = try container.decode(String.self, forKey: .op)
        self.target = try container.decodeIfPresent(String.self, forKey: .target)
        self.ms = try container.decode(Int.self, forKey: .ms)
        self.outcome = try container.decode(TraceOutcome.self, forKey: .outcome)
        self.error = try container.decodeIfPresent(String.self, forKey: .error)
        self.detail = try container.decodeIfPresent([String: JSONValue].self, forKey: .detail)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.at, forKey: .at)
        try container.encode(self.op, forKey: .op)
        try container.encodeIfPresent(self.target, forKey: .target)
        try container.encode(self.ms, forKey: .ms)
        try container.encode(self.outcome, forKey: .outcome)
        try container.encodeIfPresent(self.error, forKey: .error)
        try container.encodeIfPresent(self.detail, forKey: .detail)
    }
}

public struct TracesPage: Codable, Equatable, Sendable {
    /// Newest first
    public var decisions: [TraceDecision]
    /// How many the window holds before `limit`, so a page can say it is showing a slice
    public var total: Int
    /// How many calls were read to answer, which is the honest cost of this page
    public var spans: Int

    public init(decisions: [TraceDecision], total: Int, spans: Int) {
        self.decisions = decisions
        self.total = total
        self.spans = spans
    }

    private enum CodingKeys: String, CodingKey {
        case decisions = "decisions"
        case total = "total"
        case spans = "spans"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.decisions = try container.decode([TraceDecision].self, forKey: .decisions)
        self.total = try container.decode(Int.self, forKey: .total)
        self.spans = try container.decode(Int.self, forKey: .spans)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.decisions, forKey: .decisions)
        try container.encode(self.total, forKey: .total)
        try container.encode(self.spans, forKey: .spans)
    }
}

/// One decision, its calls, and the decisions on either side of it
public struct TraceDetail: Codable, Equatable, Sendable {
    public var decision: TraceDecision
    /// In the order they happened
    public var spans: [TraceSpan]
    /// What enqueued this, when that decision is still inside the kept window
    public var parent: TraceDecision?
    /// What this one went on to enqueue
    public var caused: [TraceDecision]

    public init(decision: TraceDecision, spans: [TraceSpan], parent: TraceDecision? = nil, caused: [TraceDecision]) {
        self.decision = decision
        self.spans = spans
        self.parent = parent
        self.caused = caused
    }

    private enum CodingKeys: String, CodingKey {
        case decision = "decision"
        case spans = "spans"
        case parent = "parent"
        case caused = "caused"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.decision = try container.decode(TraceDecision.self, forKey: .decision)
        self.spans = try container.decode([TraceSpan].self, forKey: .spans)
        self.parent = try container.decodeIfPresent(TraceDecision.self, forKey: .parent)
        self.caused = try container.decode([TraceDecision].self, forKey: .caused)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.decision, forKey: .decision)
        try container.encode(self.spans, forKey: .spans)
        try container.encodeIfPresent(self.parent, forKey: .parent)
        try container.encode(self.caused, forKey: .caused)
    }
}
