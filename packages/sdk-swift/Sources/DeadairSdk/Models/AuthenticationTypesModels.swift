// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Denotes the authorization flow to use
public enum AuthenticationGrantType: String, Codable, CaseIterable, Sendable {
    case clientCredentials = "client_credentials"
    case password = "password"
    case refreshToken = "refresh_token"
    case link = "link"
    case code = "code"
    case fido = "fido"
    case authenticator = "authenticator"
    case oidc = "oidc"
}

/// Denotes the authorization flow to use
public enum PasswordlessAuthenticationGrantType: String, Codable, CaseIterable, Sendable {
    case link = "link"
    case code = "code"
    case fido = "fido"
    case oidc = "oidc"
}

/// The type of the factor
public enum AuthenticationFactorMethod: String, Codable, CaseIterable, Sendable {
    case phone = "phone"
    case password = "password"
    case email = "email"
    case authenticator = "authenticator"
    case fido = "fido"
    case oidc = "oidc"
}

/// The kind of the factor
public enum AuthenticationFactorKind: String, Codable, CaseIterable, Sendable {
    case knowledge = "knowledge"
    case possession = "possession"
    case biometric = "biometric"
}

/// The OIDC identity provider
public enum OidcProvider: String, Codable, CaseIterable, Sendable {
    case google = "google"
}

/// Represents an authentication token
public struct AuthenticationToken: Codable, Equatable, Sendable {
    /// The access token string as issued by the authorization server
    public var accessToken: String
    /// A refresh token which applications can use to obtain another access token
    public var refreshToken: String?
    /// Unix timestamp (seconds) when the access token expires
    public var expiresIn: Int
    /// The type of token this is, typically just the string *Bearer*
    public var tokenType: String
    /// Space-separated list of scopes granted to this token
    public var scope: String

    public init(accessToken: String, refreshToken: String? = nil, expiresIn: Int, tokenType: String, scope: String) {
        self.accessToken = accessToken
        self.refreshToken = refreshToken
        self.expiresIn = expiresIn
        self.tokenType = tokenType
        self.scope = scope
    }

    private enum CodingKeys: String, CodingKey {
        case accessToken = "access_token"
        case refreshToken = "refresh_token"
        case expiresIn = "expires_in"
        case tokenType = "token_type"
        case scope = "scope"
    }

    private enum EncodingKeys: String, CodingKey {
        case accessToken = "accessToken"
        case refreshToken = "refreshToken"
        case expiresIn = "expiresIn"
        case tokenType = "tokenType"
        case scope = "scope"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.accessToken = try container.decode(String.self, forKey: .accessToken)
        self.refreshToken = try container.decodeIfPresent(String.self, forKey: .refreshToken)
        self.expiresIn = try container.decode(Int.self, forKey: .expiresIn)
        self.tokenType = try container.decode(String.self, forKey: .tokenType)
        self.scope = try container.decode(String.self, forKey: .scope)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.accessToken, forKey: .accessToken)
        try container.encodeIfPresent(self.refreshToken, forKey: .refreshToken)
        try container.encode(self.expiresIn, forKey: .expiresIn)
        try container.encode(self.tokenType, forKey: .tokenType)
        try container.encode(self.scope, forKey: .scope)
    }
}

/// Issued-token arm of /auth/token response
public struct AuthenticationTokenIssued: Codable, Equatable, Sendable {
    /// Discriminator
    public var result: String
    /// The access token string as issued by the authorization server
    public var accessToken: String
    /// A refresh token which applications can use to obtain another access token
    public var refreshToken: String?
    /// Unix timestamp (seconds) when the access token expires
    public var expiresIn: Int
    /// The type of token this is, typically just the string *Bearer*
    public var tokenType: String
    /// Space-separated list of scopes granted to this token
    public var scope: String

    public init(result: String = "token", accessToken: String, refreshToken: String? = nil, expiresIn: Int, tokenType: String, scope: String) {
        self.result = result
        self.accessToken = accessToken
        self.refreshToken = refreshToken
        self.expiresIn = expiresIn
        self.tokenType = tokenType
        self.scope = scope
    }

    private enum CodingKeys: String, CodingKey {
        case result = "result"
        case accessToken = "access_token"
        case refreshToken = "refresh_token"
        case expiresIn = "expires_in"
        case tokenType = "token_type"
        case scope = "scope"
    }

    private enum EncodingKeys: String, CodingKey {
        case result = "result"
        case accessToken = "accessToken"
        case refreshToken = "refreshToken"
        case expiresIn = "expiresIn"
        case tokenType = "tokenType"
        case scope = "scope"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.result = try container.decode(String.self, forKey: .result)
        guard self.result == "token" else {
            throw DecodingError.dataCorruptedError(forKey: .result, in: container, debugDescription: "Expected \"token\"")
        }
        self.accessToken = try container.decode(String.self, forKey: .accessToken)
        self.refreshToken = try container.decodeIfPresent(String.self, forKey: .refreshToken)
        self.expiresIn = try container.decode(Int.self, forKey: .expiresIn)
        self.tokenType = try container.decode(String.self, forKey: .tokenType)
        self.scope = try container.decode(String.self, forKey: .scope)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.result, forKey: .result)
        try container.encode(self.accessToken, forKey: .accessToken)
        try container.encodeIfPresent(self.refreshToken, forKey: .refreshToken)
        try container.encode(self.expiresIn, forKey: .expiresIn)
        try container.encode(self.tokenType, forKey: .tokenType)
        try container.encode(self.scope, forKey: .scope)
    }
}

/// Returned by /auth/step-up/start when no enrolled factor satisfies the requirement. The SPA should drive the user through enrollment and retry the gated action afterwards.
public struct EnrollmentRequiredResponse: Codable, Equatable, Sendable {
    /// Discriminator
    public var result: String

    public init(result: String = "enrollment_required") {
        self.result = result
    }

    private enum CodingKeys: String, CodingKey {
        case result = "result"
    }

    private enum EncodingKeys: String, CodingKey {
        case result = "result"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.result = try container.decode(String.self, forKey: .result)
        guard self.result == "enrollment_required" else {
            throw DecodingError.dataCorruptedError(forKey: .result, in: container, debugDescription: "Expected \"enrollment_required\"")
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.result, forKey: .result)
    }
}

/// Represents a common shape of a `PublicKeyCredential` after the client serializes the `id` and `rawId` fields to base64 strings for transport
public struct PublicKeyCredential: Codable, Equatable, Sendable {
    /// The base64url encoding of `rawId`
    public var id: String
    /// This enumeration defines the valid credential types. It is an extension point; values can be added to it in the future, as more credential types are defined. The values of this enumeration are used for versioning the Authentication Assertion and attestation structures according to the type of the authenticator. Currently one credential type is defined, namely `public-key`.
    public var type: String
    /// The credential identifier
    public var rawId: String
    /// The authenticator attachment
    public var authenticatorAttachment: PublicKeyCredentialAuthenticatorAttachment?

    public init(id: String, type: String = "public-key", rawId: String, authenticatorAttachment: PublicKeyCredentialAuthenticatorAttachment? = nil) {
        self.id = id
        self.type = type
        self.rawId = rawId
        self.authenticatorAttachment = authenticatorAttachment
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case type = "type"
        case rawId = "rawId"
        case authenticatorAttachment = "authenticatorAttachment"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.type = try container.decode(String.self, forKey: .type)
        guard self.type == "public-key" else {
            throw DecodingError.dataCorruptedError(forKey: .type, in: container, debugDescription: "Expected \"public-key\"")
        }
        self.rawId = try container.decode(String.self, forKey: .rawId)
        self.authenticatorAttachment = try container.decodeIfPresent(PublicKeyCredentialAuthenticatorAttachment.self, forKey: .authenticatorAttachment)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.type, forKey: .type)
        try container.encode(self.rawId, forKey: .rawId)
        try container.encodeIfPresent(self.authenticatorAttachment, forKey: .authenticatorAttachment)
    }
}

/// Subset of the WebAuthn client extension results the service round-trips
public struct SimpleClientExtensionResults: Codable, Equatable, Sendable {
    /// Whether the client is an application
    public var appid: Bool?
    /// Whether the client is excluded from appid verification
    public var appidExclude: Bool?
    public var credProps: SimpleClientExtensionResultsCredProps?

    public init(appid: Bool? = nil, appidExclude: Bool? = nil, credProps: SimpleClientExtensionResultsCredProps? = nil) {
        self.appid = appid
        self.appidExclude = appidExclude
        self.credProps = credProps
    }

    private enum CodingKeys: String, CodingKey {
        case appid = "appid"
        case appidExclude = "appidExclude"
        case credProps = "credProps"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.appid = try container.decodeIfPresent(Bool.self, forKey: .appid)
        self.appidExclude = try container.decodeIfPresent(Bool.self, forKey: .appidExclude)
        self.credProps = try container.decodeIfPresent(SimpleClientExtensionResultsCredProps.self, forKey: .credProps)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.appid, forKey: .appid)
        try container.encodeIfPresent(self.appidExclude, forKey: .appidExclude)
        try container.encodeIfPresent(self.credProps, forKey: .credProps)
    }
}

public struct FidoAuthenticatorAssertionResponse: Codable, Equatable, Sendable {
    /// The client data JSON
    public var clientDataJson: String
    /// The authenticator data
    public var authenticatorData: String
    /// The signature
    public var signature: String
    /// The user handle
    public var userHandle: String?

    public init(clientDataJson: String, authenticatorData: String, signature: String, userHandle: String? = nil) {
        self.clientDataJson = clientDataJson
        self.authenticatorData = authenticatorData
        self.signature = signature
        self.userHandle = userHandle
    }

    private enum CodingKeys: String, CodingKey {
        case clientDataJson = "clientDataJSON"
        case authenticatorData = "authenticatorData"
        case signature = "signature"
        case userHandle = "userHandle"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.clientDataJson = try container.decode(String.self, forKey: .clientDataJson)
        self.authenticatorData = try container.decode(String.self, forKey: .authenticatorData)
        self.signature = try container.decode(String.self, forKey: .signature)
        self.userHandle = try container.decodeIfPresent(String.self, forKey: .userHandle)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.clientDataJson, forKey: .clientDataJson)
        try container.encode(self.authenticatorData, forKey: .authenticatorData)
        try container.encode(self.signature, forKey: .signature)
        try container.encodeIfPresent(self.userHandle, forKey: .userHandle)
    }
}

public struct AuthenticationRegistration: Codable, Equatable, Sendable {
    /// The registration identifier
    public var registrationId: String
    /// The registration expiration timestamp
    public var expiresAt: Date

    public init(registrationId: String, expiresAt: Date) {
        self.registrationId = registrationId
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case registrationId = "registrationId"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.registrationId = try container.decode(String.self, forKey: .registrationId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.registrationId, forKey: .registrationId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

public struct AuthenticationRegistrationInput: Codable, Equatable, Sendable {
    /// User's email address
    public var email: String
    /// optionally set a password for the user
    public var password: String?

    public init(email: String, password: String? = nil) {
        self.email = email
        self.password = password
    }

    private enum CodingKeys: String, CodingKey {
        case email = "email"
        case password = "password"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.email = try container.decode(String.self, forKey: .email)
        self.password = try container.decodeIfPresent(String.self, forKey: .password)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.email, forKey: .email)
        try container.encodeIfPresent(self.password, forKey: .password)
    }
}

public struct AuthenticationRegistrationVerification: Codable, Equatable, Sendable {
    /// The registration identifier
    public var registrationId: String
    /// The verification code
    public var code: String

    public init(registrationId: String, code: String) {
        self.registrationId = registrationId
        self.code = code
    }

    private enum CodingKeys: String, CodingKey {
        case registrationId = "registrationId"
        case code = "code"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.registrationId = try container.decode(String.self, forKey: .registrationId)
        self.code = try container.decode(String.self, forKey: .code)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.registrationId, forKey: .registrationId)
        try container.encode(self.code, forKey: .code)
    }
}

/// A credential the relying party expects the user to be able to present
public struct PublicKeyCredentialDescriptor: Codable, Equatable, Sendable {
    /// The credential type — currently always `public-key`
    public var type: String
    /// The base64url-encoded credential identifier
    public var id: String
    /// Transports the authenticator advertises
    public var transports: [PublicKeyCredentialDescriptorTransports]?

    public init(type: String = "public-key", id: String, transports: [PublicKeyCredentialDescriptorTransports]? = nil) {
        self.type = type
        self.id = id
        self.transports = transports
    }

    private enum CodingKeys: String, CodingKey {
        case type = "type"
        case id = "id"
        case transports = "transports"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.type = try container.decode(String.self, forKey: .type)
        guard self.type == "public-key" else {
            throw DecodingError.dataCorruptedError(forKey: .type, in: container, debugDescription: "Expected \"public-key\"")
        }
        self.id = try container.decode(String.self, forKey: .id)
        self.transports = try container.decodeIfPresent([PublicKeyCredentialDescriptorTransports].self, forKey: .transports)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.type, forKey: .type)
        try container.encode(self.id, forKey: .id)
        try container.encodeIfPresent(self.transports, forKey: .transports)
    }
}

/// The transport used by the authenticator
public enum FidoAuthenticatorTransport: String, Codable, CaseIterable, Sendable {
    case hybrid = "hybrid"
    case ble = "ble"
    case `internal` = "internal"
    case nfc = "nfc"
    case usb = "usb"
}

/// Response from `/auth/login/oidc/start` instructing the client to navigate to `authorize_url`
public struct OidcLoginStartResponse: Codable, Equatable, Sendable {
    /// Fully-formed authorize URL the user-agent should be redirected to
    public var authorizeUrl: String
    /// Opaque state token bound to this authorization round-trip
    public var state: String
    /// When the cached state record expires
    public var expiresAt: Date

    public init(authorizeUrl: String, state: String, expiresAt: Date) {
        self.authorizeUrl = authorizeUrl
        self.state = state
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case authorizeUrl = "authorize_url"
        case state = "state"
        case expiresAt = "expires_at"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.authorizeUrl = try container.decode(String.self, forKey: .authorizeUrl)
        self.state = try container.decode(String.self, forKey: .state)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.authorizeUrl, forKey: .authorizeUrl)
        try container.encode(self.state, forKey: .state)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

/// Request to complete an OIDC sign-in flow
public struct OidcLoginCallback: Codable, Equatable, Sendable {
    /// The issuer of the token
    public var iss: String?
    /// The scope of the token
    public var scope: String?
    /// The user ID
    public var authuser: String?
    /// The host domain
    public var hd: String?
    /// The prompt of the token
    public var prompt: String?
    /// The authorization code returned by the IdP (absent when the IdP rejected the request)
    public var code: String?
    /// The opaque state token bound to the original authorize request
    public var state: String?
    /// OAuth 2.0 error code per RFC 6749 §4.1.2.1 (e.g. access_denied)
    public var error: String?
    /// Human-readable explanation of `error`
    public var errorDescription: String?
    /// URL to a page describing `error`
    public var errorUri: String?

    public init(iss: String? = nil, scope: String? = nil, authuser: String? = nil, hd: String? = nil, prompt: String? = nil, code: String? = nil, state: String? = nil, error: String? = nil, errorDescription: String? = nil, errorUri: String? = nil) {
        self.iss = iss
        self.scope = scope
        self.authuser = authuser
        self.hd = hd
        self.prompt = prompt
        self.code = code
        self.state = state
        self.error = error
        self.errorDescription = errorDescription
        self.errorUri = errorUri
    }

    private enum CodingKeys: String, CodingKey {
        case iss = "iss"
        case scope = "scope"
        case authuser = "authuser"
        case hd = "hd"
        case prompt = "prompt"
        case code = "code"
        case state = "state"
        case error = "error"
        case errorDescription = "error_description"
        case errorUri = "error_uri"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.iss = try container.decodeIfPresent(String.self, forKey: .iss)
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.authuser = try container.decodeIfPresent(String.self, forKey: .authuser)
        self.hd = try container.decodeIfPresent(String.self, forKey: .hd)
        self.prompt = try container.decodeIfPresent(String.self, forKey: .prompt)
        self.code = try container.decodeIfPresent(String.self, forKey: .code)
        self.state = try container.decodeIfPresent(String.self, forKey: .state)
        self.error = try container.decodeIfPresent(String.self, forKey: .error)
        self.errorDescription = try container.decodeIfPresent(String.self, forKey: .errorDescription)
        self.errorUri = try container.decodeIfPresent(String.self, forKey: .errorUri)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.iss, forKey: .iss)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.authuser, forKey: .authuser)
        try container.encodeIfPresent(self.hd, forKey: .hd)
        try container.encodeIfPresent(self.prompt, forKey: .prompt)
        try container.encodeIfPresent(self.code, forKey: .code)
        try container.encodeIfPresent(self.state, forKey: .state)
        try container.encodeIfPresent(self.error, forKey: .error)
        try container.encodeIfPresent(self.errorDescription, forKey: .errorDescription)
        try container.encodeIfPresent(self.errorUri, forKey: .errorUri)
    }
}

/// Issue a phone SMS challenge during a pending MFA round
public struct FactorChallengePhoneStart: Codable, Equatable, Sendable {
    /// Discriminator
    public var method: String
    /// Delivery channel — only `sms` is supported in this phase
    public var transport: String
    /// The MFA challenge to which this factor challenge is bound
    public var mfaChallengeId: String

    public init(method: String = "phone", transport: String = "sms", mfaChallengeId: String) {
        self.method = method
        self.transport = transport
        self.mfaChallengeId = mfaChallengeId
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case transport = "transport"
        case mfaChallengeId = "mfa_challenge_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(String.self, forKey: .method)
        guard self.method == "phone" else {
            throw DecodingError.dataCorruptedError(forKey: .method, in: container, debugDescription: "Expected \"phone\"")
        }
        self.transport = try container.decode(String.self, forKey: .transport)
        guard self.transport == "sms" else {
            throw DecodingError.dataCorruptedError(forKey: .transport, in: container, debugDescription: "Expected \"sms\"")
        }
        self.mfaChallengeId = try container.decode(String.self, forKey: .mfaChallengeId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.transport, forKey: .transport)
        try container.encode(self.mfaChallengeId, forKey: .mfaChallengeId)
    }
}

/// Issue a WebAuthn assertion challenge during a pending MFA round
public struct FactorChallengeFidoStart: Codable, Equatable, Sendable {
    /// Discriminator
    public var method: String
    /// The MFA challenge to which this factor challenge is bound
    public var mfaChallengeId: String

    public init(method: String = "fido", mfaChallengeId: String) {
        self.method = method
        self.mfaChallengeId = mfaChallengeId
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case mfaChallengeId = "mfa_challenge_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(String.self, forKey: .method)
        guard self.method == "fido" else {
            throw DecodingError.dataCorruptedError(forKey: .method, in: container, debugDescription: "Expected \"fido\"")
        }
        self.mfaChallengeId = try container.decode(String.self, forKey: .mfaChallengeId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.mfaChallengeId, forKey: .mfaChallengeId)
    }
}

/// Issue an email one-time-code challenge during a pending MFA round. Always a code: a magic link cannot complete an MFA round, since the `code` grant that redeems one takes `code(min=6, max=10)` and a link token is 43 characters
public struct FactorChallengeEmailStart: Codable, Equatable, Sendable {
    /// Discriminator
    public var method: String
    /// The MFA challenge to which this factor challenge is bound
    public var mfaChallengeId: String

    public init(method: String = "email", mfaChallengeId: String) {
        self.method = method
        self.mfaChallengeId = mfaChallengeId
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case mfaChallengeId = "mfa_challenge_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(String.self, forKey: .method)
        guard self.method == "email" else {
            throw DecodingError.dataCorruptedError(forKey: .method, in: container, debugDescription: "Expected \"email\"")
        }
        self.mfaChallengeId = try container.decode(String.self, forKey: .mfaChallengeId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.mfaChallengeId, forKey: .mfaChallengeId)
    }
}

/// Response for a phone SMS challenge
public struct FactorChallengePhoneStartResponse: Codable, Equatable, Sendable {
    /// Discriminator
    public var method: String
    /// Echo of the chosen delivery channel
    public var transport: String
    /// The phone-factor challenge id — echo back on the `code` grant as `challenge_id`
    public var phoneChallengeId: String
    /// When the phone challenge expires
    public var expiresAt: Date

    public init(method: String = "phone", transport: String = "sms", phoneChallengeId: String, expiresAt: Date) {
        self.method = method
        self.transport = transport
        self.phoneChallengeId = phoneChallengeId
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case transport = "transport"
        case phoneChallengeId = "phone_challenge_id"
        case expiresAt = "expires_at"
    }

    private enum EncodingKeys: String, CodingKey {
        case method = "method"
        case transport = "transport"
        case phoneChallengeId = "phoneChallengeId"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(String.self, forKey: .method)
        guard self.method == "phone" else {
            throw DecodingError.dataCorruptedError(forKey: .method, in: container, debugDescription: "Expected \"phone\"")
        }
        self.transport = try container.decode(String.self, forKey: .transport)
        guard self.transport == "sms" else {
            throw DecodingError.dataCorruptedError(forKey: .transport, in: container, debugDescription: "Expected \"sms\"")
        }
        self.phoneChallengeId = try container.decode(String.self, forKey: .phoneChallengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.transport, forKey: .transport)
        try container.encode(self.phoneChallengeId, forKey: .phoneChallengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

/// Response for an email one-time-code challenge
public struct FactorChallengeEmailStartResponse: Codable, Equatable, Sendable {
    /// Discriminator
    public var method: String
    /// The email-factor challenge id — echo back on the `code` grant as `challenge_id`
    public var emailChallengeId: String
    /// When the email challenge expires
    public var expiresAt: Date

    public init(method: String = "email", emailChallengeId: String, expiresAt: Date) {
        self.method = method
        self.emailChallengeId = emailChallengeId
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case emailChallengeId = "email_challenge_id"
        case expiresAt = "expires_at"
    }

    private enum EncodingKeys: String, CodingKey {
        case method = "method"
        case emailChallengeId = "emailChallengeId"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(String.self, forKey: .method)
        guard self.method == "email" else {
            throw DecodingError.dataCorruptedError(forKey: .method, in: container, debugDescription: "Expected \"email\"")
        }
        self.emailChallengeId = try container.decode(String.self, forKey: .emailChallengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.emailChallengeId, forKey: .emailChallengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

/// A factor satisfied by the session
public struct SessionFactor: Codable, Equatable, Sendable {
    /// The verification method
    public var method: SessionFactorMethod
    /// Stable identifier for the specific factor record
    public var methodId: String
    /// MFA category for the factor
    public var kind: SessionFactorKind
    /// When this factor entry was first added to the session
    public var issuedAt: Date
    /// When the factor was most recently re-verified
    public var authenticatedAt: Date

    public init(method: SessionFactorMethod, methodId: String, kind: SessionFactorKind, issuedAt: Date, authenticatedAt: Date) {
        self.method = method
        self.methodId = methodId
        self.kind = kind
        self.issuedAt = issuedAt
        self.authenticatedAt = authenticatedAt
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case methodId = "methodId"
        case kind = "kind"
        case issuedAt = "issuedAt"
        case authenticatedAt = "authenticatedAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(SessionFactorMethod.self, forKey: .method)
        self.methodId = try container.decode(String.self, forKey: .methodId)
        self.kind = try container.decode(SessionFactorKind.self, forKey: .kind)
        self.issuedAt = try container.decode(Date.self, forKey: .issuedAt)
        self.authenticatedAt = try container.decode(Date.self, forKey: .authenticatedAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.methodId, forKey: .methodId)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.issuedAt, forKey: .issuedAt)
        try container.encode(self.authenticatedAt, forKey: .authenticatedAt)
    }
}

/// A factor satisfied by the session
public struct SessionFactorInput: Codable, Equatable, Sendable {
    /// The verification method
    public var method: SessionFactorMethod
    /// Stable identifier for the specific factor record
    public var methodId: String
    /// MFA category for the factor
    public var kind: SessionFactorKind

    public init(method: SessionFactorMethod, methodId: String, kind: SessionFactorKind) {
        self.method = method
        self.methodId = methodId
        self.kind = kind
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case methodId = "methodId"
        case kind = "kind"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(SessionFactorMethod.self, forKey: .method)
        self.methodId = try container.decode(String.self, forKey: .methodId)
        self.kind = try container.decode(SessionFactorKind.self, forKey: .kind)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.methodId, forKey: .methodId)
        try container.encode(self.kind, forKey: .kind)
    }
}

/// Optional metadata supplied to a revoke action
public struct SessionRevoke: Codable, Equatable, Sendable {
    /// Free-form reason recorded with the revoke
    public var reason: String?

    public init(reason: String? = nil) {
        self.reason = reason
    }

    private enum CodingKeys: String, CodingKey {
        case reason = "reason"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.reason = try container.decodeIfPresent(String.self, forKey: .reason)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.reason, forKey: .reason)
    }
}

/// A platform-wide role held on `platform:main`. `admin` grants every operation; `listener` grants the reads
public enum PlatformRole: String, Codable, CaseIterable, Sendable {
    case admin = "admin"
    case listener = "listener"
}

/// A successful authentication record
public struct Login: Codable, Equatable, Sendable {
    /// The login event identifier
    public var id: BigIntValue
    /// The actor that authenticated
    public var actorId: UUID
    /// The factor that satisfied the primary authentication
    public var factorType: LoginFactorType
    /// The specific factor record id, when available
    public var factorId: UUID?
    /// The session minted at this login, when available
    public var sessionToken: String?
    /// Whether MFA was required and satisfied at login
    public var mfaSatisfied: Bool
    /// IP address recorded at login
    public var ip: String?
    /// User agent recorded at login
    public var userAgent: String?
    /// When the login occurred
    public var occurredAt: Date

    public init(id: BigIntValue, actorId: UUID, factorType: LoginFactorType, factorId: UUID? = nil, sessionToken: String? = nil, mfaSatisfied: Bool, ip: String? = nil, userAgent: String? = nil, occurredAt: Date) {
        self.id = id
        self.actorId = actorId
        self.factorType = factorType
        self.factorId = factorId
        self.sessionToken = sessionToken
        self.mfaSatisfied = mfaSatisfied
        self.ip = ip
        self.userAgent = userAgent
        self.occurredAt = occurredAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case actorId = "actorId"
        case factorType = "factorType"
        case factorId = "factorId"
        case sessionToken = "sessionToken"
        case mfaSatisfied = "mfaSatisfied"
        case ip = "ip"
        case userAgent = "userAgent"
        case occurredAt = "occurredAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(BigIntValue.self, forKey: .id)
        self.actorId = try container.decode(UUID.self, forKey: .actorId)
        self.factorType = try container.decode(LoginFactorType.self, forKey: .factorType)
        self.factorId = try container.decodeIfPresent(UUID.self, forKey: .factorId)
        self.sessionToken = try container.decodeIfPresent(String.self, forKey: .sessionToken)
        self.mfaSatisfied = try container.decode(Bool.self, forKey: .mfaSatisfied)
        self.ip = try container.decodeIfPresent(String.self, forKey: .ip)
        self.userAgent = try container.decodeIfPresent(String.self, forKey: .userAgent)
        self.occurredAt = try container.decode(Date.self, forKey: .occurredAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.actorId, forKey: .actorId)
        try container.encode(self.factorType, forKey: .factorType)
        try container.encodeIfPresent(self.factorId, forKey: .factorId)
        try container.encodeIfPresent(self.sessionToken, forKey: .sessionToken)
        try container.encode(self.mfaSatisfied, forKey: .mfaSatisfied)
        try container.encodeIfPresent(self.ip, forKey: .ip)
        try container.encodeIfPresent(self.userAgent, forKey: .userAgent)
        try container.encode(self.occurredAt, forKey: .occurredAt)
    }
}

/// A successful authentication record
public struct LoginInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// The current user's display preferences, auto-detected by the SPA from the browser (Intl timezone + navigator.language). Omitted fields are left unchanged (absent = never set).
public struct ActorPreferences: Codable, Equatable, Sendable {
    /// RFC 5646 locale, e.g. "en-US"
    public var locale: String?
    /// Olson timezone, e.g. "America/New_York"
    public var timezone: String?

    public init(locale: String? = nil, timezone: String? = nil) {
        self.locale = locale
        self.timezone = timezone
    }

    private enum CodingKeys: String, CodingKey {
        case locale = "locale"
        case timezone = "timezone"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.locale = try container.decodeIfPresent(String.self, forKey: .locale)
        self.timezone = try container.decodeIfPresent(String.self, forKey: .timezone)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.locale, forKey: .locale)
        try container.encodeIfPresent(self.timezone, forKey: .timezone)
    }
}

public struct BaseAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: AuthenticationGrantType
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?

    public init(grantType: AuthenticationGrantType, scope: String? = nil, clientId: UUID? = nil) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(AuthenticationGrantType.self, forKey: .grantType)
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
    }
}

public struct BaseAuthenticationLoginStart: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: PasswordlessAuthenticationGrantType
    /// The application's client identifier, if available
    public var clientId: UUID?

    public init(grantType: PasswordlessAuthenticationGrantType, clientId: UUID? = nil) {
        self.grantType = grantType
        self.clientId = clientId
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case clientId = "client_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(PasswordlessAuthenticationGrantType.self, forKey: .grantType)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
    }
}

public struct BaseAuthenticationLoginStartResponse: Codable, Equatable, Sendable {
    /// The grant type for the response
    public var grantType: PasswordlessAuthenticationGrantType
    /// The challenge identifier
    public var challengeId: String
    /// The challenge expiration timestamp
    public var expiresAt: Date

    public init(grantType: PasswordlessAuthenticationGrantType, challengeId: String, expiresAt: Date) {
        self.grantType = grantType
        self.challengeId = challengeId
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case challengeId = "challengeId"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(PasswordlessAuthenticationGrantType.self, forKey: .grantType)
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

/// A factor the SPA may use to satisfy the MFA challenge
public struct MfaChallengeFactor: Codable, Equatable, Sendable {
    /// The factor method
    public var method: AuthenticationFactorMethod
    /// The id of the enrolled factor (opaque to the SPA, must be echoed back in the proof for methods that don't bind another way)
    public var methodId: String
    /// The factor kind (knowledge, possession, biometric) — the SPA filters against step-up `acceptableKinds`/`excludeKinds` hints
    public var kind: AuthenticationFactorKind
    /// Optional human-readable label (e.g. provider name for OIDC, friendly name for FIDO)
    public var label: String?

    public init(method: AuthenticationFactorMethod, methodId: String, kind: AuthenticationFactorKind, label: String? = nil) {
        self.method = method
        self.methodId = methodId
        self.kind = kind
        self.label = label
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case methodId = "method_id"
        case kind = "kind"
        case label = "label"
    }

    private enum EncodingKeys: String, CodingKey {
        case method = "method"
        case methodId = "methodId"
        case kind = "kind"
        case label = "label"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(AuthenticationFactorMethod.self, forKey: .method)
        self.methodId = try container.decode(String.self, forKey: .methodId)
        self.kind = try container.decode(AuthenticationFactorKind.self, forKey: .kind)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.methodId, forKey: .methodId)
        try container.encode(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.label, forKey: .label)
    }
}

public struct AuthenticationFactor: Codable, Equatable, Sendable {
    /// The method of the factor
    public var method: AuthenticationFactorMethod
    /// The kind of the factor
    public var kind: AuthenticationFactorKind
    /// The method identifier
    public var methodId: String
    /// The label for the factor
    public var label: String?

    public init(method: AuthenticationFactorMethod, kind: AuthenticationFactorKind, methodId: String, label: String? = nil) {
        self.method = method
        self.kind = kind
        self.methodId = methodId
        self.label = label
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case kind = "kind"
        case methodId = "methodId"
        case label = "label"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(AuthenticationFactorMethod.self, forKey: .method)
        self.kind = try container.decode(AuthenticationFactorKind.self, forKey: .kind)
        self.methodId = try container.decode(String.self, forKey: .methodId)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.methodId, forKey: .methodId)
        try container.encodeIfPresent(self.label, forKey: .label)
    }
}

/// Mint a fresh MFA challenge for the current session so the SPA can satisfy a `step_up_required` denial. Filters mirror `StepUpRequirement` from `@maroonedsoftware/policies`.
public struct StepUpStartRequest: Codable, Equatable, Sendable {
    /// If set, only these factor methods are listed as eligible
    public var acceptableMethods: [AuthenticationFactorMethod]?
    /// If set, only these factor kinds are listed as eligible
    public var acceptableKinds: [AuthenticationFactorKind]?
    /// If set, factors with these methods are never listed
    public var excludeMethods: [AuthenticationFactorMethod]?

    public init(acceptableMethods: [AuthenticationFactorMethod]? = nil, acceptableKinds: [AuthenticationFactorKind]? = nil, excludeMethods: [AuthenticationFactorMethod]? = nil) {
        self.acceptableMethods = acceptableMethods
        self.acceptableKinds = acceptableKinds
        self.excludeMethods = excludeMethods
    }

    private enum CodingKeys: String, CodingKey {
        case acceptableMethods = "acceptableMethods"
        case acceptableKinds = "acceptableKinds"
        case excludeMethods = "excludeMethods"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.acceptableMethods = try container.decodeIfPresent([AuthenticationFactorMethod].self, forKey: .acceptableMethods)
        self.acceptableKinds = try container.decodeIfPresent([AuthenticationFactorKind].self, forKey: .acceptableKinds)
        self.excludeMethods = try container.decodeIfPresent([AuthenticationFactorMethod].self, forKey: .excludeMethods)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.acceptableMethods, forKey: .acceptableMethods)
        try container.encodeIfPresent(self.acceptableKinds, forKey: .acceptableKinds)
        try container.encodeIfPresent(self.excludeMethods, forKey: .excludeMethods)
    }
}

/// Request to begin an OIDC sign-in flow
public struct OidcLoginStart: Codable, Equatable, Sendable {
    /// The IdP to authorize against
    public var provider: OidcProvider
    /// Optional URL the SPA wants the callback to land on after token issuance
    public var redirectAfter: String?

    public init(provider: OidcProvider, redirectAfter: String? = nil) {
        self.provider = provider
        self.redirectAfter = redirectAfter
    }

    private enum CodingKeys: String, CodingKey {
        case provider = "provider"
        case redirectAfter = "redirect_after"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.provider = try container.decode(OidcProvider.self, forKey: .provider)
        self.redirectAfter = try container.decodeIfPresent(String.self, forKey: .redirectAfter)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.provider, forKey: .provider)
        try container.encodeIfPresent(self.redirectAfter, forKey: .redirectAfter)
    }
}

public struct PublicKeyCredentialWithAssertion: Codable, Equatable, Sendable {
    /// The base64url encoding of `rawId`
    public var id: String
    /// This enumeration defines the valid credential types. It is an extension point; values can be added to it in the future, as more credential types are defined. The values of this enumeration are used for versioning the Authentication Assertion and attestation structures according to the type of the authenticator. Currently one credential type is defined, namely `public-key`.
    public var type: String
    /// The credential identifier
    public var rawId: String
    /// The authenticator attachment
    public var authenticatorAttachment: PublicKeyCredentialAuthenticatorAttachment?
    /// The client extension results
    public var clientExtensionResults: SimpleClientExtensionResults
    /// The authenticator assertion response
    public var response: FidoAuthenticatorAssertionResponse

    public init(id: String, type: String = "public-key", rawId: String, authenticatorAttachment: PublicKeyCredentialAuthenticatorAttachment? = nil, clientExtensionResults: SimpleClientExtensionResults, response: FidoAuthenticatorAssertionResponse) {
        self.id = id
        self.type = type
        self.rawId = rawId
        self.authenticatorAttachment = authenticatorAttachment
        self.clientExtensionResults = clientExtensionResults
        self.response = response
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case type = "type"
        case rawId = "rawId"
        case authenticatorAttachment = "authenticatorAttachment"
        case clientExtensionResults = "clientExtensionResults"
        case response = "response"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.type = try container.decode(String.self, forKey: .type)
        guard self.type == "public-key" else {
            throw DecodingError.dataCorruptedError(forKey: .type, in: container, debugDescription: "Expected \"public-key\"")
        }
        self.rawId = try container.decode(String.self, forKey: .rawId)
        self.authenticatorAttachment = try container.decodeIfPresent(PublicKeyCredentialAuthenticatorAttachment.self, forKey: .authenticatorAttachment)
        self.clientExtensionResults = try container.decode(SimpleClientExtensionResults.self, forKey: .clientExtensionResults)
        self.response = try container.decode(FidoAuthenticatorAssertionResponse.self, forKey: .response)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.type, forKey: .type)
        try container.encode(self.rawId, forKey: .rawId)
        try container.encodeIfPresent(self.authenticatorAttachment, forKey: .authenticatorAttachment)
        try container.encode(self.clientExtensionResults, forKey: .clientExtensionResults)
        try container.encode(self.response, forKey: .response)
    }
}

public struct FidoPublicKeyCredentialRequestOptions: Codable, Equatable, Sendable {
    public var challenge: String
    /// WebAuthn timeout hint in milliseconds
    public var timeout: Int?
    public var rpId: String?
    /// The attestation
    public var attestation: FidoPublicKeyCredentialRequestOptionsAttestation?
    /// Whether the authenticator must verify the user
    public var userVerification: FidoPublicKeyCredentialRequestOptionsUserVerification?
    /// The raw challenge
    public var rawChallenge: Data?
    public var extensions: JSONValue?
    public var allowCredentials: [PublicKeyCredentialDescriptor]?

    public init(challenge: String, timeout: Int? = nil, rpId: String? = nil, attestation: FidoPublicKeyCredentialRequestOptionsAttestation? = nil, userVerification: FidoPublicKeyCredentialRequestOptionsUserVerification? = nil, rawChallenge: Data? = nil, extensions: JSONValue? = nil, allowCredentials: [PublicKeyCredentialDescriptor]? = nil) {
        self.challenge = challenge
        self.timeout = timeout
        self.rpId = rpId
        self.attestation = attestation
        self.userVerification = userVerification
        self.rawChallenge = rawChallenge
        self.extensions = extensions
        self.allowCredentials = allowCredentials
    }

    private enum CodingKeys: String, CodingKey {
        case challenge = "challenge"
        case timeout = "timeout"
        case rpId = "rpId"
        case attestation = "attestation"
        case userVerification = "userVerification"
        case rawChallenge = "rawChallenge"
        case extensions = "extensions"
        case allowCredentials = "allowCredentials"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.challenge = try container.decode(String.self, forKey: .challenge)
        self.timeout = try container.decodeIfPresent(Int.self, forKey: .timeout)
        self.rpId = try container.decodeIfPresent(String.self, forKey: .rpId)
        self.attestation = try container.decodeIfPresent(FidoPublicKeyCredentialRequestOptionsAttestation.self, forKey: .attestation)
        self.userVerification = try container.decodeIfPresent(FidoPublicKeyCredentialRequestOptionsUserVerification.self, forKey: .userVerification)
        self.rawChallenge = try container.decodeIfPresent(Data.self, forKey: .rawChallenge)
        self.extensions = try container.decodeIfPresent(JSONValue.self, forKey: .extensions)
        self.allowCredentials = try container.decodeIfPresent([PublicKeyCredentialDescriptor].self, forKey: .allowCredentials)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.challenge, forKey: .challenge)
        try container.encodeIfPresent(self.timeout, forKey: .timeout)
        try container.encodeIfPresent(self.rpId, forKey: .rpId)
        try container.encodeIfPresent(self.attestation, forKey: .attestation)
        try container.encodeIfPresent(self.userVerification, forKey: .userVerification)
        try container.encodeIfPresent(self.rawChallenge, forKey: .rawChallenge)
        try container.encodeIfPresent(self.extensions, forKey: .extensions)
        try container.encodeIfPresent(self.allowCredentials, forKey: .allowCredentials)
    }
}

/// Serialized form of `AuthenticatorAttestationResponse` — produced by the browser at registration; all binary fields are base64-encoded for transport
public struct FidoAuthenticatorAttestationResponse: Codable, Equatable, Sendable {
    /// The client data JSON
    public var clientDataJson: String
    /// The attestation object
    public var attestationObject: String
    /// The transports used by the authenticator
    public var transports: [FidoAuthenticatorTransport]?

    public init(clientDataJson: String, attestationObject: String, transports: [FidoAuthenticatorTransport]? = nil) {
        self.clientDataJson = clientDataJson
        self.attestationObject = attestationObject
        self.transports = transports
    }

    private enum CodingKeys: String, CodingKey {
        case clientDataJson = "clientDataJSON"
        case attestationObject = "attestationObject"
        case transports = "transports"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.clientDataJson = try container.decode(String.self, forKey: .clientDataJson)
        self.attestationObject = try container.decode(String.self, forKey: .attestationObject)
        self.transports = try container.decodeIfPresent([FidoAuthenticatorTransport].self, forKey: .transports)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.clientDataJson, forKey: .clientDataJson)
        try container.encode(self.attestationObject, forKey: .attestationObject)
        try container.encodeIfPresent(self.transports, forKey: .transports)
    }
}

/// An active authentication session
public struct Session: Codable, Equatable, Sendable {
    /// Opaque session token used as the cache key and embedded in JWTs
    public var sessionToken: String
    /// The actor that owns this session
    public var actorId: UUID
    /// When the session was originally issued
    public var issuedAt: Date
    /// When the session expires
    public var expiresAt: Date
    /// When the session was last accessed
    public var lastAccessedAt: Date
    /// Factors that have been satisfied in this session
    public var factors: [SessionFactor]
    /// IP address recorded when the session was created
    public var ip: String?
    /// User agent recorded when the session was created
    public var userAgent: String?
    /// True when this session matches the requesting session
    public var isCurrent: Bool

    public init(sessionToken: String, actorId: UUID, issuedAt: Date, expiresAt: Date, lastAccessedAt: Date, factors: [SessionFactor], ip: String? = nil, userAgent: String? = nil, isCurrent: Bool) {
        self.sessionToken = sessionToken
        self.actorId = actorId
        self.issuedAt = issuedAt
        self.expiresAt = expiresAt
        self.lastAccessedAt = lastAccessedAt
        self.factors = factors
        self.ip = ip
        self.userAgent = userAgent
        self.isCurrent = isCurrent
    }

    private enum CodingKeys: String, CodingKey {
        case sessionToken = "sessionToken"
        case actorId = "actorId"
        case issuedAt = "issuedAt"
        case expiresAt = "expiresAt"
        case lastAccessedAt = "lastAccessedAt"
        case factors = "factors"
        case ip = "ip"
        case userAgent = "userAgent"
        case isCurrent = "isCurrent"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.sessionToken = try container.decode(String.self, forKey: .sessionToken)
        self.actorId = try container.decode(UUID.self, forKey: .actorId)
        self.issuedAt = try container.decode(Date.self, forKey: .issuedAt)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
        self.lastAccessedAt = try container.decode(Date.self, forKey: .lastAccessedAt)
        self.factors = try container.decode([SessionFactor].self, forKey: .factors)
        self.ip = try container.decodeIfPresent(String.self, forKey: .ip)
        self.userAgent = try container.decodeIfPresent(String.self, forKey: .userAgent)
        self.isCurrent = try container.decode(Bool.self, forKey: .isCurrent)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.sessionToken, forKey: .sessionToken)
        try container.encode(self.actorId, forKey: .actorId)
        try container.encode(self.issuedAt, forKey: .issuedAt)
        try container.encode(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.lastAccessedAt, forKey: .lastAccessedAt)
        try container.encode(self.factors, forKey: .factors)
        try container.encodeIfPresent(self.ip, forKey: .ip)
        try container.encodeIfPresent(self.userAgent, forKey: .userAgent)
        try container.encode(self.isCurrent, forKey: .isCurrent)
    }
}

/// An active authentication session
public struct SessionInput: Codable, Equatable, Sendable {
    public init() {}

    public init(from decoder: Decoder) throws {
        _ = try decoder.container(keyedBy: DynamicCodingKey.self)
    }

    public func encode(to encoder: Encoder) throws {
        _ = encoder.container(keyedBy: DynamicCodingKey.self)
    }
}

/// Who the caller is, as the station sees them
public struct AuthSession: Codable, Equatable, Sendable {
    /// The actor the session belongs to
    public var actorId: String
    /// Every platform role the caller holds, sorted. Empty for an account nobody has granted one, which today is any account that did not come in through onboarding
    public var roles: [PlatformRole]

    public init(actorId: String, roles: [PlatformRole]) {
        self.actorId = actorId
        self.roles = roles
    }

    private enum CodingKeys: String, CodingKey {
        case actorId = "actorId"
        case roles = "roles"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.actorId = try container.decode(String.self, forKey: .actorId)
        self.roles = try container.decode([PlatformRole].self, forKey: .roles)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.actorId, forKey: .actorId)
        try container.encode(self.roles, forKey: .roles)
    }
}

/// Represents an application authentication request
public struct ClientCredentialsAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The client identifier
    public var clientId: UUID
    /// The client secret
    public var clientSecret: String

    public init(grantType: String = "client_credentials", scope: String? = nil, clientId: UUID, clientSecret: String) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.clientSecret = clientSecret
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case clientSecret = "client_secret"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "client_credentials" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"client_credentials\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decode(UUID.self, forKey: .clientId)
        self.clientSecret = try container.decode(String.self, forKey: .clientSecret)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encode(self.clientId, forKey: .clientId)
        try container.encode(self.clientSecret, forKey: .clientSecret)
    }
}

/// Represents an authentication password request
public struct PasswordAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// User's identifier, usually an email address
    public var username: String
    /// User's password
    public var password: String

    public init(grantType: String = "password", scope: String? = nil, clientId: UUID? = nil, username: String, password: String) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.username = username
        self.password = password
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case username = "username"
        case password = "password"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "password" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"password\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.username = try container.decode(String.self, forKey: .username)
        self.password = try container.decode(String.self, forKey: .password)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.username, forKey: .username)
        try container.encode(self.password, forKey: .password)
    }
}

/// Represents an authentication refresh request
public struct RefreshTokenAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// The refresh token issued by the authorization server. Optional: browser clients omit it and present the httpOnly refresh cookie instead
    public var refreshToken: String?

    public init(grantType: String = "refresh_token", scope: String? = nil, clientId: UUID? = nil, refreshToken: String? = nil) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.refreshToken = refreshToken
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case refreshToken = "refresh_token"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "refresh_token" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"refresh_token\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.refreshToken = try container.decodeIfPresent(String.self, forKey: .refreshToken)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encodeIfPresent(self.refreshToken, forKey: .refreshToken)
    }
}

/// Represents an authentication magic link request
public struct LinkAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// The email challenge id returned by `POST /auth/login/start` — binds the link to the issued challenge so cross-device clicks work
    public var challengeId: String
    /// The magic link token
    public var link: String

    public init(grantType: String = "link", scope: String? = nil, clientId: UUID? = nil, challengeId: String, link: String) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.challengeId = challengeId
        self.link = link
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case challengeId = "challenge_id"
        case link = "link"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "link" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"link\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.link = try container.decode(String.self, forKey: .link)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.link, forKey: .link)
    }
}

/// Represents an authentication one-time-code request
public struct CodeAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// The one-time code
    public var code: String
    /// PKCE verifier — required for a primary code login; absent when mfa_challenge_id is set
    public var codeVerifier: String?
    /// When set, completes a pending MFA challenge; replaces code_verifier as proof-of-origin
    public var mfaChallengeId: String?
    /// The phone/email challenge id (returned by POST /auth/factors/start for phone-MFA). Required when mfa_challenge_id is set; ignored otherwise (resolved via PKCE)
    public var challengeId: String?

    public init(grantType: String = "code", scope: String? = nil, clientId: UUID? = nil, code: String, codeVerifier: String? = nil, mfaChallengeId: String? = nil, challengeId: String? = nil) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.code = code
        self.codeVerifier = codeVerifier
        self.mfaChallengeId = mfaChallengeId
        self.challengeId = challengeId
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case code = "code"
        case codeVerifier = "code_verifier"
        case mfaChallengeId = "mfa_challenge_id"
        case challengeId = "challenge_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "code" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"code\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.code = try container.decode(String.self, forKey: .code)
        self.codeVerifier = try container.decodeIfPresent(String.self, forKey: .codeVerifier)
        self.mfaChallengeId = try container.decodeIfPresent(String.self, forKey: .mfaChallengeId)
        self.challengeId = try container.decodeIfPresent(String.self, forKey: .challengeId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.code, forKey: .code)
        try container.encodeIfPresent(self.codeVerifier, forKey: .codeVerifier)
        try container.encodeIfPresent(self.mfaChallengeId, forKey: .mfaChallengeId)
        try container.encodeIfPresent(self.challengeId, forKey: .challengeId)
    }
}

/// Submit a TOTP code as a second factor against a pending MFA challenge
public struct AuthenticatorAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// The TOTP code
    public var code: String
    /// The pending MFA challenge — required, TOTP has no other actor binding at initial login
    public var mfaChallengeId: String
    /// The id of the enrolled authenticator factor to verify against (must be present in the MFA challenge's eligible list)
    public var methodId: String

    public init(grantType: String = "authenticator", scope: String? = nil, clientId: UUID? = nil, code: String, mfaChallengeId: String, methodId: String) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.code = code
        self.mfaChallengeId = mfaChallengeId
        self.methodId = methodId
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case code = "code"
        case mfaChallengeId = "mfa_challenge_id"
        case methodId = "method_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "authenticator" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"authenticator\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.code = try container.decode(String.self, forKey: .code)
        self.mfaChallengeId = try container.decode(String.self, forKey: .mfaChallengeId)
        self.methodId = try container.decode(String.self, forKey: .methodId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.code, forKey: .code)
        try container.encode(self.mfaChallengeId, forKey: .mfaChallengeId)
        try container.encode(self.methodId, forKey: .methodId)
    }
}

/// Redeem a completed OIDC authorization that the callback stashed under a one-time id
public struct OidcAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// The one-time stash id from the OIDC callback redirect (the value after `?token=oidc:` on `/auth/callback`). Single-use — the API consumes it via `OidcFactorService.redeemAuthenticatedExchange`.
    public var challengeId: String

    public init(grantType: String = "oidc", scope: String? = nil, clientId: UUID? = nil, challengeId: String) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.challengeId = challengeId
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case challengeId = "challenge_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "oidc" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"oidc\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.challengeId, forKey: .challengeId)
    }
}

public struct BaseAuthenticationLoginStartWithEmail: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: PasswordlessAuthenticationGrantType
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// User's email address
    public var email: String

    public init(grantType: PasswordlessAuthenticationGrantType, clientId: UUID? = nil, email: String) {
        self.grantType = grantType
        self.clientId = clientId
        self.email = email
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case clientId = "client_id"
        case email = "email"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(PasswordlessAuthenticationGrantType.self, forKey: .grantType)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.email = try container.decode(String.self, forKey: .email)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.email, forKey: .email)
    }
}

/// Request to begin an OIDC sign-in flow
public struct OidcAuthenticationLoginStart: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// The IdP to authorize against
    public var provider: OidcProvider

    public init(grantType: String = "oidc", clientId: UUID? = nil, provider: OidcProvider) {
        self.grantType = grantType
        self.clientId = clientId
        self.provider = provider
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case clientId = "client_id"
        case provider = "provider"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "oidc" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"oidc\"")
        }
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.provider = try container.decode(OidcProvider.self, forKey: .provider)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.provider, forKey: .provider)
    }
}

public struct CodeAuthenticationLoginStartResponse: Codable, Equatable, Sendable {
    /// The grant type for the response
    public var grantType: String
    /// The challenge identifier
    public var challengeId: String
    /// The challenge expiration timestamp
    public var expiresAt: Date

    public init(grantType: String = "code", challengeId: String, expiresAt: Date) {
        self.grantType = grantType
        self.challengeId = challengeId
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case challengeId = "challengeId"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "code" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"code\"")
        }
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

public struct LinkAuthenticationLoginStartResponse: Codable, Equatable, Sendable {
    /// The grant type for the response
    public var grantType: String
    /// The challenge identifier
    public var challengeId: String
    /// The challenge expiration timestamp
    public var expiresAt: Date

    public init(grantType: String = "link", challengeId: String, expiresAt: Date) {
        self.grantType = grantType
        self.challengeId = challengeId
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case challengeId = "challengeId"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "link" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"link\"")
        }
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

/// Response from `/auth/login/oidc/start` instructing the client to navigate to `authorize_url`
public struct OidcAuthenticationLoginStartResponse: Codable, Equatable, Sendable {
    /// The grant type for the response
    public var grantType: String
    /// The challenge identifier
    public var challengeId: String
    /// The challenge expiration timestamp
    public var expiresAt: Date
    /// Fully-formed authorize URL the user-agent should be redirected to
    public var authorizeUrl: String
    /// Opaque state token bound to this authorization round-trip
    public var state: String

    public init(grantType: String = "oidc", challengeId: String, expiresAt: Date, authorizeUrl: String, state: String) {
        self.grantType = grantType
        self.challengeId = challengeId
        self.expiresAt = expiresAt
        self.authorizeUrl = authorizeUrl
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case challengeId = "challengeId"
        case expiresAt = "expiresAt"
        case authorizeUrl = "authorize_url"
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "oidc" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"oidc\"")
        }
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
        self.authorizeUrl = try container.decode(String.self, forKey: .authorizeUrl)
        self.state = try container.decode(String.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.authorizeUrl, forKey: .authorizeUrl)
        try container.encode(self.state, forKey: .state)
    }
}

/// MFA-required arm of /auth/token response
public struct MfaRequiredResponse: Codable, Equatable, Sendable {
    /// Discriminator
    public var result: String
    /// The MFA challenge identifier — pass back as `mfa_challenge_id` on the proof grant
    public var challengeId: String
    /// When the MFA challenge expires
    public var expiresAt: Date
    /// Eligible factors the SPA may use to complete the challenge
    public var factors: [MfaChallengeFactor]

    public init(result: String = "mfa_required", challengeId: String, expiresAt: Date, factors: [MfaChallengeFactor]) {
        self.result = result
        self.challengeId = challengeId
        self.expiresAt = expiresAt
        self.factors = factors
    }

    private enum CodingKeys: String, CodingKey {
        case result = "result"
        case challengeId = "challenge_id"
        case expiresAt = "expires_at"
        case factors = "factors"
    }

    private enum EncodingKeys: String, CodingKey {
        case result = "result"
        case challengeId = "challengeId"
        case expiresAt = "expiresAt"
        case factors = "factors"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.result = try container.decode(String.self, forKey: .result)
        guard self.result == "mfa_required" else {
            throw DecodingError.dataCorruptedError(forKey: .result, in: container, debugDescription: "Expected \"mfa_required\"")
        }
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
        self.factors = try container.decode([MfaChallengeFactor].self, forKey: .factors)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.result, forKey: .result)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.factors, forKey: .factors)
    }
}

/// Represents an authentication passkey request
public struct FidoAuthenticationRequest: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The scope of the request
    public var scope: String?
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// A passkey credential object
    public var credential: PublicKeyCredentialWithAssertion
    /// The FIDO assertion challenge id returned by `POST /auth/login/start` (primary) or `POST /auth/factors/start` (MFA second factor). Must be the per-challenge id, not the actor id.
    public var challengeId: String
    /// When set, completes a pending MFA challenge instead of issuing a single-factor session
    public var mfaChallengeId: String?

    public init(grantType: String = "fido", scope: String? = nil, clientId: UUID? = nil, credential: PublicKeyCredentialWithAssertion, challengeId: String, mfaChallengeId: String? = nil) {
        self.grantType = grantType
        self.scope = scope
        self.clientId = clientId
        self.credential = credential
        self.challengeId = challengeId
        self.mfaChallengeId = mfaChallengeId
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case scope = "scope"
        case clientId = "client_id"
        case credential = "credential"
        case challengeId = "challenge_id"
        case mfaChallengeId = "mfa_challenge_id"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "fido" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"fido\"")
        }
        self.scope = try container.decodeIfPresent(String.self, forKey: .scope)
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.credential = try container.decode(PublicKeyCredentialWithAssertion.self, forKey: .credential)
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.mfaChallengeId = try container.decodeIfPresent(String.self, forKey: .mfaChallengeId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.scope, forKey: .scope)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.credential, forKey: .credential)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encodeIfPresent(self.mfaChallengeId, forKey: .mfaChallengeId)
    }
}

/// WebAuthn assertion options for `navigator.credentials.get`
public struct FidoAuthenticationLoginStartResponse: Codable, Equatable, Sendable {
    /// The grant type for the response
    public var grantType: String
    /// The challenge identifier
    public var challengeId: String
    /// The challenge expiration timestamp
    public var expiresAt: Date
    /// The WebAuthn assertion options
    public var assertion: FidoPublicKeyCredentialRequestOptions

    public init(grantType: String = "fido", challengeId: String, expiresAt: Date, assertion: FidoPublicKeyCredentialRequestOptions) {
        self.grantType = grantType
        self.challengeId = challengeId
        self.expiresAt = expiresAt
        self.assertion = assertion
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case challengeId = "challengeId"
        case expiresAt = "expiresAt"
        case assertion = "assertion"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "fido" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"fido\"")
        }
        self.challengeId = try container.decode(String.self, forKey: .challengeId)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
        self.assertion = try container.decode(FidoPublicKeyCredentialRequestOptions.self, forKey: .assertion)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encode(self.challengeId, forKey: .challengeId)
        try container.encode(self.expiresAt, forKey: .expiresAt)
        try container.encode(self.assertion, forKey: .assertion)
    }
}

/// Response for a FIDO assertion challenge
public struct FactorChallengeFidoStartResponse: Codable, Equatable, Sendable {
    /// Discriminator
    public var method: String
    /// The FIDO-factor challenge id — echo back on the `fido` grant as `challenge_id`
    public var fidoChallengeId: String
    /// WebAuthn assertion options for navigator.credentials.get
    public var assertion: FidoPublicKeyCredentialRequestOptions
    /// When the FIDO challenge expires
    public var expiresAt: Date

    public init(method: String = "fido", fidoChallengeId: String, assertion: FidoPublicKeyCredentialRequestOptions, expiresAt: Date) {
        self.method = method
        self.fidoChallengeId = fidoChallengeId
        self.assertion = assertion
        self.expiresAt = expiresAt
    }

    private enum CodingKeys: String, CodingKey {
        case method = "method"
        case fidoChallengeId = "fido_challenge_id"
        case assertion = "assertion"
        case expiresAt = "expires_at"
    }

    private enum EncodingKeys: String, CodingKey {
        case method = "method"
        case fidoChallengeId = "fidoChallengeId"
        case assertion = "assertion"
        case expiresAt = "expiresAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.method = try container.decode(String.self, forKey: .method)
        guard self.method == "fido" else {
            throw DecodingError.dataCorruptedError(forKey: .method, in: container, debugDescription: "Expected \"fido\"")
        }
        self.fidoChallengeId = try container.decode(String.self, forKey: .fidoChallengeId)
        self.assertion = try container.decode(FidoPublicKeyCredentialRequestOptions.self, forKey: .assertion)
        self.expiresAt = try container.decode(Date.self, forKey: .expiresAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: EncodingKeys.self)
        try container.encode(self.method, forKey: .method)
        try container.encode(self.fidoChallengeId, forKey: .fidoChallengeId)
        try container.encode(self.assertion, forKey: .assertion)
        try container.encode(self.expiresAt, forKey: .expiresAt)
    }
}

/// The credential the client posts back to complete registration
public struct PublicKeyCredentialWithAttestation: Codable, Equatable, Sendable {
    /// The base64url encoding of `rawId`
    public var id: String
    /// This enumeration defines the valid credential types. It is an extension point; values can be added to it in the future, as more credential types are defined. The values of this enumeration are used for versioning the Authentication Assertion and attestation structures according to the type of the authenticator. Currently one credential type is defined, namely `public-key`.
    public var type: String
    /// The credential identifier
    public var rawId: String
    /// The authenticator attachment
    public var authenticatorAttachment: PublicKeyCredentialAuthenticatorAttachment?
    /// The client extension results
    public var clientExtensionResults: SimpleClientExtensionResults
    /// The authenticator attestation response
    public var response: FidoAuthenticatorAttestationResponse

    public init(id: String, type: String = "public-key", rawId: String, authenticatorAttachment: PublicKeyCredentialAuthenticatorAttachment? = nil, clientExtensionResults: SimpleClientExtensionResults, response: FidoAuthenticatorAttestationResponse) {
        self.id = id
        self.type = type
        self.rawId = rawId
        self.authenticatorAttachment = authenticatorAttachment
        self.clientExtensionResults = clientExtensionResults
        self.response = response
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case type = "type"
        case rawId = "rawId"
        case authenticatorAttachment = "authenticatorAttachment"
        case clientExtensionResults = "clientExtensionResults"
        case response = "response"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.type = try container.decode(String.self, forKey: .type)
        guard self.type == "public-key" else {
            throw DecodingError.dataCorruptedError(forKey: .type, in: container, debugDescription: "Expected \"public-key\"")
        }
        self.rawId = try container.decode(String.self, forKey: .rawId)
        self.authenticatorAttachment = try container.decodeIfPresent(PublicKeyCredentialAuthenticatorAttachment.self, forKey: .authenticatorAttachment)
        self.clientExtensionResults = try container.decode(SimpleClientExtensionResults.self, forKey: .clientExtensionResults)
        self.response = try container.decode(FidoAuthenticatorAttestationResponse.self, forKey: .response)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.type, forKey: .type)
        try container.encode(self.rawId, forKey: .rawId)
        try container.encodeIfPresent(self.authenticatorAttachment, forKey: .authenticatorAttachment)
        try container.encode(self.clientExtensionResults, forKey: .clientExtensionResults)
        try container.encode(self.response, forKey: .response)
    }
}

public struct LinkAuthenticationLoginStart: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// User's email address
    public var email: String

    public init(grantType: String = "link", clientId: UUID? = nil, email: String) {
        self.grantType = grantType
        self.clientId = clientId
        self.email = email
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case clientId = "client_id"
        case email = "email"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "link" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"link\"")
        }
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.email = try container.decode(String.self, forKey: .email)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.email, forKey: .email)
    }
}

public struct CodeAuthenticationLoginStart: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// User's email address
    public var email: String
    /// A base64url encoded SHA256 hash of a one time secret used to validate that the request starts and ends on the same device
    public var codeChallenge: String

    public init(grantType: String = "code", clientId: UUID? = nil, email: String, codeChallenge: String) {
        self.grantType = grantType
        self.clientId = clientId
        self.email = email
        self.codeChallenge = codeChallenge
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case clientId = "client_id"
        case email = "email"
        case codeChallenge = "code_challenge"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "code" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"code\"")
        }
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.email = try container.decode(String.self, forKey: .email)
        self.codeChallenge = try container.decode(String.self, forKey: .codeChallenge)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.email, forKey: .email)
        try container.encode(self.codeChallenge, forKey: .codeChallenge)
    }
}

public struct FidoAuthenticationLoginStart: Codable, Equatable, Sendable {
    /// The grant type for the request
    public var grantType: String
    /// The application's client identifier, if available
    public var clientId: UUID?
    /// User's email address
    public var email: String

    public init(grantType: String = "fido", clientId: UUID? = nil, email: String) {
        self.grantType = grantType
        self.clientId = clientId
        self.email = email
    }

    private enum CodingKeys: String, CodingKey {
        case grantType = "grant_type"
        case clientId = "client_id"
        case email = "email"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.grantType = try container.decode(String.self, forKey: .grantType)
        guard self.grantType == "fido" else {
            throw DecodingError.dataCorruptedError(forKey: .grantType, in: container, debugDescription: "Expected \"fido\"")
        }
        self.clientId = try container.decodeIfPresent(UUID.self, forKey: .clientId)
        self.email = try container.decode(String.self, forKey: .email)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.grantType, forKey: .grantType)
        try container.encodeIfPresent(self.clientId, forKey: .clientId)
        try container.encode(self.email, forKey: .email)
    }
}

public indirect enum AuthenticationRequest: Codable, Equatable, Sendable {
    case passwordAuthenticationRequest(PasswordAuthenticationRequest)
    case clientCredentialsAuthenticationRequest(ClientCredentialsAuthenticationRequest)
    case refreshTokenAuthenticationRequest(RefreshTokenAuthenticationRequest)
    case linkAuthenticationRequest(LinkAuthenticationRequest)
    case codeAuthenticationRequest(CodeAuthenticationRequest)
    case fidoAuthenticationRequest(FidoAuthenticationRequest)
    case authenticatorAuthenticationRequest(AuthenticatorAuthenticationRequest)
    case oidcAuthenticationRequest(OidcAuthenticationRequest)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "grant_type"))
        switch tag {
        case .string("password"):
            self = .passwordAuthenticationRequest(try PasswordAuthenticationRequest(from: decoder))
        case .string("client_credentials"):
            self = .clientCredentialsAuthenticationRequest(try ClientCredentialsAuthenticationRequest(from: decoder))
        case .string("refresh_token"):
            self = .refreshTokenAuthenticationRequest(try RefreshTokenAuthenticationRequest(from: decoder))
        case .string("link"):
            self = .linkAuthenticationRequest(try LinkAuthenticationRequest(from: decoder))
        case .string("code"):
            self = .codeAuthenticationRequest(try CodeAuthenticationRequest(from: decoder))
        case .string("fido"):
            self = .fidoAuthenticationRequest(try FidoAuthenticationRequest(from: decoder))
        case .string("authenticator"):
            self = .authenticatorAuthenticationRequest(try AuthenticatorAuthenticationRequest(from: decoder))
        case .string("oidc"):
            self = .oidcAuthenticationRequest(try OidcAuthenticationRequest(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "grant_type"), in: container, debugDescription: "Unknown AuthenticationRequest grant_type: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .passwordAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .clientCredentialsAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .refreshTokenAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .linkAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .codeAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .fidoAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .authenticatorAuthenticationRequest(let value):
            try value.encode(to: encoder)
        case .oidcAuthenticationRequest(let value):
            try value.encode(to: encoder)
        }
    }
}

public indirect enum StepUpStartResponse: Codable, Equatable, Sendable {
    case mfaRequiredResponse(MfaRequiredResponse)
    case enrollmentRequiredResponse(EnrollmentRequiredResponse)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "result"))
        switch tag {
        case .string("mfa_required"):
            self = .mfaRequiredResponse(try MfaRequiredResponse(from: decoder))
        case .string("enrollment_required"):
            self = .enrollmentRequiredResponse(try EnrollmentRequiredResponse(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "result"), in: container, debugDescription: "Unknown StepUpStartResponse result: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .mfaRequiredResponse(let value):
            try value.encode(to: encoder)
        case .enrollmentRequiredResponse(let value):
            try value.encode(to: encoder)
        }
    }
}

public indirect enum AuthenticationTokenResponse: Codable, Equatable, Sendable {
    case authenticationTokenIssued(AuthenticationTokenIssued)
    case mfaRequiredResponse(MfaRequiredResponse)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "result"))
        switch tag {
        case .string("token"):
            self = .authenticationTokenIssued(try AuthenticationTokenIssued(from: decoder))
        case .string("mfa_required"):
            self = .mfaRequiredResponse(try MfaRequiredResponse(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "result"), in: container, debugDescription: "Unknown AuthenticationTokenResponse result: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .authenticationTokenIssued(let value):
            try value.encode(to: encoder)
        case .mfaRequiredResponse(let value):
            try value.encode(to: encoder)
        }
    }
}

/// The authenticator attachment
public enum PublicKeyCredentialAuthenticatorAttachment: String, Codable, CaseIterable, Sendable {
    case crossPlatform = "cross-platform"
    case platform = "platform"
}

public struct SimpleClientExtensionResultsCredProps: Codable, Equatable, Sendable {
    /// Whether the client supports the rk extension
    public var rk: Bool

    public init(rk: Bool) {
        self.rk = rk
    }

    private enum CodingKeys: String, CodingKey {
        case rk = "rk"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.rk = try container.decode(Bool.self, forKey: .rk)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.rk, forKey: .rk)
    }
}

public indirect enum AuthenticationLoginStart: Codable, Equatable, Sendable {
    case linkAuthenticationLoginStart(LinkAuthenticationLoginStart)
    case codeAuthenticationLoginStart(CodeAuthenticationLoginStart)
    case fidoAuthenticationLoginStart(FidoAuthenticationLoginStart)
    case oidcAuthenticationLoginStart(OidcAuthenticationLoginStart)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "grant_type"))
        switch tag {
        case .string("link"):
            self = .linkAuthenticationLoginStart(try LinkAuthenticationLoginStart(from: decoder))
        case .string("code"):
            self = .codeAuthenticationLoginStart(try CodeAuthenticationLoginStart(from: decoder))
        case .string("fido"):
            self = .fidoAuthenticationLoginStart(try FidoAuthenticationLoginStart(from: decoder))
        case .string("oidc"):
            self = .oidcAuthenticationLoginStart(try OidcAuthenticationLoginStart(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "grant_type"), in: container, debugDescription: "Unknown AuthenticationLoginStart grant_type: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .linkAuthenticationLoginStart(let value):
            try value.encode(to: encoder)
        case .codeAuthenticationLoginStart(let value):
            try value.encode(to: encoder)
        case .fidoAuthenticationLoginStart(let value):
            try value.encode(to: encoder)
        case .oidcAuthenticationLoginStart(let value):
            try value.encode(to: encoder)
        }
    }
}

public enum PublicKeyCredentialDescriptorTransports: String, Codable, CaseIterable, Sendable {
    case usb = "usb"
    case nfc = "nfc"
    case ble = "ble"
    case `internal` = "internal"
    case hybrid = "hybrid"
}

/// The attestation
public enum FidoPublicKeyCredentialRequestOptionsAttestation: String, Codable, CaseIterable, Sendable {
    case direct = "direct"
    case indirect = "indirect"
    case none = "none"
}

/// Whether the authenticator must verify the user
public enum FidoPublicKeyCredentialRequestOptionsUserVerification: String, Codable, CaseIterable, Sendable {
    case required = "required"
    case preferred = "preferred"
    case discouraged = "discouraged"
}

public indirect enum AuthenticationLoginStartResponse: Codable, Equatable, Sendable {
    case codeAuthenticationLoginStartResponse(CodeAuthenticationLoginStartResponse)
    case linkAuthenticationLoginStartResponse(LinkAuthenticationLoginStartResponse)
    case fidoAuthenticationLoginStartResponse(FidoAuthenticationLoginStartResponse)
    case oidcAuthenticationLoginStartResponse(OidcAuthenticationLoginStartResponse)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "grant_type"))
        switch tag {
        case .string("code"):
            self = .codeAuthenticationLoginStartResponse(try CodeAuthenticationLoginStartResponse(from: decoder))
        case .string("link"):
            self = .linkAuthenticationLoginStartResponse(try LinkAuthenticationLoginStartResponse(from: decoder))
        case .string("fido"):
            self = .fidoAuthenticationLoginStartResponse(try FidoAuthenticationLoginStartResponse(from: decoder))
        case .string("oidc"):
            self = .oidcAuthenticationLoginStartResponse(try OidcAuthenticationLoginStartResponse(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "grant_type"), in: container, debugDescription: "Unknown AuthenticationLoginStartResponse grant_type: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .codeAuthenticationLoginStartResponse(let value):
            try value.encode(to: encoder)
        case .linkAuthenticationLoginStartResponse(let value):
            try value.encode(to: encoder)
        case .fidoAuthenticationLoginStartResponse(let value):
            try value.encode(to: encoder)
        case .oidcAuthenticationLoginStartResponse(let value):
            try value.encode(to: encoder)
        }
    }
}

public indirect enum FactorChallengeStartRequest: Codable, Equatable, Sendable {
    case factorChallengePhoneStart(FactorChallengePhoneStart)
    case factorChallengeFidoStart(FactorChallengeFidoStart)
    case factorChallengeEmailStart(FactorChallengeEmailStart)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "method"))
        switch tag {
        case .string("phone"):
            self = .factorChallengePhoneStart(try FactorChallengePhoneStart(from: decoder))
        case .string("fido"):
            self = .factorChallengeFidoStart(try FactorChallengeFidoStart(from: decoder))
        case .string("email"):
            self = .factorChallengeEmailStart(try FactorChallengeEmailStart(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "method"), in: container, debugDescription: "Unknown FactorChallengeStartRequest method: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .factorChallengePhoneStart(let value):
            try value.encode(to: encoder)
        case .factorChallengeFidoStart(let value):
            try value.encode(to: encoder)
        case .factorChallengeEmailStart(let value):
            try value.encode(to: encoder)
        }
    }
}

public indirect enum FactorChallengeStartResponse: Codable, Equatable, Sendable {
    case factorChallengePhoneStartResponse(FactorChallengePhoneStartResponse)
    case factorChallengeFidoStartResponse(FactorChallengeFidoStartResponse)
    case factorChallengeEmailStartResponse(FactorChallengeEmailStartResponse)

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: DynamicCodingKey.self)
        let tag = try container.decode(JSONValue.self, forKey: DynamicCodingKey(stringValue: "method"))
        switch tag {
        case .string("phone"):
            self = .factorChallengePhoneStartResponse(try FactorChallengePhoneStartResponse(from: decoder))
        case .string("fido"):
            self = .factorChallengeFidoStartResponse(try FactorChallengeFidoStartResponse(from: decoder))
        case .string("email"):
            self = .factorChallengeEmailStartResponse(try FactorChallengeEmailStartResponse(from: decoder))
        default:
            throw DecodingError.dataCorruptedError(forKey: DynamicCodingKey(stringValue: "method"), in: container, debugDescription: "Unknown FactorChallengeStartResponse method: \(tag)")
        }
    }

    public func encode(to encoder: Encoder) throws {
        switch self {
        case .factorChallengePhoneStartResponse(let value):
            try value.encode(to: encoder)
        case .factorChallengeFidoStartResponse(let value):
            try value.encode(to: encoder)
        case .factorChallengeEmailStartResponse(let value):
            try value.encode(to: encoder)
        }
    }
}

/// The verification method
public enum SessionFactorMethod: String, Codable, CaseIterable, Sendable {
    case phone = "phone"
    case password = "password"
    case authenticator = "authenticator"
    case email = "email"
    case fido = "fido"
    case oidc = "oidc"
}

/// MFA category for the factor
public enum SessionFactorKind: String, Codable, CaseIterable, Sendable {
    case knowledge = "knowledge"
    case possession = "possession"
    case biometric = "biometric"
}

/// The factor that satisfied the primary authentication
public enum LoginFactorType: String, Codable, CaseIterable, Sendable {
    case phone = "phone"
    case password = "password"
    case authenticator = "authenticator"
    case email = "email"
    case fido = "fido"
    case oidc = "oidc"
}
