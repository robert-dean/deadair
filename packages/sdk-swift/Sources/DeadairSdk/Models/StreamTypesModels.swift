// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// What the station's track fetcher holds by way of a Spotify login
public struct FetcherAuthorization: Codable, Equatable, Sendable {
    /// Whether the fetcher answered at all. False makes every field below a default rather than a reading, so a fetcher that is merely down is never reported as one that was never authorized
    public var reachable: Bool
    /// Whether this install has a stream half yet. False means there is nothing here to authorize
    public var configured: Bool
    /// Whether the fetcher holds its OWN stored authorization, which is the only kind Spotify's login accepts. False with a healthy plugin above it is a station that lists playlists perfectly and cannot fetch a single record
    public var authorized: Bool
    /// Whether a login is established right now
    public var session: Bool
    /// The last reason a login was refused. Present is not the same as fatal: a session may have recovered since
    public var loginError: String?
    /// An authorization already started and not yet finished, so an operator who lost the URL is given it back rather than having to start again
    public var pendingUrl: String?
    /// The address the browser will be sent to and will not be able to load. Reported so the console can say which page is expected to fail, rather than leaving that looking like a fault. Absent when the fetcher did not answer, since it is the only thing that knows it
    public var callbackUrl: String?

    public init(reachable: Bool, configured: Bool, authorized: Bool, session: Bool, loginError: String? = nil, pendingUrl: String? = nil, callbackUrl: String? = nil) {
        self.reachable = reachable
        self.configured = configured
        self.authorized = authorized
        self.session = session
        self.loginError = loginError
        self.pendingUrl = pendingUrl
        self.callbackUrl = callbackUrl
    }

    private enum CodingKeys: String, CodingKey {
        case reachable = "reachable"
        case configured = "configured"
        case authorized = "authorized"
        case session = "session"
        case loginError = "loginError"
        case pendingUrl = "pendingUrl"
        case callbackUrl = "callbackUrl"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.reachable = try container.decode(Bool.self, forKey: .reachable)
        self.configured = try container.decode(Bool.self, forKey: .configured)
        self.authorized = try container.decode(Bool.self, forKey: .authorized)
        self.session = try container.decode(Bool.self, forKey: .session)
        self.loginError = try container.decodeIfPresent(String.self, forKey: .loginError)
        self.pendingUrl = try container.decodeIfPresent(String.self, forKey: .pendingUrl)
        self.callbackUrl = try container.decodeIfPresent(String.self, forKey: .callbackUrl)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.reachable, forKey: .reachable)
        try container.encode(self.configured, forKey: .configured)
        try container.encode(self.authorized, forKey: .authorized)
        try container.encode(self.session, forKey: .session)
        try container.encodeIfPresent(self.loginError, forKey: .loginError)
        try container.encodeIfPresent(self.pendingUrl, forKey: .pendingUrl)
        try container.encodeIfPresent(self.callbackUrl, forKey: .callbackUrl)
    }
}

/// An authorization to open in a browser
public struct FetcherAuthorizationStart: Codable, Equatable, Sendable {
    /// The Spotify consent page, to be opened by the operator
    public var authorizeUrl: String
    /// How long this URL is good for. Starting another replaces it
    public var expiresInMs: Int

    public init(authorizeUrl: String, expiresInMs: Int) {
        self.authorizeUrl = authorizeUrl
        self.expiresInMs = expiresInMs
    }

    private enum CodingKeys: String, CodingKey {
        case authorizeUrl = "authorizeUrl"
        case expiresInMs = "expiresInMs"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.authorizeUrl = try container.decode(String.self, forKey: .authorizeUrl)
        self.expiresInMs = try container.decode(Int.self, forKey: .expiresInMs)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.authorizeUrl, forKey: .authorizeUrl)
        try container.encode(self.expiresInMs, forKey: .expiresInMs)
    }
}

/// The callback the browser could not deliver, handed over by the operator instead
public struct FetcherAuthorizationInput: Codable, Equatable, Sendable {
    /// The address the browser ended up at, pasted whole. Taken apart by the fetcher rather than here, because two readings of one address is one of them being wrong eventually
    public var redirectUrl: String

    public init(redirectUrl: String) {
        self.redirectUrl = redirectUrl
    }

    private enum CodingKeys: String, CodingKey {
        case redirectUrl = "redirectUrl"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.redirectUrl = try container.decode(String.self, forKey: .redirectUrl)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.redirectUrl, forKey: .redirectUrl)
    }
}

/// Which account the station now fetches as
public struct FetcherAuthorizationFinished: Codable, Equatable, Sendable {
    /// The Spotify account that was authorized. Reported because an operator with two accounts in two browser profiles wants to know which one this station now is
    public var username: String

    public init(username: String) {
        self.username = username
    }

    private enum CodingKeys: String, CodingKey {
        case username = "username"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.username = try container.decode(String.self, forKey: .username)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.username, forKey: .username)
    }
}
