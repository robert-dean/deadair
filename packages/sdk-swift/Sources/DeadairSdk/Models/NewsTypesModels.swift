// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// A feed one installed plugin offers
public struct StationFeed: Codable, Equatable, Sendable {
    /// Unique across the station: the plugin's own id for the feed, qualified with the plugin that offered it. Two services both calling something `world` stay distinct
    public var id: String
    public var pluginId: String
    public var name: String
    /// Broad subject, in the publisher's own word for it
    public var category: String?
    /// ISO 639-1, when the plugin knows
    public var language: String?
    public var description: String?

    public init(id: String, pluginId: String, name: String, category: String? = nil, language: String? = nil, description: String? = nil) {
        self.id = id
        self.pluginId = pluginId
        self.name = name
        self.category = category
        self.language = language
        self.description = description
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case pluginId = "pluginId"
        case name = "name"
        case category = "category"
        case language = "language"
        case description = "description"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.name = try container.decode(String.self, forKey: .name)
        self.category = try container.decodeIfPresent(String.self, forKey: .category)
        self.language = try container.decodeIfPresent(String.self, forKey: .language)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.category, forKey: .category)
        try container.encodeIfPresent(self.language, forKey: .language)
        try container.encodeIfPresent(self.description, forKey: .description)
    }
}

/// One published entry
public struct NewsStory: Codable, Equatable, Sendable {
    /// Stable for the same entry across calls, which is what lets a reader tell an arrival from something it has already seen
    public var id: String
    /// Qualified, matching `StationFeed.id`
    public var feedId: String
    public var feedName: String
    public var title: String
    /// The publisher's own teaser, as plain text. Never markup: this is written to be read out
    public var summary: String?
    /// The story itself, as the publisher's own paragraphs. Absent when the plugin could not read one, which is ordinary: an entry that links to audio, or a page nothing could be extracted from
    public var content: String?
    public var url: String?
    /// ISO-8601
    public var publishedAt: String?
    public var categories: [String]?

    public init(id: String, feedId: String, feedName: String, title: String, summary: String? = nil, content: String? = nil, url: String? = nil, publishedAt: String? = nil, categories: [String]? = nil) {
        self.id = id
        self.feedId = feedId
        self.feedName = feedName
        self.title = title
        self.summary = summary
        self.content = content
        self.url = url
        self.publishedAt = publishedAt
        self.categories = categories
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case feedId = "feedId"
        case feedName = "feedName"
        case title = "title"
        case summary = "summary"
        case content = "content"
        case url = "url"
        case publishedAt = "publishedAt"
        case categories = "categories"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.feedId = try container.decode(String.self, forKey: .feedId)
        self.feedName = try container.decode(String.self, forKey: .feedName)
        self.title = try container.decode(String.self, forKey: .title)
        self.summary = try container.decodeIfPresent(String.self, forKey: .summary)
        self.content = try container.decodeIfPresent(String.self, forKey: .content)
        self.url = try container.decodeIfPresent(String.self, forKey: .url)
        self.publishedAt = try container.decodeIfPresent(String.self, forKey: .publishedAt)
        self.categories = try container.decodeIfPresent([String].self, forKey: .categories)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.feedId, forKey: .feedId)
        try container.encode(self.feedName, forKey: .feedName)
        try container.encode(self.title, forKey: .title)
        try container.encodeIfPresent(self.summary, forKey: .summary)
        try container.encodeIfPresent(self.content, forKey: .content)
        try container.encodeIfPresent(self.url, forKey: .url)
        try container.encodeIfPresent(self.publishedAt, forKey: .publishedAt)
        try container.encodeIfPresent(self.categories, forKey: .categories)
    }
}

public struct NewsQuery: Codable, Equatable, Sendable {
    /// One feed, or absent for every feed the station can see, merged newest first
    public var feedId: String?
    public var limit: Int?
    /// Only entries published after this ISO-8601 instant
    public var since: String?
    /// Answer with headlines and teasers alone, skipping the story behind each one. A story is read from the publisher's own page, which is by far the slowest thing this route does, so a caller that will not use `content` should say so
    public var headlinesOnly: Bool?

    public init(feedId: String? = nil, limit: Int? = nil, since: String? = nil, headlinesOnly: Bool? = nil) {
        self.feedId = feedId
        self.limit = limit
        self.since = since
        self.headlinesOnly = headlinesOnly
    }

    private enum CodingKeys: String, CodingKey {
        case feedId = "feedId"
        case limit = "limit"
        case since = "since"
        case headlinesOnly = "headlinesOnly"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.feedId = try container.decodeIfPresent(String.self, forKey: .feedId)
        self.limit = try container.decodeIfPresent(Int.self, forKey: .limit)
        self.since = try container.decodeIfPresent(String.self, forKey: .since)
        self.headlinesOnly = try container.decodeIfPresent(Bool.self, forKey: .headlinesOnly)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.feedId, forKey: .feedId)
        try container.encodeIfPresent(self.limit, forKey: .limit)
        try container.encodeIfPresent(self.since, forKey: .since)
        try container.encodeIfPresent(self.headlinesOnly, forKey: .headlinesOnly)
    }
}

public struct StationFeedList: Codable, Equatable, Sendable {
    public var feeds: [StationFeed]

    public init(feeds: [StationFeed]) {
        self.feeds = feeds
    }

    private enum CodingKeys: String, CodingKey {
        case feeds = "feeds"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.feeds = try container.decode([StationFeed].self, forKey: .feeds)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.feeds, forKey: .feeds)
    }
}

public struct NewsPage: Codable, Equatable, Sendable {
    /// Newest first. Empty when nothing could be read, which is deliberately not an error: the news is something the station may talk about, never something it needs to air
    public var stories: [NewsStory]

    public init(stories: [NewsStory]) {
        self.stories = stories
    }

    private enum CodingKeys: String, CodingKey {
        case stories = "stories"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.stories = try container.decode([NewsStory].self, forKey: .stories)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.stories, forKey: .stories)
    }
}
