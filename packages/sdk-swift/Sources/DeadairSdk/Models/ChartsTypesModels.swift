// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// A chart one installed plugin offers
public struct StationChart: Codable, Equatable, Sendable {
    /// Unique across the station: the plugin's own id for the chart, qualified with the plugin that offered it. Two services both calling something `top-100` stay distinct
    public var id: String
    public var pluginId: String
    public var name: String
    /// ISO 3166-1 alpha-2, when the chart is national. Absent means global
    public var country: String?
    public var genre: String?
    public var description: String?

    public init(id: String, pluginId: String, name: String, country: String? = nil, genre: String? = nil, description: String? = nil) {
        self.id = id
        self.pluginId = pluginId
        self.name = name
        self.country = country
        self.genre = genre
        self.description = description
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case pluginId = "pluginId"
        case name = "name"
        case country = "country"
        case genre = "genre"
        case description = "description"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.pluginId = try container.decode(String.self, forKey: .pluginId)
        self.name = try container.decode(String.self, forKey: .name)
        self.country = try container.decodeIfPresent(String.self, forKey: .country)
        self.genre = try container.decodeIfPresent(String.self, forKey: .genre)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.pluginId, forKey: .pluginId)
        try container.encode(self.name, forKey: .name)
        try container.encodeIfPresent(self.country, forKey: .country)
        try container.encodeIfPresent(self.genre, forKey: .genre)
        try container.encodeIfPresent(self.description, forKey: .description)
    }
}

/// One record's place in a chart
public struct ChartRecord: Codable, Equatable, Sendable {
    public var rank: Int
    public var title: String
    /// The lead artist alone. The other credits are in `featuring`
    public var artist: String
    public var featuring: [String]?
    public var album: String?
    public var year: Int?
    /// Best position this record has reached, where the source tracks it
    public var peak: Int?
    /// How many editions it has appeared in, where the source tracks it
    public var weeksOn: Int?

    public init(rank: Int, title: String, artist: String, featuring: [String]? = nil, album: String? = nil, year: Int? = nil, peak: Int? = nil, weeksOn: Int? = nil) {
        self.rank = rank
        self.title = title
        self.artist = artist
        self.featuring = featuring
        self.album = album
        self.year = year
        self.peak = peak
        self.weeksOn = weeksOn
    }

    private enum CodingKeys: String, CodingKey {
        case rank = "rank"
        case title = "title"
        case artist = "artist"
        case featuring = "featuring"
        case album = "album"
        case year = "year"
        case peak = "peak"
        case weeksOn = "weeksOn"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.rank = try container.decode(Int.self, forKey: .rank)
        self.title = try container.decode(String.self, forKey: .title)
        self.artist = try container.decode(String.self, forKey: .artist)
        self.featuring = try container.decodeIfPresent([String].self, forKey: .featuring)
        self.album = try container.decodeIfPresent(String.self, forKey: .album)
        self.year = try container.decodeIfPresent(Int.self, forKey: .year)
        self.peak = try container.decodeIfPresent(Int.self, forKey: .peak)
        self.weeksOn = try container.decodeIfPresent(Int.self, forKey: .weeksOn)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.rank, forKey: .rank)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artist, forKey: .artist)
        try container.encodeIfPresent(self.featuring, forKey: .featuring)
        try container.encodeIfPresent(self.album, forKey: .album)
        try container.encodeIfPresent(self.year, forKey: .year)
        try container.encodeIfPresent(self.peak, forKey: .peak)
        try container.encodeIfPresent(self.weeksOn, forKey: .weeksOn)
    }
}

public struct ChartQuery: Codable, Equatable, Sendable {
    public var limit: Int?
    /// Which edition, as `YYYY-MM-DD`. Absent means the current one, and a service that keeps no history answers with the current one either way
    public var date: String?

    public init(limit: Int? = nil, date: String? = nil) {
        self.limit = limit
        self.date = date
    }

    private enum CodingKeys: String, CodingKey {
        case limit = "limit"
        case date = "date"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.limit = try container.decodeIfPresent(Int.self, forKey: .limit)
        self.date = try container.decodeIfPresent(String.self, forKey: .date)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.limit, forKey: .limit)
        try container.encodeIfPresent(self.date, forKey: .date)
    }
}

public struct StationChartList: Codable, Equatable, Sendable {
    public var charts: [StationChart]

    public init(charts: [StationChart]) {
        self.charts = charts
    }

    private enum CodingKeys: String, CodingKey {
        case charts = "charts"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.charts = try container.decode([StationChart].self, forKey: .charts)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.charts, forKey: .charts)
    }
}

public struct ChartPage: Codable, Equatable, Sendable {
    public var chartId: String
    /// Ranked. Empty when the chart could not be read, which is deliberately not an error: a chart is something to look at, never something the station needs to air
    public var records: [ChartRecord]

    public init(chartId: String, records: [ChartRecord]) {
        self.chartId = chartId
        self.records = records
    }

    private enum CodingKeys: String, CodingKey {
        case chartId = "chartId"
        case records = "records"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.chartId = try container.decode(String.self, forKey: .chartId)
        self.records = try container.decode([ChartRecord].self, forKey: .records)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.chartId, forKey: .chartId)
        try container.encode(self.records, forKey: .records)
    }
}
