// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Operations declared in `personas.ck`.
public final class PersonasClient: Sendable {
    private let http: SdkHttp

    public init(http: SdkHttp) {
        self.http = http
    }

    /// List personas
    /// Every persona this station has, oldest first
    public func listPersonas() async throws -> PersonaList {
        let request = SdkRequest(method: "GET", path: ["personas"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaList.self, from: response)
    }

    /// Create persona
    /// Writes a new persona. It is not put on air by creating it
    public func createPersona(body: PersonaInput) async throws -> PersonaList {
        var request = SdkRequest(method: "POST", path: ["personas"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaList.self, from: response)
    }

    /// Generate persona
    /// Turns a description of a character into a whole persona, checked against its own sample lines and handed back unsaved
    public func generatePersona(body: PersonaRequest) async throws -> GeneratedPersona {
        var request = SdkRequest(method: "POST", path: ["personas", "generate"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(GeneratedPersona.self, from: response)
    }

    /// Restore station personas
    /// Writes back whichever of the station's own personas this station is missing, touching nothing it already has and putting nothing on air
    public func restoreStationPersonas() async throws -> PersonaList {
        let request = SdkRequest(method: "POST", path: ["personas", "restore"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaList.self, from: response)
    }

    /// Export personas
    /// Every character this station holds, as one file
    public func exportPersonas() async throws -> ExportPersonasResult {
        let request = SdkRequest(method: "GET", path: ["personas", "export"])
        let response = try await http.execute(request)
        let headers = try ExportPersonasHeaders(
            contentDisposition: http.optionalHeader(response, "Content-Disposition", as: String.self)
        )
        return ExportPersonasResult(data: try http.decodeJSON(PersonaFile.self, from: response), headers: headers)
    }

    /// Export persona
    /// One character, its sheet and its stories, as a file
    /// - Throws: `SdkError` on 404
    public func exportPersona(id: String) async throws -> ExportPersonaResult {
        let request = try SdkRequest(method: "GET", path: ["personas", http.segment(id), "export"])
        let response = try await http.execute(request)
        let headers = try ExportPersonaHeaders(
            contentDisposition: http.optionalHeader(response, "Content-Disposition", as: String.self)
        )
        return ExportPersonaResult(data: try http.decodeJSON(PersonaFile.self, from: response), headers: headers)
    }

    /// Preview persona import
    /// Reads a file and reports what importing it would create, rewrite and skip. Writes nothing
    public func previewPersonaImport(body: PersonaFile) async throws -> PersonaImportPlan {
        var request = SdkRequest(method: "POST", path: ["personas", "import", "preview"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaImportPlan.self, from: response)
    }

    /// Import personas
    /// Writes a file into this station, merging by key, and answers with what it did
    public func importPersonas(body: PersonaFile) async throws -> PersonaImportResult {
        var request = SdkRequest(method: "POST", path: ["personas", "import"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaImportResult.self, from: response)
    }

    /// Update persona
    /// Rewrites one persona. An edit to the one on air is heard on the next break
    public func updatePersona(id: String, body: PersonaInput) async throws -> PersonaList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaList.self, from: response)
    }

    /// Delete persona
    /// Removes a persona, including the one on air, which leaves the station with none
    public func deletePersona(id: String) async throws -> PersonaList {
        let request = try SdkRequest(method: "DELETE", path: ["personas", http.segment(id)])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaList.self, from: response)
    }

    /// Put persona on air
    /// Puts this persona on air and takes the previous one off
    public func putPersonaOnAir(id: String) async throws -> PersonaList {
        let request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "active"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaList.self, from: response)
    }

    /// List persona notes
    /// Everything this character has accumulated, oldest first, in every state
    public func listPersonaNotes(id: String) async throws -> PersonaNoteList {
        let request = try SdkRequest(method: "GET", path: ["personas", http.segment(id), "notes"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaNoteList.self, from: response)
    }

    /// Write persona note
    /// Writes a note by hand. An operator's own note is active from the moment it exists; only the distil pass proposes
    public func writePersonaNote(id: String, body: PersonaNoteWrite) async throws -> PersonaNoteList {
        var request = try SdkRequest(method: "POST", path: ["personas", http.segment(id), "notes"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaNoteList.self, from: response)
    }

    /// Update persona note
    /// Rewrites one note's words, whoever wrote it. Editing what the station proposed is most of the point of the panel
    public func updatePersonaNote(id: String, noteId: String, body: PersonaNoteWrite) async throws -> PersonaNoteList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "notes", http.segment(noteId)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaNoteList.self, from: response)
    }

    /// Delete persona note
    /// Removes a note outright. Turning down a PROPOSAL is a state rather than this, or the next pass writes it again
    public func deletePersonaNote(id: String, noteId: String) async throws -> PersonaNoteList {
        let request = try SdkRequest(method: "DELETE", path: ["personas", http.segment(id), "notes", http.segment(noteId)])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaNoteList.self, from: response)
    }

    /// Set persona note state
    /// Accepts a proposal, turns one down, or rests an active note. Mirrors the lexicon's own state route
    public func setPersonaNoteState(id: String, noteId: String, body: PersonaNoteState) async throws -> PersonaNoteList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "notes", http.segment(noteId), "state"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaNoteList.self, from: response)
    }

    /// List persona stories
    /// Every story this character holds, oldest first, in every state
    public func listPersonaStories(id: String) async throws -> PersonaStoryList {
        let request = try SdkRequest(method: "GET", path: ["personas", http.segment(id), "stories"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Write persona story
    /// Writes a story by hand. An operator's own is tellable from the moment it exists; only the enrichment pass proposes
    public func writePersonaStory(id: String, body: PersonaStoryWrite) async throws -> PersonaStoryList {
        var request = try SdkRequest(method: "POST", path: ["personas", http.segment(id), "stories"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Update persona story
    /// Rewrites one story's handle and telling, whoever wrote it
    public func updatePersonaStory(id: String, storyId: String, body: PersonaStoryWrite) async throws -> PersonaStoryList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "stories", http.segment(storyId)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Delete persona story
    /// Removes a story outright, details and all. Turning down a PROPOSAL is a state rather than this, or the next pass writes it again
    public func deletePersonaStory(id: String, storyId: String) async throws -> PersonaStoryList {
        let request = try SdkRequest(method: "DELETE", path: ["personas", http.segment(id), "stories", http.segment(storyId)])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Set persona story state
    /// Accepts a proposal, turns one down, or takes a story out of the rotation without losing it
    public func setPersonaStoryState(id: String, storyId: String, body: PersonaStoryState) async throws -> PersonaStoryList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "stories", http.segment(storyId), "state"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Add persona story detail
    /// Adds one thing to a story that already exists
    public func addPersonaStoryDetail(id: String, storyId: String, body: PersonaStoryDetailWrite) async throws -> PersonaStoryList {
        var request = try SdkRequest(method: "POST", path: ["personas", http.segment(id), "stories", http.segment(storyId), "details"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Update persona story detail
    /// Rewrites one detail's words
    public func updatePersonaStoryDetail(id: String, storyId: String, detailId: String, body: PersonaStoryDetailWrite) async throws -> PersonaStoryList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "stories", http.segment(storyId), "details", http.segment(detailId)])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Delete persona story detail
    /// Removes one detail, leaving the story it was hung on alone
    public func deletePersonaStoryDetail(id: String, storyId: String, detailId: String) async throws -> PersonaStoryList {
        let request = try SdkRequest(method: "DELETE", path: ["personas", http.segment(id), "stories", http.segment(storyId), "details", http.segment(detailId)])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Set persona story detail state
    /// Accepts a proposed detail or turns it down, which has to outlive the pass that proposed it
    public func setPersonaStoryDetailState(id: String, storyId: String, detailId: String, body: PersonaStoryState) async throws -> PersonaStoryList {
        var request = try SdkRequest(method: "PUT", path: ["personas", http.segment(id), "stories", http.segment(storyId), "details", http.segment(detailId), "state"])
        try http.setJSONBody(&request, body, contentType: "application/json")
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaStoryList.self, from: response)
    }

    /// Rehearse persona
    /// Writes a talk break under this persona against two fixed invented records, and answers with every writer that was asked
    public func rehearsePersona(id: String) async throws -> PersonaRehearsal {
        let request = try SdkRequest(method: "POST", path: ["personas", http.segment(id), "rehearse"])
        let response = try await http.execute(request)
        return try http.decodeJSON(PersonaRehearsal.self, from: response)
    }
}

/// Response headers declared on GET /personas/export.
public struct ExportPersonasHeaders: Equatable, Sendable {
    public let contentDisposition: String?

    public init(contentDisposition: String?) {
        self.contentDisposition = contentDisposition
    }
}

/// The body of GET /personas/export, with the response headers the contract declares.
public struct ExportPersonasResult: Equatable, Sendable {
    public let data: PersonaFile
    public let headers: ExportPersonasHeaders

    public init(data: PersonaFile, headers: ExportPersonasHeaders) {
        self.data = data
        self.headers = headers
    }
}

/// Response headers declared on GET /personas/{id}/export.
public struct ExportPersonaHeaders: Equatable, Sendable {
    public let contentDisposition: String?

    public init(contentDisposition: String?) {
        self.contentDisposition = contentDisposition
    }
}

/// The body of GET /personas/{id}/export, with the response headers the contract declares.
public struct ExportPersonaResult: Equatable, Sendable {
    public let data: PersonaFile
    public let headers: ExportPersonaHeaders

    public init(data: PersonaFile, headers: ExportPersonaHeaders) {
        self.data = data
        self.headers = headers
    }
}
