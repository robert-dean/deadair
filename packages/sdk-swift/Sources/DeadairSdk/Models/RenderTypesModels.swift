// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// One thing the station can play that is not a record
public struct Segment: Codable, Equatable, Sendable {
    public var id: String
    /// What sort of element it is: `ident`, `stinger`, `talkbreak`, `news`
    public var kind: String
    /// One state per stage of making it. Only `ready` can go on air; the station skips anything else rather than waiting for it
    public var state: SegmentState
    /// What the console calls it, and what the mount is labelled with while it airs
    public var label: String
    /// Who made it: `library` for a file dropped into the inbox
    public var source: String
    /// Whether there is audio behind it yet
    public var playable: Bool
    /// The words, for anything that speaks. Absent for an imported recording
    public var script: String?
    /// The words as the speech engine was handed them: symbols said, years read as a person reads them, the station's pronunciation list applied. Absent until something has spoken it
    public var spokenScript: String?
    /// The file in the inbox this came from. The bytes were copied, so emptying the inbox does not take it off the air
    public var sourcePath: String?
    /// How long it runs. A display value: the player measures the audio itself
    public var durationMs: Int?
    /// Why it is `failed`
    public var error: String?
    /// The station's own name for the voice this is said in, e.g. `host`. Absent means the speech plugin's default
    public var voice: String?

    public init(id: String, kind: String, state: SegmentState, label: String, source: String, playable: Bool, script: String? = nil, spokenScript: String? = nil, sourcePath: String? = nil, durationMs: Int? = nil, error: String? = nil, voice: String? = nil) {
        self.id = id
        self.kind = kind
        self.state = state
        self.label = label
        self.source = source
        self.playable = playable
        self.script = script
        self.spokenScript = spokenScript
        self.sourcePath = sourcePath
        self.durationMs = durationMs
        self.error = error
        self.voice = voice
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case kind = "kind"
        case state = "state"
        case label = "label"
        case source = "source"
        case playable = "playable"
        case script = "script"
        case spokenScript = "spokenScript"
        case sourcePath = "sourcePath"
        case durationMs = "durationMs"
        case error = "error"
        case voice = "voice"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.state = try container.decode(SegmentState.self, forKey: .state)
        self.label = try container.decode(String.self, forKey: .label)
        self.source = try container.decode(String.self, forKey: .source)
        self.playable = try container.decode(Bool.self, forKey: .playable)
        self.script = try container.decodeIfPresent(String.self, forKey: .script)
        self.spokenScript = try container.decodeIfPresent(String.self, forKey: .spokenScript)
        self.sourcePath = try container.decodeIfPresent(String.self, forKey: .sourcePath)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.error = try container.decodeIfPresent(String.self, forKey: .error)
        self.voice = try container.decodeIfPresent(String.self, forKey: .voice)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.state, forKey: .state)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.source, forKey: .source)
        try container.encode(self.playable, forKey: .playable)
        try container.encodeIfPresent(self.script, forKey: .script)
        try container.encodeIfPresent(self.spokenScript, forKey: .spokenScript)
        try container.encodeIfPresent(self.sourcePath, forKey: .sourcePath)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.error, forKey: .error)
        try container.encodeIfPresent(self.voice, forKey: .voice)
    }
}

/// Something for the station to say, before anything has said it
public struct SegmentCreate: Codable, Equatable, Sendable {
    /// What the console calls it, and what the mount is labelled with while it airs
    public var label: String
    /// The words to say
    public var script: String
    /// What sort of element it is. Defaults to `talkbreak`
    public var kind: String?
    /// A station voice name the speech plugin knows how to map. Absent uses its default
    public var voice: String?

    public init(label: String, script: String, kind: String? = nil, voice: String? = nil) {
        self.label = label
        self.script = script
        self.kind = kind
        self.voice = voice
    }

    private enum CodingKeys: String, CodingKey {
        case label = "label"
        case script = "script"
        case kind = "kind"
        case voice = "voice"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.label = try container.decode(String.self, forKey: .label)
        self.script = try container.decode(String.self, forKey: .script)
        self.kind = try container.decodeIfPresent(String.self, forKey: .kind)
        self.voice = try container.decodeIfPresent(String.self, forKey: .voice)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.script, forKey: .script)
        try container.encodeIfPresent(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.voice, forKey: .voice)
    }
}

/// A recording arriving from the browser, as multipart form parts.
///
/// Documentation rather than validation: a multipart body reaches the service as the raw parser and
/// the generated client types the body as `FormData`, so nothing checks this shape. It says what to
/// send
public struct SegmentUpload: Codable, Equatable, Sendable {
    /// The audio itself. mp3, wav, ogg, flac or m4a, and at most 50 MB
    public var file: Data
    /// What sort of element it is, which is also the directory it is filed under. A kind nothing else uses becomes a bookable band on the format clock
    public var kind: String
    /// What the console calls it, and what the mount is labelled with while it airs. Derived from the filename when absent
    public var label: String?

    public init(file: Data, kind: String, label: String? = nil) {
        self.file = file
        self.kind = kind
        self.label = label
    }

    private enum CodingKeys: String, CodingKey {
        case file = "file"
        case kind = "kind"
        case label = "label"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.file = try container.decode(Data.self, forKey: .file)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.file, forKey: .file)
        try container.encode(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.label, forKey: .label)
    }
}

/// A voice the station can be asked to speak in
public struct Voice: Codable, Equatable, Sendable {
    /// What to pass as a segment's `voice`. Empty means the plugin's own default
    public var id: String
    /// What the console calls it
    public var label: String
    /// What it sounds like, or what it maps to on the engine
    public var description: String?

    public init(id: String, label: String, description: String? = nil) {
        self.id = id
        self.label = label
        self.description = description
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case label = "label"
        case description = "description"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.label = try container.decode(String.self, forKey: .label)
        self.description = try container.decodeIfPresent(String.self, forKey: .description)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.label, forKey: .label)
        try container.encodeIfPresent(self.description, forKey: .description)
    }
}

/// Whether there are words, and if not, which way it went wrong
public enum ScriptOutcome: String, Codable, CaseIterable, Sendable {
    case written = "written"
    case declined = "declined"
    case failed = "failed"
}

/// A record a writer was told about, kept as it was told
public struct ScriptNeighbour: Codable, Equatable, Sendable {
    public var title: String
    public var artist: String
    /// What it was shown about the record. A break that said nothing interesting and one that was TOLD nothing interesting read the same from the script alone
    public var facts: [String]?

    public init(title: String, artist: String, facts: [String]? = nil) {
        self.title = title
        self.artist = artist
        self.facts = facts
    }

    private enum CodingKeys: String, CodingKey {
        case title = "title"
        case artist = "artist"
        case facts = "facts"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.title = try container.decode(String.self, forKey: .title)
        self.artist = try container.decode(String.self, forKey: .artist)
        self.facts = try container.decodeIfPresent([String].self, forKey: .facts)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.title, forKey: .title)
        try container.encode(self.artist, forKey: .artist)
        try container.encodeIfPresent(self.facts, forKey: .facts)
    }
}

/// What the provider said the attempt cost, when it said anything
public struct ScriptUsage: Codable, Equatable, Sendable {
    public var inputTokens: Int?
    public var outputTokens: Int?
    public var totalTokens: Int?

    public init(inputTokens: Int? = nil, outputTokens: Int? = nil, totalTokens: Int? = nil) {
        self.inputTokens = inputTokens
        self.outputTokens = outputTokens
        self.totalTokens = totalTokens
    }

    private enum CodingKeys: String, CodingKey {
        case inputTokens = "inputTokens"
        case outputTokens = "outputTokens"
        case totalTokens = "totalTokens"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.inputTokens = try container.decodeIfPresent(Int.self, forKey: .inputTokens)
        self.outputTokens = try container.decodeIfPresent(Int.self, forKey: .outputTokens)
        self.totalTokens = try container.decodeIfPresent(Int.self, forKey: .totalTokens)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.inputTokens, forKey: .inputTokens)
        try container.encodeIfPresent(self.outputTokens, forKey: .outputTokens)
        try container.encodeIfPresent(self.totalTokens, forKey: .totalTokens)
    }
}

/// One turn of the conversation a writer sent
public struct ScriptPromptMessage: Codable, Equatable, Sendable {
    public var role: String
    public var content: String

    public init(role: String, content: String) {
        self.role = role
        self.content = content
    }

    private enum CodingKeys: String, CodingKey {
        case role = "role"
        case content = "content"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.role = try container.decode(String.self, forKey: .role)
        self.content = try container.decode(String.self, forKey: .content)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.role, forKey: .role)
        try container.encode(self.content, forKey: .content)
    }
}

/// What an operator thought of something the station said.
///
/// The catalog's three spellings exactly, and deliberately not a second vocabulary: an opinion is an
/// opinion whether it is about a record or about a sentence, and `catalog/rating.ts` is the one place
/// the words and the column's numbers meet.
///
/// `neutral` is a real answer rather than an absence. Rating something back to nothing is a thing an
/// operator does, and it has to be distinguishable from never having listened, which is the field
/// being absent on the attempt.
public enum ScriptRating: String, Codable, CaseIterable, Sendable {
    case liked = "liked"
    case neutral = "neutral"
    case disliked = "disliked"
}

/// Words to hear before anything has aired them
public struct SpeechPreviewRequest: Codable, Equatable, Sendable {
    /// What to say. Far under a segment's 20000 because this is one break heard once, and the cap is what bounds a cache keyed on the words themselves
    public var text: String
    /// A station voice name, as a segment's `voice`. Absent uses the plugin's own default
    public var voice: String?

    public init(text: String, voice: String? = nil) {
        self.text = text
        self.voice = voice
    }

    private enum CodingKeys: String, CodingKey {
        case text = "text"
        case voice = "voice"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.text = try container.decode(String.self, forKey: .text)
        self.voice = try container.decodeIfPresent(String.self, forKey: .voice)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.text, forKey: .text)
        try container.encodeIfPresent(self.voice, forKey: .voice)
    }
}

/// The window the counts cover
public struct ScriptHistorySummaryQuery: Codable, Equatable, Sendable {
    /// How far back to count. Defaults to 24, and a week at most, because past that the nightly sweep may already have taken the rows and the count would quietly be of what survived rather than of what happened
    public var hours: Int?

    public init(hours: Int? = nil) {
        self.hours = hours
    }

    private enum CodingKeys: String, CodingKey {
        case hours = "hours"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.hours = try container.decodeIfPresent(Int.self, forKey: .hours)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.hours, forKey: .hours)
    }
}

/// One presenter's attempts in the window
public struct ScriptHistorySummaryRow: Codable, Equatable, Sendable {
    /// Absent means nobody was presenting, which is an ordinary state rather than a gap in the data
    public var personaKey: String?
    public var written: Int
    /// A decline is the writer registry working: the model had nothing to say and the floor covered for it
    public var declined: Int
    public var failed: Int

    public init(personaKey: String? = nil, written: Int, declined: Int, failed: Int) {
        self.personaKey = personaKey
        self.written = written
        self.declined = declined
        self.failed = failed
    }

    private enum CodingKeys: String, CodingKey {
        case personaKey = "personaKey"
        case written = "written"
        case declined = "declined"
        case failed = "failed"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.personaKey = try container.decodeIfPresent(String.self, forKey: .personaKey)
        self.written = try container.decode(Int.self, forKey: .written)
        self.declined = try container.decode(Int.self, forKey: .declined)
        self.failed = try container.decode(Int.self, forKey: .failed)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.personaKey, forKey: .personaKey)
        try container.encode(self.written, forKey: .written)
        try container.encode(self.declined, forKey: .declined)
        try container.encode(self.failed, forKey: .failed)
    }
}

/// What one pass over the inbox did
public struct SegmentScanResult: Codable, Equatable, Sendable {
    /// Audio files seen, whether or not they were already known
    public var scanned: Int
    /// Segments the station did not have before this pass
    public var imported: Int
    /// Files passed over: not audio it can serve, or unreadable
    public var skipped: Int

    public init(scanned: Int, imported: Int, skipped: Int) {
        self.scanned = scanned
        self.imported = imported
        self.skipped = skipped
    }

    private enum CodingKeys: String, CodingKey {
        case scanned = "scanned"
        case imported = "imported"
        case skipped = "skipped"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.scanned = try container.decode(Int.self, forKey: .scanned)
        self.imported = try container.decode(Int.self, forKey: .imported)
        self.skipped = try container.decode(Int.self, forKey: .skipped)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.scanned, forKey: .scanned)
        try container.encode(self.imported, forKey: .imported)
        try container.encode(self.skipped, forKey: .skipped)
    }
}

/// One name the station says differently from how it is written
public struct Pronunciation: Codable, Equatable, Sendable {
    public var id: String
    /// What appears in a script. Matched case-insensitively, and whole words only
    public var written: String
    /// What the engine is handed instead, untouched. EMPTY is meaningful: it drops the words, which is the honest reading for a marker that got into a title and is not a word
    public var spoken: String
    /// `active` is said. `suggested` is proposed and says nothing yet. `rejected` outlives the pass that proposed it, or the same article proposes it again forever
    public var state: PronunciationState
    /// Who says so. `gloss` is a pronunciation key an encyclopaedia article printed for itself
    public var origin: PronunciationOrigin
    /// The article. Present on anything an operator did not type
    public var sourceUrl: String?
    /// The sentence that says so, as it stands in the article, which is what the decision is actually made on
    public var sourceQuote: String?
    /// What the article was about
    public var subjectKind: PronunciationSubjectKind?
    public var subjectId: String?
    public var createdAt: String

    public init(id: String, written: String, spoken: String, state: PronunciationState, origin: PronunciationOrigin, sourceUrl: String? = nil, sourceQuote: String? = nil, subjectKind: PronunciationSubjectKind? = nil, subjectId: String? = nil, createdAt: String) {
        self.id = id
        self.written = written
        self.spoken = spoken
        self.state = state
        self.origin = origin
        self.sourceUrl = sourceUrl
        self.sourceQuote = sourceQuote
        self.subjectKind = subjectKind
        self.subjectId = subjectId
        self.createdAt = createdAt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case written = "written"
        case spoken = "spoken"
        case state = "state"
        case origin = "origin"
        case sourceUrl = "sourceUrl"
        case sourceQuote = "sourceQuote"
        case subjectKind = "subjectKind"
        case subjectId = "subjectId"
        case createdAt = "createdAt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.written = try container.decode(String.self, forKey: .written)
        self.spoken = try container.decode(String.self, forKey: .spoken)
        self.state = try container.decode(PronunciationState.self, forKey: .state)
        self.origin = try container.decode(PronunciationOrigin.self, forKey: .origin)
        self.sourceUrl = try container.decodeIfPresent(String.self, forKey: .sourceUrl)
        self.sourceQuote = try container.decodeIfPresent(String.self, forKey: .sourceQuote)
        self.subjectKind = try container.decodeIfPresent(PronunciationSubjectKind.self, forKey: .subjectKind)
        self.subjectId = try container.decodeIfPresent(String.self, forKey: .subjectId)
        self.createdAt = try container.decode(String.self, forKey: .createdAt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.written, forKey: .written)
        try container.encode(self.spoken, forKey: .spoken)
        try container.encode(self.state, forKey: .state)
        try container.encode(self.origin, forKey: .origin)
        try container.encodeIfPresent(self.sourceUrl, forKey: .sourceUrl)
        try container.encodeIfPresent(self.sourceQuote, forKey: .sourceQuote)
        try container.encodeIfPresent(self.subjectKind, forKey: .subjectKind)
        try container.encodeIfPresent(self.subjectId, forKey: .subjectId)
        try container.encode(self.createdAt, forKey: .createdAt)
    }
}

/// A name and how to say it
public struct PronunciationWrite: Codable, Equatable, Sendable {
    public var written: String
    /// Empty drops the words rather than saying them
    public var spoken: String

    public init(written: String, spoken: String) {
        self.written = written
        self.spoken = spoken
    }

    private enum CodingKeys: String, CodingKey {
        case written = "written"
        case spoken = "spoken"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.written = try container.decode(String.self, forKey: .written)
        self.spoken = try container.decode(String.self, forKey: .spoken)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.written, forKey: .written)
        try container.encode(self.spoken, forKey: .spoken)
    }
}

/// Accepting a proposal, turning one down, or taking an entry out of use without losing it
public struct PronunciationStateWrite: Codable, Equatable, Sendable {
    public var state: PronunciationStateWriteState

    public init(state: PronunciationStateWriteState) {
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.state = try container.decode(PronunciationStateWriteState.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.state, forKey: .state)
    }
}

/// Which part of the lexicon to read
public struct PronunciationQuery: Codable, Equatable, Sendable {
    /// Absent is all of it
    public var state: PronunciationQueryState?

    public init(state: PronunciationQueryState? = nil) {
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.state = try container.decodeIfPresent(PronunciationQueryState.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.state, forKey: .state)
    }
}

/// One sound on a soundboard, as the console draws it.
///
/// `name` is what a script writes to hit it and `label` is what a person reads: two columns rather
/// than one, because a token for a model and prose for an operator are different things and the
/// filename produces both
public struct Pad: Codable, Equatable, Sendable {
    public var id: UUID
    /// Which directory it arrived in. Provenance: what reaches it is a set
    public var board: String
    /// The keys of the sets it is on. Empty means it is in the library and nothing can hit it
    public var sets: [String]
    /// What a script writes: `[sfx:airhorn]`
    public var name: String
    public var label: String
    public var durationMs: Int?
    /// How loud it came out, once something measured it. Absent on a station with no analyzer, which is ordinary
    public var loudnessLufs: Double?
    /// Who put the file there: `library` for one the operator dropped in, `upload` or `url` for one the console wrote. It decides whether the console may delete it
    public var source: String
    /// The file in the library directory it was imported from, so the console can say where it came from
    public var sourcePath: String?
    /// When it was last hit. Absent for one nothing has reached for yet
    public var lastUsedAt: Date?
    public var state: PadState2

    public init(id: UUID, board: String, sets: [String], name: String, label: String, durationMs: Int? = nil, loudnessLufs: Double? = nil, source: String, sourcePath: String? = nil, lastUsedAt: Date? = nil, state: PadState2) {
        self.id = id
        self.board = board
        self.sets = sets
        self.name = name
        self.label = label
        self.durationMs = durationMs
        self.loudnessLufs = loudnessLufs
        self.source = source
        self.sourcePath = sourcePath
        self.lastUsedAt = lastUsedAt
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case board = "board"
        case sets = "sets"
        case name = "name"
        case label = "label"
        case durationMs = "durationMs"
        case loudnessLufs = "loudnessLufs"
        case source = "source"
        case sourcePath = "sourcePath"
        case lastUsedAt = "lastUsedAt"
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.board = try container.decode(String.self, forKey: .board)
        self.sets = try container.decode([String].self, forKey: .sets)
        self.name = try container.decode(String.self, forKey: .name)
        self.label = try container.decode(String.self, forKey: .label)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.loudnessLufs = try container.decodeIfPresent(Double.self, forKey: .loudnessLufs)
        self.source = try container.decode(String.self, forKey: .source)
        self.sourcePath = try container.decodeIfPresent(String.self, forKey: .sourcePath)
        self.lastUsedAt = try container.decodeIfPresent(Date.self, forKey: .lastUsedAt)
        self.state = try container.decode(PadState2.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.board, forKey: .board)
        try container.encode(self.sets, forKey: .sets)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.label, forKey: .label)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.loudnessLufs, forKey: .loudnessLufs)
        try container.encode(self.source, forKey: .source)
        try container.encodeIfPresent(self.sourcePath, forKey: .sourcePath)
        try container.encodeIfPresent(self.lastUsedAt, forKey: .lastUsedAt)
        try container.encode(self.state, forKey: .state)
    }
}

/// One sound on a soundboard, as the console draws it.
///
/// `name` is what a script writes to hit it and `label` is what a person reads: two columns rather
/// than one, because a token for a model and prose for an operator are different things and the
/// filename produces both
public struct PadInput: Codable, Equatable, Sendable {
    /// Which directory it arrived in. Provenance: what reaches it is a set
    public var board: String
    /// What a script writes: `[sfx:airhorn]`
    public var name: String
    public var label: String
    public var durationMs: Int?
    /// How loud it came out, once something measured it. Absent on a station with no analyzer, which is ordinary
    public var loudnessLufs: Double?
    /// The file in the library directory it was imported from, so the console can say where it came from
    public var sourcePath: String?
    /// When it was last hit. Absent for one nothing has reached for yet
    public var lastUsedAt: Date?
    public var state: PadState2

    public init(board: String, name: String, label: String, durationMs: Int? = nil, loudnessLufs: Double? = nil, sourcePath: String? = nil, lastUsedAt: Date? = nil, state: PadState2) {
        self.board = board
        self.name = name
        self.label = label
        self.durationMs = durationMs
        self.loudnessLufs = loudnessLufs
        self.sourcePath = sourcePath
        self.lastUsedAt = lastUsedAt
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case board = "board"
        case name = "name"
        case label = "label"
        case durationMs = "durationMs"
        case loudnessLufs = "loudnessLufs"
        case sourcePath = "sourcePath"
        case lastUsedAt = "lastUsedAt"
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.board = try container.decode(String.self, forKey: .board)
        self.name = try container.decode(String.self, forKey: .name)
        self.label = try container.decode(String.self, forKey: .label)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.loudnessLufs = try container.decodeIfPresent(Double.self, forKey: .loudnessLufs)
        self.sourcePath = try container.decodeIfPresent(String.self, forKey: .sourcePath)
        self.lastUsedAt = try container.decodeIfPresent(Date.self, forKey: .lastUsedAt)
        self.state = try container.decode(PadState2.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.board, forKey: .board)
        try container.encode(self.name, forKey: .name)
        try container.encode(self.label, forKey: .label)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.loudnessLufs, forKey: .loudnessLufs)
        try container.encodeIfPresent(self.sourcePath, forKey: .sourcePath)
        try container.encodeIfPresent(self.lastUsedAt, forKey: .lastUsedAt)
        try container.encode(self.state, forKey: .state)
    }
}

/// A sound arriving from the browser, as multipart form parts.
///
/// Documentation rather than validation: a multipart body reaches the service as the raw parser and
/// the generated client types the body as `FormData`, so nothing checks this shape. It says what to
/// send
public struct PadUpload: Codable, Equatable, Sendable {
    /// The audio itself. mp3, wav, ogg, flac or m4a, and at most 25 MB
    public var file: Data
    /// The directory it is filed under, which is also the set it joins. A new name makes both
    public var board: String
    /// What a script will write. Derived from the filename when absent, and the FILE is named after this either way
    public var name: String?
    /// What the console calls it. Derived from the filename when absent
    public var label: String?

    public init(file: Data, board: String, name: String? = nil, label: String? = nil) {
        self.file = file
        self.board = board
        self.name = name
        self.label = label
    }

    private enum CodingKeys: String, CodingKey {
        case file = "file"
        case board = "board"
        case name = "name"
        case label = "label"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.file = try container.decode(Data.self, forKey: .file)
        self.board = try container.decode(String.self, forKey: .board)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.file, forKey: .file)
        try container.encode(self.board, forKey: .board)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.label, forKey: .label)
    }
}

/// A sound the station is being told to go and get.
///
/// The operator names the address, so this is them choosing a file exactly as dropping one in the
/// library is. Nothing inspects what comes back and nothing records a claim about its licence -- see
/// `docs/internals/render.md` under "Pads", whose line is redistribution rather than use
public struct PadFetch: Codable, Equatable, Sendable {
    /// Where the audio is. Followed once, bounded, and refused unless what comes back is a format the station serves
    public var url: String
    /// The directory it is filed under, which is also the set it joins
    public var board: String
    /// What a script will write. Derived from the address when absent
    public var name: String?
    public var label: String?

    public init(url: String, board: String, name: String? = nil, label: String? = nil) {
        self.url = url
        self.board = board
        self.name = name
        self.label = label
    }

    private enum CodingKeys: String, CodingKey {
        case url = "url"
        case board = "board"
        case name = "name"
        case label = "label"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.url = try container.decode(String.self, forKey: .url)
        self.board = try container.decode(String.self, forKey: .board)
        self.name = try container.decodeIfPresent(String.self, forKey: .name)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.url, forKey: .url)
        try container.encode(self.board, forKey: .board)
        try container.encodeIfPresent(self.name, forKey: .name)
        try container.encodeIfPresent(self.label, forKey: .label)
    }
}

/// A named collection of pads: what a presenter is actually handed.
///
/// One library, cut as many ways as an operator likes. `personas.soundboard` holds the `key`, so
/// renaming a set unpoints every persona naming it — which is why `personas` says who those are
public struct PadSet: Codable, Equatable, Sendable {
    public var id: UUID
    /// The slug a persona names. A directory in the pad library makes one of these
    public var key: String
    public var label: String
    public var position: Int
    /// How many sounds are on it. Zero is ordinary: it is what a set looks like before anybody drops a file
    public var pads: Int
    /// Who is pointed at it, so a rename or a delete can say what it is about to unpoint
    public var personas: [String]

    public init(id: UUID, key: String, label: String, position: Int, pads: Int, personas: [String]) {
        self.id = id
        self.key = key
        self.label = label
        self.position = position
        self.pads = pads
        self.personas = personas
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case key = "key"
        case label = "label"
        case position = "position"
        case pads = "pads"
        case personas = "personas"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(UUID.self, forKey: .id)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.position = try container.decode(Int.self, forKey: .position)
        self.pads = try container.decode(Int.self, forKey: .pads)
        self.personas = try container.decode([String].self, forKey: .personas)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.position, forKey: .position)
        try container.encode(self.pads, forKey: .pads)
        try container.encode(self.personas, forKey: .personas)
    }
}

/// A named collection of pads: what a presenter is actually handed.
///
/// One library, cut as many ways as an operator likes. `personas.soundboard` holds the `key`, so
/// renaming a set unpoints every persona naming it — which is why `personas` says who those are
public struct PadSetInput: Codable, Equatable, Sendable {
    /// The slug a persona names. A directory in the pad library makes one of these
    public var key: String
    public var label: String
    public var position: Int

    public init(key: String, label: String, position: Int) {
        self.key = key
        self.label = label
        self.position = position
    }

    private enum CodingKeys: String, CodingKey {
        case key = "key"
        case label = "label"
        case position = "position"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.position = try container.decode(Int.self, forKey: .position)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encode(self.position, forKey: .position)
    }
}

/// A set an operator is naming, or renaming
public struct PadSetWrite: Codable, Equatable, Sendable {
    public var key: String
    public var label: String
    public var position: Int?

    public init(key: String, label: String, position: Int? = nil) {
        self.key = key
        self.label = label
        self.position = position
    }

    private enum CodingKeys: String, CodingKey {
        case key = "key"
        case label = "label"
        case position = "position"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.key = try container.decode(String.self, forKey: .key)
        self.label = try container.decode(String.self, forKey: .label)
        self.position = try container.decodeIfPresent(Int.self, forKey: .position)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.key, forKey: .key)
        try container.encode(self.label, forKey: .label)
        try container.encodeIfPresent(self.position, forKey: .position)
    }
}

/// Which pad, and whether it is on the set
public struct PadSetMembership: Codable, Equatable, Sendable {
    public var padId: UUID
    public var on: Bool

    public init(padId: UUID, on: Bool) {
        self.padId = padId
        self.on = on
    }

    private enum CodingKeys: String, CodingKey {
        case padId = "padId"
        case on = "on"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.padId = try container.decode(UUID.self, forKey: .padId)
        self.on = try container.decode(Bool.self, forKey: .on)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.padId, forKey: .padId)
        try container.encode(self.on, forKey: .on)
    }
}

/// Turning a pad down, or putting one back
public struct PadState: Codable, Equatable, Sendable {
    public var state: PadStateState

    public init(state: PadStateState) {
        self.state = state
    }

    private enum CodingKeys: String, CodingKey {
        case state = "state"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.state = try container.decode(PadStateState.self, forKey: .state)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.state, forKey: .state)
    }
}

/// What one pass over the pad library did
public struct PadScanResult: Codable, Equatable, Sendable {
    /// Audio files seen, whether or not anything changed
    public var scanned: Int
    /// Sounds the station did not have before
    public var imported: Int
    /// Slots whose file changed under them, which every script naming them now plays
    public var replaced: Int
    /// Sounds that reached the library but not their set, because it already answered to their name. In the library and unreachable until somebody says where they go
    public var contested: Int
    /// Files passed over: not audio, unreadable, or named something no script could write
    public var skipped: Int

    public init(scanned: Int, imported: Int, replaced: Int, contested: Int, skipped: Int) {
        self.scanned = scanned
        self.imported = imported
        self.replaced = replaced
        self.contested = contested
        self.skipped = skipped
    }

    private enum CodingKeys: String, CodingKey {
        case scanned = "scanned"
        case imported = "imported"
        case replaced = "replaced"
        case contested = "contested"
        case skipped = "skipped"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.scanned = try container.decode(Int.self, forKey: .scanned)
        self.imported = try container.decode(Int.self, forKey: .imported)
        self.replaced = try container.decode(Int.self, forKey: .replaced)
        self.contested = try container.decode(Int.self, forKey: .contested)
        self.skipped = try container.decode(Int.self, forKey: .skipped)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.scanned, forKey: .scanned)
        try container.encode(self.imported, forKey: .imported)
        try container.encode(self.replaced, forKey: .replaced)
        try container.encode(self.contested, forKey: .contested)
        try container.encode(self.skipped, forKey: .skipped)
    }
}

/// Everything the station can play that is not a record
public struct SegmentList: Codable, Equatable, Sendable {
    public var segments: [Segment]

    public init(segments: [Segment]) {
        self.segments = segments
    }

    private enum CodingKeys: String, CodingKey {
        case segments = "segments"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.segments = try container.decode([Segment].self, forKey: .segments)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.segments, forKey: .segments)
    }
}

/// The voices the station's current speech plugin offers
public struct VoiceList: Codable, Equatable, Sendable {
    public var voices: [Voice]
    /// Which plugin answered. Absent when nothing can speak
    public var pluginId: String?
    /// Why there are no voices, when there are none
    public var reason: String?

    public init(voices: [Voice], pluginId: String? = nil, reason: String? = nil) {
        self.voices = voices
        self.pluginId = pluginId
        self.reason = reason
    }

    private enum CodingKeys: String, CodingKey {
        case voices = "voices"
        case pluginId = "pluginId"
        case reason = "reason"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.voices = try container.decode([Voice].self, forKey: .voices)
        self.pluginId = try container.decodeIfPresent(String.self, forKey: .pluginId)
        self.reason = try container.decodeIfPresent(String.self, forKey: .reason)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.voices, forKey: .voices)
        try container.encodeIfPresent(self.pluginId, forKey: .pluginId)
        try container.encodeIfPresent(self.reason, forKey: .reason)
    }
}

/// One page of what the station has written, newest first
public struct ScriptHistoryQuery: Codable, Equatable, Sendable {
    public var limit: Int?
    /// Where the previous page ended. Opaque, and a keyset rather than an offset because rows arrive at the head continuously. Pass back whatever `nextBefore` said and nothing else
    public var before: String?
    public var kind: String?
    public var writer: String?
    public var outcome: ScriptOutcome?
    /// Everything ONE character has said. Absent is every character and none
    public var personaKey: String?
    /// Every attempt made for ONE break, which is how a console reaches the words behind an item of the running order. Absent is the whole history
    public var segmentId: String?

    public init(limit: Int? = nil, before: String? = nil, kind: String? = nil, writer: String? = nil, outcome: ScriptOutcome? = nil, personaKey: String? = nil, segmentId: String? = nil) {
        self.limit = limit
        self.before = before
        self.kind = kind
        self.writer = writer
        self.outcome = outcome
        self.personaKey = personaKey
        self.segmentId = segmentId
    }

    private enum CodingKeys: String, CodingKey {
        case limit = "limit"
        case before = "before"
        case kind = "kind"
        case writer = "writer"
        case outcome = "outcome"
        case personaKey = "personaKey"
        case segmentId = "segmentId"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.limit = try container.decodeIfPresent(Int.self, forKey: .limit)
        self.before = try container.decodeIfPresent(String.self, forKey: .before)
        self.kind = try container.decodeIfPresent(String.self, forKey: .kind)
        self.writer = try container.decodeIfPresent(String.self, forKey: .writer)
        self.outcome = try container.decodeIfPresent(ScriptOutcome.self, forKey: .outcome)
        self.personaKey = try container.decodeIfPresent(String.self, forKey: .personaKey)
        self.segmentId = try container.decodeIfPresent(String.self, forKey: .segmentId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(self.limit, forKey: .limit)
        try container.encodeIfPresent(self.before, forKey: .before)
        try container.encodeIfPresent(self.kind, forKey: .kind)
        try container.encodeIfPresent(self.writer, forKey: .writer)
        try container.encodeIfPresent(self.outcome, forKey: .outcome)
        try container.encodeIfPresent(self.personaKey, forKey: .personaKey)
        try container.encodeIfPresent(self.segmentId, forKey: .segmentId)
    }
}

/// One attempt to write something the station would say, including the ones that came to nothing
public struct ScriptAttempt: Codable, Equatable, Sendable {
    public var id: String
    public var at: Date
    /// What sort of break it was for: `talkbreak`, `welcome`, `news`
    public var kind: String
    /// The binding that produced or declined it
    public var writer: String
    public var outcome: ScriptOutcome
    /// Who was presenting, as the persona's own key. Absent means nobody was, which is an ordinary state. Stamped on every attempt including the declined ones, so a character whose model breaks are all being refused is visible rather than hidden behind the floor
    public var personaKey: String?
    public var label: String?
    /// The words. Absent for an attempt that produced none
    public var script: String?
    /// The model that said it, for a writer that used one
    public var model: String?
    /// What the line was rendered from, for a writer working from something an operator can edit
    public var source: String?
    /// Why, for anything that is not `written`
    public var reason: String?
    /// The segment this was for, while it is still known. The row outlives it
    public var segmentId: String?
    public var previous: ScriptNeighbour?
    public var next: ScriptNeighbour?
    /// How long the attempt took
    public var durationMs: Int?
    public var usage: ScriptUsage?
    /// The answer before anything read it. Only while `llm.captureWrites` is on
    public var raw: String?
    /// What the writer sent. Only while `llm.captureWrites` is on
    public var prompt: [ScriptPromptMessage]?
    /// What the operator thought of it. ABSENT means nobody has said, which `neutral` does not
    public var rating: ScriptRating?

    public init(id: String, at: Date, kind: String, writer: String, outcome: ScriptOutcome, personaKey: String? = nil, label: String? = nil, script: String? = nil, model: String? = nil, source: String? = nil, reason: String? = nil, segmentId: String? = nil, previous: ScriptNeighbour? = nil, next: ScriptNeighbour? = nil, durationMs: Int? = nil, usage: ScriptUsage? = nil, raw: String? = nil, prompt: [ScriptPromptMessage]? = nil, rating: ScriptRating? = nil) {
        self.id = id
        self.at = at
        self.kind = kind
        self.writer = writer
        self.outcome = outcome
        self.personaKey = personaKey
        self.label = label
        self.script = script
        self.model = model
        self.source = source
        self.reason = reason
        self.segmentId = segmentId
        self.previous = previous
        self.next = next
        self.durationMs = durationMs
        self.usage = usage
        self.raw = raw
        self.prompt = prompt
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case at = "at"
        case kind = "kind"
        case writer = "writer"
        case outcome = "outcome"
        case personaKey = "personaKey"
        case label = "label"
        case script = "script"
        case model = "model"
        case source = "source"
        case reason = "reason"
        case segmentId = "segmentId"
        case previous = "previous"
        case next = "next"
        case durationMs = "durationMs"
        case usage = "usage"
        case raw = "raw"
        case prompt = "prompt"
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.at = try container.decode(Date.self, forKey: .at)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.writer = try container.decode(String.self, forKey: .writer)
        self.outcome = try container.decode(ScriptOutcome.self, forKey: .outcome)
        self.personaKey = try container.decodeIfPresent(String.self, forKey: .personaKey)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
        self.script = try container.decodeIfPresent(String.self, forKey: .script)
        self.model = try container.decodeIfPresent(String.self, forKey: .model)
        self.source = try container.decodeIfPresent(String.self, forKey: .source)
        self.reason = try container.decodeIfPresent(String.self, forKey: .reason)
        self.segmentId = try container.decodeIfPresent(String.self, forKey: .segmentId)
        self.previous = try container.decodeIfPresent(ScriptNeighbour.self, forKey: .previous)
        self.next = try container.decodeIfPresent(ScriptNeighbour.self, forKey: .next)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.usage = try container.decodeIfPresent(ScriptUsage.self, forKey: .usage)
        self.raw = try container.decodeIfPresent(String.self, forKey: .raw)
        self.prompt = try container.decodeIfPresent([ScriptPromptMessage].self, forKey: .prompt)
        self.rating = try container.decodeIfPresent(ScriptRating.self, forKey: .rating)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.at, forKey: .at)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.writer, forKey: .writer)
        try container.encode(self.outcome, forKey: .outcome)
        try container.encodeIfPresent(self.personaKey, forKey: .personaKey)
        try container.encodeIfPresent(self.label, forKey: .label)
        try container.encodeIfPresent(self.script, forKey: .script)
        try container.encodeIfPresent(self.model, forKey: .model)
        try container.encodeIfPresent(self.source, forKey: .source)
        try container.encodeIfPresent(self.reason, forKey: .reason)
        try container.encodeIfPresent(self.segmentId, forKey: .segmentId)
        try container.encodeIfPresent(self.previous, forKey: .previous)
        try container.encodeIfPresent(self.next, forKey: .next)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.usage, forKey: .usage)
        try container.encodeIfPresent(self.raw, forKey: .raw)
        try container.encodeIfPresent(self.prompt, forKey: .prompt)
        try container.encodeIfPresent(self.rating, forKey: .rating)
    }
}

/// One attempt to write something the station would say, including the ones that came to nothing
public struct ScriptAttemptInput: Codable, Equatable, Sendable {
    public var id: String
    public var at: Date
    /// What sort of break it was for: `talkbreak`, `welcome`, `news`
    public var kind: String
    /// The binding that produced or declined it
    public var writer: String
    public var outcome: ScriptOutcome
    /// Who was presenting, as the persona's own key. Absent means nobody was, which is an ordinary state. Stamped on every attempt including the declined ones, so a character whose model breaks are all being refused is visible rather than hidden behind the floor
    public var personaKey: String?
    public var label: String?
    /// The words. Absent for an attempt that produced none
    public var script: String?
    /// The model that said it, for a writer that used one
    public var model: String?
    /// What the line was rendered from, for a writer working from something an operator can edit
    public var source: String?
    /// Why, for anything that is not `written`
    public var reason: String?
    /// The segment this was for, while it is still known. The row outlives it
    public var segmentId: String?
    public var previous: ScriptNeighbour?
    public var next: ScriptNeighbour?
    /// How long the attempt took
    public var durationMs: Int?
    public var usage: ScriptUsage?
    /// The answer before anything read it. Only while `llm.captureWrites` is on
    public var raw: String?
    /// What the writer sent. Only while `llm.captureWrites` is on
    public var prompt: [ScriptPromptMessage]?

    public init(id: String, at: Date, kind: String, writer: String, outcome: ScriptOutcome, personaKey: String? = nil, label: String? = nil, script: String? = nil, model: String? = nil, source: String? = nil, reason: String? = nil, segmentId: String? = nil, previous: ScriptNeighbour? = nil, next: ScriptNeighbour? = nil, durationMs: Int? = nil, usage: ScriptUsage? = nil, raw: String? = nil, prompt: [ScriptPromptMessage]? = nil) {
        self.id = id
        self.at = at
        self.kind = kind
        self.writer = writer
        self.outcome = outcome
        self.personaKey = personaKey
        self.label = label
        self.script = script
        self.model = model
        self.source = source
        self.reason = reason
        self.segmentId = segmentId
        self.previous = previous
        self.next = next
        self.durationMs = durationMs
        self.usage = usage
        self.raw = raw
        self.prompt = prompt
    }

    private enum CodingKeys: String, CodingKey {
        case id = "id"
        case at = "at"
        case kind = "kind"
        case writer = "writer"
        case outcome = "outcome"
        case personaKey = "personaKey"
        case label = "label"
        case script = "script"
        case model = "model"
        case source = "source"
        case reason = "reason"
        case segmentId = "segmentId"
        case previous = "previous"
        case next = "next"
        case durationMs = "durationMs"
        case usage = "usage"
        case raw = "raw"
        case prompt = "prompt"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.id = try container.decode(String.self, forKey: .id)
        self.at = try container.decode(Date.self, forKey: .at)
        self.kind = try container.decode(String.self, forKey: .kind)
        self.writer = try container.decode(String.self, forKey: .writer)
        self.outcome = try container.decode(ScriptOutcome.self, forKey: .outcome)
        self.personaKey = try container.decodeIfPresent(String.self, forKey: .personaKey)
        self.label = try container.decodeIfPresent(String.self, forKey: .label)
        self.script = try container.decodeIfPresent(String.self, forKey: .script)
        self.model = try container.decodeIfPresent(String.self, forKey: .model)
        self.source = try container.decodeIfPresent(String.self, forKey: .source)
        self.reason = try container.decodeIfPresent(String.self, forKey: .reason)
        self.segmentId = try container.decodeIfPresent(String.self, forKey: .segmentId)
        self.previous = try container.decodeIfPresent(ScriptNeighbour.self, forKey: .previous)
        self.next = try container.decodeIfPresent(ScriptNeighbour.self, forKey: .next)
        self.durationMs = try container.decodeIfPresent(Int.self, forKey: .durationMs)
        self.usage = try container.decodeIfPresent(ScriptUsage.self, forKey: .usage)
        self.raw = try container.decodeIfPresent(String.self, forKey: .raw)
        self.prompt = try container.decodeIfPresent([ScriptPromptMessage].self, forKey: .prompt)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.id, forKey: .id)
        try container.encode(self.at, forKey: .at)
        try container.encode(self.kind, forKey: .kind)
        try container.encode(self.writer, forKey: .writer)
        try container.encode(self.outcome, forKey: .outcome)
        try container.encodeIfPresent(self.personaKey, forKey: .personaKey)
        try container.encodeIfPresent(self.label, forKey: .label)
        try container.encodeIfPresent(self.script, forKey: .script)
        try container.encodeIfPresent(self.model, forKey: .model)
        try container.encodeIfPresent(self.source, forKey: .source)
        try container.encodeIfPresent(self.reason, forKey: .reason)
        try container.encodeIfPresent(self.segmentId, forKey: .segmentId)
        try container.encodeIfPresent(self.previous, forKey: .previous)
        try container.encodeIfPresent(self.next, forKey: .next)
        try container.encodeIfPresent(self.durationMs, forKey: .durationMs)
        try container.encodeIfPresent(self.usage, forKey: .usage)
        try container.encodeIfPresent(self.raw, forKey: .raw)
        try container.encodeIfPresent(self.prompt, forKey: .prompt)
    }
}

public struct ScriptRatingInput: Codable, Equatable, Sendable {
    public var rating: ScriptRating

    public init(rating: ScriptRating) {
        self.rating = rating
    }

    private enum CodingKeys: String, CodingKey {
        case rating = "rating"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.rating = try container.decode(ScriptRating.self, forKey: .rating)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.rating, forKey: .rating)
    }
}

/// What each presenter has written lately, and over how long
public struct ScriptHistorySummary: Codable, Equatable, Sendable {
    /// The window actually counted, echoed so a console can label the numbers it draws
    public var hours: Int
    public var rows: [ScriptHistorySummaryRow]

    public init(hours: Int, rows: [ScriptHistorySummaryRow]) {
        self.hours = hours
        self.rows = rows
    }

    private enum CodingKeys: String, CodingKey {
        case hours = "hours"
        case rows = "rows"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.hours = try container.decode(Int.self, forKey: .hours)
        self.rows = try container.decode([ScriptHistorySummaryRow].self, forKey: .rows)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.hours, forKey: .hours)
        try container.encode(self.rows, forKey: .rows)
    }
}

/// The station's lexicon, oldest first
public struct PronunciationList: Codable, Equatable, Sendable {
    public var pronunciations: [Pronunciation]

    public init(pronunciations: [Pronunciation]) {
        self.pronunciations = pronunciations
    }

    private enum CodingKeys: String, CodingKey {
        case pronunciations = "pronunciations"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pronunciations = try container.decode([Pronunciation].self, forKey: .pronunciations)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pronunciations, forKey: .pronunciations)
    }
}

/// Every sound the station holds, and the sets over it
public struct PadList: Codable, Equatable, Sendable {
    public var pads: [Pad]
    public var sets: [PadSet]

    public init(pads: [Pad], sets: [PadSet]) {
        self.pads = pads
        self.sets = sets
    }

    private enum CodingKeys: String, CodingKey {
        case pads = "pads"
        case sets = "sets"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pads = try container.decode([Pad].self, forKey: .pads)
        self.sets = try container.decode([PadSet].self, forKey: .sets)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pads, forKey: .pads)
        try container.encode(self.sets, forKey: .sets)
    }
}

/// Every sound the station holds, and the sets over it
public struct PadListInput: Codable, Equatable, Sendable {
    public var pads: [PadInput]
    public var sets: [PadSetInput]

    public init(pads: [PadInput], sets: [PadSetInput]) {
        self.pads = pads
        self.sets = sets
    }

    private enum CodingKeys: String, CodingKey {
        case pads = "pads"
        case sets = "sets"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.pads = try container.decode([PadInput].self, forKey: .pads)
        self.sets = try container.decode([PadSetInput].self, forKey: .sets)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.pads, forKey: .pads)
        try container.encode(self.sets, forKey: .sets)
    }
}

public struct ScriptHistoryPage: Codable, Equatable, Sendable {
    public var attempts: [ScriptAttempt]
    /// The cursor for the page after this one, absent once the history has been read to its end
    public var nextBefore: String?

    public init(attempts: [ScriptAttempt], nextBefore: String? = nil) {
        self.attempts = attempts
        self.nextBefore = nextBefore
    }

    private enum CodingKeys: String, CodingKey {
        case attempts = "attempts"
        case nextBefore = "nextBefore"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.attempts = try container.decode([ScriptAttempt].self, forKey: .attempts)
        self.nextBefore = try container.decodeIfPresent(String.self, forKey: .nextBefore)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.attempts, forKey: .attempts)
        try container.encodeIfPresent(self.nextBefore, forKey: .nextBefore)
    }
}

public struct ScriptHistoryPageInput: Codable, Equatable, Sendable {
    public var attempts: [ScriptAttemptInput]
    /// The cursor for the page after this one, absent once the history has been read to its end
    public var nextBefore: String?

    public init(attempts: [ScriptAttemptInput], nextBefore: String? = nil) {
        self.attempts = attempts
        self.nextBefore = nextBefore
    }

    private enum CodingKeys: String, CodingKey {
        case attempts = "attempts"
        case nextBefore = "nextBefore"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.attempts = try container.decode([ScriptAttemptInput].self, forKey: .attempts)
        self.nextBefore = try container.decodeIfPresent(String.self, forKey: .nextBefore)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.attempts, forKey: .attempts)
        try container.encodeIfPresent(self.nextBefore, forKey: .nextBefore)
    }
}

/// One state per stage of making it. Only `ready` can go on air; the station skips anything else rather than waiting for it
public enum SegmentState: String, Codable, CaseIterable, Sendable {
    case planned = "planned"
    case writing = "writing"
    case written = "written"
    case rendering = "rendering"
    case ready = "ready"
    case failed = "failed"
}

/// `active` is said. `suggested` is proposed and says nothing yet. `rejected` outlives the pass that proposed it, or the same article proposes it again forever
public enum PronunciationState: String, Codable, CaseIterable, Sendable {
    case active = "active"
    case suggested = "suggested"
    case rejected = "rejected"
}

/// Who says so. `gloss` is a pronunciation key an encyclopaedia article printed for itself
public enum PronunciationOrigin: String, Codable, CaseIterable, Sendable {
    case `operator` = "operator"
    case gloss = "gloss"
}

/// What the article was about
public enum PronunciationSubjectKind: String, Codable, CaseIterable, Sendable {
    case track = "track"
    case album = "album"
    case artist = "artist"
}

public enum PronunciationStateWriteState: String, Codable, CaseIterable, Sendable {
    case active = "active"
    case suggested = "suggested"
    case rejected = "rejected"
}

/// Absent is all of it
public enum PronunciationQueryState: String, Codable, CaseIterable, Sendable {
    case active = "active"
    case suggested = "suggested"
    case rejected = "rejected"
}

public enum PadState2: String, Codable, CaseIterable, Sendable {
    case active = "active"
    case rejected = "rejected"
}

public enum PadStateState: String, Codable, CaseIterable, Sendable {
    case active = "active"
    case rejected = "rejected"
}
