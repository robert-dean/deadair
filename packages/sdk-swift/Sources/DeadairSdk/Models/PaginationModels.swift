// Auto-generated by @contractkit/plugin-swift. Do not edit manually.
import Foundation

/// Represents a pagination object
public struct Pagination: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    /// The total number of items
    public var total: Int

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, total: Int) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.total = total
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case total = "total"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.total = try container.decode(Int.self, forKey: .total)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encode(self.total, forKey: .total)
    }
}

/// Represents a pagination object
public struct PaginationInput: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
    }
}

public struct PaginationWithActive: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    /// The total number of items
    public var total: Int
    /// Optionally filter by whether the items are active
    public var active: Bool?

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, total: Int, active: Bool? = nil) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.total = total
        self.active = active
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case total = "total"
        case active = "active"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.total = try container.decode(Int.self, forKey: .total)
        self.active = try container.decodeIfPresent(Bool.self, forKey: .active)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encode(self.total, forKey: .total)
        try container.encodeIfPresent(self.active, forKey: .active)
    }
}

public struct PaginationWithActiveInput: Codable, Equatable, Sendable {
    /// The page number
    public var page: Int
    /// The page size
    public var pageSize: Int
    /// The sort order
    public var sort: PaginationSort
    /// Optionally filter by whether the items are active
    public var active: Bool?

    public init(page: Int = 0, pageSize: Int = 25, sort: PaginationSort = .desc, active: Bool? = nil) {
        self.page = page
        self.pageSize = pageSize
        self.sort = sort
        self.active = active
    }

    private enum CodingKeys: String, CodingKey {
        case page = "page"
        case pageSize = "pageSize"
        case sort = "sort"
        case active = "active"
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)
        self.page = try container.decodeIfPresent(Int.self, forKey: .page) ?? 0
        self.pageSize = try container.decodeIfPresent(Int.self, forKey: .pageSize) ?? 25
        self.sort = try container.decodeIfPresent(PaginationSort.self, forKey: .sort) ?? .desc
        self.active = try container.decodeIfPresent(Bool.self, forKey: .active)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(self.page, forKey: .page)
        try container.encode(self.pageSize, forKey: .pageSize)
        try container.encode(self.sort, forKey: .sort)
        try container.encodeIfPresent(self.active, forKey: .active)
    }
}

/// The sort order
public enum PaginationSort: String, Codable, CaseIterable, Sendable {
    case asc = "asc"
    case desc = "desc"
}
