// <auto-generated/>
// Generated by @contractkit/plugin-csharp. Do not edit manually.
#nullable enable

using System;
using System.Collections.Generic;
using System.Globalization;
using System.Net.Http;
using System.Numerics;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading;
using System.Threading.Tasks;
using System.Xml;
using MaroonedSoftware.Deadair.Sdk.Models;
using MaroonedSoftware.Deadair.Sdk.Runtime;

namespace MaroonedSoftware.Deadair.Sdk.Clients;

/// <summary>Operations declared in &lt;c&gt;personas.ck&lt;/c&gt;.</summary>
public sealed class PersonasClient(SdkHttp http)
{
    /// <summary>
    /// List personas
    /// Every persona this station has, oldest first
    /// </summary>
    public async Task<PersonaList> ListPersonasAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("personas"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaList>(response);
    }

    /// <summary>
    /// Create persona
    /// Writes a new persona. It is not put on air by creating it
    /// </summary>
    public async Task<PersonaList> CreatePersonaAsync(PersonaInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaList>(response);
    }

    /// <summary>
    /// Generate persona
    /// Turns a description of a character into a whole persona, checked against its own sample lines and handed back unsaved
    /// </summary>
    public async Task<GeneratedPersona> GeneratePersonaAsync(PersonaRequest body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", "generate"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<GeneratedPersona>(response);
    }

    /// <summary>
    /// Restore station personas
    /// Writes back whichever of the station's own personas this station is missing, touching nothing it already has and putting nothing on air
    /// </summary>
    public async Task<PersonaList> RestoreStationPersonasAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", "restore"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaList>(response);
    }

    /// <summary>
    /// Export personas
    /// Every character this station holds, as one file
    /// </summary>
    public async Task<ExportPersonasResult> ExportPersonasAsync(CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("personas", "export"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        var headers = new ExportPersonasHeaders(
            response.Header("Content-Disposition") is { } contentDisposition ? contentDisposition : null);
        return new ExportPersonasResult(http.ReadJson<PersonaFile>(response), headers);
    }

    /// <summary>
    /// Export persona
    /// One character, its sheet and its stories, as a file
    /// </summary>
    /// <exception cref="SdkException">On 404.</exception>
    public async Task<ExportPersonaResult> ExportPersonaAsync(string id, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("personas", http.Segment(id), "export"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        var headers = new ExportPersonaHeaders(
            response.Header("Content-Disposition") is { } contentDisposition ? contentDisposition : null);
        return new ExportPersonaResult(http.ReadJson<PersonaFile>(response), headers);
    }

    /// <summary>
    /// Preview persona import
    /// Reads a file and reports what importing it would create, rewrite and skip. Writes nothing
    /// </summary>
    public async Task<PersonaImportPlan> PreviewPersonaImportAsync(PersonaFile body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", "import", "preview"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaImportPlan>(response);
    }

    /// <summary>
    /// Import personas
    /// Writes a file into this station, merging by key, and answers with what it did
    /// </summary>
    public async Task<PersonaImportResult> ImportPersonasAsync(PersonaFile body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", "import"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaImportResult>(response);
    }

    /// <summary>
    /// Update persona
    /// Rewrites one persona. An edit to the one on air is heard on the next break
    /// </summary>
    public async Task<PersonaList> UpdatePersonaAsync(string id, PersonaInput body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id)),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaList>(response);
    }

    /// <summary>
    /// Delete persona
    /// Removes a persona, including the one on air, which leaves the station with none
    /// </summary>
    public async Task<PersonaList> DeletePersonaAsync(string id, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("personas", http.Segment(id)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaList>(response);
    }

    /// <summary>
    /// Put persona on air
    /// Puts this persona on air and takes the previous one off
    /// </summary>
    public async Task<PersonaList> PutPersonaOnAirAsync(string id, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "active"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaList>(response);
    }

    /// <summary>
    /// List persona notes
    /// Everything this character has accumulated, oldest first, in every state
    /// </summary>
    public async Task<PersonaNoteList> ListPersonaNotesAsync(string id, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("personas", http.Segment(id), "notes"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaNoteList>(response);
    }

    /// <summary>
    /// Write persona note
    /// Writes a note by hand. An operator's own note is active from the moment it exists; only the distil pass proposes
    /// </summary>
    public async Task<PersonaNoteList> WritePersonaNoteAsync(string id, PersonaNoteWrite body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", http.Segment(id), "notes"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaNoteList>(response);
    }

    /// <summary>
    /// Update persona note
    /// Rewrites one note's words, whoever wrote it. Editing what the station proposed is most of the point of the panel
    /// </summary>
    public async Task<PersonaNoteList> UpdatePersonaNoteAsync(string id, string noteId, PersonaNoteWrite body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "notes", http.Segment(noteId)),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaNoteList>(response);
    }

    /// <summary>
    /// Delete persona note
    /// Removes a note outright. Turning down a PROPOSAL is a state rather than this, or the next pass writes it again
    /// </summary>
    public async Task<PersonaNoteList> DeletePersonaNoteAsync(string id, string noteId, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("personas", http.Segment(id), "notes", http.Segment(noteId)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaNoteList>(response);
    }

    /// <summary>
    /// Set persona note state
    /// Accepts a proposal, turns one down, or rests an active note. Mirrors the lexicon's own state route
    /// </summary>
    public async Task<PersonaNoteList> SetPersonaNoteStateAsync(string id, string noteId, PersonaNoteState body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "notes", http.Segment(noteId), "state"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaNoteList>(response);
    }

    /// <summary>
    /// List persona stories
    /// Every story this character holds, oldest first, in every state
    /// </summary>
    public async Task<PersonaStoryList> ListPersonaStoriesAsync(string id, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Get,
            http.Path("personas", http.Segment(id), "stories"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Write persona story
    /// Writes a story by hand. An operator's own is tellable from the moment it exists; only the enrichment pass proposes
    /// </summary>
    public async Task<PersonaStoryList> WritePersonaStoryAsync(string id, PersonaStoryWrite body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", http.Segment(id), "stories"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Update persona story
    /// Rewrites one story's handle and telling, whoever wrote it
    /// </summary>
    public async Task<PersonaStoryList> UpdatePersonaStoryAsync(string id, string storyId, PersonaStoryWrite body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId)),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Delete persona story
    /// Removes a story outright, details and all. Turning down a PROPOSAL is a state rather than this, or the next pass writes it again
    /// </summary>
    public async Task<PersonaStoryList> DeletePersonaStoryAsync(string id, string storyId, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Set persona story state
    /// Accepts a proposal, turns one down, or takes a story out of the rotation without losing it
    /// </summary>
    public async Task<PersonaStoryList> SetPersonaStoryStateAsync(string id, string storyId, PersonaStoryState body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId), "state"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Add persona story detail
    /// Adds one thing to a story that already exists
    /// </summary>
    public async Task<PersonaStoryList> AddPersonaStoryDetailAsync(string id, string storyId, PersonaStoryDetailWrite body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId), "details"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Update persona story detail
    /// Rewrites one detail's words
    /// </summary>
    public async Task<PersonaStoryList> UpdatePersonaStoryDetailAsync(string id, string storyId, string detailId, PersonaStoryDetailWrite body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId), "details", http.Segment(detailId)),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Delete persona story detail
    /// Removes one detail, leaving the story it was hung on alone
    /// </summary>
    public async Task<PersonaStoryList> DeletePersonaStoryDetailAsync(string id, string storyId, string detailId, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Delete,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId), "details", http.Segment(detailId)),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Set persona story detail state
    /// Accepts a proposed detail or turns it down, which has to outlive the pass that proposed it
    /// </summary>
    public async Task<PersonaStoryList> SetPersonaStoryDetailStateAsync(string id, string storyId, string detailId, PersonaStoryState body, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Put,
            http.Path("personas", http.Segment(id), "stories", http.Segment(storyId), "details", http.Segment(detailId), "state"),
            content: http.JsonContent(body, "application/json"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaStoryList>(response);
    }

    /// <summary>
    /// Rehearse persona
    /// Writes a talk break under this persona against two fixed invented records, and answers with every writer that was asked
    /// </summary>
    public async Task<PersonaRehearsal> RehearsePersonaAsync(string id, CancellationToken cancellationToken = default)
    {
        var response = await http.ExecuteAsync(
            HttpMethod.Post,
            http.Path("personas", http.Segment(id), "rehearse"),
            cancellationToken: cancellationToken).ConfigureAwait(false);
        return http.ReadJson<PersonaRehearsal>(response);
    }
}

/// <summary>Response headers declared on GET /personas/export.</summary>
public sealed record ExportPersonasHeaders(string? ContentDisposition);

/// <summary>The body of GET /personas/export, with the response headers the contract declares.</summary>
public sealed record ExportPersonasResult(PersonaFile Data, ExportPersonasHeaders Headers);

/// <summary>Response headers declared on GET /personas/{id}/export.</summary>
public sealed record ExportPersonaHeaders(string? ContentDisposition);

/// <summary>The body of GET /personas/{id}/export, with the response headers the contract declares.</summary>
public sealed record ExportPersonaResult(PersonaFile Data, ExportPersonaHeaders Headers);
